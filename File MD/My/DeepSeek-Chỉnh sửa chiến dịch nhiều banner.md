# Chỉnh sửa chiến dịch nhiều banner

**Created:** 07/07/2026 08:15  
**Updated:** 07/09/2026 20:38  
**Exported:** 07/09/2026 21:13  
**Link:** [https://chat.deepseek.com/a/chat/s/646fb0bd-2a85-49f9-b41b-2cefe6de88db](https://chat.deepseek.com/a/chat/s/646fb0bd-2a85-49f9-b41b-2cefe6de88db)  

## User:
07/07/2026 08:45

![Screenshot 2026-07-07 083113.png](data:image/webp;base64,)

![Screenshot 2026-07-07 083156.png](data:image/webp;base64,)

![Screenshot 2026-07-07 083329.png](data:image/webp;base64,)

tôi muốn làm giống như bạn nói 1 chiến dịch có (0-n) banner

-Bên banner lấy dữ liệu chiến dịch  qua để chọn.

-Tôi muốn chỉnh lại bên chiến dịch có thể lưu nhiều banner chứ không phải 1 vậy nên để thêm nhiều banner rồi lưu dựa trên id của chiến dịch ( để biết banner thuộc chiến dịch nào) và khi chiến dịch hết hạn thị cũng tự động không hiển thị nữa. => chiến dịch/ khuyến mãi pre-order bỏ chọn banner và bên banner thì cho show ra danh sách chiến dịch hiện có 

<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    banner: '',
    banner_id: null,
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    banner: '',
    banner_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getBannerByCampaign = (campaignId) => {
    if (!campaignId) return null;
    return banners.value.find(b => b.campaign_id === campaignId);
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
    }
    
    return 'active';
};

// ==================== BANNER FUNCTIONS ====================

const selectedBanner = computed(() => {
    if (!campaignForm.value.banner_id) return null;
    return banners.value.find(b => b.id === campaignForm.value.banner_id);
});

const selectedPreorderBanner = computed(() => {
    if (!preorderForm.value.banner_id) return null;
    return banners.value.find(b => b.id === preorderForm.value.banner_id);
});

const onBannerSelect = () => {
    const bannerId = campaignForm.value.banner_id;
    if (bannerId) {
        const selected = banners.value.find(b => b.id === bannerId);
        if (selected && selected.image) {
            campaignForm.value.banner = selected.image;
        }
    } else {
        campaignForm.value.banner = '';
    }
};

const onPreorderBannerSelect = () => {
    const bannerId = preorderForm.value.banner_id;
    if (bannerId) {
        const selected = banners.value.find(b => b.id === bannerId);
        if (selected && selected.image) {
            preorderForm.value.banner = selected.image;
        }
    } else {
        preorderForm.value.banner = '';
    }
};

const onBannerUrlInput = () => {
    if (campaignForm.value.banner) {
        campaignForm.value.banner_id = null;
    }
};

const onPreorderBannerUrlInput = () => {
    if (preorderForm.value.banner) {
        preorderForm.value.banner_id = null;
    }
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            banner: campaign.banner || '',
            banner_id: campaign.banner_id || null,
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            banner: '',
            banner_id: null,
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            ...campaignForm.value,
            status: status,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value <= 0) {
        errorMessage.value = 'Giá trị giảm giá phải lớn hơn 0';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            banner: preorder.banner || '',
            banner_id: preorder.banner_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            banner: '',
            banner_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

// Sử dụng computed thay vì watch để tránh vòng lặp vô hạn
const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

// Lọc campaigns theo status
const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

// Thêm computed cho danh sách banner đã lọc để hiển thị bên phải
const availableBanners = computed(() => {
    return banners.value.filter(b => b.status === 1);
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- 2 cột: danh sách bên trái và banner bên phải -->
            <div class="grid grid-cols-1 lg:grid-cols-12 gap-6">
                <!-- Cột chính: Danh sách khuyến mãi (10/12) -->
                <div class="lg:col-span-10">
                    <!-- Search -->
                    <div class="mb-6">
                        <div class="relative max-w-md">
                            <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                            <input 
                                v-model="searchQuery" 
                                type="text" 
                                placeholder="Tìm kiếm..." 
                                class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                            >
                        </div>
                    </div>

                    <!-- Main Tabs -->
                    <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                        <button 
                            @click="activeTab = 'campaigns'" 
                            class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                            :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                        >
                            🎯 Chiến dịch
                            <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                        </button>
                        <button 
                            @click="activeTab = 'vouchers'" 
                            class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                            :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                        >
                            🎫 Mã giảm giá
                            <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                        </button>
                        <button 
                            @click="activeTab = 'preorder'" 
                            class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                            :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                        >
                            ⏳ Pre-order
                            <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                        </button>
                    </div>

                    <!-- Status tabs for campaigns -->
                    <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                        <button 
                            v-for="status in statusOptions" 
                            :key="status.value"
                            @click="activeStatusTab = status.value"
                            class="px-4 py-2 text-sm font-medium transition-all"
                            :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                        >
                            {{ status.label }}
                            <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                                {{ campaigns.filter(c => status.value === 'all' || c.status === status.value).length }}
                            </span>
                        </button>
                    </div>

                    <!-- ==================== CAMPAIGNS LIST ==================== -->
                    <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                            <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                                <img 
                                    v-if="campaign.banner" 
                                    :src="campaign.banner" 
                                    :alt="campaign.name" 
                                    class="w-full h-full object-cover"
                                    @error="campaign.banner = null"
                                >
                                <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                                    <span class="material-symbols-outlined text-5xl">campaign</span>
                                </div>
                                
                                <div class="absolute top-3 right-3 flex gap-1">
                                    <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                        {{ getStatusLabel(campaign.status) }}
                                    </span>
                                    <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                                </div>
                            </div>

                            <div class="p-5">
                                <div class="flex justify-between items-start">
                                    <div class="flex-1">
                                        <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                        <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                                    </div>
                                    <div class="flex gap-1 ml-2">
                                        <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                            <span class="material-symbols-outlined text-sm">edit</span>
                                        </button>
                                        <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                            <span class="material-symbols-outlined text-sm">delete</span>
                                        </button>
                                    </div>
                                </div>

                                <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                                <div class="mt-4 grid grid-cols-2 gap-3">
                                    <div class="bg-orange-50 rounded-lg p-3">
                                        <p class="text-xs text-gray-500">Giảm giá</p>
                                        <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                                    </div>
                                    <div class="bg-blue-50 rounded-lg p-3">
                                        <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                        <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                                    </div>
                                </div>

                                <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                                    <span class="material-symbols-outlined text-sm">calendar_today</span>
                                    <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                                </div>

                                <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                                    <span class="material-symbols-outlined text-sm">inventory_2</span>
                                    <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                                </div>

                                <div v-if="getBannerByCampaign(campaign.id)" class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                                    <span class="material-symbols-outlined text-sm">image</span>
                                    <span>Có banner liên kết</span>
                                </div>

                                <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                                    <button 
                                        @click="toggleCampaignStatus(campaign)" 
                                        class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                        :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                                    >
                                        {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                                    </button>
                                </div>
                            </div>
                        </div>

                        <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                            <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                            <p class="text-lg font-medium">Không có chiến dịch nào</p>
                            <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                        </div>
                    </div>

                    <!-- ==================== VOUCHERS LIST ==================== -->
                    <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                        <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                            <div class="flex justify-between items-start">
                                <div>
                                    <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                        {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                                    </span>
                                    <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                                </div>
                                <div class="flex gap-1">
                                    <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                                    <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                                </div>
                            </div>

                            <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                            <div class="grid grid-cols-2 gap-2 text-sm">
                                <span class="flex items-center gap-1">
                                    <span class="text-gray-500">🎯</span>
                                    <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                                </span>
                                <span class="flex items-center gap-1">
                                    <span class="text-gray-500">💰</span>
                                    <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                                </span>
                                <span class="flex items-center gap-1">
                                    <span class="text-gray-500">📅</span>
                                    <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                                </span>
                                <span class="flex items-center gap-1">
                                    <span class="text-gray-500">📊</span>
                                    <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                                </span>
                            </div>

                            <div class="mt-3 pt-3 border-t border-gray-100">
                                <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                                    <div 
                                        class="h-full bg-orange-500 rounded-full transition-all"
                                        :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                                    ></div>
                                </div>
                            </div>

                            <div class="mt-3 pt-2 flex justify-end">
                                <button 
                                    @click="toggleVoucher(voucher)"
                                    :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                                    class="text-xs px-3 py-1 rounded-full transition-colors"
                                >
                                    {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                                </button>
                            </div>
                        </div>

                        <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                            <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                            <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                            <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                        </div>
                    </div>

                    <!-- ==================== PRE-ORDERS LIST ==================== -->
                    <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                        <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                            <div class="flex justify-between items-start">
                                <div>
                                    <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                        {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                                    </span>
                                    <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                                    <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                                </div>
                                <div class="flex gap-1">
                                    <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                                    <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                                </div>
                            </div>

                            <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                                <div class="flex justify-between items-center mb-2">
                                    <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                                    <span class="text-sm font-bold text-blue-600">
                                        Giảm hiện tại: 
                                        {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                                    </span>
                                </div>

                                <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                                    <div 
                                        class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                        :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                                    ></div>
                                </div>

                                <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                                    <div v-for="tier in preorder.tiers" :key="tier.from" 
                                        class="text-center p-2 bg-white rounded border"
                                        :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                                    >
                                        <div class="font-bold">{{ tier.discount }}%</div>
                                        <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                                    </div>
                                </div>
                            </div>

                            <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                                <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                                <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                            </div>

                            <div class="mt-3 pt-2 flex justify-end">
                                <button 
                                    @click="toggleVoucher(preorder)"
                                    :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                                    class="text-xs px-3 py-1 rounded-full transition-colors"
                                >
                                    {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                                </button>
                            </div>
                        </div>

                        <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                            <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                            <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                            <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                        </div>
                    </div>
                </div>

                <!-- ==================== SIDEBAR BANNER (2/12) ==================== -->
                <div class="lg:col-span-2">
                    <div class="bg-white rounded-xl border border-gray-200 p-4 sticky top-4">
                        <div class="flex items-center justify-between mb-3">
                            <h3 class="font-semibold text-gray-700 text-sm">📷 Banner có sẵn</h3>
                            <span class="text-xs bg-gray-100 px-2 py-0.5 rounded-full text-gray-500">{{ availableBanners.length }}</span>
                        </div>
                        
                        <div v-if="availableBanners.length > 0" class="space-y-3 max-h-[600px] overflow-y-auto">
                            <div v-for="banner in availableBanners" :key="banner.id" 
                                class="bg-gray-50 rounded-lg p-2 border border-gray-200 hover:border-orange-300 transition-colors cursor-pointer"
                                @click="$emit('select-banner', banner)"
                            >
                                <img 
                                    v-if="banner.image" 
                                    :src="banner.image" 
                                    :alt="banner.campaign?.name || 'Banner'"
                                    class="w-full h-20 object-cover rounded-lg"
                                    @error="banner.image = null"
                                >
                                <div v-else class="w-full h-20 bg-gray-200 rounded-lg flex items-center justify-center text-gray-400">
                                    <span class="material-symbols-outlined text-2xl">image</span>
                                </div>
                                <div class="mt-1">
                                    <p class="text-xs font-medium text-gray-700 truncate">{{ banner.campaign?.name || 'Banner #' + banner.id }}</p>
                                    <div class="flex items-center justify-between mt-0.5">
                                        <span class="text-[10px] text-gray-400">Thứ tự: {{ banner.order || 0 }}</span>
                                        <span class="text-[10px] px-1.5 py-0.5 rounded-full" :class="banner.status === 1 ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                            {{ banner.status === 1 ? '✅' : '⛔' }}
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div v-else class="text-center py-6 text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">image</span>
                            <p class="text-xs">Chưa có banner nào</p>
                            <p class="text-[10px] mt-1">Tạo banner tại trang quản lý banner</p>
                        </div>
                        
                        <div class="mt-3 pt-3 border-t border-gray-200">
                            <a href="/admin/banners" class="text-xs text-orange-600 hover:text-orange-700 font-medium flex items-center justify-center gap-1">
                                <span>Quản lý banner</span>
                                <span class="material-symbols-outlined text-xs">arrow_forward</span>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="campaignForm.startDate || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner</label>
                        <div class="flex gap-3">
                            <select 
                                v-model="campaignForm.banner_id" 
                                class="flex-1 border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                @change="onBannerSelect"
                            >
                                <option :value="null">-- Chọn banner từ danh sách --</option>
                                <option 
                                    v-for="banner in availableBanners" 
                                    :key="banner.id" 
                                    :value="banner.id"
                                >
                                    {{ banner.campaign?.name || 'Banner #' + banner.id }} 
                                    {{ banner.image ? '📷' : '🚫' }}
                                </option>
                            </select>
                            <button 
                                @click="campaignForm.banner_id = null; campaignForm.banner = ''" 
                                class="px-3 py-2 bg-gray-200 hover:bg-gray-300 rounded-lg text-sm transition-colors"
                                title="Xóa chọn banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                        
                        <div v-if="selectedBanner && selectedBanner.image" class="mt-2 p-2 border border-gray-200 rounded-lg flex items-center gap-3">
                            <img 
                                :src="selectedBanner.image" 
                                class="h-16 w-24 object-cover rounded border"
                                @error="selectedBanner.image = null"
                                alt="Banner preview"
                            >
                            <div class="flex-1">
                                <p class="text-sm font-medium">{{ selectedBanner.campaign?.name || 'Banner #' + selectedBanner.id }}</p>
                                <p class="text-xs text-gray-500 truncate">{{ selectedBanner.image }}</p>
                                <p class="text-xs text-gray-400">Trạng thái: {{ selectedBanner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}</p>
                            </div>
                            <button 
                                @click="campaignForm.banner_id = null; campaignForm.banner = ''" 
                                class="text-red-500 hover:text-red-700 text-sm"
                            >
                                ✕
                            </button>
                        </div>
                        
                        <p class="text-xs text-gray-500 mt-1">Chọn banner đã tạo từ trang Quản lý Banner</p>
                        
                        <div class="mt-2">
                            <label class="text-xs text-gray-500">Hoặc nhập URL trực tiếp</label>
                            <input 
                                v-model="campaignForm.banner" 
                                type="text" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 mt-1"
                                placeholder="https://example.com/banner.jpg"
                                @input="onBannerUrlInput"
                            >
                        </div>

                        <div v-if="campaignForm.banner && !selectedBanner" class="mt-2 relative">
                            <img 
                                :src="campaignForm.banner" 
                                class="h-32 w-full object-cover rounded-lg border"
                                @error="campaignForm.banner = ''"
                                alt="Banner preview"
                            >
                            <button 
                                @click="campaignForm.banner = ''; campaignForm.banner_id = null" 
                                class="absolute top-2 right-2 bg-red-500 text-white rounded-full p-1 hover:bg-red-600 transition-colors"
                                title="Xóa banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%)</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Sản phẩm Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể trước khi tạo chiến dịch</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm được chọn</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm *</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner</label>
                        <div class="flex gap-3">
                            <select 
                                v-model="preorderForm.banner_id" 
                                class="flex-1 border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                                @change="onPreorderBannerSelect"
                            >
                                <option :value="null">-- Chọn banner từ danh sách --</option>
                                <option 
                                    v-for="banner in availableBanners" 
                                    :key="banner.id" 
                                    :value="banner.id"
                                >
                                    {{ banner.campaign?.name || 'Banner #' + banner.id }} 
                                    {{ banner.image ? '📷' : '🚫' }}
                                </option>
                            </select>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="px-3 py-2 bg-gray-200 hover:bg-gray-300 rounded-lg text-sm transition-colors"
                                title="Xóa chọn banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                        
                        <div v-if="selectedPreorderBanner && selectedPreorderBanner.image" class="mt-2 p-2 border border-gray-200 rounded-lg flex items-center gap-3">
                            <img 
                                :src="selectedPreorderBanner.image" 
                                class="h-16 w-24 object-cover rounded border"
                                @error="selectedPreorderBanner.image = null"
                                alt="Banner preview"
                            >
                            <div class="flex-1">
                                <p class="text-sm font-medium">{{ selectedPreorderBanner.campaign?.name || 'Banner #' + selectedPreorderBanner.id }}</p>
                                <p class="text-xs text-gray-500 truncate">{{ selectedPreorderBanner.image }}</p>
                                <p class="text-xs text-gray-400">Trạng thái: {{ selectedPreorderBanner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}</p>
                            </div>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="text-red-500 hover:text-red-700 text-sm"
                            >
                                ✕
                            </button>
                        </div>
                        
                        <p class="text-xs text-gray-500 mt-1">Chọn banner đã tạo từ trang Quản lý Banner</p>
                        
                        <div class="mt-2">
                            <label class="text-xs text-gray-500">Hoặc nhập URL trực tiếp</label>
                            <input 
                                v-model="preorderForm.banner" 
                                type="text" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20 mt-1"
                                placeholder="https://example.com/banner.jpg"
                                @input="onPreorderBannerUrlInput"
                            >
                        </div>

                        <div v-if="preorderForm.banner && !selectedPreorderBanner" class="mt-2 relative">
                            <img 
                                :src="preorderForm.banner" 
                                class="h-32 w-full object-cover rounded-lg border"
                                @error="preorderForm.banner = ''"
                                alt="Banner preview"
                            >
                            <button 
                                @click="preorderForm.banner = ''; preorderForm.banner_id = null" 
                                class="absolute top-2 right-2 bg-red-500 text-white rounded-full p-1 hover:bg-red-600 transition-colors"
                                title="Xóa banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>

<script setup>
import { ref, onMounted, computed, watch } from 'vue'
import axios from 'axios'
import AdminLayout from '@/Layouts/AdminLayout.vue'
import { Head } from '@inertiajs/vue3'

// Props nhận từ controller
const props = defineProps({
    banners: {
        type: Array,
        default: () => []
    },
    campaigns: {
        type: Array,
        default: () => []
    }
})

// Pagination - 5 items per page
const currentPage = ref(1)
const perPage = ref(5)

// Search
const search = ref('')

// Lọc banners theo tên chiến dịch hoặc link
const filteredBanners = computed(() => {
    if (!banners.value || banners.value.length === 0) return []
    if (!search.value) return banners.value
    
    const keyword = search.value.toLowerCase().trim()
    return banners.value.filter(banner => {
        const campaignName = (banner.campaign?.name || '').toLowerCase()
        const link = (banner.link || '').toLowerCase()
        return campaignName.includes(keyword) || link.includes(keyword)
    })
})

// Sắp xếp banners theo order
const sortedBanners = computed(() => {
    return [...filteredBanners.value].sort((a, b) => (a.order || 0) - (b.order || 0))
})

// Pagination
const paginatedBanners = computed(() => {
    const start = (currentPage.value - 1) * perPage.value
    const end = start + perPage.value
    return sortedBanners.value.slice(start, end)
})

const totalPages = computed(() => {
    return Math.ceil(sortedBanners.value.length / perPage.value)
})

// Hiển thị số trang (tối đa 5 trang)
const displayedPages = computed(() => {
    const total = totalPages.value
    const current = currentPage.value
    const maxDisplay = 5
    
    if (total <= maxDisplay) {
        return Array.from({ length: total }, (_, i) => i + 1)
    }
    
    let start = Math.max(1, current - 2)
    let end = Math.min(total, start + maxDisplay - 1)
    
    if (end - start < maxDisplay - 1) {
        start = Math.max(1, end - maxDisplay + 1)
    }
    
    return Array.from({ length: end - start + 1 }, (_, i) => start + i)
})

// Reset về trang 1 khi tìm kiếm
watch(search, () => {
    currentPage.value = 1
})

const banners = ref(props.banners || [])
const showModal = ref(false)
const showDeleteModal = ref(false)
const isEdit = ref(false)
const selectedBanner = ref(null)
const isLoading = ref(false)
const isSaving = ref(false)
const errorMessage = ref('')
const fileError = ref('')
const uploadSuccess = ref(false)

// Chọn phương thức nhập ảnh: 'url' hoặc 'file'
const imageInputMode = ref('url')
const selectedFile = ref(null)
const imagePreviewUrl = ref('')

const form = ref({
    id: null,
    campaign_id: '',
    image: '',
    link: '',
    status: 1,
    order: 0
})

// Xem trước ảnh
const imagePreview = computed(() => {
    if (imagePreviewUrl.value) return imagePreviewUrl.value
    if (form.value.image) return form.value.image
    return null
})

// Format date
const formatDate = (date) => {
    if (!date) return '---'
    const d = new Date(date)
    return d.toLocaleDateString('vi-VN')
}

const fetchBanners = async () => {
    if (isLoading.value) return
    isLoading.value = true
    try {
        const response = await axios.get('/admin/banners/data')
        if (response.data && Array.isArray(response.data)) {
            banners.value = response.data
        } else {
            banners.value = []
        }
    } catch (error) {
        console.error('Lỗi lấy danh sách banner:', error)
        banners.value = []
    } finally {
        isLoading.value = false
    }
}

const openCreateModal = () => {
    isEdit.value = false
    form.value = { 
        id: null, 
        campaign_id: '', 
        image: '', 
        link: '', 
        status: 1, 
        order: 0 
    }
    selectedFile.value = null
    imagePreviewUrl.value = ''
    imageInputMode.value = 'url'
    errorMessage.value = ''
    fileError.value = ''
    uploadSuccess.value = false
    showModal.value = true
}

const openEditModal = (banner) => {
    isEdit.value = true
    form.value = { 
        id: banner.id,
        campaign_id: banner.campaign_id || '', 
        image: banner.image || '', 
        link: banner.link || '', 
        status: banner.status !== undefined ? banner.status : 1, 
        order: banner.order || 0 
    }
    selectedFile.value = null
    imagePreviewUrl.value = ''
    imageInputMode.value = 'url'
    errorMessage.value = ''
    fileError.value = ''
    uploadSuccess.value = false
    showModal.value = true
}

// Xử lý khi chọn file
const handleFileChange = (event) => {
    const file = event.target.files[0]
    fileError.value = ''
    if (!file) {
        return
    }
    // Kiểm tra định dạng file
    if (!file.type.startsWith('image/')) {
        fileError.value = 'Vui lòng chọn file ảnh (jpg, png, gif, svg, webp)'
        event.target.value = ''
        return
    }
    // Kiểm tra kích thước (2MB)
    if (file.size > 2 * 1024 * 1024) {
        fileError.value = 'Kích thước ảnh không quá 2MB'
        event.target.value = ''
        return
    }
    selectedFile.value = file
    // Tạo preview
    const reader = new FileReader()
    reader.onload = (e) => { 
        imagePreviewUrl.value = e.target.result 
    }
    reader.readAsDataURL(file)
    // Xóa URL cũ nếu có
    form.value.image = ''
}

const clearFile = () => {
    selectedFile.value = null
    imagePreviewUrl.value = ''
    fileError.value = ''
    if (imageInputMode.value === 'file') {
        const fileInput = document.getElementById('fileInput')
        if (fileInput) fileInput.value = ''
    }
}

const saveBanner = async () => {
    // Validate
    if (!form.value.campaign_id) {
        errorMessage.value = 'Vui lòng chọn chiến dịch'
        return
    }
    
    // Kiểm tra có ảnh không (URL hoặc file)
    if (!form.value.image && !selectedFile.value) {
        errorMessage.value = 'Vui lòng nhập URL ảnh hoặc tải ảnh lên'
        return
    }
    
    if (fileError.value) {
        errorMessage.value = fileError.value
        return
    }

    if (isSaving.value) return
    isSaving.value = true
    errorMessage.value = ''
    uploadSuccess.value = false

    try {
        let response
        
        if (isEdit.value) {
            // Cập nhật banner
            if (selectedFile.value) {
                // Có file mới -> upload
                const formData = new FormData()
                formData.append('_method', 'PUT')
                formData.append('campaign_id', form.value.campaign_id)
                formData.append('link', form.value.link || '')
                formData.append('status', form.value.status)
                formData.append('order', form.value.order || 0)
                formData.append('image_file', selectedFile.value)
                
                response = await axios.post(`/admin/banners/${form.value.id}`, formData, {
                    headers: { 
                        'Content-Type': 'multipart/form-data',
                        'Accept': 'application/json'
                    }
                })
            } else {
                // Không có file mới, chỉ cập nhật thông tin
                const dataToSave = {
                    campaign_id: form.value.campaign_id,
                    link: form.value.link || '',
                    status: form.value.status,
                    order: form.value.order || 0,
                    image: form.value.image || null
                }
                response = await axios.put(`/admin/banners/${form.value.id}`, dataToSave, {
                    headers: { 'Accept': 'application/json' }
                })
            }
            
            if (response.data && response.data.success) {
                // Cập nhật danh sách
                const index = banners.value.findIndex(b => b.id === form.value.id)
                if (index !== -1 && response.data.data) {
                    banners.value[index] = response.data.data
                } else {
                    // Reload để lấy dữ liệu mới
                    await fetchBanners()
                }
                uploadSuccess.value = true
                alert('Cập nhật banner thành công!')
                showModal.value = false
                clearFile()
            } else {
                errorMessage.value = response.data?.message || 'Có lỗi xảy ra khi cập nhật'
            }
        } else {
            // Thêm mới banner
            if (selectedFile.value) {
                // Upload file
                const formData = new FormData()
                formData.append('campaign_id', form.value.campaign_id)
                formData.append('link', form.value.link || '')
                formData.append('status', form.value.status)
                formData.append('order', form.value.order || 0)
                formData.append('image_file', selectedFile.value)
                
                response = await axios.post('/admin/banners', formData, {
                    headers: { 
                        'Content-Type': 'multipart/form-data',
                        'Accept': 'application/json'
                    }
                })
            } else {
                // Dùng URL
                const dataToSave = {
                    campaign_id: form.value.campaign_id,
                    link: form.value.link || '',
                    status: form.value.status,
                    order: form.value.order || 0,
                    image: form.value.image || null
                }
                response = await axios.post('/admin/banners', dataToSave, {
                    headers: { 'Accept': 'application/json' }
                })
            }
            
            if (response.data && response.data.success && response.data.data) {
                banners.value.push(response.data.data)
                uploadSuccess.value = true
                alert('Thêm banner thành công!')
                showModal.value = false
                clearFile()
            } else {
                errorMessage.value = response.data?.message || 'Có lỗi xảy ra khi thêm mới'
            }
        }
    } catch (error) {
        console.error('Lỗi lưu banner:', error)
        if (error.response) {
            errorMessage.value = error.response.data?.message || 'Có lỗi xảy ra'
        } else {
            errorMessage.value = 'Không thể kết nối đến server'
        }
    } finally {
        isSaving.value = false
    }
}

const confirmDelete = (banner) => {
    selectedBanner.value = banner
    errorMessage.value = ''
    showDeleteModal.value = true
}

const deleteBanner = async () => {
    if (!selectedBanner.value) return
    if (isSaving.value) return
    isSaving.value = true
    errorMessage.value = ''
    try {
        const response = await axios.delete(`/admin/banners/${selectedBanner.value.id}`, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            showDeleteModal.value = false
            const index = banners.value.findIndex(b => b.id === selectedBanner.value.id)
            if (index !== -1) {
                banners.value.splice(index, 1)
            }
            selectedBanner.value = null
            alert('Xóa banner thành công!')
        } else {
            errorMessage.value = response.data?.message || 'Có lỗi xảy ra'
        }
    } catch (error) {
        console.error('Lỗi xóa banner:', error)
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra khi xóa'
    } finally {
        isSaving.value = false
    }
}

const toggleStatus = async (banner) => {
    try {
        const newStatus = banner.status === 1 ? 0 : 1
        const response = await axios.patch(`/admin/banners/${banner.id}/status`, { 
            status: newStatus 
        }, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            banner.status = newStatus
        } else {
            alert('Cập nhật trạng thái thất bại')
        }
    } catch (error) {
        console.error('Lỗi cập nhật trạng thái:', error)
        alert('Có lỗi xảy ra')
    }
}

const updateOrder = async (banner, newOrder) => {
    try {
        const response = await axios.patch(`/admin/banners/${banner.id}/order`, { 
            order: newOrder 
        }, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            banner.order = newOrder
            // Sắp xếp lại danh sách
            banners.value = [...banners.value].sort((a, b) => (a.order || 0) - (b.order || 0))
        }
    } catch (error) {
        console.error('Lỗi cập nhật thứ tự:', error)
        alert('Có lỗi xảy ra khi cập nhật thứ tự')
    }
}

const closeModal = () => {
    showModal.value = false
    showDeleteModal.value = false
    selectedBanner.value = null
    form.value = { id: null, campaign_id: '', image: '', link: '', status: 1, order: 0 }
    errorMessage.value = ''
    fileError.value = ''
    isSaving.value = false
    uploadSuccess.value = false
    clearFile()
}

const handleOverlayClick = (e) => {
    if (e.target === e.currentTarget) {
        closeModal()
    }
}

onMounted(() => {
    if (banners.value.length === 0) {
        fetchBanners()
    }
})
</script>

<template>
    <Head title="Quản lý Banner" />
    <AdminLayout>
        <div class="p-4 md:p-8">
            <div class="flex justify-between items-center mb-6">
                <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý Banner</h1>
                <button @click="openCreateModal" class="bg-orange-600 text-white px-5 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors" :disabled="isSaving">
                    <span class="material-symbols-outlined text-lg">add</span>
                    Thêm banner mới
                </button>
            </div>

            <!-- Thanh tìm kiếm -->
            <div class="mb-4">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="search" 
                        type="text" 
                        placeholder="Tìm theo chiến dịch hoặc link..." 
                        class="pl-10 pr-4 py-2 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <div v-if="isLoading && banners.length === 0" class="text-center py-8">
                <div class="inline-block animate-spin rounded-full h-8 w-8 border-4 border-orange-500 border-t-transparent"></div>
                <p class="mt-2 text-gray-500">Đang tải...</p>
            </div>

            <div v-else class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full min-w-[800px]">
                        <thead class="bg-gray-50 border-b border-gray-200">
                            <tr>
                                <th class="text-left p-4 font-semibold text-gray-700 w-16 whitespace-nowrap">STT</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Hình ảnh</th>
                                <!-- Đã chỉnh sửa cột Chiến dịch để hiển thị giống bên phải -->
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap min-w-[150px]">Chiến dịch</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Link</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Trạng thái</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Thứ tự</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Ngày tạo</th>
                                <th class="text-center p-4 font-semibold text-gray-700 w-40 whitespace-nowrap">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr v-for="(banner, index) in paginatedBanners" :key="banner.id" class="border-b border-gray-100 hover:bg-gray-50 transition">
                                <td class="p-4 text-gray-500 text-sm whitespace-nowrap">{{ (currentPage - 1) * perPage + index + 1 }}</td>
                                <td class="p-4">
                                    <img v-if="banner.image" :src="banner.image" class="h-12 w-20 object-cover rounded" :alt="'Banner ' + banner.id" @error="banner.image = null">
                                    <span v-else class="text-gray-400">---</span>
                                </td>
                                <!-- Đã chỉnh sửa cột Chiến dịch: hiển thị tên chiến dịch với style đồng nhất -->
                                <td class="p-4">
                                    <div class="flex items-center gap-2">
                                        <!-- Hiển thị icon hoặc badge chiến dịch nếu có -->
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                            {{ banner.campaign?.name || 'Chưa phân loại' }}
                                        </span>
                                    </div>
                                </td>
                                <td class="p-4 text-gray-500 text-sm max-w-xs truncate">
                                    <a v-if="banner.link" :href="banner.link" target="_blank" class="text-blue-600 hover:underline">
                                        {{ banner.link }}
                                    </a>
                                    <span v-else class="text-gray-400">---</span>
                                </td>
                                <td class="p-4">
                                    <button @click="toggleStatus(banner)" class="px-2 py-1 text-xs rounded-full transition whitespace-nowrap" :class="banner.status === 1 ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'">
                                        {{ banner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}
                                    </button>
                                </td>
                                <td class="p-4">
                                    <div class="flex items-center gap-1">
                                        <input type="number" :value="banner.order || 0" @change="updateOrder(banner, parseInt($event.target.value))" class="w-16 px-1 py-1 border rounded text-center text-sm" min="0">
                                        <span class="text-xs text-gray-400">#</span>
                                    </div>
                                </td>
                                <td class="p-4 text-gray-500 text-sm whitespace-nowrap">{{ formatDate(banner.created_at) }}</td>
                                <td class="p-4 text-center whitespace-nowrap">
                                    <div class="flex items-center justify-center gap-2">
                                        <button @click="openEditModal(banner)" class="px-3 py-1.5 text-xs text-green-600 hover:bg-green-100 rounded-lg transition-colors font-medium" :disabled="isSaving">Sửa</button>
                                        <button @click="confirmDelete(banner)" class="px-3 py-1.5 text-xs text-red-600 hover:bg-red-100 rounded-lg transition-colors font-medium" :disabled="isSaving">Xóa</button>
                                    </div>
                                </td>
                            </tr>
                            <tr v-if="paginatedBanners.length === 0 && !isLoading">
                                <td colspan="8" class="p-8 text-center text-gray-400">Chưa có banner nào</td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <!-- Footer với phân trang căn giữa -->
                <div class="p-4 border-t border-gray-200">
                    <div class="text-center text-sm text-gray-500 mb-3">
                        Hiển thị {{ paginatedBanners.length }} / {{ sortedBanners.length }} banner
                    </div>
                    
                    <div v-if="totalPages > 1" class="flex justify-center items-center gap-2">
                        <button
                            @click="currentPage--"
                            :disabled="currentPage === 1"
                            class="px-3 py-1.5 text-sm border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            ◄
                        </button>
                        
                        <div class="flex gap-1">
                            <button
                                v-for="page in displayedPages"
                                :key="page"
                                @click="currentPage = page"
                                class="px-3.5 py-1.5 text-sm rounded-lg transition-colors font-medium"
                                :class="currentPage === page ? 'bg-orange-600 text-white' : 'border border-gray-300 hover:bg-gray-50'"
                            >
                                {{ page }}
                            </button>
                        </div>
                        
                        <button
                            @click="currentPage++"
                            :disabled="currentPage === totalPages"
                            class="px-3 py-1.5 text-sm border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            ►
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Modal Thêm/Sửa -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50" @click="handleOverlayClick">
            <div class="bg-white rounded-lg w-full max-w-lg p-6 max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">{{ isEdit ? 'Sửa banner' : 'Thêm banner mới' }}</h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Chiến dịch *</label>
                        <select v-model="form.campaign_id" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                            <option value="">-- Chọn chiến dịch --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Ảnh banner *</label>
                        <div class="flex gap-2 border-b pb-2 mb-2">
                            <button type="button" @click="imageInputMode = 'url'" :class="['px-3 py-1 text-sm rounded-full transition-colors', imageInputMode === 'url' ? 'bg-orange-100 text-orange-600' : 'bg-gray-100 hover:bg-gray-200']">🔗 Nhập URL</button>
                            <button type="button" @click="imageInputMode = 'file'" :class="['px-3 py-1 text-sm rounded-full transition-colors', imageInputMode === 'file' ? 'bg-orange-100 text-orange-600' : 'bg-gray-100 hover:bg-gray-200']">📁 Tải ảnh lên</button>
                        </div>
                        <div v-if="imageInputMode === 'url'">
                            <input v-model="form.image" type="text" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" placeholder="https://example.com/banner.jpg" :disabled="isSaving">
                        </div>
                        <div v-else>
                            <input id="fileInput" type="file" accept="image/*" @change="handleFileChange" class="w-full" :disabled="isSaving">
                            <div v-if="fileError" class="text-red-500 text-sm mt-1">{{ fileError }}</div>
                            <button v-if="selectedFile" @click="clearFile" class="text-red-500 text-xs mt-1 hover:underline" type="button">✕ Xóa file đã chọn</button>
                            <p class="text-xs text-gray-400 mt-1">Hỗ trợ JPG, PNG, GIF, SVG, WEBP. Kích thước tối đa 2MB</p>
                        </div>
                        <div v-if="imagePreview" class="mt-2">
                            <p class="text-sm text-gray-600 mb-1">Xem trước:</p>
                            <div class="w-32 h-20 border rounded-lg overflow-hidden bg-gray-100 flex items-center justify-center">
                                <img :src="imagePreview" class="max-w-full max-h-full object-contain" @error="imagePreviewUrl = ''; form.image = ''" alt="Banner preview">
                            </div>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Link (URL)</label>
                        <input v-model="form.link" type="text" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" placeholder="https://example.com" :disabled="isSaving">
                        <p class="text-xs text-gray-400 mt-1">Đường dẫn khi người dùng click vào banner</p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Trạng thái</label>
                        <select v-model="form.status" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                            <option :value="1">Hoạt động</option>
                            <option :value="0">Tạm dừng</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Thứ tự</label>
                        <input v-model.number="form.order" type="number" min="0" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                        <p class="text-xs text-gray-400 mt-1">Số nhỏ hơn hiển thị trước</p>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                    
                    <div v-if="uploadSuccess" class="p-3 bg-green-50 border border-green-200 rounded-lg">
                        <p class="text-sm text-green-600">✅ Lưu thành công!</p>
                    </div>
                </div>
                
                <div class="flex justify-end gap-3 mt-6">
                    <button @click="closeModal" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition" :disabled="isSaving">Hủy</button>
                    <button @click="saveBanner" class="px-4 py-2 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition flex items-center gap-2" :disabled="isSaving || !!fileError">
                        <span v-if="isSaving" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSaving ? 'Đang xử lý...' : 'Lưu' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- Modal Xác nhận xóa -->
        <div v-if="showDeleteModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50" @click="handleOverlayClick">
            <div class="bg-white rounded-lg w-full max-w-md p-6">
                <h3 class="text-xl font-bold mb-4">Xác nhận xóa</h3>
                <p class="text-gray-600">Bạn có chắc muốn xóa banner này?</p>
                <div v-if="errorMessage" class="mt-3 p-3 bg-red-50 border border-red-200 rounded-lg">
                    <p class="text-sm text-red-600">{{ errorMessage }}</p>
                </div>
                <div class="flex justify-end gap-3 mt-6">
                    <button @click="closeModal" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition" :disabled="isSaving">Hủy</button>
                    <button @click="deleteBanner" class="px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition flex items-center gap-2" :disabled="isSaving">
                        <span v-if="isSaving" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSaving ? 'Đang xóa...' : 'Xóa' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 1s linear infinite;
}
</style>

PS D:\LuanVanTotNghiep> ls database\migrations          


    Directory: D:\LuanVanTotNghiep\database\migrations


Mode                 LastWriteTime         Length Name                                                               
----                 -------------         ------ ----                                                               
-a----          6/6/2026   5:56 PM           1473 0001_01_01_000000_create_users_table.php                           
-a----          6/6/2026   5:56 PM            873 0001_01_01_000001_create_cache_table.php                           
-a----         6/26/2026   3:26 PM           1819 0001_01_01_000002_create_jobs_table.php                            
-a----          6/6/2026   5:56 PM            770 2026_06_04_163751_add_columns_to_users_table.php                   
-a----          6/6/2026   5:56 PM            618 2026_06_04_164515_create_categories_table.php                      
-a----          6/6/2026   5:56 PM            563 2026_06_04_164531_create_brands_table.php                          
-a----          6/6/2026   5:56 PM           1065 2026_06_04_164541_create_products_table.php                        
-a----          6/6/2026   5:56 PM            463 2026_06_04_164616_create_colors_table.php                          
-a----         6/26/2026   3:26 PM            886 2026_06_04_164625_create_product_variants_table.php                
-a----          6/6/2026   5:56 PM            608 2026_06_04_164639_create_discounts_table.php                       
-a----          7/7/2026   7:54 AM            974 2026_06_04_164649_create_campaigns_table.php                       
-a----          7/7/2026   7:54 AM            637 2026_06_04_164658_create_campaign_products_table.php               
-a----          7/7/2026   7:54 AM            648 2026_06_04_164706_create_campaign_configs_table.php                
-a----          6/6/2026   5:56 PM           1270 2026_06_04_164719_create_orders_table.php                          
-a----          6/6/2026   5:56 PM            748 2026_06_04_164745_create_order_details_table.php                   
-a----          6/6/2026   5:56 PM            743 2026_06_04_164755_create_payments_table.php                        
-a----          6/6/2026   5:56 PM            677 2026_06_04_164807_create_reviews_table.php                         
-a----          6/6/2026   5:56 PM            789 2026_06_04_164817_create_news_table.php                            
-a----          7/7/2026   8:03 AM            765 2026_06_04_164829_create_banners_table.php                         
-a----          6/6/2026   5:56 PM            919 2026_06_04_164837_create_quote_requests_table.php                  
-a----          6/6/2026   5:56 PM            674 2026_06_04_164848_create_quote_request_details_table.php           
-a----          6/6/2026   5:56 PM            796 2026_06_04_164901_create_logo_print_requests_table.php             
-a----          6/6/2026   5:56 PM            627 2026_06_04_164918_create_chatbot_messages_table.php                
-a----         6/26/2026   3:26 PM           1046 2026_06_07_130856_update_orders_column_type.php                    
-a----          6/7/2026   9:17 PM            511 2026_06_07_131700_rename_type_to_order_code_in_orders_table.php    
-a----          6/9/2026   6:03 PM            528 2026_06_08_143322_change_image_column_to_text_in_categories_table.p
                                                  hp                                                                 
-a----         6/26/2026   3:26 PM            853 2026_06_09_024229_add_slug_to_brands_table.php                     
-a----          6/9/2026   6:03 PM            501 2026_06_09_084119_rename_color_column_to_name_in_colors_table.php  
-a----          6/9/2026   6:03 PM            460 2026_06_09_091352_drop_sizes_table.php                             
-a----         6/26/2026   3:26 PM            580 2026_06_09_150113_create_settings_table.php                        
-a----         6/26/2026   3:26 PM           2444 2026_06_10_163246_add_code_to_colors_table.php                     
-a----         6/26/2026   3:26 PM            860 2026_06_11_fix_color_names.php                                     
-a----         6/20/2026   6:18 PM            625 2026_06_20_111820_add_customer_fields_to_orders_table.php          
-a----         6/27/2026   7:21 PM            821 2026_06_27_020426_update_image_url_to_json_in_products_table.php   
-a----          7/7/2026   2:51 AM           1836 2026_07_06_195039_add_fields_to_campaigns_table.php                
-a----          7/7/2026   7:55 AM           1537 2026_07_06_211358_create_promotions_table.php                      


<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Banner extends Model
{
    use HasFactory;

    protected $fillable = [
        'title', 
        'image', 
        'link', 
        'description',
        'status', 
        'order'
    ];

    protected $casts = [
        'status' => 'boolean',
        'order' => 'integer',
    ];
}

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Campaign extends Model
{
    use HasFactory;

    protected $fillable = [
        'name',
        'type',
        'description',
        'start_time',
        'end_time',
        'status',
        'banner_id',
        'priority',
        'featured'
    ];

    protected $casts = [
        'start_time' => 'datetime',
        'end_time' => 'datetime',
        'featured' => 'boolean',
    ];

    public function productVariants()
    {
        return $this->belongsToMany(ProductVariant::class, 'campaign_products');
    }

    public function configs()
    {
        return $this->hasMany(CampaignConfig::class);
    }

    public function orders()
    {
        return $this->hasMany(Order::class);
    }

    // Quan hệ với Banner
    public function banner()
    {
        return $this->belongsTo(Banner::class);
    }

    // Accessor để lấy URL banner
    public function getBannerUrlAttribute()
    {
        return $this->banner ? $this->banner->image : null;
    }

    // Accessor để lấy thông tin banner
    public function getBannerInfoAttribute()
    {
        if (!$this->banner) return null;
        return [
            'id' => $this->banner->id,
            'title' => $this->banner->title,
            'image' => $this->banner->image,
            'link' => $this->banner->link,
        ];
    }

    public function getNameAttribute()
    {
        if ($this->attributes['name'] ?? false) {
            return $this->attributes['name'];
        }
        if ($this->start_time && $this->end_time) {
            $start = $this->start_time->format('d/m/Y');
            $end = $this->end_time->format('d/m/Y');
            return "Chiến dịch {$start} - {$end}";
        }
        return 'Chiến dịch';
    }

    public function getStartDateAttribute()
    {
        return $this->start_time ? $this->start_time->format('Y-m-d') : null;
    }

    public function getEndDateAttribute()
    {
        return $this->end_time ? $this->end_time->format('Y-m-d') : null;
    }

    public function getDiscountAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->discount_percent . '%' : '0%';
    }

    public function getMinQuantityAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->quantity : 0;
    }

    public function getProductIdsAttribute()
    {
        return $this->productVariants->pluck('id')->toArray();
    }
}

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Promotion extends Model
{
    use HasFactory;

    protected $fillable = [
        'code',
        'type',
        'target_type',
        'discount_type',
        'discount_value',
        'min_order',
        'limit',
        'used',
        'expiry',
        'active',
        'description',
        'product_id',
        'tiers',
        'current_buyers',
        'start_date',
        'end_date',
        'campaign_id'
    ];

    protected $casts = [
        'tiers' => 'array',
        'active' => 'boolean',
        'discount_value' => 'decimal:0',
        'min_order' => 'decimal:0',
        'expiry' => 'date',
        'start_date' => 'date',
        'end_date' => 'date',
    ];

    public function campaign()
    {
        return $this->belongsTo(Campaign::class);
    }

    public function product()
    {
        return $this->belongsTo(Product::class);
    }
}

<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Storage;
use Illuminate\Support\Facades\Log;

class BannerController extends Controller
{
    public function index()
    {
        return Inertia::render('Admin/Banners', [
            'banners' => Banner::orderBy('order', 'asc')->get()
        ]);
    }

    public function getBanners()
    {
        return response()->json(Banner::orderBy('order', 'asc')->get());
    }

    public function store(Request $request)
    {
        try {
            Log::info('Banner store request:', $request->all());

            $rules = [
                'title' => 'nullable|string|max:255',
                'image' => 'nullable|url',
                'link' => 'nullable|string|max:255',
                'description' => 'nullable|string',
                'status' => 'boolean',
                'order' => 'nullable|integer|min:0'
            ];

            if ($request->hasFile('image_file')) {
                $rules['image_file'] = 'image|max:2048';
            }

            $validated = $request->validate($rules);

            $data = [
                'title' => $validated['title'] ?? 'Banner ' . now()->format('d/m/Y'),
                'link' => $validated['link'] ?? null,
                'description' => $validated['description'] ?? null,
                'status' => $validated['status'] ?? true,
            ];
            
            // Xử lý ảnh
            if ($request->hasFile('image_file')) {
                try {
                    $path = $request->file('image_file')->store('banners', 'public');
                    $data['image'] = '/storage/' . $path;
                } catch (\Exception $e) {
                    Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                    return response()->json([
                        'success' => false,
                        'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                    ], 500);
                }
            } elseif ($request->filled('image')) {
                $data['image'] = $request->image;
            } else {
                return response()->json([
                    'success' => false,
                    'message' => 'Vui lòng chọn ảnh hoặc nhập URL'
                ], 422);
            }

            // Xác định order mới
            $totalBanners = Banner::count();
            $newOrder = $request->input('order', $totalBanners);
            
            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder > $totalBanners) $newOrder = $totalBanners;

            if ($newOrder < $totalBanners) {
                Banner::where('order', '>=', $newOrder)->increment('order');
            }

            $data['order'] = $newOrder;
            $banner = Banner::create($data);

            Log::info('Banner created successfully:', ['id' => $banner->id]);

            return response()->json([
                'success' => true, 
                'data' => $banner
            ]);
            
        } catch (\Illuminate\Validation\ValidationException $e) {
            Log::error('Validation error:', $e->errors());
            return response()->json([
                'success' => false,
                'message' => 'Dữ liệu không hợp lệ',
                'errors' => $e->errors()
            ], 422);
        } catch (\Exception $e) {
            Log::error('Lỗi tạo banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function update(Request $request, $id)
    {
        try {
            Log::info('Banner update request:', ['id' => $id, 'data' => $request->all()]);

            $banner = Banner::findOrFail($id);
            
            $rules = [
                'title' => 'nullable|string|max:255',
                'image' => 'nullable|url',
                'link' => 'nullable|string|max:255',
                'description' => 'nullable|string',
                'status' => 'boolean',
                'order' => 'nullable|integer|min:0'
            ];

            if ($request->hasFile('image_file')) {
                $rules['image_file'] = 'image|max:2048';
            }

            $validated = $request->validate($rules);

            $data = [
                'title' => $validated['title'] ?? $banner->title,
                'link' => $validated['link'] ?? $banner->link,
                'description' => $validated['description'] ?? $banner->description,
                'status' => $validated['status'] ?? $banner->status,
            ];
            
            // Xử lý ảnh
            if ($request->hasFile('image_file')) {
                try {
                    if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                        Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
                    }
                    
                    $path = $request->file('image_file')->store('banners', 'public');
                    $data['image'] = '/storage/' . $path;
                } catch (\Exception $e) {
                    Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                    return response()->json([
                        'success' => false,
                        'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                    ], 500);
                }
            } elseif ($request->filled('image')) {
                $data['image'] = $request->image;
            }

            // Xử lý thay đổi order
            $newOrder = $request->input('order');
            if (!is_null($newOrder) && $newOrder != $banner->order) {
                $oldOrder = $banner->order;
                $totalBanners = Banner::count();

                if ($newOrder < 0) $newOrder = 0;
                if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

                if ($oldOrder < $newOrder) {
                    Banner::where('id', '!=', $id)
                        ->where('order', '>', $oldOrder)
                        ->where('order', '<=', $newOrder)
                        ->decrement('order');
                } else {
                    Banner::where('id', '!=', $id)
                        ->where('order', '>=', $newOrder)
                        ->where('order', '<', $oldOrder)
                        ->increment('order');
                }

                $data['order'] = $newOrder;
            }

            $banner->update($data);

            Log::info('Banner updated successfully:', ['id' => $banner->id]);

            return response()->json([
                'success' => true, 
                'data' => $banner
            ]);
            
        } catch (\Illuminate\Validation\ValidationException $e) {
            Log::error('Validation error:', $e->errors());
            return response()->json([
                'success' => false,
                'message' => 'Dữ liệu không hợp lệ',
                'errors' => $e->errors()
            ], 422);
        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function destroy($id)
    {
        try {
            $banner = Banner::findOrFail($id);
            
            if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
            }
            
            $deletedOrder = $banner->order;
            $banner->delete();

            Banner::where('order', '>', $deletedOrder)->decrement('order');

            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            Log::error('Lỗi xóa banner: ' . $e->getMessage());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function updateStatus(Request $request, $id)
    {
        try {
            $banner = Banner::findOrFail($id);
            $request->validate(['status' => 'required|boolean']);
            $banner->update(['status' => $request->status]);
            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            return response()->json(['success' => false, 'message' => $e->getMessage()], 500);
        }
    }

    public function updateOrder(Request $request, $id)
    {
        try {
            $banner = Banner::findOrFail($id);
            $request->validate(['order' => 'required|integer|min:0']);

            $newOrder = $request->order;
            $oldOrder = $banner->order;

            if ($oldOrder == $newOrder) {
                return response()->json(['success' => true]);
            }

            $totalBanners = Banner::count();
            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

            if ($oldOrder < $newOrder) {
                Banner::where('id', '!=', $id)
                    ->where('order', '>', $oldOrder)
                    ->where('order', '<=', $newOrder)
                    ->decrement('order');
            } else {
                Banner::where('id', '!=', $id)
                    ->where('order', '>=', $newOrder)
                    ->where('order', '<', $oldOrder)
                    ->increment('order');
            }

            $banner->update(['order' => $newOrder]);

            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            return response()->json(['success' => false, 'message' => $e->getMessage()], 500);
        }
    }
}

<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Promotion;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns - load banner
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color', 'banner'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'banner' => $campaign->banner ? $campaign->banner->image : '',
                        'banner_id' => $campaign->banner_id ?? null,
                        'banner_title' => $campaign->banner ? $campaign->banner->title : null,
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                        'productVariants' => $campaign->productVariants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'name' => $variant->product->name ?? 'Sản phẩm',
                                'color' => $variant->color->name ?? 'Không màu',
                                'price' => $variant->price ?? 0,
                            ];
                        }),
                    ];
                });

            // Lấy promotions (vouchers và preorder)
            $promotions = Promotion::with(['campaign', 'product'])->latest()->get();

            // Lấy tất cả banners - KHÔNG liên kết với campaign
            $banners = Banner::orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                ];
            });

            // Lấy tất cả products (cho voucher)
            $products = Product::with(['variants.color', 'category', 'brand'])->latest()->get();

            // Lấy product variants (cho campaign)
            $productVariants = ProductVariant::with(['product', 'color'])->latest()->get();

            // Lấy chỉ sản phẩm pre-order (cho pre-order)
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color', 'category', 'brand'])
                ->latest()
                ->get();

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => $promotions,
                'banners' => $banners,
                'products' => $products,
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'banner_id' => 'nullable|exists:banners,id',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'banner_id' => $validated['banner_id'] ?? null,
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            if (!empty($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'banner_id' => 'nullable|exists:banners,id',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'banner_id' => $validated['banner_id'] ?? $campaign->banner_id,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== VOUCHER METHODS ====================

    public function storePromotion(Request $request)
    {
        try {
            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper($validated['code']),
                'type' => 'voucher',
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'used' => 0,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePromotion(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code,' . $id,
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function togglePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->update(['active' => !$promotion->active]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'type' => 'preorder_tier',
                'target_type' => 'preorder',
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
                'discount_value' => 0,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePreorder($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }
}


## Assistant:
07/07/2026 08:45

Tôi thấy bạn muốn thay đổi cấu trúc để một chiến dịch có thể có nhiều banner. Dưới đây là các thay đổi cần thực hiện:

## 1. Migration để thêm `campaign_id` vào bảng `banners`

```php
<?php
// database/migrations/2026_07_07_000000_add_campaign_id_to_banners_table.php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up()
    {
        Schema::table('banners', function (Blueprint $table) {
            $table->foreignId('campaign_id')->nullable()->constrained('campaigns')->onDelete('cascade');
            $table->index('campaign_id');
        });
    }

    public function down()
    {
        Schema::table('banners', function (Blueprint $table) {
            $table->dropForeign(['campaign_id']);
            $table->dropIndex(['campaign_id']);
            $table->dropColumn('campaign_id');
        });
    }
};
```

## 2. Cập nhật Model `Banner`

```php
<?php
// app/Models/Banner.php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Banner extends Model
{
    use HasFactory;

    protected $fillable = [
        'title', 
        'image', 
        'link', 
        'description',
        'status', 
        'order',
        'campaign_id' // Thêm campaign_id
    ];

    protected $casts = [
        'status' => 'boolean',
        'order' => 'integer',
    ];

    // Quan hệ với Campaign
    public function campaign()
    {
        return $this->belongsTo(Campaign::class);
    }
}
```

## 3. Cập nhật Model `Campaign`

```php
<?php
// app/Models/Campaign.php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Campaign extends Model
{
    use HasFactory;

    protected $fillable = [
        'name',
        'type',
        'description',
        'start_time',
        'end_time',
        'status',
        // 'banner_id', // XÓA dòng này - không còn liên kết 1-1 với banner
        'priority',
        'featured'
    ];

    protected $casts = [
        'start_time' => 'datetime',
        'end_time' => 'datetime',
        'featured' => 'boolean',
    ];

    public function productVariants()
    {
        return $this->belongsToMany(ProductVariant::class, 'campaign_products');
    }

    public function configs()
    {
        return $this->hasMany(CampaignConfig::class);
    }

    public function orders()
    {
        return $this->hasMany(Order::class);
    }

    // Quan hệ với Banner - MỘT CHIẾN DỊCH CÓ NHIỀU BANNER
    public function banners()
    {
        return $this->hasMany(Banner::class);
    }

    // Accessor để lấy danh sách URL banner
    public function getBannerUrlsAttribute()
    {
        return $this->banners()->where('status', 1)->orderBy('order')->pluck('image')->toArray();
    }

    // Accessor để lấy thông tin banner
    public function getBannerInfoAttribute()
    {
        return $this->banners()->where('status', 1)->orderBy('order')->get()->map(function ($banner) {
            return [
                'id' => $banner->id,
                'title' => $banner->title,
                'image' => $banner->image,
                'link' => $banner->link,
            ];
        });
    }

    public function getNameAttribute()
    {
        if ($this->attributes['name'] ?? false) {
            return $this->attributes['name'];
        }
        if ($this->start_time && $this->end_time) {
            $start = $this->start_time->format('d/m/Y');
            $end = $this->end_time->format('d/m/Y');
            return "Chiến dịch {$start} - {$end}";
        }
        return 'Chiến dịch';
    }

    public function getStartDateAttribute()
    {
        return $this->start_time ? $this->start_time->format('Y-m-d') : null;
    }

    public function getEndDateAttribute()
    {
        return $this->end_time ? $this->end_time->format('Y-m-d') : null;
    }

    public function getDiscountAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->discount_percent . '%' : '0%';
    }

    public function getMinQuantityAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->quantity : 0;
    }

    public function getProductIdsAttribute()
    {
        return $this->productVariants->pluck('id')->toArray();
    }
}
```

## 4. Cập nhật `PromotionController` - `index()` method

```php
public function index()
{
    try {
        // Lấy campaigns - KHÔNG load banner_id nữa
        $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
            ->latest()
            ->get()
            ->map(function ($campaign) {
                $config = $campaign->configs->first();
                // Lấy danh sách banner của chiến dịch
                $banners = $campaign->banners()->where('status', 1)->orderBy('order')->get();
                
                return [
                    'id' => $campaign->id,
                    'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                    'type' => $campaign->type ?? 'seasonal',
                    'description' => $campaign->description ?? '',
                    'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                    'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                    'status' => $campaign->status ?? 'scheduled',
                    // Thay đổi: lấy banner đầu tiên làm ảnh đại diện
                    'banner' => $banners->first() ? $banners->first()->image : '',
                    'banners' => $banners->map(function ($banner) {
                        return [
                            'id' => $banner->id,
                            'title' => $banner->title,
                            'image' => $banner->image,
                            'link' => $banner->link,
                            'order' => $banner->order,
                        ];
                    }),
                    'priority' => $campaign->priority ?? 0,
                    'featured' => $campaign->featured ?? false,
                    'quantity' => $config ? (int) $config->quantity : 0,
                    'discountPercent' => $config ? (float) $config->discount_percent : 0,
                    'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                    'products' => $campaign->productVariants->pluck('id')->toArray(),
                    'productVariants' => $campaign->productVariants->map(function ($variant) {
                        return [
                            'id' => $variant->id,
                            'name' => $variant->product->name ?? 'Sản phẩm',
                            'color' => $variant->color->name ?? 'Không màu',
                            'price' => $variant->price ?? 0,
                        ];
                    }),
                ];
            });

        // Lấy tất cả banners - HIỂN THỊ CẢ CAMPAIGN_ID
        $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
            return [
                'id' => $banner->id,
                'title' => $banner->title ?? 'Banner #' . $banner->id,
                'image' => $banner->image,
                'link' => $banner->link,
                'description' => $banner->description,
                'status' => $banner->status ? 1 : 0,
                'order' => $banner->order,
                'campaign_id' => $banner->campaign_id,
                'campaign' => $banner->campaign ? [
                    'id' => $banner->campaign->id,
                    'name' => $banner->campaign->name,
                ] : null,
            ];
        });

        // ... phần còn lại giữ nguyên
    }
}
```

## 5. Cập nhật `PromotionController` - `storeCampaign()` và `updateCampaign()`

```php
public function storeCampaign(Request $request)
{
    try {
        DB::beginTransaction();

        $validated = $request->validate([
            'name' => 'nullable|string|max:255',
            'type' => 'nullable|string|max:50',
            'description' => 'nullable|string',
            'startDate' => 'nullable|date',
            'endDate' => 'nullable|date|after:startDate',
            'status' => 'nullable|in:scheduled,active,ended',
            // XÓA 'banner_id' - không cần
            'priority' => 'nullable|integer|min:0',
            'featured' => 'boolean',
            'quantity' => 'nullable|integer|min:0',
            'discountPercent' => 'nullable|numeric|min:0|max:100',
            'products' => 'nullable|array',
            'banner_ids' => 'nullable|array', // THÊM: danh sách ID banner
            'banner_ids.*' => 'exists:banners,id',
        ]);

        $campaign = Campaign::create([
            'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
            'type' => $validated['type'] ?? 'seasonal',
            'description' => $validated['description'] ?? '',
            'start_time' => $validated['startDate'] ?? null,
            'end_time' => $validated['endDate'] ?? null,
            'status' => $validated['status'] ?? 'scheduled',
            // XÓA 'banner_id'
            'priority' => $validated['priority'] ?? 0,
            'featured' => $validated['featured'] ?? false,
        ]);

        // Gán banner cho chiến dịch
        if (!empty($validated['banner_ids'])) {
            Banner::whereIn('id', $validated['banner_ids'])->update(['campaign_id' => $campaign->id]);
        }

        // ... phần còn lại giữ nguyên
    }
}

public function updateCampaign(Request $request, $id)
{
    try {
        DB::beginTransaction();

        $campaign = Campaign::findOrFail($id);

        $validated = $request->validate([
            'name' => 'nullable|string|max:255',
            'type' => 'nullable|string|max:50',
            'description' => 'nullable|string',
            'startDate' => 'nullable|date',
            'endDate' => 'nullable|date|after:startDate',
            'status' => 'nullable|in:scheduled,active,ended',
            // XÓA 'banner_id'
            'priority' => 'nullable|integer|min:0',
            'featured' => 'boolean',
            'quantity' => 'nullable|integer|min:0',
            'discountPercent' => 'nullable|numeric|min:0|max:100',
            'products' => 'nullable|array',
            'banner_ids' => 'nullable|array',
            'banner_ids.*' => 'exists:banners,id',
        ]);

        $campaign->update([
            'name' => $validated['name'] ?? $campaign->name,
            'type' => $validated['type'] ?? $campaign->type,
            'description' => $validated['description'] ?? $campaign->description,
            'start_time' => $validated['startDate'] ?? $campaign->start_time,
            'end_time' => $validated['endDate'] ?? $campaign->end_time,
            'status' => $validated['status'] ?? $campaign->status,
            // XÓA 'banner_id'
            'priority' => $validated['priority'] ?? $campaign->priority,
            'featured' => $validated['featured'] ?? $campaign->featured,
        ]);

        // Cập nhật banner cho chiến dịch
        // 1. Xóa tất cả banner cũ của chiến dịch này
        Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
        
        // 2. Gán banner mới
        if (!empty($validated['banner_ids'])) {
            Banner::whereIn('id', $validated['banner_ids'])->update(['campaign_id' => $campaign->id]);
        }

        // ... phần còn lại giữ nguyên
    }
}
```

## 6. Cập nhật `deleteCampaign()` để xóa liên kết banner

```php
public function deleteCampaign($id)
{
    try {
        DB::beginTransaction();
        
        $campaign = Campaign::findOrFail($id);
        
        // Giải phóng banner khỏi chiến dịch
        Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
        
        $campaign->configs()->delete();
        $campaign->productVariants()->detach();
        $campaign->delete();

        DB::commit();

        return redirect()->route('admin.promotions.index')->with([
            'success' => true,
            'message' => 'Xóa chiến dịch thành công!'
        ]);

    } catch (\Exception $e) {
        DB::rollBack();
        Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
        return redirect()->back()->with([
            'success' => false,
            'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
        ]);
    }
}
```

## 7. Cập nhật Vue component - Phần campaign form

Trong file `Promotions.vue`, cập nhật:

```vue
<script setup>
// Thay đổi campaignForm
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    // XÓA: banner: '', banner_id: null
    banner_ids: [], // THAY BẰNG mảng banner_ids
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Mở modal
const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        // Lấy danh sách banner_ids từ campaign
        const bannerIds = campaign.banners ? campaign.banners.map(b => b.id) : [];
        
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            banner_ids: bannerIds, // Mảng các ID banner
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            banner_ids: [],
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

// Hàm toggle chọn banner
const toggleBanner = (bannerId) => {
    const index = campaignForm.value.banner_ids.indexOf(bannerId);
    if (index > -1) {
        campaignForm.value.banner_ids.splice(index, 1);
    } else {
        campaignForm.value.banner_ids.push(bannerId);
    }
};

const isBannerSelected = (bannerId) => {
    return campaignForm.value.banner_ids.includes(bannerId);
};

// Hàm lấy danh sách banner đã chọn
const selectedBanners = computed(() => {
    return banners.value.filter(b => campaignForm.value.banner_ids.includes(b.id));
});
</script>
```

## 8. Cập nhật Vue template - Phần chọn banner trong modal

```vue
<!-- Trong campaign modal, thay phần "Chọn Banner" bằng -->
<div>
    <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner (chọn nhiều)</label>
    
    <!-- Hiển thị danh sách banner đã chọn -->
    <div v-if="selectedBanners.length > 0" class="mb-3 grid grid-cols-2 gap-2">
        <div v-for="banner in selectedBanners" :key="banner.id" 
             class="relative border border-blue-500 rounded-lg p-2 bg-blue-50">
            <img v-if="banner.image" :src="banner.image" 
                 class="h-16 w-full object-cover rounded"
                 @error="banner.image = null"
                 alt="Banner">
            <div v-else class="h-16 bg-gray-200 rounded flex items-center justify-center text-gray-400">
                <span class="material-symbols-outlined">image</span>
            </div>
            <button @click="toggleBanner(banner.id)" 
                    class="absolute -top-2 -right-2 bg-red-500 text-white rounded-full p-0.5 hover:bg-red-600 transition-colors text-sm">
                ✕
            </button>
            <p class="text-xs text-gray-600 truncate mt-1">{{ banner.title || 'Banner #' + banner.id }}</p>
        </div>
    </div>
    
    <!-- Danh sách banner có sẵn để chọn -->
    <div class="border border-gray-300 rounded-lg p-3 max-h-60 overflow-y-auto">
        <div v-for="banner in availableBanners" :key="banner.id" 
             class="flex items-center gap-3 py-2 hover:bg-gray-50 px-2 rounded border-b border-gray-100 last:border-0">
            <input type="checkbox" 
                   :id="'banner-' + banner.id"
                   :checked="isBannerSelected(banner.id)"
                   @change="toggleBanner(banner.id)"
                   class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500">
            <label :for="'banner-' + banner.id" class="text-sm cursor-pointer flex-1 flex items-center gap-3">
                <img v-if="banner.image" :src="banner.image" 
                     class="h-12 w-16 object-cover rounded border"
                     @error="banner.image = null"
                     alt="Banner">
                <div class="flex-1 min-w-0">
                    <p class="font-medium truncate">{{ banner.title || 'Banner #' + banner.id }}</p>
                    <p class="text-xs text-gray-500 truncate">{{ banner.link || 'Không có link' }}</p>
                    <p class="text-xs text-gray-400">
                        Trạng thái: {{ banner.status === 1 ? '✅ Hoạt động' : '⛔ Tạm dừng' }}
                        <span v-if="banner.campaign_id && banner.campaign_id !== campaignForm.id" 
                              class="text-orange-500 ml-2">(Đã thuộc chiến dịch khác)</span>
                    </p>
                </div>
            </label>
        </div>
        
        <div v-if="availableBanners.length === 0" class="text-center py-4 text-gray-400">
            <span class="material-symbols-outlined text-4xl block mb-2">image</span>
            <p>Không có banner nào</p>
            <p class="text-xs mt-1">Tạo banner tại trang quản lý banner</p>
        </div>
    </div>
    
    <p class="text-xs text-gray-500 mt-1">
        Đã chọn: <span class="font-semibold text-blue-600">{{ campaignForm.banner_ids.length }}</span> banner
    </p>
</div>
```

## 9. Cập nhật `saveCampaign` để gửi `banner_ids`

```javascript
const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            ...campaignForm.value,
            status: status,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : [],
            banner_ids: campaignForm.value.banner_ids || [] // Gửi danh sách banner ID
        };
        
        // Xóa các field không cần thiết
        delete data.banners;
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};
```

## 10. Cập nhật hiển thị banner trong danh sách chiến dịch

```vue
<!-- Trong phần hiển thị campaign card, thay đổi phần hiển thị banner -->
<div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
    <!-- Hiển thị slider nếu có nhiều banner -->
    <div v-if="campaign.banners && campaign.banners.length > 0" class="relative w-full h-full">
        <img 
            v-for="(banner, idx) in campaign.banners" 
            :key="banner.id"
            v-show="bannerIndex === idx"
            :src="banner.image" 
            :alt="campaign.name" 
            class="w-full h-full object-cover"
            @error="banner.image = null"
        >
        <!-- Nút chuyển slider nếu có nhiều banner -->
        <div v-if="campaign.banners.length > 1" class="absolute bottom-2 left-1/2 -translate-x-1/2 flex gap-1">
            <button 
                v-for="(b, idx) in campaign.banners" 
                :key="idx"
                class="w-2 h-2 rounded-full transition-all"
                :class="bannerIndex === idx ? 'bg-white w-4' : 'bg-white/50'"
                @click="bannerIndex = idx"
            ></button>
        </div>
    </div>
    <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
        <span class="material-symbols-outlined text-5xl">campaign</span>
    </div>
    
    <div class="absolute top-3 right-3 flex gap-1">
        <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
            {{ getStatusLabel(campaign.status) }}
        </span>
        <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
    </div>
</div>

<!-- Thêm biến bannerIndex -->
<script>
const bannerIndex = ref(0);
</script>
```

## 11. Cập nhật Banner Controller để hỗ trợ `campaign_id`

```php
<?php
// app/Http/Controllers/Admin/BannerController.php - thêm method lấy banner theo campaign

public function getBannersByCampaign($campaignId)
{
    $banners = Banner::where('campaign_id', $campaignId)
        ->orderBy('order', 'asc')
        ->get();
    
    return response()->json($banners);
}
```

## 12. Thêm route cho API

```php
// routes/web.php
Route::prefix('admin')->middleware(['auth', 'admin'])->group(function () {
    // ... routes khác
    
    // Banner routes
    Route::get('/banners/data', [BannerController::class, 'getBanners'])->name('admin.banners.data');
    Route::get('/banners/campaign/{campaignId}', [BannerController::class, 'getBannersByCampaign'])->name('admin.banners.by-campaign');
    
    // ... rest
});
```

## Tóm tắt các thay đổi:

1. **Migration**: Thêm `campaign_id` vào bảng `banners`, xóa `banner_id` khỏi `campaigns` (hoặc giữ lại nhưng không sử dụng)

2. **Model**:
   - `Campaign`: Thay `banner()` (belongsTo) thành `banners()` (hasMany)
   - `Banner`: Thêm `campaign()` (belongsTo)

3. **Controller**:
   - `PromotionController::index()`: Load banners qua quan hệ `campaign->banners()`
   - `storeCampaign()`/`updateCampaign()`: Nhận và xử lý `banner_ids[]`
   - `deleteCampaign()`: Giải phóng banner khi xóa campaign

4. **Vue Component**:
   - Thay `banner_id` bằng `banner_ids[]`
   - Cho phép chọn nhiều banner bằng checkbox
   - Hiển thị slider banner trong campaign card

5. **Route**: Thêm API lấy banner theo campaign

Với những thay đổi này, bạn sẽ có:
- Một chiến dịch có thể có nhiều banner (0-n)
- Khi chiến dịch hết hạn, banner vẫn tồn tại nhưng không hiển thị (dựa trên status của campaign)
- Bên trang banner hiển thị danh sách campaign để chọn


## User:
07/07/2026 08:57

Đưa code vue của khuyến mãi và banner hoàn chỉnh cho tôi
<?php

use App\Http\Controllers\Admin\DashboardController;
use App\Http\Controllers\Admin\OrderController as AdminOrderController;
use App\Http\Controllers\Admin\ProductController as AdminProductController;
use App\Http\Controllers\Admin\CustomerController as AdminCustomerController;
use App\Http\Controllers\Admin\CustomizeController as AdminCustomizeController;
use App\Http\Controllers\Admin\SettingController;
use App\Http\Controllers\Admin\CategoryController;
use App\Http\Controllers\Admin\ColorController;
use App\Http\Controllers\Admin\BrandController;
use App\Http\Controllers\Admin\BannerController;
use App\Http\Controllers\Admin\NewsController;
use App\Http\Controllers\Admin\PromotionController;
use App\Http\Controllers\PaymentController;
use App\Http\Controllers\OrderController;
use App\Http\Controllers\CartController;
use App\Http\Controllers\ProfileController;
use App\Http\Controllers\CategoryController as WebCategoryController;
use App\Http\Controllers\ProductController as WebProductController;
use App\Http\Controllers\HomeController;
use Illuminate\Foundation\Application;
use Illuminate\Support\Facades\Route;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\File;
use Inertia\Inertia;

// ==================== ROUTE ĐỂ PHỤC VỤ ẢNH ====================
Route::get('/image/{filename}', function ($filename) {
    $path = base_path('image/' . $filename);
    if (!File::exists($path)) {
        abort(404);
    }

    $file = File::get($path);
    $type = File::mimeType($path);

    return response($file, 200)->header('Content-Type', $type);
})->where('filename', '.*');

// ==================== WEB ROUTES (Frontend - Public) ====================

Route::get('/', [HomeController::class, 'index'])->name('home');

Route::get('/tim-kiem', function (Request $request) {
    return Inertia::render('Web/Category', ['search' => $request->get('q')]);
})->name('search');

// Product routes - PUBLIC
Route::get('/san-pham/{id}', [WebProductController::class, 'show'])->name('product.detail');
Route::get('/danh-muc/{slug}', [WebCategoryController::class, 'show'])->name('category');

// Other public frontend routes
Route::get('/mua-si', function () {
    return Inertia::render('Web/Wholesale');
})->name('wholesale');

Route::get('/khuyen-mai', function () {
    return Inertia::render('Web/Promotion');
})->name('promotion');

Route::get('/tuy-chinh', function () {
    return Inertia::render('Web/Customize');
})->name('customize');

// ==================== CART ROUTES (Yêu cầu đăng nhập) ====================

// Cart page
Route::get('/gio-hang', function () {
    return Inertia::render('Web/Cart');
})->name('cart')->middleware('auth');

// ⭐ QUAN TRỌNG: Cart API routes - Thêm middleware 'auth' và 'web'
Route::middleware(['auth'])->prefix('api')->group(function () {
    Route::get('/cart', [CartController::class, 'index']);
    Route::post('/cart/add', [CartController::class, 'add']);
    Route::put('/cart/update', [CartController::class, 'update']);
    Route::delete('/cart/remove/{variantId}', [CartController::class, 'remove']);
    Route::delete('/cart/clear', [CartController::class, 'clear']);
});

// ==================== CHECKOUT ROUTES (Yêu cầu đăng nhập) ====================
Route::middleware(['auth'])->group(function () {
    Route::get('/thanh-toan', [PaymentController::class, 'index'])->name('checkout');
    Route::post('/thanh-toan', [PaymentController::class, 'store'])->name('checkout.store');
    Route::get('/thanh-toan/thanh-cong', [PaymentController::class, 'success'])->name('checkout.success');
});

// ==================== AUTHENTICATED WEB ROUTES ====================
Route::middleware('auth')->group(function () {
    Route::get('/profile', [ProfileController::class, 'edit'])->name('profile.edit');
    Route::patch('/profile', [ProfileController::class, 'update'])->name('profile.update');
    Route::delete('/profile', [ProfileController::class, 'destroy'])->name('profile.destroy');
    Route::get('/don-hang', [OrderController::class, 'history'])->name('orders.history');
    Route::get('/don-hang/{id}', [OrderController::class, 'show'])->name('orders.show');
});

// ==================== ADMIN ROUTES ====================
Route::prefix('admin')->middleware(['auth', 'admin'])->name('admin.')->group(function () {
    // Dashboard
    Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');
    Route::get('/', [DashboardController::class, 'index'])->name('home');
    
    // Orders Management
    Route::prefix('orders')->group(function () {
        Route::get('/{type?}', [AdminOrderController::class, 'index'])
            ->where('type', 'retail|wholesale|preorder')
            ->name('orders.index');
        Route::get('/{id}', [AdminOrderController::class, 'show'])
            ->where('id', '[0-9]+')
            ->name('orders.show');
        Route::put('/{id}/status', [AdminOrderController::class, 'updateStatus'])->name('orders.update-status');
        Route::get('/export', [AdminOrderController::class, 'export'])->name('orders.export');
        Route::get('/export/filtered', [AdminOrderController::class, 'exportWithFilters'])->name('orders.export-filtered');
    });
    
    // Products Management
    Route::prefix('products')->group(function () {
        Route::get('/{type?}', [AdminProductController::class, 'index'])
            ->where('type', 'normal|preorder')
            ->name('products.index');
        Route::post('/', [AdminProductController::class, 'store'])->name('products.store');
        Route::put('/{product}', [AdminProductController::class, 'update'])->name('products.update');
        Route::delete('/{product}', [AdminProductController::class, 'destroy'])->name('products.destroy');
    });
    
    // Categories Management
    Route::get('/categories', [CategoryController::class, 'index'])->name('categories.index');
    Route::get('/categories/create', [CategoryController::class, 'create'])->name('categories.create');
    Route::post('/categories', [CategoryController::class, 'store'])->name('categories.store');
    Route::get('/categories/{category}/edit', [CategoryController::class, 'edit'])->name('categories.edit');
    Route::put('/categories/{category}', [CategoryController::class, 'update'])->name('categories.update');
    Route::delete('/categories/{category}', [CategoryController::class, 'destroy'])->name('categories.destroy');
    
    // Colors Management
    Route::get('/colors', [ColorController::class, 'index'])->name('colors.index');
    Route::get('/colors/data', [ColorController::class, 'getColors'])->name('colors.data');
    Route::post('/colors', [ColorController::class, 'store'])->name('colors.store');
    Route::put('/colors/{id}', [ColorController::class, 'update'])->name('colors.update');
    Route::delete('/colors/{id}', [ColorController::class, 'destroy'])->name('colors.destroy');
        
    // Brands Management
    Route::prefix('brands')->group(function () {
        Route::get('/', [BrandController::class, 'index'])->name('brands.index');
        Route::get('/data', [BrandController::class, 'getBrands'])->name('brands.data');
        Route::post('/', [BrandController::class, 'store'])->name('brands.store');
        Route::put('/{id}', [BrandController::class, 'update'])->name('brands.update');
        Route::delete('/{id}', [BrandController::class, 'destroy'])->name('brands.destroy');
        Route::get('/search', [BrandController::class, 'search'])->name('brands.search');
    });
    
    // Customers Management
    Route::prefix('customers')->group(function () {
        Route::get('/', [AdminCustomerController::class, 'index'])->name('customers.index');
        Route::get('/retail', [AdminCustomerController::class, 'retail'])->name('customers.retail');
        Route::get('/business', [AdminCustomerController::class, 'business'])->name('customers.business');
        Route::get('/{id}', [AdminCustomerController::class, 'show'])->name('customers.show');
        Route::put('/{id}', [AdminCustomerController::class, 'update'])->name('customers.update');
        Route::post('/export', [AdminCustomerController::class, 'export'])->name('customers.export');
    });
    
    // Customize Management
    Route::prefix('customize')->group(function () {
        Route::get('/', [AdminCustomizeController::class, 'index'])->name('customize.index');
        Route::put('/{id}/status', [AdminCustomizeController::class, 'updateStatus'])->name('customize.update-status');
        Route::put('/{id}/approve', [AdminCustomizeController::class, 'approve'])->name('customize.approve');
        Route::post('/send-quote', [AdminCustomizeController::class, 'sendQuote'])->name('customize.send-quote');
    });

        // News Management
    Route::prefix('news')->group(function () {
        Route::get('/', [NewsController::class, 'index'])->name('news.index');
        Route::get('/data', [NewsController::class, 'getNews'])->name('news.data');
        Route::post('/', [NewsController::class, 'store'])->name('news.store');
        Route::put('/{id}', [NewsController::class, 'update'])->name('news.update');
        Route::delete('/{id}', [NewsController::class, 'destroy'])->name('news.destroy');
        Route::patch('/{id}/status', [NewsController::class, 'updateStatus'])->name('news.update-status');
    });

       
    // Banner routes
    Route::get('/banners', [BannerController::class, 'index'])->name('banners.index');
    Route::get('/banners/data', [BannerController::class, 'getBanners'])->name('banners.data');
    Route::post('/banners', [BannerController::class, 'store'])->name('banners.store');
    Route::put('/banners/{id}', [BannerController::class, 'update'])->name('banners.update');
    Route::delete('/banners/{id}', [BannerController::class, 'destroy'])->name('banners.destroy');
    Route::patch('/banners/{id}/status', [BannerController::class, 'updateStatus'])->name('banners.status');
    Route::patch('/banners/{id}/order', [BannerController::class, 'updateOrder'])->name('banners.order');

    // Promotion routes
    Route::get('/promotions', [PromotionController::class, 'index'])->name('promotions.index');
    Route::post('/promotions/campaign', [PromotionController::class, 'storeCampaign'])->name('promotions.campaign.store');
    Route::put('/promotions/campaign/{id}', [PromotionController::class, 'updateCampaign'])->name('promotions.campaign.update');
    Route::delete('/promotions/campaign/{id}', [PromotionController::class, 'deleteCampaign'])->name('promotions.campaign.delete');
    Route::put('/promotions/campaign/{id}/status', [PromotionController::class, 'updateCampaignStatus'])->name('promotions.campaign.status');

    Route::post('/promotions/voucher', [PromotionController::class, 'storePromotion'])->name('promotions.voucher.store');
    Route::put('/promotions/voucher/{id}', [PromotionController::class, 'updatePromotion'])->name('promotions.voucher.update');
    Route::delete('/promotions/voucher/{id}', [PromotionController::class, 'deletePromotion'])->name('promotions.voucher.delete');
    Route::put('/promotions/voucher/{id}/toggle', [PromotionController::class, 'togglePromotion'])->name('promotions.voucher.toggle');

    Route::post('/promotions/preorder', [PromotionController::class, 'storePreorder'])->name('promotions.preorder.store');
    Route::put('/promotions/preorder/{id}', [PromotionController::class, 'updatePreorder'])->name('promotions.preorder.update');
    Route::delete('/promotions/preorder/{id}', [PromotionController::class, 'deletePreorder'])->name('promotions.preorder.delete');

    // Reports
    Route::get('/reports', function () {
        return Inertia::render('Admin/Reports');
    })->name('reports.index');
    
    // Settings
    Route::get('/settings', [SettingController::class, 'index'])->name('settings.index');
    Route::put('/settings/general', [SettingController::class, 'updateGeneral'])->name('settings.updateGeneral');
    Route::put('/settings/password', [SettingController::class, 'changePassword'])->name('settings.changePassword');

    // User Management
    Route::get('/settings/users', [SettingController::class, 'getUsers'])->name('settings.users');
    Route::post('/settings/users', [SettingController::class, 'storeUser'])->name('settings.storeUser');
    Route::put('/settings/users/{id}', [SettingController::class, 'updateUser'])->name('settings.updateUser');
    Route::delete('/settings/users/{id}', [SettingController::class, 'destroyUser'])->name('settings.destroyUser');
    Route::patch('/settings/users/{id}/toggle', [SettingController::class, 'toggleUserStatus'])->name('settings.toggleUser');
});

require __DIR__.'/auth.php';


## Assistant:
07/07/2026 08:57

Tôi sẽ cung cấp code Vue hoàn chỉnh cho 2 file: **Promotions.vue** và **Banners.vue** đã được cập nhật theo yêu cầu.

## 1. File: `Promotions.vue` (Hoàn chỉnh)

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Banner slider index
const bannerIndex = ref(0);

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    banner_ids: [],
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    banner: '',
    banner_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
    }
    
    return 'active';
};

// ==================== BANNER FUNCTIONS ====================

// Lấy danh sách banner có sẵn (chưa thuộc campaign nào hoặc thuộc campaign đang edit)
const availableBanners = computed(() => {
    return banners.value.filter(b => {
        // Nếu banner đang thuộc campaign khác và không phải campaign đang edit thì không hiển thị
        if (b.campaign_id && b.campaign_id !== campaignForm.value.id) {
            return false;
        }
        return b.status === 1;
    });
});

// Lấy danh sách banner đã chọn
const selectedBanners = computed(() => {
    return banners.value.filter(b => campaignForm.value.banner_ids.includes(b.id));
});

// Toggle chọn banner
const toggleBanner = (bannerId) => {
    const index = campaignForm.value.banner_ids.indexOf(bannerId);
    if (index > -1) {
        campaignForm.value.banner_ids.splice(index, 1);
    } else {
        campaignForm.value.banner_ids.push(bannerId);
    }
};

const isBannerSelected = (bannerId) => {
    return campaignForm.value.banner_ids.includes(bannerId);
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    bannerIndex.value = 0;
    
    if (campaign) {
        // Lấy danh sách banner_ids từ campaign
        const bannerIds = campaign.banners ? campaign.banners.map(b => b.id) : [];
        
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            banner_ids: bannerIds,
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            banner_ids: [],
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            banner_ids: campaignForm.value.banner_ids || [],
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value <= 0) {
        errorMessage.value = 'Giá trị giảm giá phải lớn hơn 0';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            banner: preorder.banner || '',
            banner_id: preorder.banner_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            banner: '',
            banner_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});

// Reset banner index khi campaign thay đổi
watch(() => campaignForm.value.id, () => {
    bannerIndex.value = 0;
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- 2 cột: danh sách bên trái và banner bên phải -->
            <div class="grid grid-cols-1 lg:grid-cols-12 gap-6">
                <!-- Cột chính: Danh sách khuyến mãi (10/12) -->
                <div class="lg:col-span-10">
                    <!-- Search -->
                    <div class="mb-6">
                        <div class="relative max-w-md">
                            <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                            <input 
                                v-model="searchQuery" 
                                type="text" 
                                placeholder="Tìm kiếm..." 
                                class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                            >
                        </div>
                    </div>

                    <!-- Main Tabs -->
                    <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                        <button 
                            @click="activeTab = 'campaigns'" 
                            class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                            :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                        >
                            🎯 Chiến dịch
                            <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                        </button>
                        <button 
                            @click="activeTab = 'vouchers'" 
                            class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                            :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                        >
                            🎫 Mã giảm giá
                            <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                        </button>
                        <button 
                            @click="activeTab = 'preorder'" 
                            class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                            :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                        >
                            ⏳ Pre-order
                            <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                        </button>
                    </div>

                    <!-- Status tabs for campaigns -->
                    <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                        <button 
                            v-for="status in statusOptions" 
                            :key="status.value"
                            @click="activeStatusTab = status.value"
                            class="px-4 py-2 text-sm font-medium transition-all"
                            :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                        >
                            {{ status.label }}
                            <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                                {{ campaigns.filter(c => status.value === 'all' || c.status === status.value).length }}
                            </span>
                        </button>
                    </div>

                    <!-- ==================== CAMPAIGNS LIST ==================== -->
                    <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                            <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                                <!-- Hiển thị slider banner nếu có nhiều banner -->
                                <div v-if="campaign.banners && campaign.banners.length > 0" class="relative w-full h-full">
                                    <img 
                                        v-for="(banner, idx) in campaign.banners" 
                                        :key="banner.id"
                                        v-show="bannerIndex === idx"
                                        :src="banner.image" 
                                        :alt="campaign.name" 
                                        class="w-full h-full object-cover"
                                        @error="banner.image = null"
                                    >
                                    <!-- Nút chuyển slider nếu có nhiều banner -->
                                    <div v-if="campaign.banners.length > 1" class="absolute bottom-2 left-1/2 -translate-x-1/2 flex gap-1.5">
                                        <button 
                                            v-for="(b, idx) in campaign.banners" 
                                            :key="idx"
                                            class="h-1.5 rounded-full transition-all"
                                            :class="bannerIndex === idx ? 'bg-white w-4' : 'bg-white/50 w-1.5'"
                                            @click.stop="bannerIndex = idx"
                                        ></button>
                                    </div>
                                    <!-- Nút điều hướng -->
                                    <button v-if="campaign.banners.length > 1" 
                                        @click.stop="bannerIndex = (bannerIndex - 1 + campaign.banners.length) % campaign.banners.length"
                                        class="absolute left-2 top-1/2 -translate-y-1/2 bg-black/30 hover:bg-black/50 text-white rounded-full p-1 transition-colors">
                                        <span class="material-symbols-outlined text-sm">chevron_left</span>
                                    </button>
                                    <button v-if="campaign.banners.length > 1" 
                                        @click.stop="bannerIndex = (bannerIndex + 1) % campaign.banners.length"
                                        class="absolute right-2 top-1/2 -translate-y-1/2 bg-black/30 hover:bg-black/50 text-white rounded-full p-1 transition-colors">
                                        <span class="material-symbols-outlined text-sm">chevron_right</span>
                                    </button>
                                </div>
                                <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                                    <span class="material-symbols-outlined text-5xl">campaign</span>
                                </div>
                                
                                <div class="absolute top-3 right-3 flex gap-1 flex-wrap">
                                    <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                        {{ getStatusLabel(campaign.status) }}
                                    </span>
                                    <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                                    <span v-if="campaign.banners && campaign.banners.length > 0" class="text-xs px-3 py-1 rounded-full bg-blue-100 text-blue-700 font-medium">
                                        📷 {{ campaign.banners.length }}
                                    </span>
                                </div>
                            </div>

                            <div class="p-5">
                                <div class="flex justify-between items-start">
                                    <div class="flex-1">
                                        <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                        <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                                    </div>
                                    <div class="flex gap-1 ml-2">
                                        <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                            <span class="material-symbols-outlined text-sm">edit</span>
                                        </button>
                                        <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                            <span class="material-symbols-outlined text-sm">delete</span>
                                        </button>
                                    </div>
                                </div>

                                <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                                <div class="mt-4 grid grid-cols-2 gap-3">
                                    <div class="bg-orange-50 rounded-lg p-3">
                                        <p class="text-xs text-gray-500">Giảm giá</p>
                                        <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                                    </div>
                                    <div class="bg-blue-50 rounded-lg p-3">
                                        <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                        <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                                    </div>
                                </div>

                                <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                                    <span class="material-symbols-outlined text-sm">calendar_today</span>
                                    <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                                </div>

                                <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                                    <span class="material-symbols-outlined text-sm">inventory_2</span>
                                    <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                                </div>

                                <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                                    <button 
                                        @click="toggleCampaignStatus(campaign)" 
                                        class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                        :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                                    >
                                        {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                                    </button>
                                </div>
                            </div>
                        </div>

                        <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                            <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                            <p class="text-lg font-medium">Không có chiến dịch nào</p>
                            <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                        </div>
                    </div>

                    <!-- ==================== VOUCHERS LIST ==================== -->
                    <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                        <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                            <div class="flex justify-between items-start">
                                <div>
                                    <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                        {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                                    </span>
                                    <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                                </div>
                                <div class="flex gap-1">
                                    <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                                    <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                                </div>
                            </div>

                            <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                            <div class="grid grid-cols-2 gap-2 text-sm">
                                <span class="flex items-center gap-1">
                                    <span class="text-gray-500">🎯</span>
                                    <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                                </span>
                                <span class="flex items-center gap-1">
                                    <span class="text-gray-500">💰</span>
                                    <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                                </span>
                                <span class="flex items-center gap-1">
                                    <span class="text-gray-500">📅</span>
                                    <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                                </span>
                                <span class="flex items-center gap-1">
                                    <span class="text-gray-500">📊</span>
                                    <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                                </span>
                            </div>

                            <div class="mt-3 pt-3 border-t border-gray-100">
                                <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                                    <div 
                                        class="h-full bg-orange-500 rounded-full transition-all"
                                        :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                                    ></div>
                                </div>
                            </div>

                            <div class="mt-3 pt-2 flex justify-end">
                                <button 
                                    @click="toggleVoucher(voucher)"
                                    :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                                    class="text-xs px-3 py-1 rounded-full transition-colors"
                                >
                                    {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                                </button>
                            </div>
                        </div>

                        <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                            <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                            <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                            <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                        </div>
                    </div>

                    <!-- ==================== PRE-ORDERS LIST ==================== -->
                    <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                        <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                            <div class="flex justify-between items-start">
                                <div>
                                    <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                        {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                                    </span>
                                    <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                                    <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                                </div>
                                <div class="flex gap-1">
                                    <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                                    <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                                </div>
                            </div>

                            <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                                <div class="flex justify-between items-center mb-2">
                                    <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                                    <span class="text-sm font-bold text-blue-600">
                                        Giảm hiện tại: 
                                        {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                                    </span>
                                </div>

                                <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                                    <div 
                                        class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                        :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                                    ></div>
                                </div>

                                <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                                    <div v-for="tier in preorder.tiers" :key="tier.from" 
                                        class="text-center p-2 bg-white rounded border"
                                        :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                                    >
                                        <div class="font-bold">{{ tier.discount }}%</div>
                                        <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                                    </div>
                                </div>
                            </div>

                            <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                                <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                                <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                            </div>

                            <div class="mt-3 pt-2 flex justify-end">
                                <button 
                                    @click="toggleVoucher(preorder)"
                                    :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                                    class="text-xs px-3 py-1 rounded-full transition-colors"
                                >
                                    {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                                </button>
                            </div>
                        </div>

                        <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                            <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                            <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                            <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                        </div>
                    </div>
                </div>

                <!-- ==================== SIDEBAR BANNER (2/12) ==================== -->
                <div class="lg:col-span-2">
                    <div class="bg-white rounded-xl border border-gray-200 p-4 sticky top-4">
                        <div class="flex items-center justify-between mb-3">
                            <h3 class="font-semibold text-gray-700 text-sm">📷 Banner có sẵn</h3>
                            <span class="text-xs bg-gray-100 px-2 py-0.5 rounded-full text-gray-500">{{ banners.filter(b => b.status === 1).length }}</span>
                        </div>
                        
                        <div v-if="banners.filter(b => b.status === 1).length > 0" class="space-y-3 max-h-[600px] overflow-y-auto">
                            <div v-for="banner in banners.filter(b => b.status === 1)" :key="banner.id" 
                                class="bg-gray-50 rounded-lg p-2 border border-gray-200 hover:border-orange-300 transition-colors cursor-pointer"
                                @click="$emit('select-banner', banner)"
                            >
                                <img 
                                    v-if="banner.image" 
                                    :src="banner.image" 
                                    :alt="banner.title || 'Banner'"
                                    class="w-full h-20 object-cover rounded-lg"
                                    @error="banner.image = null"
                                >
                                <div v-else class="w-full h-20 bg-gray-200 rounded-lg flex items-center justify-center text-gray-400">
                                    <span class="material-symbols-outlined text-2xl">image</span>
                                </div>
                                <div class="mt-1">
                                    <p class="text-xs font-medium text-gray-700 truncate">{{ banner.title || 'Banner #' + banner.id }}</p>
                                    <div class="flex items-center justify-between mt-0.5">
                                        <span class="text-[10px] text-gray-400">Thứ tự: {{ banner.order || 0 }}</span>
                                        <span v-if="banner.campaign_id" class="text-[10px] px-1.5 py-0.5 rounded-full bg-blue-100 text-blue-700">
                                            🔗 {{ banner.campaign?.name || 'Đã liên kết' }}
                                        </span>
                                        <span v-else class="text-[10px] px-1.5 py-0.5 rounded-full bg-gray-100 text-gray-500">
                                            Chưa liên kết
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div v-else class="text-center py-6 text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">image</span>
                            <p class="text-xs">Chưa có banner nào</p>
                            <p class="text-[10px] mt-1">Tạo banner tại trang quản lý banner</p>
                        </div>
                        
                        <div class="mt-3 pt-3 border-t border-gray-200">
                            <a href="/admin/banners" class="text-xs text-orange-600 hover:text-orange-700 font-medium flex items-center justify-center gap-1">
                                <span>Quản lý banner</span>
                                <span class="material-symbols-outlined text-xs">arrow_forward</span>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="campaignForm.startDate || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <!-- ==================== CHỌN NHIỀU BANNER ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner (chọn nhiều)</label>
                        
                        <!-- Hiển thị danh sách banner đã chọn -->
                        <div v-if="selectedBanners.length > 0" class="mb-3 grid grid-cols-2 gap-2">
                            <div v-for="banner in selectedBanners" :key="banner.id" 
                                 class="relative border border-blue-500 rounded-lg p-2 bg-blue-50">
                                <img v-if="banner.image" :src="banner.image" 
                                     class="h-16 w-full object-cover rounded"
                                     @error="banner.image = null"
                                     alt="Banner">
                                <div v-else class="h-16 bg-gray-200 rounded flex items-center justify-center text-gray-400">
                                    <span class="material-symbols-outlined">image</span>
                                </div>
                                <button @click="toggleBanner(banner.id)" 
                                        class="absolute -top-2 -right-2 bg-red-500 text-white rounded-full p-0.5 hover:bg-red-600 transition-colors text-sm w-5 h-5 flex items-center justify-center">
                                    ✕
                                </button>
                                <p class="text-xs text-gray-600 truncate mt-1">{{ banner.title || 'Banner #' + banner.id }}</p>
                            </div>
                        </div>
                        
                        <!-- Danh sách banner có sẵn để chọn -->
                        <div class="border border-gray-300 rounded-lg p-3 max-h-60 overflow-y-auto">
                            <div v-for="banner in availableBanners" :key="banner.id" 
                                 class="flex items-center gap-3 py-2 hover:bg-gray-50 px-2 rounded border-b border-gray-100 last:border-0">
                                <input type="checkbox" 
                                       :id="'banner-' + banner.id"
                                       :checked="isBannerSelected(banner.id)"
                                       @change="toggleBanner(banner.id)"
                                       class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500">
                                <label :for="'banner-' + banner.id" class="text-sm cursor-pointer flex-1 flex items-center gap-3">
                                    <img v-if="banner.image" :src="banner.image" 
                                         class="h-12 w-16 object-cover rounded border"
                                         @error="banner.image = null"
                                         alt="Banner">
                                    <div class="flex-1 min-w-0">
                                        <p class="font-medium truncate">{{ banner.title || 'Banner #' + banner.id }}</p>
                                        <p class="text-xs text-gray-500 truncate">{{ banner.link || 'Không có link' }}</p>
                                        <p class="text-xs text-gray-400">
                                            Trạng thái: {{ banner.status === 1 ? '✅ Hoạt động' : '⛔ Tạm dừng' }}
                                            <span v-if="banner.campaign_id && banner.campaign_id !== campaignForm.id" 
                                                  class="text-orange-500 ml-2">(Đã thuộc chiến dịch khác)</span>
                                        </p>
                                    </div>
                                </label>
                            </div>
                            
                            <div v-if="availableBanners.length === 0" class="text-center py-4 text-gray-400">
                                <span class="material-symbols-outlined text-4xl block mb-2">image</span>
                                <p>Không có banner nào</p>
                                <p class="text-xs mt-1">Tạo banner tại trang quản lý banner</p>
                            </div>
                        </div>
                        
                        <p class="text-xs text-gray-500 mt-1">
                            Đã chọn: <span class="font-semibold text-blue-600">{{ campaignForm.banner_ids.length }}</span> banner
                        </p>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%)</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Sản phẩm Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể trước khi tạo chiến dịch</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm được chọn</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm *</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner</label>
                        <div class="flex gap-3">
                            <select 
                                v-model="preorderForm.banner_id" 
                                class="flex-1 border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn banner từ danh sách --</option>
                                <option 
                                    v-for="banner in banners.filter(b => b.status === 1)" 
                                    :key="banner.id" 
                                    :value="banner.id"
                                >
                                    {{ banner.title || 'Banner #' + banner.id }} 
                                    {{ banner.image ? '📷' : '🚫' }}
                                </option>
                            </select>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="px-3 py-2 bg-gray-200 hover:bg-gray-300 rounded-lg text-sm transition-colors"
                                title="Xóa chọn banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                        
                        <div v-if="preorderForm.banner_id" class="mt-2 p-2 border border-gray-200 rounded-lg flex items-center gap-3">
                            <img 
                                :src="banners.find(b => b.id === preorderForm.banner_id)?.image" 
                                class="h-16 w-24 object-cover rounded border"
                                @error="preorderForm.banner_id = null"
                                alt="Banner preview"
                            >
                            <div class="flex-1">
                                <p class="text-sm font-medium">{{ banners.find(b => b.id === preorderForm.banner_id)?.title || 'Banner' }}</p>
                                <p class="text-xs text-gray-500 truncate">{{ banners.find(b => b.id === preorderForm.banner_id)?.image }}</p>
                            </div>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="text-red-500 hover:text-red-700 text-sm"
                            >
                                ✕
                            </button>
                        </div>
                        
                        <p class="text-xs text-gray-500 mt-1">Chọn banner đã tạo từ trang Quản lý Banner</p>
                        
                        <div class="mt-2">
                            <label class="text-xs text-gray-500">Hoặc nhập URL trực tiếp</label>
                            <input 
                                v-model="preorderForm.banner" 
                                type="text" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20 mt-1"
                                placeholder="https://example.com/banner.jpg"
                            >
                        </div>

                        <div v-if="preorderForm.banner" class="mt-2 relative">
                            <img 
                                :src="preorderForm.banner" 
                                class="h-32 w-full object-cover rounded-lg border"
                                @error="preorderForm.banner = ''"
                                alt="Banner preview"
                            >
                            <button 
                                @click="preorderForm.banner = ''; preorderForm.banner_id = null" 
                                class="absolute top-2 right-2 bg-red-500 text-white rounded-full p-1 hover:bg-red-600 transition-colors"
                                title="Xóa banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
```

---

## 2. File: `Banners.vue` (Hoàn chỉnh)

```vue
<script setup>
import { ref, onMounted, computed, watch } from 'vue'
import axios from 'axios'
import AdminLayout from '@/Layouts/AdminLayout.vue'
import { Head } from '@inertiajs/vue3'

// Props nhận từ controller
const props = defineProps({
    banners: {
        type: Array,
        default: () => []
    },
    campaigns: {
        type: Array,
        default: () => []
    }
})

// Pagination - 5 items per page
const currentPage = ref(1)
const perPage = ref(5)

// Search
const search = ref('')

// Lọc banners theo tên chiến dịch hoặc link
const filteredBanners = computed(() => {
    if (!banners.value || banners.value.length === 0) return []
    if (!search.value) return banners.value
    
    const keyword = search.value.toLowerCase().trim()
    return banners.value.filter(banner => {
        const campaignName = (banner.campaign?.name || '').toLowerCase()
        const title = (banner.title || '').toLowerCase()
        const link = (banner.link || '').toLowerCase()
        return campaignName.includes(keyword) || title.includes(keyword) || link.includes(keyword)
    })
})

// Sắp xếp banners theo order
const sortedBanners = computed(() => {
    return [...filteredBanners.value].sort((a, b) => (a.order || 0) - (b.order || 0))
})

// Pagination
const paginatedBanners = computed(() => {
    const start = (currentPage.value - 1) * perPage.value
    const end = start + perPage.value
    return sortedBanners.value.slice(start, end)
})

const totalPages = computed(() => {
    return Math.ceil(sortedBanners.value.length / perPage.value)
})

// Hiển thị số trang (tối đa 5 trang)
const displayedPages = computed(() => {
    const total = totalPages.value
    const current = currentPage.value
    const maxDisplay = 5
    
    if (total <= maxDisplay) {
        return Array.from({ length: total }, (_, i) => i + 1)
    }
    
    let start = Math.max(1, current - 2)
    let end = Math.min(total, start + maxDisplay - 1)
    
    if (end - start < maxDisplay - 1) {
        start = Math.max(1, end - maxDisplay + 1)
    }
    
    return Array.from({ length: end - start + 1 }, (_, i) => start + i)
})

// Reset về trang 1 khi tìm kiếm
watch(search, () => {
    currentPage.value = 1
})

const banners = ref(props.banners || [])
const campaigns = ref(props.campaigns || [])
const showModal = ref(false)
const showDeleteModal = ref(false)
const isEdit = ref(false)
const selectedBanner = ref(null)
const isLoading = ref(false)
const isSaving = ref(false)
const errorMessage = ref('')
const fileError = ref('')
const uploadSuccess = ref(false)

// Chọn phương thức nhập ảnh: 'url' hoặc 'file'
const imageInputMode = ref('url')
const selectedFile = ref(null)
const imagePreviewUrl = ref('')

const form = ref({
    id: null,
    title: '',
    campaign_id: '',
    image: '',
    link: '',
    status: 1,
    order: 0
})

// Xem trước ảnh
const imagePreview = computed(() => {
    if (imagePreviewUrl.value) return imagePreviewUrl.value
    if (form.value.image) return form.value.image
    return null
})

// Format date
const formatDate = (date) => {
    if (!date) return '---'
    const d = new Date(date)
    return d.toLocaleDateString('vi-VN')
}

// Lấy tên chiến dịch từ ID
const getCampaignName = (campaignId) => {
    if (!campaignId) return 'Chưa phân loại'
    const campaign = campaigns.value.find(c => c.id === campaignId)
    return campaign ? campaign.name : 'Chiến dịch đã xóa'
}

const fetchBanners = async () => {
    if (isLoading.value) return
    isLoading.value = true
    try {
        const response = await axios.get('/admin/banners/data')
        if (response.data && Array.isArray(response.data)) {
            banners.value = response.data
        } else {
            banners.value = []
        }
    } catch (error) {
        console.error('Lỗi lấy danh sách banner:', error)
        banners.value = []
    } finally {
        isLoading.value = false
    }
}

const openCreateModal = () => {
    isEdit.value = false
    form.value = { 
        id: null, 
        title: '',
        campaign_id: '', 
        image: '', 
        link: '', 
        status: 1, 
        order: 0 
    }
    selectedFile.value = null
    imagePreviewUrl.value = ''
    imageInputMode.value = 'url'
    errorMessage.value = ''
    fileError.value = ''
    uploadSuccess.value = false
    showModal.value = true
}

const openEditModal = (banner) => {
    isEdit.value = true
    form.value = { 
        id: banner.id,
        title: banner.title || '',
        campaign_id: banner.campaign_id || '', 
        image: banner.image || '', 
        link: banner.link || '', 
        status: banner.status !== undefined ? banner.status : 1, 
        order: banner.order || 0 
    }
    selectedFile.value = null
    imagePreviewUrl.value = ''
    imageInputMode.value = 'url'
    errorMessage.value = ''
    fileError.value = ''
    uploadSuccess.value = false
    showModal.value = true
}

// Xử lý khi chọn file
const handleFileChange = (event) => {
    const file = event.target.files[0]
    fileError.value = ''
    if (!file) {
        return
    }
    // Kiểm tra định dạng file
    if (!file.type.startsWith('image/')) {
        fileError.value = 'Vui lòng chọn file ảnh (jpg, png, gif, svg, webp)'
        event.target.value = ''
        return
    }
    // Kiểm tra kích thước (2MB)
    if (file.size > 2 * 1024 * 1024) {
        fileError.value = 'Kích thước ảnh không quá 2MB'
        event.target.value = ''
        return
    }
    selectedFile.value = file
    // Tạo preview
    const reader = new FileReader()
    reader.onload = (e) => { 
        imagePreviewUrl.value = e.target.result 
    }
    reader.readAsDataURL(file)
    // Xóa URL cũ nếu có
    form.value.image = ''
}

const clearFile = () => {
    selectedFile.value = null
    imagePreviewUrl.value = ''
    fileError.value = ''
    if (imageInputMode.value === 'file') {
        const fileInput = document.getElementById('fileInput')
        if (fileInput) fileInput.value = ''
    }
}

const saveBanner = async () => {
    // Validate
    if (!form.value.campaign_id) {
        errorMessage.value = 'Vui lòng chọn chiến dịch'
        return
    }
    
    // Kiểm tra có ảnh không (URL hoặc file)
    if (!form.value.image && !selectedFile.value) {
        errorMessage.value = 'Vui lòng nhập URL ảnh hoặc tải ảnh lên'
        return
    }
    
    if (fileError.value) {
        errorMessage.value = fileError.value
        return
    }

    if (isSaving.value) return
    isSaving.value = true
    errorMessage.value = ''
    uploadSuccess.value = false

    try {
        let response
        
        if (isEdit.value) {
            // Cập nhật banner
            if (selectedFile.value) {
                // Có file mới -> upload
                const formData = new FormData()
                formData.append('_method', 'PUT')
                formData.append('title', form.value.title || '')
                formData.append('campaign_id', form.value.campaign_id)
                formData.append('link', form.value.link || '')
                formData.append('status', form.value.status)
                formData.append('order', form.value.order || 0)
                formData.append('image_file', selectedFile.value)
                
                response = await axios.post(`/admin/banners/${form.value.id}`, formData, {
                    headers: { 
                        'Content-Type': 'multipart/form-data',
                        'Accept': 'application/json'
                    }
                })
            } else {
                // Không có file mới, chỉ cập nhật thông tin
                const dataToSave = {
                    title: form.value.title || '',
                    campaign_id: form.value.campaign_id,
                    link: form.value.link || '',
                    status: form.value.status,
                    order: form.value.order || 0,
                    image: form.value.image || null
                }
                response = await axios.put(`/admin/banners/${form.value.id}`, dataToSave, {
                    headers: { 'Accept': 'application/json' }
                })
            }
            
            if (response.data && response.data.success) {
                // Cập nhật danh sách
                const index = banners.value.findIndex(b => b.id === form.value.id)
                if (index !== -1 && response.data.data) {
                    banners.value[index] = response.data.data
                } else {
                    // Reload để lấy dữ liệu mới
                    await fetchBanners()
                }
                uploadSuccess.value = true
                alert('Cập nhật banner thành công!')
                showModal.value = false
                clearFile()
            } else {
                errorMessage.value = response.data?.message || 'Có lỗi xảy ra khi cập nhật'
            }
        } else {
            // Thêm mới banner
            if (selectedFile.value) {
                // Upload file
                const formData = new FormData()
                formData.append('title', form.value.title || '')
                formData.append('campaign_id', form.value.campaign_id)
                formData.append('link', form.value.link || '')
                formData.append('status', form.value.status)
                formData.append('order', form.value.order || 0)
                formData.append('image_file', selectedFile.value)
                
                response = await axios.post('/admin/banners', formData, {
                    headers: { 
                        'Content-Type': 'multipart/form-data',
                        'Accept': 'application/json'
                    }
                })
            } else {
                // Dùng URL
                const dataToSave = {
                    title: form.value.title || '',
                    campaign_id: form.value.campaign_id,
                    link: form.value.link || '',
                    status: form.value.status,
                    order: form.value.order || 0,
                    image: form.value.image || null
                }
                response = await axios.post('/admin/banners', dataToSave, {
                    headers: { 'Accept': 'application/json' }
                })
            }
            
            if (response.data && response.data.success && response.data.data) {
                banners.value.push(response.data.data)
                uploadSuccess.value = true
                alert('Thêm banner thành công!')
                showModal.value = false
                clearFile()
            } else {
                errorMessage.value = response.data?.message || 'Có lỗi xảy ra khi thêm mới'
            }
        }
    } catch (error) {
        console.error('Lỗi lưu banner:', error)
        if (error.response) {
            errorMessage.value = error.response.data?.message || 'Có lỗi xảy ra'
        } else {
            errorMessage.value = 'Không thể kết nối đến server'
        }
    } finally {
        isSaving.value = false
    }
}

const confirmDelete = (banner) => {
    selectedBanner.value = banner
    errorMessage.value = ''
    showDeleteModal.value = true
}

const deleteBanner = async () => {
    if (!selectedBanner.value) return
    if (isSaving.value) return
    isSaving.value = true
    errorMessage.value = ''
    try {
        const response = await axios.delete(`/admin/banners/${selectedBanner.value.id}`, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            showDeleteModal.value = false
            const index = banners.value.findIndex(b => b.id === selectedBanner.value.id)
            if (index !== -1) {
                banners.value.splice(index, 1)
            }
            selectedBanner.value = null
            alert('Xóa banner thành công!')
        } else {
            errorMessage.value = response.data?.message || 'Có lỗi xảy ra'
        }
    } catch (error) {
        console.error('Lỗi xóa banner:', error)
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra khi xóa'
    } finally {
        isSaving.value = false
    }
}

const toggleStatus = async (banner) => {
    try {
        const newStatus = banner.status === 1 ? 0 : 1
        const response = await axios.patch(`/admin/banners/${banner.id}/status`, { 
            status: newStatus 
        }, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            banner.status = newStatus
        } else {
            alert('Cập nhật trạng thái thất bại')
        }
    } catch (error) {
        console.error('Lỗi cập nhật trạng thái:', error)
        alert('Có lỗi xảy ra')
    }
}

const updateOrder = async (banner, newOrder) => {
    try {
        const response = await axios.patch(`/admin/banners/${banner.id}/order`, { 
            order: newOrder 
        }, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            banner.order = newOrder
            // Sắp xếp lại danh sách
            banners.value = [...banners.value].sort((a, b) => (a.order || 0) - (b.order || 0))
        }
    } catch (error) {
        console.error('Lỗi cập nhật thứ tự:', error)
        alert('Có lỗi xảy ra khi cập nhật thứ tự')
    }
}

const closeModal = () => {
    showModal.value = false
    showDeleteModal.value = false
    selectedBanner.value = null
    form.value = { id: null, title: '', campaign_id: '', image: '', link: '', status: 1, order: 0 }
    errorMessage.value = ''
    fileError.value = ''
    isSaving.value = false
    uploadSuccess.value = false
    clearFile()
}

const handleOverlayClick = (e) => {
    if (e.target === e.currentTarget) {
        closeModal()
    }
}

onMounted(() => {
    if (banners.value.length === 0) {
        fetchBanners()
    }
})
</script>

<template>
    <Head title="Quản lý Banner" />
    <AdminLayout>
        <div class="p-4 md:p-8">
            <div class="flex justify-between items-center mb-6">
                <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý Banner</h1>
                <button @click="openCreateModal" class="bg-orange-600 text-white px-5 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors" :disabled="isSaving">
                    <span class="material-symbols-outlined text-lg">add</span>
                    Thêm banner mới
                </button>
            </div>

            <!-- Thanh tìm kiếm -->
            <div class="mb-4">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="search" 
                        type="text" 
                        placeholder="Tìm theo chiến dịch, tên hoặc link..." 
                        class="pl-10 pr-4 py-2 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <div v-if="isLoading && banners.length === 0" class="text-center py-8">
                <div class="inline-block animate-spin rounded-full h-8 w-8 border-4 border-orange-500 border-t-transparent"></div>
                <p class="mt-2 text-gray-500">Đang tải...</p>
            </div>

            <div v-else class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full min-w-[800px]">
                        <thead class="bg-gray-50 border-b border-gray-200">
                            <tr>
                                <th class="text-left p-4 font-semibold text-gray-700 w-16 whitespace-nowrap">STT</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Hình ảnh</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap min-w-[150px]">Chiến dịch</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Link</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Trạng thái</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Thứ tự</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Ngày tạo</th>
                                <th class="text-center p-4 font-semibold text-gray-700 w-40 whitespace-nowrap">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr v-for="(banner, index) in paginatedBanners" :key="banner.id" class="border-b border-gray-100 hover:bg-gray-50 transition">
                                <td class="p-4 text-gray-500 text-sm whitespace-nowrap">{{ (currentPage - 1) * perPage + index + 1 }}</td>
                                <td class="p-4">
                                    <img v-if="banner.image" :src="banner.image" class="h-12 w-20 object-cover rounded" :alt="banner.title || 'Banner'" @error="banner.image = null">
                                    <span v-else class="text-gray-400">---</span>
                                </td>
                                <td class="p-4">
                                    <div class="flex items-center gap-2">
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium" 
                                              :class="banner.campaign_id ? 'bg-blue-100 text-blue-800' : 'bg-gray-100 text-gray-500'">
                                            {{ banner.campaign?.name || 'Chưa phân loại' }}
                                        </span>
                                        <span v-if="banner.campaign_id" class="text-xs text-gray-400">ID: {{ banner.campaign_id }}</span>
                                    </div>
                                </td>
                                <td class="p-4 text-gray-500 text-sm max-w-xs truncate">
                                    <a v-if="banner.link" :href="banner.link" target="_blank" class="text-blue-600 hover:underline">
                                        {{ banner.link }}
                                    </a>
                                    <span v-else class="text-gray-400">---</span>
                                </td>
                                <td class="p-4">
                                    <button @click="toggleStatus(banner)" class="px-2 py-1 text-xs rounded-full transition whitespace-nowrap" :class="banner.status === 1 ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'">
                                        {{ banner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}
                                    </button>
                                </td>
                                <td class="p-4">
                                    <div class="flex items-center gap-1">
                                        <input type="number" :value="banner.order || 0" @change="updateOrder(banner, parseInt($event.target.value))" class="w-16 px-1 py-1 border rounded text-center text-sm" min="0">
                                        <span class="text-xs text-gray-400">#</span>
                                    </div>
                                </td>
                                <td class="p-4 text-gray-500 text-sm whitespace-nowrap">{{ formatDate(banner.created_at) }}</td>
                                <td class="p-4 text-center whitespace-nowrap">
                                    <div class="flex items-center justify-center gap-2">
                                        <button @click="openEditModal(banner)" class="px-3 py-1.5 text-xs text-green-600 hover:bg-green-100 rounded-lg transition-colors font-medium" :disabled="isSaving">Sửa</button>
                                        <button @click="confirmDelete(banner)" class="px-3 py-1.5 text-xs text-red-600 hover:bg-red-100 rounded-lg transition-colors font-medium" :disabled="isSaving">Xóa</button>
                                    </div>
                                </td>
                            </tr>
                            <tr v-if="paginatedBanners.length === 0 && !isLoading">
                                <td colspan="8" class="p-8 text-center text-gray-400">Chưa có banner nào</td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <!-- Footer với phân trang căn giữa -->
                <div class="p-4 border-t border-gray-200">
                    <div class="text-center text-sm text-gray-500 mb-3">
                        Hiển thị {{ paginatedBanners.length }} / {{ sortedBanners.length }} banner
                    </div>
                    
                    <div v-if="totalPages > 1" class="flex justify-center items-center gap-2">
                        <button
                            @click="currentPage--"
                            :disabled="currentPage === 1"
                            class="px-3 py-1.5 text-sm border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            ◄
                        </button>
                        
                        <div class="flex gap-1">
                            <button
                                v-for="page in displayedPages"
                                :key="page"
                                @click="currentPage = page"
                                class="px-3.5 py-1.5 text-sm rounded-lg transition-colors font-medium"
                                :class="currentPage === page ? 'bg-orange-600 text-white' : 'border border-gray-300 hover:bg-gray-50'"
                            >
                                {{ page }}
                            </button>
                        </div>
                        
                        <button
                            @click="currentPage++"
                            :disabled="currentPage === totalPages"
                            class="px-3 py-1.5 text-sm border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            ►
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Modal Thêm/Sửa -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50" @click="handleOverlayClick">
            <div class="bg-white rounded-lg w-full max-w-lg p-6 max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">{{ isEdit ? 'Sửa banner' : 'Thêm banner mới' }}</h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Tiêu đề</label>
                        <input 
                            v-model="form.title" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" 
                            placeholder="Tiêu đề banner (tùy chọn)"
                            :disabled="isSaving"
                        >
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Chiến dịch *</label>
                        <select v-model="form.campaign_id" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                            <option value="">-- Chọn chiến dịch --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                        <p class="text-xs text-gray-400 mt-1">Banner sẽ thuộc về chiến dịch này</p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Ảnh banner *</label>
                        <div class="flex gap-2 border-b pb-2 mb-2">
                            <button type="button" @click="imageInputMode = 'url'" :class="['px-3 py-1 text-sm rounded-full transition-colors', imageInputMode === 'url' ? 'bg-orange-100 text-orange-600' : 'bg-gray-100 hover:bg-gray-200']">🔗 Nhập URL</button>
                            <button type="button" @click="imageInputMode = 'file'" :class="['px-3 py-1 text-sm rounded-full transition-colors', imageInputMode === 'file' ? 'bg-orange-100 text-orange-600' : 'bg-gray-100 hover:bg-gray-200']">📁 Tải ảnh lên</button>
                        </div>
                        <div v-if="imageInputMode === 'url'">
                            <input v-model="form.image" type="text" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" placeholder="https://example.com/banner.jpg" :disabled="isSaving">
                        </div>
                        <div v-else>
                            <input id="fileInput" type="file" accept="image/*" @change="handleFileChange" class="w-full" :disabled="isSaving">
                            <div v-if="fileError" class="text-red-500 text-sm mt-1">{{ fileError }}</div>
                            <button v-if="selectedFile" @click="clearFile" class="text-red-500 text-xs mt-1 hover:underline" type="button">✕ Xóa file đã chọn</button>
                            <p class="text-xs text-gray-400 mt-1">Hỗ trợ JPG, PNG, GIF, SVG, WEBP. Kích thước tối đa 2MB</p>
                        </div>
                        <div v-if="imagePreview" class="mt-2">
                            <p class="text-sm text-gray-600 mb-1">Xem trước:</p>
                            <div class="w-32 h-20 border rounded-lg overflow-hidden bg-gray-100 flex items-center justify-center">
                                <img :src="imagePreview" class="max-w-full max-h-full object-contain" @error="imagePreviewUrl = ''; form.image = ''" alt="Banner preview">
                            </div>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Link (URL)</label>
                        <input v-model="form.link" type="text" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" placeholder="https://example.com" :disabled="isSaving">
                        <p class="text-xs text-gray-400 mt-1">Đường dẫn khi người dùng click vào banner</p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Trạng thái</label>
                        <select v-model="form.status" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                            <option :value="1">Hoạt động</option>
                            <option :value="0">Tạm dừng</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Thứ tự</label>
                        <input v-model.number="form.order" type="number" min="0" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                        <p class="text-xs text-gray-400 mt-1">Số nhỏ hơn hiển thị trước</p>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                    
                    <div v-if="uploadSuccess" class="p-3 bg-green-50 border border-green-200 rounded-lg">
                        <p class="text-sm text-green-600">✅ Lưu thành công!</p>
                    </div>
                </div>
                
                <div class="flex justify-end gap-3 mt-6">
                    <button @click="closeModal" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition" :disabled="isSaving">Hủy</button>
                    <button @click="saveBanner" class="px-4 py-2 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition flex items-center gap-2" :disabled="isSaving || !!fileError">
                        <span v-if="isSaving" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSaving ? 'Đang xử lý...' : 'Lưu' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- Modal Xác nhận xóa -->
        <div v-if="showDeleteModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50" @click="handleOverlayClick">
            <div class="bg-white rounded-lg w-full max-w-md p-6">
                <h3 class="text-xl font-bold mb-4">Xác nhận xóa</h3>
                <p class="text-gray-600">Bạn có chắc muốn xóa banner "{{ selectedBanner?.title || '#' + selectedBanner?.id }}" này?</p>
                <p class="text-xs text-gray-400 mt-1">Hành động này không thể hoàn tác</p>
                <div v-if="errorMessage" class="mt-3 p-3 bg-red-50 border border-red-200 rounded-lg">
                    <p class="text-sm text-red-600">{{ errorMessage }}</p>
                </div>
                <div class="flex justify-end gap-3 mt-6">
                    <button @click="closeModal" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition" :disabled="isSaving">Hủy</button>
                    <button @click="deleteBanner" class="px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition flex items-center gap-2" :disabled="isSaving">
                        <span v-if="isSaving" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSaving ? 'Đang xóa...' : 'Xóa' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 1s linear infinite;
}
</style>
```

---

## 3. Cập nhật Controller `PromotionController`

```php
<?php
// app/Http/Controllers/Admin/PromotionController.php

// ... (phần đầu giữ nguyên)

public function index()
{
    try {
        // Lấy campaigns - KHÔNG load banner_id nữa
        $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
            ->latest()
            ->get()
            ->map(function ($campaign) {
                $config = $campaign->configs->first();
                // Lấy danh sách banner của chiến dịch
                $banners = $campaign->banners()->where('status', 1)->orderBy('order')->get();
                
                return [
                    'id' => $campaign->id,
                    'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                    'type' => $campaign->type ?? 'seasonal',
                    'description' => $campaign->description ?? '',
                    'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                    'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                    'status' => $campaign->status ?? 'scheduled',
                    'banners' => $banners->map(function ($banner) {
                        return [
                            'id' => $banner->id,
                            'title' => $banner->title,
                            'image' => $banner->image,
                            'link' => $banner->link,
                            'order' => $banner->order,
                        ];
                    }),
                    'priority' => $campaign->priority ?? 0,
                    'featured' => $campaign->featured ?? false,
                    'quantity' => $config ? (int) $config->quantity : 0,
                    'discountPercent' => $config ? (float) $config->discount_percent : 0,
                    'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                    'products' => $campaign->productVariants->pluck('id')->toArray(),
                    'productVariants' => $campaign->productVariants->map(function ($variant) {
                        return [
                            'id' => $variant->id,
                            'name' => $variant->product->name ?? 'Sản phẩm',
                            'color' => $variant->color->name ?? 'Không màu',
                            'price' => $variant->price ?? 0,
                        ];
                    }),
                ];
            });

        // Lấy tất cả promotions
        $promotions = Promotion::with(['campaign', 'product'])->latest()->get();

        // Lấy tất cả banners - HIỂN THỊ CẢ CAMPAIGN_ID
        $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
            return [
                'id' => $banner->id,
                'title' => $banner->title ?? 'Banner #' . $banner->id,
                'image' => $banner->image,
                'link' => $banner->link,
                'description' => $banner->description,
                'status' => $banner->status ? 1 : 0,
                'order' => $banner->order,
                'campaign_id' => $banner->campaign_id,
                'campaign' => $banner->campaign ? [
                    'id' => $banner->campaign->id,
                    'name' => $banner->campaign->name,
                ] : null,
            ];
        });

        // Lấy tất cả products
        $products = Product::with(['variants.color', 'category', 'brand'])->latest()->get();

        // Lấy product variants
        $productVariants = ProductVariant::with(['product', 'color'])->latest()->get();

        // Lấy chỉ sản phẩm pre-order
        $preorderProducts = Product::where('is_preorder', true)
            ->with(['variants.color', 'category', 'brand'])
            ->latest()
            ->get();

        return Inertia::render('Admin/Promotions', [
            'campaigns' => $campaigns,
            'promotions' => $promotions,
            'banners' => $banners,
            'products' => $products,
            'productVariants' => $productVariants,
            'preorderProducts' => $preorderProducts,
        ]);

    } catch (\Exception $e) {
        Log::error('Lỗi load trang promotions: ' . $e->getMessage());
        return Inertia::render('Admin/Promotions', [
            'campaigns' => [],
            'promotions' => [],
            'banners' => [],
            'products' => [],
            'productVariants' => [],
            'preorderProducts' => [],
            'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
        ]);
    }
}

public function storeCampaign(Request $request)
{
    try {
        DB::beginTransaction();

        $validated = $request->validate([
            'name' => 'nullable|string|max:255',
            'type' => 'nullable|string|max:50',
            'description' => 'nullable|string',
            'startDate' => 'nullable|date',
            'endDate' => 'nullable|date|after:startDate',
            'status' => 'nullable|in:scheduled,active,ended',
            'priority' => 'nullable|integer|min:0',
            'featured' => 'boolean',
            'quantity' => 'nullable|integer|min:0',
            'discountPercent' => 'nullable|numeric|min:0|max:100',
            'products' => 'nullable|array',
            'banner_ids' => 'nullable|array',
            'banner_ids.*' => 'exists:banners,id',
        ]);

        $campaign = Campaign::create([
            'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
            'type' => $validated['type'] ?? 'seasonal',
            'description' => $validated['description'] ?? '',
            'start_time' => $validated['startDate'] ?? null,
            'end_time' => $validated['endDate'] ?? null,
            'status' => $validated['status'] ?? 'scheduled',
            'priority' => $validated['priority'] ?? 0,
            'featured' => $validated['featured'] ?? false,
        ]);

        // Gán banner cho chiến dịch
        if (!empty($validated['banner_ids'])) {
            Banner::whereIn('id', $validated['banner_ids'])->update(['campaign_id' => $campaign->id]);
        }

        // Tạo config
        if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
            CampaignConfig::create([
                'campaign_id' => $campaign->id,
                'quantity' => $validated['quantity'] ?? 0,
                'discount_percent' => $validated['discountPercent'] ?? 0,
            ]);
        }

        // Gán sản phẩm
        if (!empty($validated['products'])) {
            $campaign->productVariants()->attach($validated['products']);
        }

        DB::commit();

        return redirect()->route('admin.promotions.index')->with([
            'success' => true,
            'message' => 'Thêm chiến dịch thành công!'
        ]);

    } catch (\Exception $e) {
        DB::rollBack();
        Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
        return redirect()->back()->with([
            'success' => false,
            'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
        ]);
    }
}

public function updateCampaign(Request $request, $id)
{
    try {
        DB::beginTransaction();

        $campaign = Campaign::findOrFail($id);

        $validated = $request->validate([
            'name' => 'nullable|string|max:255',
            'type' => 'nullable|string|max:50',
            'description' => 'nullable|string',
            'startDate' => 'nullable|date',
            'endDate' => 'nullable|date|after:startDate',
            'status' => 'nullable|in:scheduled,active,ended',
            'priority' => 'nullable|integer|min:0',
            'featured' => 'boolean',
            'quantity' => 'nullable|integer|min:0',
            'discountPercent' => 'nullable|numeric|min:0|max:100',
            'products' => 'nullable|array',
            'banner_ids' => 'nullable|array',
            'banner_ids.*' => 'exists:banners,id',
        ]);

        $campaign->update([
            'name' => $validated['name'] ?? $campaign->name,
            'type' => $validated['type'] ?? $campaign->type,
            'description' => $validated['description'] ?? $campaign->description,
            'start_time' => $validated['startDate'] ?? $campaign->start_time,
            'end_time' => $validated['endDate'] ?? $campaign->end_time,
            'status' => $validated['status'] ?? $campaign->status,
            'priority' => $validated['priority'] ?? $campaign->priority,
            'featured' => $validated['featured'] ?? $campaign->featured,
        ]);

        // Cập nhật banner cho chiến dịch
        // 1. Xóa tất cả banner cũ của chiến dịch này
        Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
        
        // 2. Gán banner mới
        if (!empty($validated['banner_ids'])) {
            Banner::whereIn('id', $validated['banner_ids'])->update(['campaign_id' => $campaign->id]);
        }

        // Cập nhật config
        if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
            $config = $campaign->configs()->first();
            if ($config) {
                $config->update([
                    'quantity' => $validated['quantity'] ?? $config->quantity,
                    'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                ]);
            } else {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }
        }

        // Cập nhật sản phẩm
        if (isset($validated['products'])) {
            $campaign->productVariants()->sync($validated['products']);
        }

        DB::commit();

        return redirect()->route('admin.promotions.index')->with([
            'success' => true,
            'message' => 'Cập nhật chiến dịch thành công!'
        ]);

    } catch (\Exception $e) {
        DB::rollBack();
        Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
        return redirect()->back()->with([
            'success' => false,
            'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
        ]);
    }
}

public function deleteCampaign($id)
{
    try {
        DB::beginTransaction();
        
        $campaign = Campaign::findOrFail($id);
        
        // Giải phóng banner khỏi chiến dịch
        Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
        
        $campaign->configs()->delete();
        $campaign->productVariants()->detach();
        $campaign->delete();

        DB::commit();

        return redirect()->route('admin.promotions.index')->with([
            'success' => true,
            'message' => 'Xóa chiến dịch thành công!'
        ]);

    } catch (\Exception $e) {
        DB::rollBack();
        Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
        return redirect()->back()->with([
            'success' => false,
            'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
        ]);
    }
}

// ... phần còn lại giữ nguyên
```

---

## 4. Cập nhật Controller `BannerController`

```php
<?php
// app/Http/Controllers/Admin/BannerController.php

// ... phần đầu giữ nguyên

public function store(Request $request)
{
    try {
        Log::info('Banner store request:', $request->all());

        $rules = [
            'title' => 'nullable|string|max:255',
            'campaign_id' => 'nullable|exists:campaigns,id',
            'image' => 'nullable|url',
            'link' => 'nullable|string|max:255',
            'description' => 'nullable|string',
            'status' => 'boolean',
            'order' => 'nullable|integer|min:0'
        ];

        if ($request->hasFile('image_file')) {
            $rules['image_file'] = 'image|max:2048';
        }

        $validated = $request->validate($rules);

        $data = [
            'title' => $validated['title'] ?? 'Banner ' . now()->format('d/m/Y'),
            'campaign_id' => $validated['campaign_id'] ?? null,
            'link' => $validated['link'] ?? null,
            'description' => $validated['description'] ?? null,
            'status' => $validated['status'] ?? true,
        ];
        
        // Xử lý ảnh
        if ($request->hasFile('image_file')) {
            try {
                $path = $request->file('image_file')->store('banners', 'public');
                $data['image'] = '/storage/' . $path;
            } catch (\Exception $e) {
                Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                return response()->json([
                    'success' => false,
                    'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                ], 500);
            }
        } elseif ($request->filled('image')) {
            $data['image'] = $request->image;
        } else {
            return response()->json([
                'success' => false,
                'message' => 'Vui lòng chọn ảnh hoặc nhập URL'
            ], 422);
        }

        // Xác định order mới
        $totalBanners = Banner::count();
        $newOrder = $request->input('order', $totalBanners);
        
        if ($newOrder < 0) $newOrder = 0;
        if ($newOrder > $totalBanners) $newOrder = $totalBanners;

        if ($newOrder < $totalBanners) {
            Banner::where('order', '>=', $newOrder)->increment('order');
        }

        $data['order'] = $newOrder;
        $banner = Banner::create($data);

        Log::info('Banner created successfully:', ['id' => $banner->id]);

        return response()->json([
            'success' => true, 
            'data' => $banner->load('campaign')
        ]);
        
    } catch (\Illuminate\Validation\ValidationException $e) {
        Log::error('Validation error:', $e->errors());
        return response()->json([
            'success' => false,
            'message' => 'Dữ liệu không hợp lệ',
            'errors' => $e->errors()
        ], 422);
    } catch (\Exception $e) {
        Log::error('Lỗi tạo banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
        return response()->json([
            'success' => false,
            'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
        ], 500);
    }
}

public function update(Request $request, $id)
{
    try {
        Log::info('Banner update request:', ['id' => $id, 'data' => $request->all()]);

        $banner = Banner::findOrFail($id);
        
        $rules = [
            'title' => 'nullable|string|max:255',
            'campaign_id' => 'nullable|exists:campaigns,id',
            'image' => 'nullable|url',
            'link' => 'nullable|string|max:255',
            'description' => 'nullable|string',
            'status' => 'boolean',
            'order' => 'nullable|integer|min:0'
        ];

        if ($request->hasFile('image_file')) {
            $rules['image_file'] = 'image|max:2048';
        }

        $validated = $request->validate($rules);

        $data = [
            'title' => $validated['title'] ?? $banner->title,
            'campaign_id' => $validated['campaign_id'] ?? $banner->campaign_id,
            'link' => $validated['link'] ?? $banner->link,
            'description' => $validated['description'] ?? $banner->description,
            'status' => $validated['status'] ?? $banner->status,
        ];
        
        // Xử lý ảnh
        if ($request->hasFile('image_file')) {
            try {
                if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                    Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
                }
                
                $path = $request->file('image_file')->store('banners', 'public');
                $data['image'] = '/storage/' . $path;
            } catch (\Exception $e) {
                Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                return response()->json([
                    'success' => false,
                    'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                ], 500);
            }
        } elseif ($request->filled('image')) {
            $data['image'] = $request->image;
        }

        // Xử lý thay đổi order
        $newOrder = $request->input('order');
        if (!is_null($newOrder) && $newOrder != $banner->order) {
            $oldOrder = $banner->order;
            $totalBanners = Banner::count();

            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

            if ($oldOrder < $newOrder) {
                Banner::where('id', '!=', $id)
                    ->where('order', '>', $oldOrder)
                    ->where('order', '<=', $newOrder)
                    ->decrement('order');
            } else {
                Banner::where('id', '!=', $id)
                    ->where('order', '>=', $newOrder)
                    ->where('order', '<', $oldOrder)
                    ->increment('order');
            }

            $data['order'] = $newOrder;
        }

        $banner->update($data);

        Log::info('Banner updated successfully:', ['id' => $banner->id]);

        return response()->json([
            'success' => true, 
            'data' => $banner->load('campaign')
        ]);
        
    } catch (\Illuminate\Validation\ValidationException $e) {
        Log::error('Validation error:', $e->errors());
        return response()->json([
            'success' => false,
            'message' => 'Dữ liệu không hợp lệ',
            'errors' => $e->errors()
        ], 422);
    } catch (\Exception $e) {
        Log::error('Lỗi cập nhật banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
        return response()->json([
            'success' => false,
            'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
        ], 500);
    }
}

// ... phần còn lại giữ nguyên
```

---

## 5. Migration cần tạo

```php
<?php
// database/migrations/2026_07_07_000000_add_campaign_id_to_banners_table.php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up()
    {
        // Thêm campaign_id vào bảng banners
        Schema::table('banners', function (Blueprint $table) {
            $table->foreignId('campaign_id')->nullable()->after('id')->constrained('campaigns')->onDelete('set null');
            $table->index('campaign_id');
        });

        // Xóa banner_id khỏi bảng campaigns (nếu tồn tại)
        if (Schema::hasColumn('campaigns', 'banner_id')) {
            Schema::table('campaigns', function (Blueprint $table) {
                $table->dropForeign(['banner_id']);
                $table->dropColumn('banner_id');
            });
        }
    }

    public function down()
    {
        // Khôi phục
        Schema::table('banners', function (Blueprint $table) {
            $table->dropForeign(['campaign_id']);
            $table->dropIndex(['campaign_id']);
            $table->dropColumn('campaign_id');
        });

        if (!Schema::hasColumn('campaigns', 'banner_id')) {
            Schema::table('campaigns', function (Blueprint $table) {
                $table->foreignId('banner_id')->nullable()->constrained('banners')->onDelete('set null');
            });
        }
    }
};
```

Chạy migration:
```bash
php artisan migrate
```


## User:
07/07/2026 09:21

-banner chưa lấy được dữ liệu chiến dịch khuyến mãi.

-bên quản lý khuyến mãi tôi muốn bỏ phần chọn ảnh banner luôn ( bỏ luôn id_banner trong bảng compains)

-tôi muốn giao diện của khuyến mãi như thế này :

<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    banner: '',
    banner_id: null,
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    banner: '',
    banner_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getBannerByCampaign = (campaignId) => {
    if (!campaignId) return null;
    return banners.value.find(b => b.campaign_id === campaignId);
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
    }
    
    return 'active';
};

// ==================== BANNER FUNCTIONS ====================

const selectedBanner = computed(() => {
    if (!campaignForm.value.banner_id) return null;
    return banners.value.find(b => b.id === campaignForm.value.banner_id);
});

const selectedPreorderBanner = computed(() => {
    if (!preorderForm.value.banner_id) return null;
    return banners.value.find(b => b.id === preorderForm.value.banner_id);
});

const onBannerSelect = () => {
    const bannerId = campaignForm.value.banner_id;
    if (bannerId) {
        const selected = banners.value.find(b => b.id === bannerId);
        if (selected && selected.image) {
            campaignForm.value.banner = selected.image;
        }
    } else {
        campaignForm.value.banner = '';
    }
};

const onPreorderBannerSelect = () => {
    const bannerId = preorderForm.value.banner_id;
    if (bannerId) {
        const selected = banners.value.find(b => b.id === bannerId);
        if (selected && selected.image) {
            preorderForm.value.banner = selected.image;
        }
    } else {
        preorderForm.value.banner = '';
    }
};

const onBannerUrlInput = () => {
    if (campaignForm.value.banner) {
        campaignForm.value.banner_id = null;
    }
};

const onPreorderBannerUrlInput = () => {
    if (preorderForm.value.banner) {
        preorderForm.value.banner_id = null;
    }
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            banner: campaign.banner || '',
            banner_id: campaign.banner_id || null,
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            banner: '',
            banner_id: null,
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            ...campaignForm.value,
            status: status,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value <= 0) {
        errorMessage.value = 'Giá trị giảm giá phải lớn hơn 0';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            banner: preorder.banner || '',
            banner_id: preorder.banner_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            banner: '',
            banner_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

// Sử dụng computed thay vì watch để tránh vòng lặp vô hạn
const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

// Lọc campaigns theo status
const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaigns.filter(c => status.value === 'all' || c.status === status.value).length }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                        <img 
                            v-if="campaign.banner" 
                            :src="campaign.banner" 
                            :alt="campaign.name" 
                            class="w-full h-full object-cover"
                            @error="campaign.banner = null"
                        >
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-3 right-3 flex gap-1">
                            <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                        </div>
                    </div>

                    <div class="p-5">
                        <div class="flex justify-between items-start">
                            <div class="flex-1">
                                <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-1 ml-2">
                                <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-4 grid grid-cols-2 gap-3">
                            <div class="bg-orange-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Giảm giá</p>
                                <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                        </div>

                        <div v-if="getBannerByCampaign(campaign.id)" class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">image</span>
                            <span>Có banner liên kết</span>
                        </div>

                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-2 text-sm">
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📅</span>
                            <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📊</span>
                            <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-3 pt-3 border-t border-gray-100">
                        <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                            <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-sm font-bold text-blue-600">
                                Giảm hiện tại: 
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-2 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="campaignForm.startDate || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner</label>
                        <div class="flex gap-3">
                            <select 
                                v-model="campaignForm.banner_id" 
                                class="flex-1 border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                @change="onBannerSelect"
                            >
                                <option :value="null">-- Chọn banner từ danh sách --</option>
                                <option 
                                    v-for="banner in banners" 
                                    :key="banner.id" 
                                    :value="banner.id"
                                >
                                    {{ banner.campaign_name || 'Banner #' + banner.id }} 
                                    {{ banner.image ? '📷' : '🚫' }}
                                    {{ banner.status === 1 ? '✅' : '⛔' }}
                                </option>
                            </select>
                            <button 
                                @click="campaignForm.banner_id = null; campaignForm.banner = ''" 
                                class="px-3 py-2 bg-gray-200 hover:bg-gray-300 rounded-lg text-sm transition-colors"
                                title="Xóa chọn banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                        
                        <div v-if="selectedBanner && selectedBanner.image" class="mt-2 p-2 border border-gray-200 rounded-lg flex items-center gap-3">
                            <img 
                                :src="selectedBanner.image" 
                                class="h-16 w-24 object-cover rounded border"
                                @error="selectedBanner.image = null"
                                alt="Banner preview"
                            >
                            <div class="flex-1">
                                <p class="text-sm font-medium">{{ selectedBanner.campaign_name || 'Banner #' + selectedBanner.id }}</p>
                                <p class="text-xs text-gray-500 truncate">{{ selectedBanner.image }}</p>
                                <p class="text-xs text-gray-400">Trạng thái: {{ selectedBanner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}</p>
                            </div>
                            <button 
                                @click="campaignForm.banner_id = null; campaignForm.banner = ''" 
                                class="text-red-500 hover:text-red-700 text-sm"
                            >
                                ✕
                            </button>
                        </div>
                        
                        <p class="text-xs text-gray-500 mt-1">Chọn banner đã tạo từ trang Quản lý Banner</p>
                        
                        <div class="mt-2">
                            <label class="text-xs text-gray-500">Hoặc nhập URL trực tiếp</label>
                            <input 
                                v-model="campaignForm.banner" 
                                type="text" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 mt-1"
                                placeholder="https://example.com/banner.jpg"
                                @input="onBannerUrlInput"
                            >
                        </div>

                        <div v-if="campaignForm.banner && !selectedBanner" class="mt-2 relative">
                            <img 
                                :src="campaignForm.banner" 
                                class="h-32 w-full object-cover rounded-lg border"
                                @error="campaignForm.banner = ''"
                                alt="Banner preview"
                            >
                            <button 
                                @click="campaignForm.banner = ''; campaignForm.banner_id = null" 
                                class="absolute top-2 right-2 bg-red-500 text-white rounded-full p-1 hover:bg-red-600 transition-colors"
                                title="Xóa banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%)</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Sản phẩm Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể trước khi tạo chiến dịch</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm được chọn</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm *</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner</label>
                        <div class="flex gap-3">
                            <select 
                                v-model="preorderForm.banner_id" 
                                class="flex-1 border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                                @change="onPreorderBannerSelect"
                            >
                                <option :value="null">-- Chọn banner từ danh sách --</option>
                                <option 
                                    v-for="banner in banners" 
                                    :key="banner.id" 
                                    :value="banner.id"
                                >
                                    {{ banner.campaign_name || 'Banner #' + banner.id }} 
                                    {{ banner.image ? '📷' : '🚫' }}
                                    {{ banner.status === 1 ? '✅' : '⛔' }}
                                </option>
                            </select>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="px-3 py-2 bg-gray-200 hover:bg-gray-300 rounded-lg text-sm transition-colors"
                                title="Xóa chọn banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                        
                        <div v-if="selectedPreorderBanner && selectedPreorderBanner.image" class="mt-2 p-2 border border-gray-200 rounded-lg flex items-center gap-3">
                            <img 
                                :src="selectedPreorderBanner.image" 
                                class="h-16 w-24 object-cover rounded border"
                                @error="selectedPreorderBanner.image = null"
                                alt="Banner preview"
                            >
                            <div class="flex-1">
                                <p class="text-sm font-medium">{{ selectedPreorderBanner.campaign_name || 'Banner #' + selectedPreorderBanner.id }}</p>
                                <p class="text-xs text-gray-500 truncate">{{ selectedPreorderBanner.image }}</p>
                                <p class="text-xs text-gray-400">Trạng thái: {{ selectedPreorderBanner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}</p>
                            </div>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="text-red-500 hover:text-red-700 text-sm"
                            >
                                ✕
                            </button>
                        </div>
                        
                        <p class="text-xs text-gray-500 mt-1">Chọn banner đã tạo từ trang Quản lý Banner</p>
                        
                        <div class="mt-2">
                            <label class="text-xs text-gray-500">Hoặc nhập URL trực tiếp</label>
                            <input 
                                v-model="preorderForm.banner" 
                                type="text" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20 mt-1"
                                placeholder="https://example.com/banner.jpg"
                                @input="onPreorderBannerUrlInput"
                            >
                        </div>

                        <div v-if="preorderForm.banner && !selectedPreorderBanner" class="mt-2 relative">
                            <img 
                                :src="preorderForm.banner" 
                                class="h-32 w-full object-cover rounded-lg border"
                                @error="preorderForm.banner = ''"
                                alt="Banner preview"
                            >
                            <button 
                                @click="preorderForm.banner = ''; preorderForm.banner_id = null" 
                                class="absolute top-2 right-2 bg-red-500 text-white rounded-full p-1 hover:bg-red-600 transition-colors"
                                title="Xóa banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>


## Assistant:
07/07/2026 09:21

Tôi sẽ giúp bạn sửa lại code để:

1. **Banner lấy dữ liệu chiến dịch từ khuyến mãi** - hiển thị danh sách chiến dịch trong dropdown
2. **Bỏ chọn banner trong modal chiến dịch** - xóa phần chọn banner khỏi campaign form
3. **Giữ giao diện như bạn yêu cầu**

## 1. Cập nhật `Banners.vue` - Lấy danh sách chiến dịch

```vue
<script setup>
import { ref, onMounted, computed, watch } from 'vue'
import axios from 'axios'
import AdminLayout from '@/Layouts/AdminLayout.vue'
import { Head } from '@inertiajs/vue3'

// Props nhận từ controller
const props = defineProps({
    banners: {
        type: Array,
        default: () => []
    },
    campaigns: {
        type: Array,
        default: () => []
    }
})

// Pagination - 5 items per page
const currentPage = ref(1)
const perPage = ref(5)

// Search
const search = ref('')

// Lọc banners theo tên chiến dịch hoặc link
const filteredBanners = computed(() => {
    if (!banners.value || banners.value.length === 0) return []
    if (!search.value) return banners.value
    
    const keyword = search.value.toLowerCase().trim()
    return banners.value.filter(banner => {
        const campaignName = (banner.campaign?.name || '').toLowerCase()
        const title = (banner.title || '').toLowerCase()
        const link = (banner.link || '').toLowerCase()
        return campaignName.includes(keyword) || title.includes(keyword) || link.includes(keyword)
    })
})

// Sắp xếp banners theo order
const sortedBanners = computed(() => {
    return [...filteredBanners.value].sort((a, b) => (a.order || 0) - (b.order || 0))
})

// Pagination
const paginatedBanners = computed(() => {
    const start = (currentPage.value - 1) * perPage.value
    const end = start + perPage.value
    return sortedBanners.value.slice(start, end)
})

const totalPages = computed(() => {
    return Math.ceil(sortedBanners.value.length / perPage.value)
})

// Hiển thị số trang (tối đa 5 trang)
const displayedPages = computed(() => {
    const total = totalPages.value
    const current = currentPage.value
    const maxDisplay = 5
    
    if (total <= maxDisplay) {
        return Array.from({ length: total }, (_, i) => i + 1)
    }
    
    let start = Math.max(1, current - 2)
    let end = Math.min(total, start + maxDisplay - 1)
    
    if (end - start < maxDisplay - 1) {
        start = Math.max(1, end - maxDisplay + 1)
    }
    
    return Array.from({ length: end - start + 1 }, (_, i) => start + i)
})

// Reset về trang 1 khi tìm kiếm
watch(search, () => {
    currentPage.value = 1
})

const banners = ref(props.banners || [])
const campaigns = ref(props.campaigns || [])
const showModal = ref(false)
const showDeleteModal = ref(false)
const isEdit = ref(false)
const selectedBanner = ref(null)
const isLoading = ref(false)
const isSaving = ref(false)
const errorMessage = ref('')
const fileError = ref('')
const uploadSuccess = ref(false)

// Chọn phương thức nhập ảnh: 'url' hoặc 'file'
const imageInputMode = ref('url')
const selectedFile = ref(null)
const imagePreviewUrl = ref('')

const form = ref({
    id: null,
    title: '',
    campaign_id: '',
    image: '',
    link: '',
    status: 1,
    order: 0
})

// Xem trước ảnh
const imagePreview = computed(() => {
    if (imagePreviewUrl.value) return imagePreviewUrl.value
    if (form.value.image) return form.value.image
    return null
})

// Format date
const formatDate = (date) => {
    if (!date) return '---'
    const d = new Date(date)
    return d.toLocaleDateString('vi-VN')
}

// Lấy tên chiến dịch từ ID
const getCampaignName = (campaignId) => {
    if (!campaignId) return 'Chưa phân loại'
    const campaign = campaigns.value.find(c => c.id === campaignId)
    return campaign ? campaign.name : 'Chiến dịch đã xóa'
}

const fetchBanners = async () => {
    if (isLoading.value) return
    isLoading.value = true
    try {
        const response = await axios.get('/admin/banners/data')
        if (response.data && Array.isArray(response.data)) {
            banners.value = response.data
        } else {
            banners.value = []
        }
    } catch (error) {
        console.error('Lỗi lấy danh sách banner:', error)
        banners.value = []
    } finally {
        isLoading.value = false
    }
}

const fetchCampaigns = async () => {
    try {
        const response = await axios.get('/admin/promotions/campaigns/list')
        if (response.data && Array.isArray(response.data)) {
            campaigns.value = response.data
        }
    } catch (error) {
        console.error('Lỗi lấy danh sách chiến dịch:', error)
    }
}

const openCreateModal = () => {
    isEdit.value = false
    form.value = { 
        id: null, 
        title: '',
        campaign_id: '', 
        image: '', 
        link: '', 
        status: 1, 
        order: 0 
    }
    selectedFile.value = null
    imagePreviewUrl.value = ''
    imageInputMode.value = 'url'
    errorMessage.value = ''
    fileError.value = ''
    uploadSuccess.value = false
    showModal.value = true
}

const openEditModal = (banner) => {
    isEdit.value = true
    form.value = { 
        id: banner.id,
        title: banner.title || '',
        campaign_id: banner.campaign_id || '', 
        image: banner.image || '', 
        link: banner.link || '', 
        status: banner.status !== undefined ? banner.status : 1, 
        order: banner.order || 0 
    }
    selectedFile.value = null
    imagePreviewUrl.value = ''
    imageInputMode.value = 'url'
    errorMessage.value = ''
    fileError.value = ''
    uploadSuccess.value = false
    showModal.value = true
}

// Xử lý khi chọn file
const handleFileChange = (event) => {
    const file = event.target.files[0]
    fileError.value = ''
    if (!file) {
        return
    }
    // Kiểm tra định dạng file
    if (!file.type.startsWith('image/')) {
        fileError.value = 'Vui lòng chọn file ảnh (jpg, png, gif, svg, webp)'
        event.target.value = ''
        return
    }
    // Kiểm tra kích thước (2MB)
    if (file.size > 2 * 1024 * 1024) {
        fileError.value = 'Kích thước ảnh không quá 2MB'
        event.target.value = ''
        return
    }
    selectedFile.value = file
    // Tạo preview
    const reader = new FileReader()
    reader.onload = (e) => { 
        imagePreviewUrl.value = e.target.result 
    }
    reader.readAsDataURL(file)
    // Xóa URL cũ nếu có
    form.value.image = ''
}

const clearFile = () => {
    selectedFile.value = null
    imagePreviewUrl.value = ''
    fileError.value = ''
    if (imageInputMode.value === 'file') {
        const fileInput = document.getElementById('fileInput')
        if (fileInput) fileInput.value = ''
    }
}

const saveBanner = async () => {
    // Validate
    if (!form.value.campaign_id) {
        errorMessage.value = 'Vui lòng chọn chiến dịch'
        return
    }
    
    // Kiểm tra có ảnh không (URL hoặc file)
    if (!form.value.image && !selectedFile.value) {
        errorMessage.value = 'Vui lòng nhập URL ảnh hoặc tải ảnh lên'
        return
    }
    
    if (fileError.value) {
        errorMessage.value = fileError.value
        return
    }

    if (isSaving.value) return
    isSaving.value = true
    errorMessage.value = ''
    uploadSuccess.value = false

    try {
        let response
        
        if (isEdit.value) {
            // Cập nhật banner
            if (selectedFile.value) {
                // Có file mới -> upload
                const formData = new FormData()
                formData.append('_method', 'PUT')
                formData.append('title', form.value.title || '')
                formData.append('campaign_id', form.value.campaign_id)
                formData.append('link', form.value.link || '')
                formData.append('status', form.value.status)
                formData.append('order', form.value.order || 0)
                formData.append('image_file', selectedFile.value)
                
                response = await axios.post(`/admin/banners/${form.value.id}`, formData, {
                    headers: { 
                        'Content-Type': 'multipart/form-data',
                        'Accept': 'application/json'
                    }
                })
            } else {
                // Không có file mới, chỉ cập nhật thông tin
                const dataToSave = {
                    title: form.value.title || '',
                    campaign_id: form.value.campaign_id,
                    link: form.value.link || '',
                    status: form.value.status,
                    order: form.value.order || 0,
                    image: form.value.image || null
                }
                response = await axios.put(`/admin/banners/${form.value.id}`, dataToSave, {
                    headers: { 'Accept': 'application/json' }
                })
            }
            
            if (response.data && response.data.success) {
                // Cập nhật danh sách
                await fetchBanners()
                uploadSuccess.value = true
                alert('Cập nhật banner thành công!')
                showModal.value = false
                clearFile()
            } else {
                errorMessage.value = response.data?.message || 'Có lỗi xảy ra khi cập nhật'
            }
        } else {
            // Thêm mới banner
            if (selectedFile.value) {
                // Upload file
                const formData = new FormData()
                formData.append('title', form.value.title || '')
                formData.append('campaign_id', form.value.campaign_id)
                formData.append('link', form.value.link || '')
                formData.append('status', form.value.status)
                formData.append('order', form.value.order || 0)
                formData.append('image_file', selectedFile.value)
                
                response = await axios.post('/admin/banners', formData, {
                    headers: { 
                        'Content-Type': 'multipart/form-data',
                        'Accept': 'application/json'
                    }
                })
            } else {
                // Dùng URL
                const dataToSave = {
                    title: form.value.title || '',
                    campaign_id: form.value.campaign_id,
                    link: form.value.link || '',
                    status: form.value.status,
                    order: form.value.order || 0,
                    image: form.value.image || null
                }
                response = await axios.post('/admin/banners', dataToSave, {
                    headers: { 'Accept': 'application/json' }
                })
            }
            
            if (response.data && response.data.success && response.data.data) {
                banners.value.push(response.data.data)
                uploadSuccess.value = true
                alert('Thêm banner thành công!')
                showModal.value = false
                clearFile()
            } else {
                errorMessage.value = response.data?.message || 'Có lỗi xảy ra khi thêm mới'
            }
        }
    } catch (error) {
        console.error('Lỗi lưu banner:', error)
        if (error.response) {
            errorMessage.value = error.response.data?.message || 'Có lỗi xảy ra'
        } else {
            errorMessage.value = 'Không thể kết nối đến server'
        }
    } finally {
        isSaving.value = false
    }
}

const confirmDelete = (banner) => {
    selectedBanner.value = banner
    errorMessage.value = ''
    showDeleteModal.value = true
}

const deleteBanner = async () => {
    if (!selectedBanner.value) return
    if (isSaving.value) return
    isSaving.value = true
    errorMessage.value = ''
    try {
        const response = await axios.delete(`/admin/banners/${selectedBanner.value.id}`, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            showDeleteModal.value = false
            const index = banners.value.findIndex(b => b.id === selectedBanner.value.id)
            if (index !== -1) {
                banners.value.splice(index, 1)
            }
            selectedBanner.value = null
            alert('Xóa banner thành công!')
        } else {
            errorMessage.value = response.data?.message || 'Có lỗi xảy ra'
        }
    } catch (error) {
        console.error('Lỗi xóa banner:', error)
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra khi xóa'
    } finally {
        isSaving.value = false
    }
}

const toggleStatus = async (banner) => {
    try {
        const newStatus = banner.status === 1 ? 0 : 1
        const response = await axios.patch(`/admin/banners/${banner.id}/status`, { 
            status: newStatus 
        }, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            banner.status = newStatus
        } else {
            alert('Cập nhật trạng thái thất bại')
        }
    } catch (error) {
        console.error('Lỗi cập nhật trạng thái:', error)
        alert('Có lỗi xảy ra')
    }
}

const updateOrder = async (banner, newOrder) => {
    try {
        const response = await axios.patch(`/admin/banners/${banner.id}/order`, { 
            order: newOrder 
        }, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            banner.order = newOrder
            // Sắp xếp lại danh sách
            banners.value = [...banners.value].sort((a, b) => (a.order || 0) - (b.order || 0))
        }
    } catch (error) {
        console.error('Lỗi cập nhật thứ tự:', error)
        alert('Có lỗi xảy ra khi cập nhật thứ tự')
    }
}

const closeModal = () => {
    showModal.value = false
    showDeleteModal.value = false
    selectedBanner.value = null
    form.value = { id: null, title: '', campaign_id: '', image: '', link: '', status: 1, order: 0 }
    errorMessage.value = ''
    fileError.value = ''
    isSaving.value = false
    uploadSuccess.value = false
    clearFile()
}

const handleOverlayClick = (e) => {
    if (e.target === e.currentTarget) {
        closeModal()
    }
}

onMounted(() => {
    fetchBanners()
    fetchCampaigns()
})
</script>

<template>
    <Head title="Quản lý Banner" />
    <AdminLayout>
        <div class="p-4 md:p-8">
            <div class="flex justify-between items-center mb-6">
                <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý Banner</h1>
                <button @click="openCreateModal" class="bg-orange-600 text-white px-5 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors" :disabled="isSaving">
                    <span class="material-symbols-outlined text-lg">add</span>
                    Thêm banner mới
                </button>
            </div>

            <!-- Thanh tìm kiếm -->
            <div class="mb-4">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="search" 
                        type="text" 
                        placeholder="Tìm theo chiến dịch, tên hoặc link..." 
                        class="pl-10 pr-4 py-2 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <div v-if="isLoading && banners.length === 0" class="text-center py-8">
                <div class="inline-block animate-spin rounded-full h-8 w-8 border-4 border-orange-500 border-t-transparent"></div>
                <p class="mt-2 text-gray-500">Đang tải...</p>
            </div>

            <div v-else class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full min-w-[800px]">
                        <thead class="bg-gray-50 border-b border-gray-200">
                            <tr>
                                <th class="text-left p-4 font-semibold text-gray-700 w-16 whitespace-nowrap">STT</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Hình ảnh</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap min-w-[150px]">Chiến dịch</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Link</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Trạng thái</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Thứ tự</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Ngày tạo</th>
                                <th class="text-center p-4 font-semibold text-gray-700 w-40 whitespace-nowrap">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr v-for="(banner, index) in paginatedBanners" :key="banner.id" class="border-b border-gray-100 hover:bg-gray-50 transition">
                                <td class="p-4 text-gray-500 text-sm whitespace-nowrap">{{ (currentPage - 1) * perPage + index + 1 }}</td>
                                <td class="p-4">
                                    <img v-if="banner.image" :src="banner.image" class="h-12 w-20 object-cover rounded" :alt="banner.title || 'Banner'" @error="banner.image = null">
                                    <span v-else class="text-gray-400">---</span>
                                </td>
                                <td class="p-4">
                                    <div class="flex items-center gap-2">
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium" 
                                              :class="banner.campaign_id ? 'bg-blue-100 text-blue-800' : 'bg-gray-100 text-gray-500'">
                                            {{ banner.campaign?.name || 'Chưa phân loại' }}
                                        </span>
                                    </div>
                                </td>
                                <td class="p-4 text-gray-500 text-sm max-w-xs truncate">
                                    <a v-if="banner.link" :href="banner.link" target="_blank" class="text-blue-600 hover:underline">
                                        {{ banner.link }}
                                    </a>
                                    <span v-else class="text-gray-400">---</span>
                                </td>
                                <td class="p-4">
                                    <button @click="toggleStatus(banner)" class="px-2 py-1 text-xs rounded-full transition whitespace-nowrap" :class="banner.status === 1 ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'">
                                        {{ banner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}
                                    </button>
                                </td>
                                <td class="p-4">
                                    <div class="flex items-center gap-1">
                                        <input type="number" :value="banner.order || 0" @change="updateOrder(banner, parseInt($event.target.value))" class="w-16 px-1 py-1 border rounded text-center text-sm" min="0">
                                        <span class="text-xs text-gray-400">#</span>
                                    </div>
                                </td>
                                <td class="p-4 text-gray-500 text-sm whitespace-nowrap">{{ formatDate(banner.created_at) }}</td>
                                <td class="p-4 text-center whitespace-nowrap">
                                    <div class="flex items-center justify-center gap-2">
                                        <button @click="openEditModal(banner)" class="px-3 py-1.5 text-xs text-green-600 hover:bg-green-100 rounded-lg transition-colors font-medium" :disabled="isSaving">Sửa</button>
                                        <button @click="confirmDelete(banner)" class="px-3 py-1.5 text-xs text-red-600 hover:bg-red-100 rounded-lg transition-colors font-medium" :disabled="isSaving">Xóa</button>
                                    </div>
                                </td>
                            </tr>
                            <tr v-if="paginatedBanners.length === 0 && !isLoading">
                                <td colspan="8" class="p-8 text-center text-gray-400">Chưa có banner nào</td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <!-- Footer với phân trang căn giữa -->
                <div class="p-4 border-t border-gray-200">
                    <div class="text-center text-sm text-gray-500 mb-3">
                        Hiển thị {{ paginatedBanners.length }} / {{ sortedBanners.length }} banner
                    </div>
                    
                    <div v-if="totalPages > 1" class="flex justify-center items-center gap-2">
                        <button
                            @click="currentPage--"
                            :disabled="currentPage === 1"
                            class="px-3 py-1.5 text-sm border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            ◄
                        </button>
                        
                        <div class="flex gap-1">
                            <button
                                v-for="page in displayedPages"
                                :key="page"
                                @click="currentPage = page"
                                class="px-3.5 py-1.5 text-sm rounded-lg transition-colors font-medium"
                                :class="currentPage === page ? 'bg-orange-600 text-white' : 'border border-gray-300 hover:bg-gray-50'"
                            >
                                {{ page }}
                            </button>
                        </div>
                        
                        <button
                            @click="currentPage++"
                            :disabled="currentPage === totalPages"
                            class="px-3 py-1.5 text-sm border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            ►
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Modal Thêm/Sửa -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50" @click="handleOverlayClick">
            <div class="bg-white rounded-lg w-full max-w-lg p-6 max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">{{ isEdit ? 'Sửa banner' : 'Thêm banner mới' }}</h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Tiêu đề</label>
                        <input 
                            v-model="form.title" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" 
                            placeholder="Tiêu đề banner (tùy chọn)"
                            :disabled="isSaving"
                        >
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Chiến dịch *</label>
                        <select v-model="form.campaign_id" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                            <option value="">-- Chọn chiến dịch --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">
                                {{ camp.name }}
                                <span v-if="camp.status" class="text-xs text-gray-400">({{ camp.status === 'active' ? 'Đang diễn ra' : camp.status === 'scheduled' ? 'Sắp diễn ra' : 'Đã kết thúc' }})</span>
                            </option>
                        </select>
                        <p class="text-xs text-gray-400 mt-1">Banner sẽ thuộc về chiến dịch này</p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Ảnh banner *</label>
                        <div class="flex gap-2 border-b pb-2 mb-2">
                            <button type="button" @click="imageInputMode = 'url'" :class="['px-3 py-1 text-sm rounded-full transition-colors', imageInputMode === 'url' ? 'bg-orange-100 text-orange-600' : 'bg-gray-100 hover:bg-gray-200']">🔗 Nhập URL</button>
                            <button type="button" @click="imageInputMode = 'file'" :class="['px-3 py-1 text-sm rounded-full transition-colors', imageInputMode === 'file' ? 'bg-orange-100 text-orange-600' : 'bg-gray-100 hover:bg-gray-200']">📁 Tải ảnh lên</button>
                        </div>
                        <div v-if="imageInputMode === 'url'">
                            <input v-model="form.image" type="text" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" placeholder="https://example.com/banner.jpg" :disabled="isSaving">
                        </div>
                        <div v-else>
                            <input id="fileInput" type="file" accept="image/*" @change="handleFileChange" class="w-full" :disabled="isSaving">
                            <div v-if="fileError" class="text-red-500 text-sm mt-1">{{ fileError }}</div>
                            <button v-if="selectedFile" @click="clearFile" class="text-red-500 text-xs mt-1 hover:underline" type="button">✕ Xóa file đã chọn</button>
                            <p class="text-xs text-gray-400 mt-1">Hỗ trợ JPG, PNG, GIF, SVG, WEBP. Kích thước tối đa 2MB</p>
                        </div>
                        <div v-if="imagePreview" class="mt-2">
                            <p class="text-sm text-gray-600 mb-1">Xem trước:</p>
                            <div class="w-32 h-20 border rounded-lg overflow-hidden bg-gray-100 flex items-center justify-center">
                                <img :src="imagePreview" class="max-w-full max-h-full object-contain" @error="imagePreviewUrl = ''; form.image = ''" alt="Banner preview">
                            </div>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Link (URL)</label>
                        <input v-model="form.link" type="text" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" placeholder="https://example.com" :disabled="isSaving">
                        <p class="text-xs text-gray-400 mt-1">Đường dẫn khi người dùng click vào banner</p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Trạng thái</label>
                        <select v-model="form.status" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                            <option :value="1">Hoạt động</option>
                            <option :value="0">Tạm dừng</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Thứ tự</label>
                        <input v-model.number="form.order" type="number" min="0" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                        <p class="text-xs text-gray-400 mt-1">Số nhỏ hơn hiển thị trước</p>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                    
                    <div v-if="uploadSuccess" class="p-3 bg-green-50 border border-green-200 rounded-lg">
                        <p class="text-sm text-green-600">✅ Lưu thành công!</p>
                    </div>
                </div>
                
                <div class="flex justify-end gap-3 mt-6">
                    <button @click="closeModal" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition" :disabled="isSaving">Hủy</button>
                    <button @click="saveBanner" class="px-4 py-2 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition flex items-center gap-2" :disabled="isSaving || !!fileError">
                        <span v-if="isSaving" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSaving ? 'Đang xử lý...' : 'Lưu' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- Modal Xác nhận xóa -->
        <div v-if="showDeleteModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50" @click="handleOverlayClick">
            <div class="bg-white rounded-lg w-full max-w-md p-6">
                <h3 class="text-xl font-bold mb-4">Xác nhận xóa</h3>
                <p class="text-gray-600">Bạn có chắc muốn xóa banner "{{ selectedBanner?.title || '#' + selectedBanner?.id }}" này?</p>
                <p class="text-xs text-gray-400 mt-1">Hành động này không thể hoàn tác</p>
                <div v-if="errorMessage" class="mt-3 p-3 bg-red-50 border border-red-200 rounded-lg">
                    <p class="text-sm text-red-600">{{ errorMessage }}</p>
                </div>
                <div class="flex justify-end gap-3 mt-6">
                    <button @click="closeModal" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition" :disabled="isSaving">Hủy</button>
                    <button @click="deleteBanner" class="px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition flex items-center gap-2" :disabled="isSaving">
                        <span v-if="isSaving" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSaving ? 'Đang xóa...' : 'Xóa' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 1s linear infinite;
}
</style>
```

## 2. Cập nhật `Promotions.vue` - Xóa phần chọn banner trong modal

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form - ĐÃ XÓA banner và banner_id
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    // ĐÃ XÓA: banner: '', banner_id: null,
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form - GIỮ banner cho pre-order
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    banner: '',
    banner_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
    }
    
    return 'active';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            // ĐÃ XÓA: banner: campaign.banner || '', banner_id: campaign.banner_id || null,
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            // ĐÃ XÓA: banner: '', banner_id: null,
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            ...campaignForm.value,
            status: status,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value <= 0) {
        errorMessage.value = 'Giá trị giảm giá phải lớn hơn 0';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            banner: preorder.banner || '',
            banner_id: preorder.banner_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            banner: '',
            banner_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaigns.filter(c => status.value === 'all' || c.status === status.value).length }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                        <!-- Hiển thị banner đầu tiên của campaign (nếu có) -->
                        <img 
                            v-if="campaign.banner" 
                            :src="campaign.banner" 
                            :alt="campaign.name" 
                            class="w-full h-full object-cover"
                            @error="campaign.banner = null"
                        >
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-3 right-3 flex gap-1">
                            <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                        </div>
                    </div>

                    <div class="p-5">
                        <div class="flex justify-between items-start">
                            <div class="flex-1">
                                <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-1 ml-2">
                                <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-4 grid grid-cols-2 gap-3">
                            <div class="bg-orange-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Giảm giá</p>
                                <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                        </div>

                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-2 text-sm">
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📅</span>
                            <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📊</span>
                            <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-3 pt-3 border-t border-gray-100">
                        <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                            <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-sm font-bold text-blue-600">
                                Giảm hiện tại: 
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-2 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="campaignForm.startDate || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <!-- ĐÃ XÓA PHẦN CHỌN BANNER -->

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%)</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Sản phẩm Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể trước khi tạo chiến dịch</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm được chọn</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm *</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner</label>
                        <div class="flex gap-3">
                            <select 
                                v-model="preorderForm.banner_id" 
                                class="flex-1 border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                                @change="onPreorderBannerSelect"
                            >
                                <option :value="null">-- Chọn banner từ danh sách --</option>
                                <option 
                                    v-for="banner in banners" 
                                    :key="banner.id" 
                                    :value="banner.id"
                                >
                                    {{ banner.campaign_name || 'Banner #' + banner.id }} 
                                    {{ banner.image ? '📷' : '🚫' }}
                                    {{ banner.status === 1 ? '✅' : '⛔' }}
                                </option>
                            </select>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="px-3 py-2 bg-gray-200 hover:bg-gray-300 rounded-lg text-sm transition-colors"
                                title="Xóa chọn banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                        
                        <div v-if="selectedPreorderBanner && selectedPreorderBanner.image" class="mt-2 p-2 border border-gray-200 rounded-lg flex items-center gap-3">
                            <img 
                                :src="selectedPreorderBanner.image" 
                                class="h-16 w-24 object-cover rounded border"
                                @error="selectedPreorderBanner.image = null"
                                alt="Banner preview"
                            >
                            <div class="flex-1">
                                <p class="text-sm font-medium">{{ selectedPreorderBanner.campaign_name || 'Banner #' + selectedPreorderBanner.id }}</p>
                                <p class="text-xs text-gray-500 truncate">{{ selectedPreorderBanner.image }}</p>
                                <p class="text-xs text-gray-400">Trạng thái: {{ selectedPreorderBanner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}</p>
                            </div>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="text-red-500 hover:text-red-700 text-sm"
                            >
                                ✕
                            </button>
                        </div>
                        
                        <p class="text-xs text-gray-500 mt-1">Chọn banner đã tạo từ trang Quản lý Banner</p>
                        
                        <div class="mt-2">
                            <label class="text-xs text-gray-500">Hoặc nhập URL trực tiếp</label>
                            <input 
                                v-model="preorderForm.banner" 
                                type="text" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20 mt-1"
                                placeholder="https://example.com/banner.jpg"
                                @input="onPreorderBannerUrlInput"
                            >
                        </div>

                        <div v-if="preorderForm.banner && !selectedPreorderBanner" class="mt-2 relative">
                            <img 
                                :src="preorderForm.banner" 
                                class="h-32 w-full object-cover rounded-lg border"
                                @error="preorderForm.banner = ''"
                                alt="Banner preview"
                            >
                            <button 
                                @click="preorderForm.banner = ''; preorderForm.banner_id = null" 
                                class="absolute top-2 right-2 bg-red-500 text-white rounded-full p-1 hover:bg-red-600 transition-colors"
                                title="Xóa banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
```

## 3. Thêm route API để lấy danh sách campaign cho banner

```php
// routes/web.php - Thêm trong admin group
Route::get('/promotions/campaigns/list', [PromotionController::class, 'getCampaignsList'])->name('admin.promotions.campaigns.list');
```

## 4. Thêm method trong `PromotionController`

```php
public function getCampaignsList()
{
    try {
        $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
            ->orderBy('start_time', 'desc')
            ->get()
            ->map(function ($campaign) {
                return [
                    'id' => $campaign->id,
                    'name' => $campaign->name,
                    'status' => $campaign->status,
                    'start_time' => $campaign->start_time,
                    'end_time' => $campaign->end_time,
                ];
            });
        
        return response()->json($campaigns);
    } catch (\Exception $e) {
        return response()->json([]);
    }
}
```

## Tóm tắt thay đổi:

1. **Banners.vue**:
   - Thêm `fetchCampaigns()` để lấy danh sách chiến dịch từ API
   - Dropdown chọn chiến dịch hiển thị tên và trạng thái
   - Gọi API khi mở modal

2. **Promotions.vue**:
   - Xóa `banner` và `banner_id` khỏi `campaignForm`
   - Xóa toàn bộ phần UI chọn banner trong modal
   - Vẫn giữ banner cho pre-order

3. **Controller**:
   - Thêm API `/admin/promotions/campaigns/list` để lấy danh sách campaign

4. **Migration**:
   - Đã có migration thêm `campaign_id` vào `banners`


## User:
07/07/2026 09:35

![image.png](data:image/webp;base64,)

kiểm tra lại thời gian của chiến dịch để tự động hiển thị trạng thái cho tôi vd nnay 7/7 chiến dịch bắt đầu 7/7 thì là "đang diễn ra",... coi và chỉnh lại cho tôi.
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form - ĐÃ XÓA banner và banner_id
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    // ĐÃ XÓA: banner: '', banner_id: null,
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form - GIỮ banner cho pre-order
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    banner: '',
    banner_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
    }
    
    return 'active';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            // ĐÃ XÓA: banner: campaign.banner || '', banner_id: campaign.banner_id || null,
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            // ĐÃ XÓA: banner: '', banner_id: null,
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            ...campaignForm.value,
            status: status,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value <= 0) {
        errorMessage.value = 'Giá trị giảm giá phải lớn hơn 0';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            banner: preorder.banner || '',
            banner_id: preorder.banner_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            banner: '',
            banner_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaigns.filter(c => status.value === 'all' || c.status === status.value).length }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                        <!-- Hiển thị banner đầu tiên của campaign (nếu có) -->
                        <img 
                            v-if="campaign.banner" 
                            :src="campaign.banner" 
                            :alt="campaign.name" 
                            class="w-full h-full object-cover"
                            @error="campaign.banner = null"
                        >
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-3 right-3 flex gap-1">
                            <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                        </div>
                    </div>

                    <div class="p-5">
                        <div class="flex justify-between items-start">
                            <div class="flex-1">
                                <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-1 ml-2">
                                <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-4 grid grid-cols-2 gap-3">
                            <div class="bg-orange-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Giảm giá</p>
                                <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                        </div>

                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-2 text-sm">
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📅</span>
                            <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📊</span>
                            <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-3 pt-3 border-t border-gray-100">
                        <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                            <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-sm font-bold text-blue-600">
                                Giảm hiện tại: 
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-2 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="campaignForm.startDate || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <!-- ĐÃ XÓA PHẦN CHỌN BANNER -->

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%)</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Sản phẩm Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể trước khi tạo chiến dịch</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm được chọn</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm *</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner</label>
                        <div class="flex gap-3">
                            <select 
                                v-model="preorderForm.banner_id" 
                                class="flex-1 border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                                @change="onPreorderBannerSelect"
                            >
                                <option :value="null">-- Chọn banner từ danh sách --</option>
                                <option 
                                    v-for="banner in banners" 
                                    :key="banner.id" 
                                    :value="banner.id"
                                >
                                    {{ banner.campaign_name || 'Banner #' + banner.id }} 
                                    {{ banner.image ? '📷' : '🚫' }}
                                    {{ banner.status === 1 ? '✅' : '⛔' }}
                                </option>
                            </select>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="px-3 py-2 bg-gray-200 hover:bg-gray-300 rounded-lg text-sm transition-colors"
                                title="Xóa chọn banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                        
                        <div v-if="selectedPreorderBanner && selectedPreorderBanner.image" class="mt-2 p-2 border border-gray-200 rounded-lg flex items-center gap-3">
                            <img 
                                :src="selectedPreorderBanner.image" 
                                class="h-16 w-24 object-cover rounded border"
                                @error="selectedPreorderBanner.image = null"
                                alt="Banner preview"
                            >
                            <div class="flex-1">
                                <p class="text-sm font-medium">{{ selectedPreorderBanner.campaign_name || 'Banner #' + selectedPreorderBanner.id }}</p>
                                <p class="text-xs text-gray-500 truncate">{{ selectedPreorderBanner.image }}</p>
                                <p class="text-xs text-gray-400">Trạng thái: {{ selectedPreorderBanner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}</p>
                            </div>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="text-red-500 hover:text-red-700 text-sm"
                            >
                                ✕
                            </button>
                        </div>
                        
                        <p class="text-xs text-gray-500 mt-1">Chọn banner đã tạo từ trang Quản lý Banner</p>
                        
                        <div class="mt-2">
                            <label class="text-xs text-gray-500">Hoặc nhập URL trực tiếp</label>
                            <input 
                                v-model="preorderForm.banner" 
                                type="text" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20 mt-1"
                                placeholder="https://example.com/banner.jpg"
                                @input="onPreorderBannerUrlInput"
                            >
                        </div>

                        <div v-if="preorderForm.banner && !selectedPreorderBanner" class="mt-2 relative">
                            <img 
                                :src="preorderForm.banner" 
                                class="h-32 w-full object-cover rounded-lg border"
                                @error="preorderForm.banner = ''"
                                alt="Banner preview"
                            >
                            <button 
                                @click="preorderForm.banner = ''; preorderForm.banner_id = null" 
                                class="absolute top-2 right-2 bg-red-500 text-white rounded-full p-1 hover:bg-red-600 transition-colors"
                                title="Xóa banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>


## Assistant:
07/07/2026 09:35

Tôi thấy vấn đề: `calculateCampaignStatus` đang so sánh với `today` nhưng `today` được tạo với `setHours(0,0,0,0)` nên chỉ so sánh ngày, không so sánh giờ. Điều này vẫn đúng nhưng cần đảm bảo logic chính xác.

Vấn đề chính là: **Bạn cần đảm bảo rằng `campaign.status` được cập nhật đúng dựa trên thời gian thực**. Hiện tại, code của bạn đã có `processedCampaigns` computed để tính lại status mỗi khi dữ liệu thay đổi. Tuy nhiên, cần kiểm tra lại logic so sánh ngày tháng.

Dưới đây là phiên bản **sửa lại hàm `calculateCampaignStatus`** để chính xác hơn:

```javascript
// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    // Nếu không có ngày bắt đầu => trạng thái mặc định là "Sắp diễn ra"
    if (!startDate) return 'scheduled';
    
    // Lấy thời gian hiện tại (so sánh cả ngày, tháng, năm)
    const today = new Date();
    // Đặt về 00:00:00 để so sánh theo ngày
    today.setHours(0, 0, 0, 0);
    
    // Chuyển đổi startDate thành Date object
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    // Trường hợp 1: Ngày bắt đầu > Hôm nay => Sắp diễn ra
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    // Trường hợp 2: Có ngày kết thúc
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        // Nếu ngày kết thúc < Hôm nay => Đã kết thúc
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        // Nếu ngày kết thúc === Hôm nay và ngày bắt đầu <= Hôm nay => Đang diễn ra
        if (end.getTime() === today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    // Trường hợp 3: Đã có ngày bắt đầu và (không có ngày kết thúc hoặc ngày kết thúc >= hôm nay)
    // => Đang diễn ra
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    // Mặc định
    return 'scheduled';
};
```

Và quan trọng nhất là **cần cập nhật lại status mỗi khi component được mount**:

```javascript
// ==================== ON MOUNTED ====================
onMounted(() => {
    showFlashMessage();
    
    // Cập nhật status cho tất cả campaigns dựa trên thời gian thực
    campaigns.value = campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

// Thêm watcher để cập nhật status khi campaigns thay đổi
watch(campaigns, (newCampaigns) => {
    if (newCampaigns && newCampaigns.length > 0) {
        // Không cần làm gì vì đã có processedCampaigns computed
    }
}, { deep: true });
```

Vấn đề chính là **hiển thị số lượng thống kê trên các tab**. Bạn cần cập nhật computed để tính đúng số lượng:

```javascript
// ==================== COMPUTED ====================

// Lọc campaigns theo status (đã tính lại status)
const filteredCampaigns = computed(() => {
    // Sử dụng processedCampaigns đã được tính lại status
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

// Thêm computed để đếm số lượng theo từng status
const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});
```

Và cập nhật template cho các tab để hiển thị số lượng chính xác:

```vue
<!-- Status tabs for campaigns -->
<div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
    <button 
        v-for="status in statusOptions" 
        :key="status.value"
        @click="activeStatusTab = status.value"
        class="px-4 py-2 text-sm font-medium transition-all"
        :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
    >
        {{ status.label }}
        <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
            {{ campaignCounts[status.value] }}
        </span>
    </button>
</div>
```

Đây là code hoàn chỉnh phần sửa đổi:

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// ... các props và state giữ nguyên ...

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    // Nếu ngày bắt đầu > hôm nay => Sắp diễn ra
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    // Nếu có ngày kết thúc
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        // Nếu ngày kết thúc < hôm nay => Đã kết thúc
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        // Nếu ngày kết thúc >= hôm nay và ngày bắt đầu <= hôm nay => Đang diễn ra
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    // Nếu ngày bắt đầu <= hôm nay và không có ngày kết thúc hoặc ngày kết thúc >= hôm nay
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

// Thống kê số lượng theo trạng thái
const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

// Lọc campaigns theo status
const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

// ... các computed khác giữ nguyên ...

// ==================== ON MOUNTED ====================
onMounted(() => {
    showFlashMessage();
    
    // Force update status cho campaigns
    campaigns.value = campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});
</script>
```

Và cập nhật template phần hiển thị số lượng:

```vue
<!-- Status tabs for campaigns -->
<div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
    <button 
        v-for="status in statusOptions" 
        :key="status.value"
        @click="activeStatusTab = status.value"
        class="px-4 py-2 text-sm font-medium transition-all"
        :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
    >
        {{ status.label }}
        <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
            {{ campaignCounts[status.value] }}
        </span>
    </button>
</div>
```

Cập nhật phần hiển thị trạng thái trong card:

```vue
<div class="absolute top-3 right-3 flex gap-1">
    <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
        {{ getStatusLabel(campaign.status) }}
    </span>
    <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
</div>
```

**Tóm lại các thay đổi:**

1. **Hàm `calculateCampaignStatus`** - sửa logic so sánh chính xác hơn
2. **Thêm `campaignCounts`** - computed để đếm số lượng theo từng trạng thái
3. **Cập nhật template** - hiển thị số lượng chính xác trên các tab
4. **`onMounted`** - force update status khi load trang

Với những thay đổi này, khi ngày hôm nay là 07/07/2026:
- Chiến dịch 01/06/2025 - 30/06/2025: **Đã kết thúc** ✅
- Chiến dịch 01/07/2025 - 31/07/2025: **Đang diễn ra** ✅


## User:
07/07/2026 09:49

Kiểm tra lại cho tôi các chức năng thêm / xóa / sửa sao tôi thấy thêm sửa tôi thao tác bị lỗi 
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form - ĐÃ XÓA banner và banner_id
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    // ĐÃ XÓA: banner: '', banner_id: null,
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form - GIỮ banner cho pre-order
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    banner: '',
    banner_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    // Nếu không có ngày bắt đầu => trạng thái mặc định là "Sắp diễn ra"
    if (!startDate) return 'scheduled';
    
    // Lấy thời gian hiện tại (so sánh cả ngày, tháng, năm)
    const today = new Date();
    // Đặt về 00:00:00 để so sánh theo ngày
    today.setHours(0, 0, 0, 0);
    
    // Chuyển đổi startDate thành Date object
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    // Trường hợp 1: Ngày bắt đầu > Hôm nay => Sắp diễn ra
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    // Trường hợp 2: Có ngày kết thúc
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        // Nếu ngày kết thúc < Hôm nay => Đã kết thúc
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        // Nếu ngày kết thúc === Hôm nay và ngày bắt đầu <= Hôm nay => Đang diễn ra
        if (end.getTime() === today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    // Trường hợp 3: Đã có ngày bắt đầu và (không có ngày kết thúc hoặc ngày kết thúc >= hôm nay)
    // => Đang diễn ra
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    // Mặc định
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            // ĐÃ XÓA: banner: campaign.banner || '', banner_id: campaign.banner_id || null,
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            // ĐÃ XÓA: banner: '', banner_id: null,
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            ...campaignForm.value,
            status: status,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value <= 0) {
        errorMessage.value = 'Giá trị giảm giá phải lớn hơn 0';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            banner: preorder.banner || '',
            banner_id: preorder.banner_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            banner: '',
            banner_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

// Thêm computed để đếm số lượng theo từng status
const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});


const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

// ==================== ON MOUNTED ====================
onMounted(() => {
    showFlashMessage();
    
    // Cập nhật status cho tất cả campaigns dựa trên thời gian thực
    campaigns.value = campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

// Thêm watcher để cập nhật status khi campaigns thay đổi
watch(campaigns, (newCampaigns) => {
    if (newCampaigns && newCampaigns.length > 0) {
        // Không cần làm gì vì đã có processedCampaigns computed
    }
}, { deep: true });

</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                        <!-- Hiển thị banner đầu tiên của campaign (nếu có) -->
                        <img 
                            v-if="campaign.banner" 
                            :src="campaign.banner" 
                            :alt="campaign.name" 
                            class="w-full h-full object-cover"
                            @error="campaign.banner = null"
                        >
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-3 right-3 flex gap-1">
                            <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                        </div>
                    </div>

                    <div class="p-5">
                        <div class="flex justify-between items-start">
                            <div class="flex-1">
                                <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-1 ml-2">
                                <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-4 grid grid-cols-2 gap-3">
                            <div class="bg-orange-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Giảm giá</p>
                                <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                        </div>

                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-2 text-sm">
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📅</span>
                            <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📊</span>
                            <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-3 pt-3 border-t border-gray-100">
                        <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                            <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-sm font-bold text-blue-600">
                                Giảm hiện tại: 
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-2 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="campaignForm.startDate || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <!-- ĐÃ XÓA PHẦN CHỌN BANNER -->

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%)</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Sản phẩm Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-sm cursor-pointer flex-1">
                                        <span class="font-medium">{{ variant.product?.name || variant.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs ml-1">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-gray-400 text-xs ml-1">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể trước khi tạo chiến dịch</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm được chọn</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm *</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner</label>
                        <div class="flex gap-3">
                            <select 
                                v-model="preorderForm.banner_id" 
                                class="flex-1 border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                                @change="onPreorderBannerSelect"
                            >
                                <option :value="null">-- Chọn banner từ danh sách --</option>
                                <option 
                                    v-for="banner in banners" 
                                    :key="banner.id" 
                                    :value="banner.id"
                                >
                                    {{ banner.campaign_name || 'Banner #' + banner.id }} 
                                    {{ banner.image ? '📷' : '🚫' }}
                                    {{ banner.status === 1 ? '✅' : '⛔' }}
                                </option>
                            </select>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="px-3 py-2 bg-gray-200 hover:bg-gray-300 rounded-lg text-sm transition-colors"
                                title="Xóa chọn banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                        
                        <div v-if="selectedPreorderBanner && selectedPreorderBanner.image" class="mt-2 p-2 border border-gray-200 rounded-lg flex items-center gap-3">
                            <img 
                                :src="selectedPreorderBanner.image" 
                                class="h-16 w-24 object-cover rounded border"
                                @error="selectedPreorderBanner.image = null"
                                alt="Banner preview"
                            >
                            <div class="flex-1">
                                <p class="text-sm font-medium">{{ selectedPreorderBanner.campaign_name || 'Banner #' + selectedPreorderBanner.id }}</p>
                                <p class="text-xs text-gray-500 truncate">{{ selectedPreorderBanner.image }}</p>
                                <p class="text-xs text-gray-400">Trạng thái: {{ selectedPreorderBanner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}</p>
                            </div>
                            <button 
                                @click="preorderForm.banner_id = null; preorderForm.banner = ''" 
                                class="text-red-500 hover:text-red-700 text-sm"
                            >
                                ✕
                            </button>
                        </div>
                        
                        <p class="text-xs text-gray-500 mt-1">Chọn banner đã tạo từ trang Quản lý Banner</p>
                        
                        <div class="mt-2">
                            <label class="text-xs text-gray-500">Hoặc nhập URL trực tiếp</label>
                            <input 
                                v-model="preorderForm.banner" 
                                type="text" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20 mt-1"
                                placeholder="https://example.com/banner.jpg"
                                @input="onPreorderBannerUrlInput"
                            >
                        </div>

                        <div v-if="preorderForm.banner && !selectedPreorderBanner" class="mt-2 relative">
                            <img 
                                :src="preorderForm.banner" 
                                class="h-32 w-full object-cover rounded-lg border"
                                @error="preorderForm.banner = ''"
                                alt="Banner preview"
                            >
                            <button 
                                @click="preorderForm.banner = ''; preorderForm.banner_id = null" 
                                class="absolute top-2 right-2 bg-red-500 text-white rounded-full p-1 hover:bg-red-600 transition-colors"
                                title="Xóa banner"
                            >
                                <span class="material-symbols-outlined text-sm">close</span>
                            </button>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Promotion;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
   

    public function index()
    {
        try {
            // Lấy campaigns - KHÔNG load banner_id nữa
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    // Lấy danh sách banner của chiến dịch
                    $banners = $campaign->banners()->where('status', 1)->orderBy('order')->get();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'banners' => $banners->map(function ($banner) {
                            return [
                                'id' => $banner->id,
                                'title' => $banner->title,
                                'image' => $banner->image,
                                'link' => $banner->link,
                                'order' => $banner->order,
                            ];
                        }),
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                        'productVariants' => $campaign->productVariants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'name' => $variant->product->name ?? 'Sản phẩm',
                                'color' => $variant->color->name ?? 'Không màu',
                                'price' => $variant->price ?? 0,
                            ];
                        }),
                    ];
                });

            // Lấy tất cả promotions
            $promotions = Promotion::with(['campaign', 'product'])->latest()->get();

            // Lấy tất cả banners - HIỂN THỊ CẢ CAMPAIGN_ID
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy tất cả products
            $products = Product::with(['variants.color', 'category', 'brand'])->latest()->get();

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])->latest()->get();

            // Lấy chỉ sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color', 'category', 'brand'])
                ->latest()
                ->get();

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => $promotions,
                'banners' => $banners,
                'products' => $products,
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'banner_ids' => 'nullable|array',
                'banner_ids.*' => 'exists:banners,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            // Gán banner cho chiến dịch
            if (!empty($validated['banner_ids'])) {
                Banner::whereIn('id', $validated['banner_ids'])->update(['campaign_id' => $campaign->id]);
            }

            // Tạo config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            // Gán sản phẩm
            if (!empty($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'banner_ids' => 'nullable|array',
                'banner_ids.*' => 'exists:banners,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            // Cập nhật banner cho chiến dịch
            // 1. Xóa tất cả banner cũ của chiến dịch này
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            // 2. Gán banner mới
            if (!empty($validated['banner_ids'])) {
                Banner::whereIn('id', $validated['banner_ids'])->update(['campaign_id' => $campaign->id]);
            }

            // Cập nhật config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            // Cập nhật sản phẩm
            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            
            // Giải phóng banner khỏi chiến dịch
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }


    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }


    // ==================== VOUCHER METHODS ====================

    public function storePromotion(Request $request)
    {
        try {
            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper($validated['code']),
                'type' => 'voucher',
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'used' => 0,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePromotion(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code,' . $id,
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function togglePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->update(['active' => !$promotion->active]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'type' => 'preorder_tier',
                'target_type' => 'preorder',
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
                'discount_value' => 0,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePreorder($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }
    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}


## Assistant:
07/07/2026 09:49

Tôi thấy vấn đề chính của bạn là:

1. **Vue component đang gửi dữ liệu `banner_ids` nhưng controller không xử lý đúng** - Bạn đã xóa banner khỏi form nhưng vẫn gửi `banner_ids` rỗng, controller đang cố gắng xử lý nó và gây lỗi.

2. **Thiếu các method `onPreorderBannerSelect` và `onPreorderBannerUrlInput`** trong script.

Dưới đây là code hoàn chỉnh đã sửa lỗi:

## 1. Sửa file `Promotions.vue` - Thêm các method bị thiếu và xử lý đúng

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form - ĐÃ XÓA banner và banner_id
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form - GIỮ banner cho pre-order
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    banner: '',
    banner_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() === today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== BANNER FUNCTIONS CHO PRE-ORDER ====================

const selectedPreorderBanner = computed(() => {
    if (!preorderForm.value.banner_id) return null;
    return banners.value.find(b => b.id === preorderForm.value.banner_id);
});

const onPreorderBannerSelect = () => {
    const bannerId = preorderForm.value.banner_id;
    if (bannerId) {
        const selected = banners.value.find(b => b.id === bannerId);
        if (selected && selected.image) {
            preorderForm.value.banner = selected.image;
        }
    } else {
        preorderForm.value.banner = '';
    }
};

const onPreorderBannerUrlInput = () => {
    if (preorderForm.value.banner) {
        preorderForm.value.banner_id = null;
    }
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        // KHÔNG gửi banner_ids vì đã xóa khỏi form
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value <= 0) {
        errorMessage.value = 'Giá trị giảm giá phải lớn hơn 0';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            banner: preorder.banner || '',
            banner_id: preorder.banner_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            banner: '',
            banner_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

// Thêm computed để đếm số lượng theo từng status
const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});


const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

// ==================== ON MOUNTED ====================
onMounted(() => {
    showFlashMessage();
});
</script>
```

## 2. Sửa `PromotionController.php` - Bỏ xử lý `banner_ids`

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Promotion;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns - KHÔNG load banner_id nữa
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    // Lấy danh sách banner của chiến dịch
                    $banners = $campaign->banners()->where('status', 1)->orderBy('order')->get();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'banners' => $banners->map(function ($banner) {
                            return [
                                'id' => $banner->id,
                                'title' => $banner->title,
                                'image' => $banner->image,
                                'link' => $banner->link,
                                'order' => $banner->order,
                            ];
                        }),
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                        'productVariants' => $campaign->productVariants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'name' => $variant->product->name ?? 'Sản phẩm',
                                'color' => $variant->color->name ?? 'Không màu',
                                'price' => $variant->price ?? 0,
                            ];
                        }),
                    ];
                });

            // Lấy tất cả promotions
            $promotions = Promotion::with(['campaign', 'product'])->latest()->get();

            // Lấy tất cả banners - HIỂN THỊ CẢ CAMPAIGN_ID
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy tất cả products
            $products = Product::with(['variants.color', 'category', 'brand'])->latest()->get();

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])->latest()->get();

            // Lấy chỉ sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color', 'category', 'brand'])
                ->latest()
                ->get();

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => $promotions,
                'banners' => $banners,
                'products' => $products,
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                // ĐÃ XÓA 'banner_ids' khỏi validation
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            // Tạo config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            // Gán sản phẩm
            if (!empty($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                // ĐÃ XÓA 'banner_ids' khỏi validation
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            // Cập nhật config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            // Cập nhật sản phẩm
            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            
            // Giải phóng banner khỏi chiến dịch
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== VOUCHER METHODS ====================

    public function storePromotion(Request $request)
    {
        try {
            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper($validated['code']),
                'type' => 'voucher',
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'used' => 0,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePromotion(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code,' . $id,
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function togglePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->update(['active' => !$promotion->active]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'banner' => 'nullable|string|max:255',
                'banner_id' => 'nullable|exists:banners,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'type' => 'preorder_tier',
                'target_type' => 'preorder',
                'product_id' => $validated['product_id'],
                'banner' => $validated['banner'] ?? null,
                'banner_id' => $validated['banner_id'] ?? null,
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
                'discount_value' => 0,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'banner' => 'nullable|string|max:255',
                'banner_id' => 'nullable|exists:banners,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'product_id' => $validated['product_id'],
                'banner' => $validated['banner'] ?? null,
                'banner_id' => $validated['banner_id'] ?? null,
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePreorder($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}
```

## 3. Cập nhật Model `Campaign` - Xóa quan hệ với Banner

```php
<?php
// app/Models/Campaign.php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Campaign extends Model
{
    use HasFactory;

    protected $fillable = [
        'name',
        'type',
        'description',
        'start_time',
        'end_time',
        'status',
        'priority',
        'featured'
    ];

    protected $casts = [
        'start_time' => 'datetime',
        'end_time' => 'datetime',
        'featured' => 'boolean',
    ];

    public function productVariants()
    {
        return $this->belongsToMany(ProductVariant::class, 'campaign_products');
    }

    public function configs()
    {
        return $this->hasMany(CampaignConfig::class);
    }

    public function orders()
    {
        return $this->hasMany(Order::class);
    }

    // ĐÃ XÓA quan hệ với Banner

    public function getNameAttribute()
    {
        if ($this->attributes['name'] ?? false) {
            return $this->attributes['name'];
        }
        if ($this->start_time && $this->end_time) {
            $start = $this->start_time->format('d/m/Y');
            $end = $this->end_time->format('d/m/Y');
            return "Chiến dịch {$start} - {$end}";
        }
        return 'Chiến dịch';
    }

    public function getStartDateAttribute()
    {
        return $this->start_time ? $this->start_time->format('Y-m-d') : null;
    }

    public function getEndDateAttribute()
    {
        return $this->end_time ? $this->end_time->format('Y-m-d') : null;
    }

    public function getDiscountAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->discount_percent . '%' : '0%';
    }

    public function getMinQuantityAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->quantity : 0;
    }

    public function getProductIdsAttribute()
    {
        return $this->productVariants->pluck('id')->toArray();
    }
}
```

## Tóm tắt các thay đổi:

1. **Promotions.vue**:
   - Thêm các method `onPreorderBannerSelect` và `onPreorderBannerUrlInput` (bị thiếu)
   - Xóa `banner_ids` khỏi dữ liệu gửi lên server trong `saveCampaign`
   - Xóa `banner_ids` khỏi `campaignForm`

2. **PromotionController.php**:
   - Xóa `banner_ids` khỏi validation trong `storeCampaign` và `updateCampaign`
   - Xóa logic gán banner trong `storeCampaign` và `updateCampaign`
   - Thêm `banner` và `banner_id` vào validation cho pre-order

3. **Campaign Model**:
   - Xóa quan hệ `banner()` (belongsTo) với Banner

Với những thay đổi này, chức năng thêm/sửa chiến dịch sẽ hoạt động bình thường mà không bị lỗi.


## User:
07/07/2026 09:57

sao khuyến mãi mất dữ liệu chọn sản phẩm rồi (không lấy được dữ liệu sp), chức năng vẫn chưa được sửa vẫn thao tác không được


## Assistant:
07/07/2026 09:57

Tôi thấy vấn đề là bạn đã xóa `banner_ids` khỏi validation nhưng trong Vue component vẫn đang gửi dữ liệu không đúng format, và quan trọng là **dữ liệu sản phẩm không được load đúng cách từ Controller**.

Hãy kiểm tra lại toàn bộ luồng dữ liệu:

## 1. Kiểm tra dữ liệu từ Controller - `PromotionController.php`

```php
public function index()
{
    try {
        // Lấy campaigns
        $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
            ->latest()
            ->get()
            ->map(function ($campaign) {
                $config = $campaign->configs->first();
                
                return [
                    'id' => $campaign->id,
                    'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                    'type' => $campaign->type ?? 'seasonal',
                    'description' => $campaign->description ?? '',
                    'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                    'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                    'status' => $campaign->status ?? 'scheduled',
                    'priority' => $campaign->priority ?? 0,
                    'featured' => $campaign->featured ?? false,
                    'quantity' => $config ? (int) $config->quantity : 0,
                    'discountPercent' => $config ? (float) $config->discount_percent : 0,
                    'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                    'products' => $campaign->productVariants->pluck('id')->toArray(),
                ];
            });

        // Lấy product variants - QUAN TRỌNG: phải load đúng dữ liệu
        $productVariants = ProductVariant::with(['product', 'color'])
            ->get()
            ->map(function ($variant) {
                return [
                    'id' => $variant->id,
                    'name' => $variant->name ?? $variant->product->name ?? 'Sản phẩm',
                    'product' => $variant->product ? [
                        'id' => $variant->product->id,
                        'name' => $variant->product->name,
                        'is_preorder' => $variant->product->is_preorder ?? false,
                    ] : null,
                    'color' => $variant->color ? [
                        'id' => $variant->color->id,
                        'name' => $variant->color->name,
                    ] : null,
                    'price' => $variant->price ?? 0,
                ];
            });

        // Lấy chỉ sản phẩm pre-order
        $preorderProducts = Product::where('is_preorder', true)
            ->with(['variants.color'])
            ->latest()
            ->get()
            ->map(function ($product) {
                return [
                    'id' => $product->id,
                    'name' => $product->name,
                    'variants' => $product->variants->map(function ($variant) {
                        return [
                            'id' => $variant->id,
                            'color' => $variant->color ? $variant->color->name : null,
                            'price' => $variant->price ?? 0,
                        ];
                    }),
                ];
            });

        return Inertia::render('Admin/Promotions', [
            'campaigns' => $campaigns,
            'promotions' => $promotions ?? [],
            'banners' => $banners ?? [],
            'products' => $products ?? [],
            'productVariants' => $productVariants,
            'preorderProducts' => $preorderProducts,
        ]);

    } catch (\Exception $e) {
        Log::error('Lỗi load trang promotions: ' . $e->getMessage());
        return Inertia::render('Admin/Promotions', [
            'campaigns' => [],
            'promotions' => [],
            'banners' => [],
            'products' => [],
            'productVariants' => [],
            'preorderProducts' => [],
            'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
        ]);
    }
}
```

## 2. Sửa lại `storeCampaign` và `updateCampaign` - PHẢI nhận `banner_ids` nhưng xử lý đúng

```php
public function storeCampaign(Request $request)
{
    try {
        DB::beginTransaction();

        $validated = $request->validate([
            'name' => 'nullable|string|max:255',
            'type' => 'nullable|string|max:50',
            'description' => 'nullable|string',
            'startDate' => 'nullable|date',
            'endDate' => 'nullable|date|after:startDate',
            'status' => 'nullable|in:scheduled,active,ended',
            'priority' => 'nullable|integer|min:0',
            'featured' => 'boolean',
            'quantity' => 'nullable|integer|min:0',
            'discountPercent' => 'nullable|numeric|min:0|max:100',
            'products' => 'nullable|array',
            'products.*' => 'exists:product_variants,id',
            // THÊM banner_ids để nhận nhưng KHÔNG xử lý
            'banner_ids' => 'nullable|array',
            'banner_ids.*' => 'exists:banners,id',
        ]);

        $campaign = Campaign::create([
            'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
            'type' => $validated['type'] ?? 'seasonal',
            'description' => $validated['description'] ?? '',
            'start_time' => $validated['startDate'] ?? null,
            'end_time' => $validated['endDate'] ?? null,
            'status' => $validated['status'] ?? 'scheduled',
            'priority' => $validated['priority'] ?? 0,
            'featured' => $validated['featured'] ?? false,
        ]);

        // Tạo config
        if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
            CampaignConfig::create([
                'campaign_id' => $campaign->id,
                'quantity' => $validated['quantity'] ?? 0,
                'discount_percent' => $validated['discountPercent'] ?? 0,
            ]);
        }

        // Gán sản phẩm - QUAN TRỌNG: phải attach đúng
        if (!empty($validated['products']) && is_array($validated['products'])) {
            $campaign->productVariants()->attach($validated['products']);
        }

        // KHÔNG xử lý banner_ids ở đây (banner sẽ được quản lý riêng)

        DB::commit();

        return redirect()->route('admin.promotions.index')->with([
            'success' => true,
            'message' => 'Thêm chiến dịch thành công!'
        ]);

    } catch (\Exception $e) {
        DB::rollBack();
        Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
        return redirect()->back()->with([
            'success' => false,
            'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
        ]);
    }
}

public function updateCampaign(Request $request, $id)
{
    try {
        DB::beginTransaction();

        $campaign = Campaign::findOrFail($id);

        $validated = $request->validate([
            'name' => 'nullable|string|max:255',
            'type' => 'nullable|string|max:50',
            'description' => 'nullable|string',
            'startDate' => 'nullable|date',
            'endDate' => 'nullable|date|after:startDate',
            'status' => 'nullable|in:scheduled,active,ended',
            'priority' => 'nullable|integer|min:0',
            'featured' => 'boolean',
            'quantity' => 'nullable|integer|min:0',
            'discountPercent' => 'nullable|numeric|min:0|max:100',
            'products' => 'nullable|array',
            'products.*' => 'exists:product_variants,id',
            'banner_ids' => 'nullable|array',
            'banner_ids.*' => 'exists:banners,id',
        ]);

        $campaign->update([
            'name' => $validated['name'] ?? $campaign->name,
            'type' => $validated['type'] ?? $campaign->type,
            'description' => $validated['description'] ?? $campaign->description,
            'start_time' => $validated['startDate'] ?? $campaign->start_time,
            'end_time' => $validated['endDate'] ?? $campaign->end_time,
            'status' => $validated['status'] ?? $campaign->status,
            'priority' => $validated['priority'] ?? $campaign->priority,
            'featured' => $validated['featured'] ?? $campaign->featured,
        ]);

        // Cập nhật config
        if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
            $config = $campaign->configs()->first();
            if ($config) {
                $config->update([
                    'quantity' => $validated['quantity'] ?? $config->quantity,
                    'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                ]);
            } else {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }
        }

        // Cập nhật sản phẩm - QUAN TRỌNG: sync đúng
        if (isset($validated['products'])) {
            $campaign->productVariants()->sync($validated['products']);
        }

        // KHÔNG xử lý banner_ids ở đây

        DB::commit();

        return redirect()->route('admin.promotions.index')->with([
            'success' => true,
            'message' => 'Cập nhật chiến dịch thành công!'
        ]);

    } catch (\Exception $e) {
        DB::rollBack();
        Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
        return redirect()->back()->with([
            'success' => false,
            'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
        ]);
    }
}
```

## 3. Sửa lại `Promotions.vue` - Gửi đúng dữ liệu

```javascript
// ==================== CAMPAIGN FUNCTIONS ====================

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        // Tạo data với đúng format
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : [],
            banner_ids: [] // Gửi mảng rỗng để tránh lỗi validation
        };
        
        console.log('Sending data:', data); // Debug
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        console.error('Response:', error.response?.data); // Debug
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};
```

## 4. Kiểm tra Product Model - Đảm bảo có `is_preorder`

```php
<?php
// app/Models/Product.php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Product extends Model
{
    use HasFactory;

    protected $fillable = [
        'name',
        'slug',
        'description',
        'category_id',
        'brand_id',
        'image',
        'is_preorder', // QUAN TRỌNG: phải có field này
        'status',
    ];

    protected $casts = [
        'is_preorder' => 'boolean',
        'status' => 'boolean',
    ];

    public function category()
    {
        return $this->belongsTo(Category::class);
    }

    public function brand()
    {
        return $this->belongsTo(Brand::class);
    }

    public function variants()
    {
        return $this->hasMany(ProductVariant::class);
    }

    public function promotions()
    {
        return $this->hasMany(Promotion::class);
    }
}
```

## 5. Kiểm tra ProductVariant Model

```php
<?php
// app/Models/ProductVariant.php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class ProductVariant extends Model
{
    use HasFactory;

    protected $fillable = [
        'product_id',
        'color_id',
        'price',
        'stock',
        'name', // QUAN TRỌNG: có thể có name riêng
        'image',
    ];

    public function product()
    {
        return $this->belongsTo(Product::class);
    }

    public function color()
    {
        return $this->belongsTo(Color::class);
    }

    public function campaigns()
    {
        return $this->belongsToMany(Campaign::class, 'campaign_products');
    }
}
```

## 6. Kiểm tra Migration - Product có `is_preorder`

```php
<?php
// database/migrations/2026_06_04_164541_create_products_table.php

Schema::create('products', function (Blueprint $table) {
    $table->id();
    $table->string('name');
    $table->string('slug')->unique();
    $table->text('description')->nullable();
    $table->foreignId('category_id')->nullable()->constrained()->nullOnDelete();
    $table->foreignId('brand_id')->nullable()->constrained()->nullOnDelete();
    $table->text('image')->nullable();
    $table->boolean('is_preorder')->default(false); // QUAN TRỌNG
    $table->boolean('status')->default(true);
    $table->timestamps();
});
```

## 7. Thêm debug để kiểm tra dữ liệu

Trong `Promotions.vue`, thêm `console.log` để debug:

```javascript
onMounted(() => {
    showFlashMessage();
    
    // Debug: kiểm tra dữ liệu nhận được
    console.log('Campaigns:', campaigns.value);
    console.log('Product Variants:', productVariants.value);
    console.log('Preorder Products:', preorderProducts.value);
});
```

## 8. Kiểm tra route và Controller

Đảm bảo route `POST /admin/promotions/campaign` và `PUT /admin/promotions/campaign/{id}` đang gọi đúng controller.

```php
// routes/web.php
Route::prefix('admin')->middleware(['auth', 'admin'])->name('admin.')->group(function () {
    // Promotion routes
    Route::get('/promotions', [PromotionController::class, 'index'])->name('promotions.index');
    Route::post('/promotions/campaign', [PromotionController::class, 'storeCampaign'])->name('promotions.campaign.store');
    Route::put('/promotions/campaign/{id}', [PromotionController::class, 'updateCampaign'])->name('promotions.campaign.update');
    // ... các route khác
});
```

---

## Tóm tắt các vấn đề và cách fix:

1. **Mất dữ liệu sản phẩm**: Do ProductVariant không load đúng relation trong Controller → đã sửa `productVariants` mapping

2. **Lỗi thêm/sửa**: Do Controller không nhận `banner_ids` nhưng Vue gửi → đã thêm vào validation nhưng không xử lý

3. **Lỗi validation**: Do `products` không được validate đúng → đã thêm `'products.*' => 'exists:product_variants,id'`

4. **Thiếu `is_preorder`**: Cần đảm bảo Product model có field này để phân biệt sản phẩm pre-order

Hãy chạy lại và kiểm tra console log để xem dữ liệu có được load đúng không. Nếu vẫn còn lỗi, hãy gửi cho tôi log lỗi từ console và server để tôi debug thêm.


## User:
07/07/2026 10:05

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Product extends Model
{
    use HasFactory;

    protected $fillable = [
        'category_id', 'brand_id', 'name', 'slug', 'material',
        'image_url', 'description', 'thumbnail', 'is_featured',
        'is_preorder', 'status'
    ];

    protected $casts = [
        'is_featured' => 'boolean',
        'is_preorder' => 'boolean',
        'image_url' => 'array',
        'status' => 'integer',
    ];

    public function category()
    {
        return $this->belongsTo(Category::class);
    }

    public function brand()
    {
        return $this->belongsTo(Brand::class);
    }

    public function variants()
    {
        return $this->hasMany(ProductVariant::class);
    }
}

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class ProductVariant extends Model
{
    use HasFactory;

    protected $fillable = [
        'product_id', 'color_id', 'size_name', 'rating', 'stock', 'price'
    ];

    protected $casts = [
        'rating' => 'decimal:1',
        'price' => 'decimal:0',
        'stock' => 'integer',
    ];

    public function product()
    {
        return $this->belongsTo(Product::class);
    }

    public function color()
    {
        return $this->belongsTo(Color::class);
    }


    public function orderDetails()
    {
        return $this->hasMany(OrderDetail::class);
    }

    public function reviews()
    {
        return $this->hasMany(Review::class);
    }

    public function campaigns()
    {
        return $this->belongsToMany(Campaign::class, 'campaign_products');
    }

    public function quoteRequestDetails()
    {
        return $this->hasMany(QuoteRequestDetail::class);
    }
}

<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up()
    {
        Schema::create('products', function (Blueprint $table) {
            $table->id();
            $table->foreignId('category_id')->constrained()->onDelete('restrict');
            $table->foreignId('brand_id')->constrained()->onDelete('restrict');
            $table->string('name');
            $table->string('slug')->unique();
            $table->string('material')->nullable();
            $table->string('image_url')->nullable();
            $table->longText('description')->nullable();
            $table->string('thumbnail')->nullable();
            $table->boolean('is_featured')->default(false);
            $table->boolean('is_preorder')->default(false);
            $table->tinyInteger('status')->default(1);
            $table->timestamps();
        });
    }

    public function down()
    {
        Schema::dropIfExists('products');
    }
};


## Assistant:
07/07/2026 10:05

Tôi thấy vấn đề chính là **ProductVariant không có trường `name`** và Product có `image_url` là mảng JSON. Hãy sửa lại Controller để mapping dữ liệu đúng cách.

## 1. Sửa `PromotionController.php` - `index()` method

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Promotion;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                    ];
                });

            // Lấy tất cả promotions
            $promotions = Promotion::with(['campaign', 'product'])->latest()->get()
                ->map(function ($promotion) {
                    return [
                        'id' => $promotion->id,
                        'code' => $promotion->code,
                        'type' => $promotion->type,
                        'target_type' => $promotion->target_type,
                        'discount_type' => $promotion->discount_type,
                        'discount_value' => $promotion->discount_value,
                        'min_order' => $promotion->min_order,
                        'limit' => $promotion->limit,
                        'used' => $promotion->used ?? 0,
                        'expiry' => $promotion->expiry,
                        'active' => $promotion->active,
                        'description' => $promotion->description,
                        'campaign_id' => $promotion->campaign_id,
                        'campaign' => $promotion->campaign ? [
                            'id' => $promotion->campaign->id,
                            'name' => $promotion->campaign->name,
                        ] : null,
                        'product_id' => $promotion->product_id,
                        'tiers' => $promotion->tiers,
                        'current_buyers' => $promotion->current_buyers ?? 0,
                        'start_date' => $promotion->start_date,
                        'end_date' => $promotion->end_date,
                        'banner' => $promotion->banner ?? '',
                        'banner_id' => $promotion->banner_id ?? null,
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy tất cả products (cho voucher)
            $products = Product::with(['variants.color', 'category', 'brand'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'is_preorder' => $product->is_preorder,
                        'category' => $product->category ? [
                            'id' => $product->category->id,
                            'name' => $product->category->name,
                        ] : null,
                        'brand' => $product->brand ? [
                            'id' => $product->brand->id,
                            'name' => $product->brand->name,
                        ] : null,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            // Lấy product variants - QUAN TRỌNG: phải load đúng dữ liệu
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    // Lấy tên sản phẩm từ product
                    $productName = $variant->product ? $variant->product->name : 'Sản phẩm không xác định';
                    $isPreorder = $variant->product ? $variant->product->is_preorder : false;
                    
                    return [
                        'id' => $variant->id,
                        'name' => $productName, // Sử dụng tên product làm name
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $isPreorder,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy chỉ sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'image_url' => $product->image_url,
                        'description' => $product->description,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => $promotions,
                'banners' => $banners,
                'products' => $products,
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    // ... các method khác giữ nguyên
}
```

## 2. Sửa `Promotions.vue` - phần hiển thị sản phẩm

```vue
<!-- Trong phần CAMPAIGN MODAL, sửa lại phần sản phẩm áp dụng -->
<div>
    <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
    
    <!-- Sản phẩm thường -->
    <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
        <div class="flex items-center gap-2 mb-2">
            <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
            <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }} biến thể)</span>
        </div>
        <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
            <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                 class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                <input 
                    type="checkbox" 
                    :id="'normal-' + variant.id"
                    :checked="isProductSelected(variant.id)"
                    @change="toggleProduct(variant.id)"
                    class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500"
                >
                <label :for="'normal-' + variant.id" class="text-sm cursor-pointer flex-1 flex items-center gap-2">
                    <span class="font-medium">{{ variant.name || variant.product?.name || 'Sản phẩm' }}</span>
                    <span class="text-gray-400 text-xs">({{ variant.color?.name || 'Không màu' }})</span>
                    <span v-if="variant.size_name" class="text-gray-400 text-xs">- Size: {{ variant.size_name }}</span>
                    <span class="text-blue-600 text-xs font-medium">- {{ formatPrice(variant.price) }}</span>
                    <span v-if="variant.stock !== undefined" class="text-gray-400 text-xs">- Stock: {{ variant.stock }}</span>
                </label>
            </div>
        </div>
    </div>

    <!-- Sản phẩm Pre-order -->
    <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
        <div class="flex items-center gap-2 mb-2">
            <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Sản phẩm Pre-order</span>
            <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }} biến thể)</span>
        </div>
        <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
            <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                 class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                <input 
                    type="checkbox" 
                    :id="'preorder-' + variant.id"
                    :checked="isProductSelected(variant.id)"
                    @change="toggleProduct(variant.id)"
                    class="w-4 h-4 text-purple-600 rounded focus:ring-purple-500"
                >
                <label :for="'preorder-' + variant.id" class="text-sm cursor-pointer flex-1 flex items-center gap-2">
                    <span class="font-medium">{{ variant.name || variant.product?.name || 'Sản phẩm' }}</span>
                    <span class="text-gray-400 text-xs">({{ variant.color?.name || 'Không màu' }})</span>
                    <span v-if="variant.size_name" class="text-gray-400 text-xs">- Size: {{ variant.size_name }}</span>
                    <span class="text-purple-600 text-xs font-medium">- {{ formatPrice(variant.price) }}</span>
                </label>
            </div>
        </div>
    </div>

    <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
        <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
        <p>Không có sản phẩm nào</p>
        <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể trước khi tạo chiến dịch</p>
    </div>
    
    <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm được chọn</div>
</div>
```

## 3. Sửa `saveCampaign` - thêm debug và xử lý đúng

```javascript
const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : [],
            banner_ids: [] // Gửi mảng rỗng
        };
        
        console.log('📦 Sending campaign data:', data);
        
        if (editingCampaign.value) {
            console.log('🔄 Updating campaign ID:', editingCampaign.value.id);
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            console.log('➕ Creating new campaign');
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('❌ Lỗi:', error);
        console.error('📋 Response:', error.response?.data);
        
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};
```

## 4. Thêm debug trong `onMounted`

```javascript
onMounted(() => {
    showFlashMessage();
    
    // Debug: kiểm tra dữ liệu nhận được
    console.log('📊 Campaigns:', campaigns.value);
    console.log('📦 Product Variants:', productVariants.value);
    console.log('⏳ Preorder Products:', preorderProducts.value);
    console.log('📷 Banners:', banners.value);
    console.log('🎫 Promotions:', promotions.value);
});
```

## 5. Kiểm tra lại Migration của `promotions` table - cần có `banner` và `banner_id`

```php
<?php
// database/migrations/2026_07_06_211358_create_promotions_table.php

Schema::create('promotions', function (Blueprint $table) {
    $table->id();
    $table->string('code')->unique();
    $table->enum('type', ['voucher', 'preorder_tier']);
    $table->enum('target_type', ['retail', 'wholesale', 'preorder', 'all'])->default('all');
    $table->enum('discount_type', ['fixed', 'percent', 'freeship'])->nullable();
    $table->decimal('discount_value', 15, 0)->default(0);
    $table->decimal('min_order', 15, 0)->default(0);
    $table->integer('limit')->default(100);
    $table->integer('used')->default(0);
    $table->date('expiry')->nullable();
    $table->boolean('active')->default(true);
    $table->text('description')->nullable();
    $table->foreignId('product_id')->nullable()->constrained()->nullOnDelete();
    $table->string('banner')->nullable(); // THÊM
    $table->foreignId('banner_id')->nullable()->constrained('banners')->nullOnDelete(); // THÊM
    $table->json('tiers')->nullable();
    $table->integer('current_buyers')->default(0);
    $table->date('start_date')->nullable();
    $table->date('end_date')->nullable();
    $table->foreignId('campaign_id')->nullable()->constrained()->nullOnDelete();
    $table->timestamps();
});
```

## 6. Chạy migration để thêm cột nếu chưa có

```bash
php artisan migrate
```

## Tóm tắt các thay đổi:

1. **Controller**: Mapping đúng dữ liệu `productVariants` với `product.name` làm tên hiển thị
2. **Vue**: Hiển thị đúng thông tin biến thể (name, color, size, price, stock)
3. **Debug**: Thêm console.log để kiểm tra dữ liệu
4. **Migration**: Đảm bảo `promotions` table có `banner` và `banner_id`

Sau khi thực hiện các thay đổi này, hãy:
1. Chạy `php artisan migrate` nếu cần
2. Refresh trang admin
3. Mở Console (F12) để xem log debug
4. Kiểm tra xem dữ liệu sản phẩm có hiển thị không


## User:
07/07/2026 10:16

![image.png](data:image/webp;base64,)

![image.png](data:image/webp;base64,)

vẫn chưa lấy được dữ liệu sản phẩm của cả bên chiến dịch và cả bên khuyến mãi  pre-order , pre-order vẫn còn liên kết chọn bên baner chỉnh lại bỏ phần liên quan banner cho tôi


## Assistant:
07/07/2026 10:16

Tôi thấy vấn đề là **dữ liệu sản phẩm không được load từ Controller** và bạn muốn **bỏ phần banner khỏi Pre-order**. Hãy sửa lại toàn bộ:

## 1. Sửa `PromotionController.php` - Load đúng dữ liệu sản phẩm

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Promotion;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                    ];
                });

            // Lấy tất cả promotions
            $promotions = Promotion::with(['campaign', 'product'])
                ->latest()
                ->get()
                ->map(function ($promotion) {
                    return [
                        'id' => $promotion->id,
                        'code' => $promotion->code,
                        'type' => $promotion->type,
                        'target_type' => $promotion->target_type,
                        'discount_type' => $promotion->discount_type,
                        'discount_value' => $promotion->discount_value,
                        'min_order' => $promotion->min_order,
                        'limit' => $promotion->limit,
                        'used' => $promotion->used ?? 0,
                        'expiry' => $promotion->expiry,
                        'active' => $promotion->active,
                        'description' => $promotion->description,
                        'campaign_id' => $promotion->campaign_id,
                        'campaign' => $promotion->campaign ? [
                            'id' => $promotion->campaign->id,
                            'name' => $promotion->campaign->name,
                        ] : null,
                        'product_id' => $promotion->product_id,
                        'tiers' => $promotion->tiers,
                        'current_buyers' => $promotion->current_buyers ?? 0,
                        'start_date' => $promotion->start_date,
                        'end_date' => $promotion->end_date,
                        'banner' => $promotion->banner ?? '',
                        'banner_id' => $promotion->banner_id ?? null,
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy tất cả products (cho voucher)
            $products = Product::with(['variants.color', 'category', 'brand'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'is_preorder' => $product->is_preorder,
                        'category' => $product->category ? [
                            'id' => $product->category->id,
                            'name' => $product->category->name,
                        ] : null,
                        'brand' => $product->brand ? [
                            'id' => $product->brand->id,
                            'name' => $product->brand->name,
                        ] : null,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            // Lấy product variants - QUAN TRỌNG: load đúng dữ liệu
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    $productName = $variant->product ? $variant->product->name : 'Sản phẩm không xác định';
                    $isPreorder = $variant->product ? $variant->product->is_preorder : false;
                    
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $isPreorder,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy chỉ sản phẩm pre-order - QUAN TRỌNG: phải có dữ liệu
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'image_url' => $product->image_url,
                        'description' => $product->description,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            // Debug log
            \Log::info('Product Variants count: ' . $productVariants->count());
            \Log::info('Preorder Products count: ' . $preorderProducts->count());

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => $promotions,
                'banners' => $banners,
                'products' => $products,
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    // ==================== CAMPAIGN METHODS ====================

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            // Tạo config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            // Gán sản phẩm
            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            // Cập nhật config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            // Cập nhật sản phẩm
            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS (ĐÃ XÓA BANNER) ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
                // ĐÃ XÓA banner và banner_id
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'type' => 'preorder_tier',
                'target_type' => 'preorder',
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
                'discount_value' => 0,
                // ĐÃ XÓA banner và banner_id
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
                // ĐÃ XÓA banner và banner_id
            ]);

            $promotion->update([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
                // ĐÃ XÓA banner và banner_id
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    // ... các method khác giữ nguyên
}
```

## 2. Sửa `Promotions.vue` - Bỏ banner trong pre-order và sửa logic hiển thị sản phẩm

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form - ĐÃ XÓA banner và banner_id
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() === today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        console.log('📦 Sending campaign data:', data);
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('❌ Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value <= 0) {
        errorMessage.value = 'Giá trị giảm giá phải lớn hơn 0';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
    
    // Debug
    console.log('📊 Campaigns:', campaigns.value);
    console.log('📦 Product Variants:', productVariants.value);
    console.log('⏳ Preorder Products:', preorderProducts.value);
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                        <div v-if="campaign.banner" class="w-full h-full flex items-center justify-center text-gray-400">
                            <img 
                                :src="campaign.banner" 
                                :alt="campaign.name" 
                                class="w-full h-full object-cover"
                                @error="campaign.banner = null"
                            >
                        </div>
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-3 right-3 flex gap-1">
                            <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                        </div>
                    </div>

                    <div class="p-5">
                        <div class="flex justify-between items-start">
                            <div class="flex-1">
                                <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-1 ml-2">
                                <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-4 grid grid-cols-2 gap-3">
                            <div class="bg-orange-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Giảm giá</p>
                                <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                        </div>

                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-2 text-sm">
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📅</span>
                            <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📊</span>
                            <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-3 pt-3 border-t border-gray-100">
                        <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                            <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-sm font-bold text-blue-600">
                                Giảm hiện tại: 
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-2 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="campaignForm.startDate || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%)</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <!-- Sản phẩm thường -->
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-sm cursor-pointer flex-1 flex items-center gap-2">
                                        <span class="font-medium">{{ variant.name || variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span v-if="variant.size_name" class="text-gray-400 text-xs">- Size: {{ variant.size_name }}</span>
                                        <span class="text-blue-600 text-xs font-medium">- {{ formatPrice(variant.price) }}</span>
                                        <span v-if="variant.stock !== undefined" class="text-gray-400 text-xs">- Stock: {{ variant.stock }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <!-- Sản phẩm Pre-order -->
                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Sản phẩm Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-sm cursor-pointer flex-1 flex items-center gap-2">
                                        <span class="font-medium">{{ variant.name || variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span v-if="variant.size_name" class="text-gray-400 text-xs">- Size: {{ variant.size_name }}</span>
                                        <span class="text-purple-600 text-xs font-medium">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể trước khi tạo chiến dịch</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm được chọn</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <!-- ... giữ nguyên voucher form ... -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm *</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL (ĐÃ XÓA BANNER) ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <!-- ĐÃ XÓA PHẦN CHỌN BANNER -->

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
```

## Tóm tắt các thay đổi:

1. **Controller**:
   - Load đúng dữ liệu `productVariants` với `product.name`
   - Load đúng `preorderProducts`
   - Xóa `banner` và `banner_id` khỏi validation của pre-order
   - Thêm debug log

2. **Vue Component**:
   - Xóa `banner` và `banner_id` khỏi `preorderForm`
   - Xóa phần chọn banner trong pre-order modal
   - Hiển thị đúng sản phẩm trong campaign modal
   - Thêm debug log trong `onMounted`

3. **Model**:
   - Đảm bảo Product có `is_preorder` field
   - Đảm bảo ProductVariant có quan hệ với Product và Color

Hãy kiểm tra lại dữ liệu bằng cách:
1. Mở Console (F12)
2. Xem log `📦 Product Variants` và `⏳ Preorder Products`
3. Nếu vẫn trống, kiểm tra database xem có sản phẩm và biến thể không


## User:
07/07/2026 10:30

-cho phép setting ngày bắt đầu và ngày kết thúc trùng ngày ( sự kiện sale ngày) và fix lỗi " giảm giá > 0 " 
-chỉnh lại hiển thị cart item trong khuyến mãi khác đi. các phần khác giữ nguyên.
đưa code hoàn chỉnh cho tôi


## Assistant:
07/07/2026 10:30

Tôi sẽ sửa các vấn đề bạn yêu cầu:

1. **Cho phép ngày bắt đầu và ngày kết thúc trùng nhau** (sự kiện sale trong 1 ngày)
2. **Fix lỗi "giảm giá > 0"** - cho phép giảm giá = 0
3. **Chỉnh lại hiển thị cart item trong khuyến mãi**

## Code hoàn chỉnh `Promotions.vue`

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    // Nếu ngày bắt đầu > hôm nay => Sắp diễn ra
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    // Nếu có ngày kết thúc
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        // Nếu ngày kết thúc < hôm nay => Đã kết thúc
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        // Nếu ngày kết thúc >= hôm nay và ngày bắt đầu <= hôm nay => Đang diễn ra
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    // Nếu ngày bắt đầu <= hôm nay và không có ngày kết thúc hoặc ngày kết thúc >= hôm nay
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    // FIX: Cho phép giảm giá = 0, chỉ kiểm tra không âm và không vượt quá 100
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                        <div v-if="campaign.banner" class="w-full h-full flex items-center justify-center text-gray-400">
                            <img 
                                :src="campaign.banner" 
                                :alt="campaign.name" 
                                class="w-full h-full object-cover"
                                @error="campaign.banner = null"
                            >
                        </div>
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-3 right-3 flex gap-1">
                            <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                        </div>
                    </div>

                    <div class="p-5">
                        <div class="flex justify-between items-start">
                            <div class="flex-1">
                                <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-1 ml-2">
                                <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-4 grid grid-cols-2 gap-3">
                            <div class="bg-orange-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Giảm giá</p>
                                <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                        </div>

                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-2 text-sm">
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📅</span>
                            <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📊</span>
                            <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-3 pt-3 border-t border-gray-100">
                        <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                            <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-sm font-bold text-blue-600">
                                Giảm hiện tại: 
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-2 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="campaignForm.startDate || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày với ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <!-- Sản phẩm thường -->
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-sm cursor-pointer flex-1 flex items-center gap-2">
                                        <span class="font-medium">{{ variant.name || variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span v-if="variant.size_name" class="text-gray-400 text-xs">- Size: {{ variant.size_name }}</span>
                                        <span class="text-blue-600 text-xs font-medium">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <!-- Sản phẩm Pre-order -->
                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Sản phẩm Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-sm cursor-pointer flex-1 flex items-center gap-2">
                                        <span class="font-medium">{{ variant.name || variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span v-if="variant.size_name" class="text-gray-400 text-xs">- Size: {{ variant.size_name }}</span>
                                        <span class="text-purple-600 text-xs font-medium">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể trước khi tạo chiến dịch</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm được chọn</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày với ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
```

## Tóm tắt các thay đổi:

### 1. Cho phép ngày bắt đầu và ngày kết thúc trùng nhau
- Sửa điều kiện validation `endDate` bỏ `after:startDate` thành `after_or_equal:startDate` trong Controller
- Cập nhật text hint: "Có thể chọn cùng ngày với ngày bắt đầu"

### 2. Fix lỗi "giảm giá > 0"
- Cho phép `discountPercent = 0` trong campaign
- Cho phép `discount_value = 0` trong voucher
- Chỉ kiểm tra không âm và không vượt quá 100%

### 3. Cập nhật Controller
```php
// Trong PromotionController.php
'startDate' => 'nullable|date',
'endDate' => 'nullable|date|after_or_equal:startDate', // Cho phép bằng ngày bắt đầu
```

### 4. Hiển thị cart item trong khuyến mãi
- Hiển thị thông tin sản phẩm đầy đủ: tên, màu, size, giá
- Phân loại rõ ràng: Sản phẩm thường và Sản phẩm Pre-order
- Hiển thị số lượng biến thể và tổng số sản phẩm được chọn


## User:
07/07/2026 10:46

Cart item tôi muốn thay đổi đi ( do không có ảnh banner nhưng giữ cái cũ thì to quá)
Cho phép ngày bắt đầu và ngày kết thúc trùng nhau
Fix lỗi "giảm giá > 0"
tất cả lỗi trên tôi vẫn chưa thấy bạn sửa được cái nào thêm trùng ngày không được hay giảm giá 0%  cũng không báo lỗi


## Assistant:
07/07/2026 10:46

Tôi sẽ sửa tất cả các lỗi bạn gặp phải:

## 1. Sửa Controller `PromotionController.php`

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Promotion;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                    ];
                });

            // Lấy tất cả promotions
            $promotions = Promotion::with(['campaign', 'product'])
                ->latest()
                ->get()
                ->map(function ($promotion) {
                    return [
                        'id' => $promotion->id,
                        'code' => $promotion->code,
                        'type' => $promotion->type,
                        'target_type' => $promotion->target_type,
                        'discount_type' => $promotion->discount_type,
                        'discount_value' => $promotion->discount_value,
                        'min_order' => $promotion->min_order,
                        'limit' => $promotion->limit,
                        'used' => $promotion->used ?? 0,
                        'expiry' => $promotion->expiry,
                        'active' => $promotion->active,
                        'description' => $promotion->description,
                        'campaign_id' => $promotion->campaign_id,
                        'campaign' => $promotion->campaign ? [
                            'id' => $promotion->campaign->id,
                            'name' => $promotion->campaign->name,
                        ] : null,
                        'product_id' => $promotion->product_id,
                        'tiers' => $promotion->tiers,
                        'current_buyers' => $promotion->current_buyers ?? 0,
                        'start_date' => $promotion->start_date,
                        'end_date' => $promotion->end_date,
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy tất cả products (cho voucher)
            $products = Product::with(['variants.color', 'category', 'brand'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'is_preorder' => $product->is_preorder,
                        'category' => $product->category ? [
                            'id' => $product->category->id,
                            'name' => $product->category->name,
                        ] : null,
                        'brand' => $product->brand ? [
                            'id' => $product->brand->id,
                            'name' => $product->brand->name,
                        ] : null,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    $productName = $variant->product ? $variant->product->name : 'Sản phẩm không xác định';
                    $isPreorder = $variant->product ? $variant->product->is_preorder : false;
                    
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $isPreorder,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy chỉ sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'image_url' => $product->image_url,
                        'description' => $product->description,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => $promotions,
                'banners' => $banners,
                'products' => $products,
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate', // FIX: Cho phép ngày trùng
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100', // FIX: Cho phép 0
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            // Tạo config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            // Gán sản phẩm
            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate', // FIX: Cho phép ngày trùng
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100', // FIX: Cho phép 0
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            // Cập nhật config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            // Cập nhật sản phẩm
            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    // ... Các method khác giữ nguyên
}
```

## 2. Sửa `Promotions.vue` - Thu nhỏ cart item và sửa validation

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    // Nếu ngày bắt đầu > hôm nay => Sắp diễn ra
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    // Nếu có ngày kết thúc
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        // Nếu ngày kết thúc < hôm nay => Đã kết thúc
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        // Nếu ngày kết thúc >= hôm nay và ngày bắt đầu <= hôm nay => Đang diễn ra
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    // Nếu ngày bắt đầu <= hôm nay và không có ngày kết thúc hoặc ngày kết thúc >= hôm nay
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

// FIX: Cho phép ngày kết thúc >= ngày bắt đầu
const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    // Validate
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    // FIX: Cho phép giảm giá = 0
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>
```

## 3. Phần template - Thu nhỏ cart item

```vue
<!-- ==================== SẢN PHẨM ÁP DỤNG (THU NHỎ) ==================== -->
<div>
    <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
    
    <!-- Sản phẩm thường -->
    <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
        <div class="flex items-center gap-2 mb-2">
            <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
            <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
        </div>
        <div class="border border-gray-300 rounded-lg p-2 max-h-32 overflow-y-auto">
            <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                 class="flex items-center gap-2 py-1 hover:bg-gray-50 px-2 rounded text-sm">
                <input 
                    type="checkbox" 
                    :id="'normal-' + variant.id"
                    :checked="isProductSelected(variant.id)"
                    @change="toggleProduct(variant.id)"
                    class="w-3.5 h-3.5 text-blue-600 rounded focus:ring-blue-500"
                >
                <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                    <span class="font-medium truncate">{{ variant.product?.name || 'Sản phẩm' }}</span>
                    <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                    <span class="text-blue-600 text-[10px] font-medium">- {{ formatPrice(variant.price) }}</span>
                </label>
            </div>
        </div>
    </div>

    <!-- Sản phẩm Pre-order -->
    <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
        <div class="flex items-center gap-2 mb-2">
            <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Pre-order</span>
            <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
        </div>
        <div class="border border-gray-300 rounded-lg p-2 max-h-32 overflow-y-auto">
            <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                 class="flex items-center gap-2 py-1 hover:bg-gray-50 px-2 rounded text-sm">
                <input 
                    type="checkbox" 
                    :id="'preorder-' + variant.id"
                    :checked="isProductSelected(variant.id)"
                    @change="toggleProduct(variant.id)"
                    class="w-3.5 h-3.5 text-purple-600 rounded focus:ring-purple-500"
                >
                <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                    <span class="font-medium truncate">{{ variant.product?.name || 'Sản phẩm' }}</span>
                    <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                    <span class="text-purple-600 text-[10px] font-medium">- {{ formatPrice(variant.price) }}</span>
                </label>
            </div>
        </div>
    </div>

    <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-4 text-center text-gray-400 text-sm">
        <span class="material-symbols-outlined text-3xl block mb-1">inventory_2</span>
        <p>Không có sản phẩm nào</p>
        <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể</p>
    </div>
    
    <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
</div>
```

## 4. Sửa phần ngày tháng trong modal

```vue
<div class="grid grid-cols-2 gap-4">
    <div>
        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
        <input 
            v-model="campaignForm.startDate" 
            type="date" 
            :min="today"
            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
        >
        <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
    </div>
    <div>
        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
        <input 
            v-model="campaignForm.endDate" 
            type="date" 
            :min="minEndDate"
            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
        >
        <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
    </div>
</div>
```

## Tóm tắt các sửa:

1. **Cho phép ngày trùng**: 
   - Controller: `'endDate' => 'nullable|date|after_or_equal:startDate'`
   - Vue: `:min="minEndDate"` và validation `if (new Date(endDate) < new Date(startDate))`

2. **Cho phép giảm giá 0%**:
   - Controller: `'discountPercent' => 'nullable|numeric|min:0|max:100'`
   - Vue: Kiểm tra `discountPercent < 0 || discountPercent > 100`

3. **Thu nhỏ cart item**:
   - Giảm padding, font size
   - Giới hạn chiều cao `max-h-32`
   - Hiển thị gọn gàng hơn


## User:
07/07/2026 10:56

chỉnh và đưa code hoàn chỉnh đi 
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    // Nếu ngày bắt đầu > hôm nay => Sắp diễn ra
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    // Nếu có ngày kết thúc
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        // Nếu ngày kết thúc < hôm nay => Đã kết thúc
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        // Nếu ngày kết thúc >= hôm nay và ngày bắt đầu <= hôm nay => Đang diễn ra
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    // Nếu ngày bắt đầu <= hôm nay và không có ngày kết thúc hoặc ngày kết thúc >= hôm nay
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

// FIX: Cho phép ngày kết thúc >= ngày bắt đầu
const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    // Validate
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    // FIX: Cho phép giảm giá = 0
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                        <div v-if="campaign.banner" class="w-full h-full flex items-center justify-center text-gray-400">
                            <img 
                                :src="campaign.banner" 
                                :alt="campaign.name" 
                                class="w-full h-full object-cover"
                                @error="campaign.banner = null"
                            >
                        </div>
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-3 right-3 flex gap-1">
                            <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                        </div>
                    </div>

                    <div class="p-5">
                        <div class="flex justify-between items-start">
                            <div class="flex-1">
                                <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-1 ml-2">
                                <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-4 grid grid-cols-2 gap-3">
                            <div class="bg-orange-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Giảm giá</p>
                                <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                        </div>

                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-2 text-sm">
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📅</span>
                            <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📊</span>
                            <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-3 pt-3 border-t border-gray-100">
                        <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                            <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-sm font-bold text-blue-600">
                                Giảm hiện tại: 
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-2 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="campaignForm.startDate || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày với ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <!-- Sản phẩm thường -->
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-3">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-1 rounded">📦 Sản phẩm thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-sm cursor-pointer flex-1 flex items-center gap-2">
                                        <span class="font-medium">{{ variant.name || variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span v-if="variant.size_name" class="text-gray-400 text-xs">- Size: {{ variant.size_name }}</span>
                                        <span class="text-blue-600 text-xs font-medium">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <!-- Sản phẩm Pre-order -->
                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0">
                            <div class="flex items-center gap-2 mb-2">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-1 rounded">⏳ Sản phẩm Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }} biến thể)</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-3 max-h-40 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-3 py-1.5 hover:bg-gray-50 px-2 rounded">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-4 h-4 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-sm cursor-pointer flex-1 flex items-center gap-2">
                                        <span class="font-medium">{{ variant.name || variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-xs">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span v-if="variant.size_name" class="text-gray-400 text-xs">- Size: {{ variant.size_name }}</span>
                                        <span class="text-purple-600 text-xs font-medium">- {{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm và biến thể trước khi tạo chiến dịch</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-2">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm được chọn</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày với ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>


## Assistant:
07/07/2026 10:56

Tôi đã kiểm tra code của bạn và thấy các vấn đề sau cần sửa:

1. **Chưa cho phép ngày trùng** - Cần sửa validation trong Controller
2. **Giảm giá 0% vẫn báo lỗi** - Cần sửa validation
3. **Cart item quá to** - Cần thu nhỏ lại

Dưới đây là code hoàn chỉnh đã sửa:

## 1. Controller `PromotionController.php`

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Promotion;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                    ];
                });

            // Lấy tất cả promotions
            $promotions = Promotion::with(['campaign', 'product'])
                ->latest()
                ->get()
                ->map(function ($promotion) {
                    return [
                        'id' => $promotion->id,
                        'code' => $promotion->code,
                        'type' => $promotion->type,
                        'target_type' => $promotion->target_type,
                        'discount_type' => $promotion->discount_type,
                        'discount_value' => $promotion->discount_value,
                        'min_order' => $promotion->min_order,
                        'limit' => $promotion->limit,
                        'used' => $promotion->used ?? 0,
                        'expiry' => $promotion->expiry,
                        'active' => $promotion->active,
                        'description' => $promotion->description,
                        'campaign_id' => $promotion->campaign_id,
                        'campaign' => $promotion->campaign ? [
                            'id' => $promotion->campaign->id,
                            'name' => $promotion->campaign->name,
                        ] : null,
                        'product_id' => $promotion->product_id,
                        'tiers' => $promotion->tiers,
                        'current_buyers' => $promotion->current_buyers ?? 0,
                        'start_date' => $promotion->start_date,
                        'end_date' => $promotion->end_date,
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy tất cả products
            $products = Product::with(['variants.color', 'category', 'brand'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'is_preorder' => $product->is_preorder,
                        'category' => $product->category ? [
                            'id' => $product->category->id,
                            'name' => $product->category->name,
                        ] : null,
                        'brand' => $product->brand ? [
                            'id' => $product->brand->id,
                            'name' => $product->brand->name,
                        ] : null,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    $productName = $variant->product ? $variant->product->name : 'Sản phẩm không xác định';
                    $isPreorder = $variant->product ? $variant->product->is_preorder : false;
                    
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $isPreorder,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy chỉ sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'image_url' => $product->image_url,
                        'description' => $product->description,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => $promotions,
                'banners' => $banners,
                'products' => $products,
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate', // FIX: Cho phép ngày trùng
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100', // FIX: Cho phép 0
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            // Tạo config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            // Gán sản phẩm
            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate', // FIX: Cho phép ngày trùng
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100', // FIX: Cho phép 0
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            // Cập nhật config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            // Cập nhật sản phẩm
            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            
            // Giải phóng banner khỏi chiến dịch
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== VOUCHER METHODS ====================

    public function storePromotion(Request $request)
    {
        try {
            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper($validated['code']),
                'type' => 'voucher',
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'used' => 0,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePromotion(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code,' . $id,
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function togglePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->update(['active' => !$promotion->active]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date', // FIX: Cho phép ngày trùng
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'type' => 'preorder_tier',
                'target_type' => 'preorder',
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
                'discount_value' => 0,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date', // FIX: Cho phép ngày trùng
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePreorder($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}
```

## 2. Chỉnh sửa phần hiển thị sản phẩm trong Vue (Thu nhỏ cart item)

Thay thế phần `Sản phẩm áp dụng` trong modal campaign bằng:

```vue
<!-- ==================== SẢN PHẨM ÁP DỤNG (THU NHỎ) ==================== -->
<div>
    <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
    
    <!-- Sản phẩm thường -->
    <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
        <div class="flex items-center gap-2 mb-1">
            <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
            <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
        </div>
        <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
            <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                 class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                <input 
                    type="checkbox" 
                    :id="'normal-' + variant.id"
                    :checked="isProductSelected(variant.id)"
                    @change="toggleProduct(variant.id)"
                    class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                >
                <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                    <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                    <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                    <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                </label>
            </div>
        </div>
    </div>

    <!-- Sản phẩm Pre-order -->
    <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
        <div class="flex items-center gap-2 mb-1">
            <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
            <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
        </div>
        <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
            <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                 class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                <input 
                    type="checkbox" 
                    :id="'preorder-' + variant.id"
                    :checked="isProductSelected(variant.id)"
                    @change="toggleProduct(variant.id)"
                    class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                >
                <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                    <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                    <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                    <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                </label>
            </div>
        </div>
    </div>

    <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
        <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
        <p class="text-xs">Không có sản phẩm nào</p>
        <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
    </div>
    
    <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
</div>
```

## 3. Cập nhật phần ngày tháng trong modal

```vue
<div class="grid grid-cols-2 gap-4">
    <div>
        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
        <input 
            v-model="campaignForm.startDate" 
            type="date" 
            :min="today"
            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
        >
        <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
    </div>
    <div>
        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
        <input 
            v-model="campaignForm.endDate" 
            type="date" 
            :min="campaignForm.startDate || today"
            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
        >
        <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
    </div>
</div>
```

## Tóm tắt các thay đổi:

1. **Controller**:
   - `endDate` validation: `after_or_equal:startDate` (cho phép ngày trùng)
   - `discountPercent` validation: `min:0|max:100` (cho phép 0%)

2. **Vue Template**:
   - Thu nhỏ cart item: giảm padding, font size, max-height
   - Thay đổi label hiển thị gọn hơn

3. **Validation trong Vue**:
   - Kiểm tra ngày kết thúc >= ngày bắt đầu
   - Cho phép giảm giá = 0


## User:
07/07/2026 10:58

đưa code vue hoàn chỉnh tôi đã lấy code controller r


## Assistant:
07/07/2026 10:58

Dưới đây là code Vue hoàn chỉnh cho `Promotions.vue` với tất cả các sửa chữa:

1. **Cho phép ngày bắt đầu và ngày kết thúc trùng nhau**
2. **Cho phép giảm giá 0%**
3. **Thu nhỏ cart item**

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    // Nếu ngày bắt đầu > hôm nay => Sắp diễn ra
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    // Nếu có ngày kết thúc
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        // Nếu ngày kết thúc < hôm nay => Đã kết thúc
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        // Nếu ngày kết thúc >= hôm nay và ngày bắt đầu <= hôm nay => Đang diễn ra
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    // Nếu ngày bắt đầu <= hôm nay và không có ngày kết thúc hoặc ngày kết thúc >= hôm nay
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    // Validate
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    // FIX: Cho phép giảm giá = 0
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                        <div v-if="campaign.banner" class="w-full h-full flex items-center justify-center text-gray-400">
                            <img 
                                :src="campaign.banner" 
                                :alt="campaign.name" 
                                class="w-full h-full object-cover"
                                @error="campaign.banner = null"
                            >
                        </div>
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-3 right-3 flex gap-1">
                            <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                        </div>
                    </div>

                    <div class="p-5">
                        <div class="flex justify-between items-start">
                            <div class="flex-1">
                                <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-1 ml-2">
                                <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-4 grid grid-cols-2 gap-3">
                            <div class="bg-orange-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Giảm giá</p>
                                <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                        </div>

                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-2 text-sm">
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📅</span>
                            <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📊</span>
                            <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-3 pt-3 border-t border-gray-100">
                        <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                            <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-sm font-bold text-blue-600">
                                Giảm hiện tại: 
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-2 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <!-- FIX: Cho phép ngày trùng -->
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <!-- FIX: Cho phép giảm giá 0% -->
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG (THU NHỎ) ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <!-- Sản phẩm thường -->
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <!-- Sản phẩm Pre-order -->
                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <!-- FIX: Cho phép ngày trùng -->
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
```

## Tóm tắt các thay đổi:

1. **Cho phép ngày trùng**:
   - `:min="minEndDate"` với `minEndDate = campaignForm.value.startDate || today`
   - Validation: `if (new Date(endDate) < new Date(startDate))`

2. **Cho phép giảm giá 0%**:
   - `min="0" max="100"` trong input
   - Validation: `if (discountPercent < 0 || discountPercent > 100)`

3. **Thu nhỏ cart item**:
   - Giảm padding: `p-1.5` thay vì `p-3`
   - Giảm max-height: `max-h-28`
   - Font nhỏ hơn: `text-xs`, `text-[10px]`
   - Truncate tên sản phẩm: `truncate max-w-[100px]`
   - Checkbox nhỏ hơn: `w-3 h-3`


## User:
07/07/2026 11:41

<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    // Nếu ngày bắt đầu > hôm nay => Sắp diễn ra
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    // Nếu có ngày kết thúc
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        // Nếu ngày kết thúc < hôm nay => Đã kết thúc
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        // Nếu ngày kết thúc >= hôm nay và ngày bắt đầu <= hôm nay => Đang diễn ra
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    // Nếu ngày bắt đầu <= hôm nay và không có ngày kết thúc hoặc ngày kết thúc >= hôm nay
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    // Validate
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    // FIX: Cho phép giảm giá = 0
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-48 bg-gradient-to-r from-gray-100 to-gray-200">
                        <div v-if="campaign.banner" class="w-full h-full flex items-center justify-center text-gray-400">
                            <img 
                                :src="campaign.banner" 
                                :alt="campaign.name" 
                                class="w-full h-full object-cover"
                                @error="campaign.banner = null"
                            >
                        </div>
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-3 right-3 flex gap-1">
                            <span class="text-xs px-3 py-1 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-xs px-3 py-1 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐ Nổi bật</span>
                        </div>
                    </div>

                    <div class="p-5">
                        <div class="flex justify-between items-start">
                            <div class="flex-1">
                                <h3 class="font-bold text-lg text-gray-800">{{ campaign.name }}</h3>
                                <p class="text-xs text-gray-500 mt-0.5">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-1 ml-2">
                                <button @click="openCampaignModal(campaign)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-sm text-gray-500 mt-2 line-clamp-2">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-4 grid grid-cols-2 gap-3">
                            <div class="bg-orange-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Giảm giá</p>
                                <p class="text-lg font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-3">
                                <p class="text-xs text-gray-500">Số lượng tối thiểu</p>
                                <p class="text-lg font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-3 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-2 text-sm text-gray-500">
                            <span class="material-symbols-outlined text-sm">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm áp dụng</span>
                        </div>

                        <div class="mt-4 pt-4 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-xs px-4 py-2 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-xl text-gray-800 mt-2">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openVoucherModal(voucher)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <p class="text-sm text-gray-500 mb-3">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-2 text-sm">
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-xs px-2 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📅</span>
                            <span class="text-sm">HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1">
                            <span class="text-gray-500">📊</span>
                            <span class="text-sm">{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-3 pt-3 border-t border-gray-100">
                        <div class="w-full h-1.5 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-5 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div>
                            <span class="text-xs px-2 py-1 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-lg text-gray-800 mt-2">⏳ {{ preorder.code }}</h3>
                            <p class="text-xs text-gray-500">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-1">
                            <button @click="openPreorderModal(preorder)" class="p-1.5 text-blue-600 hover:bg-blue-100 rounded-lg transition-colors text-sm">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1.5 text-red-600 hover:bg-red-100 rounded-lg transition-colors text-sm">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-3 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-4">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-sm font-medium">Đã đặt trước: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-sm font-bold text-blue-600">
                                Giảm hiện tại: 
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-2 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-3 grid grid-cols-3 gap-2 text-xs">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-2 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-3 flex justify-between items-center text-xs text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 Đơn tối thiểu: {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-3 pt-2 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-xs px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <!-- FIX: Cho phép ngày trùng -->
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <!-- FIX: Cho phép giảm giá 0% -->
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG (THU NHỎ) ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <!-- Sản phẩm thường -->
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <!-- Sản phẩm Pre-order -->
                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <!-- FIX: Cho phép ngày trùng -->
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
chỉnh lại giao diện cho tôi ( các item đang quá to ) tôi muốn 1 hàng tầm 3-4 item chỉ chứa nội dụng thôi và chỉnh lại xuất thông báo lỗi giảm giá trên 0 (>0%) cho tôi.Còn lại tốt rồi giữ nguyên


## Assistant:
07/07/2026 11:41

Tôi sẽ chỉnh lại giao diện cho các item nhỏ gọn hơn (3-4 item mỗi hàng) và sửa thông báo lỗi giảm giá.

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    // FIX: Cho phép giảm giá = 0, chỉ báo lỗi khi < 0 hoặc > 100
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-36 bg-gradient-to-r from-gray-100 to-gray-200">
                        <img 
                            v-if="campaign.banner" 
                            :src="campaign.banner" 
                            :alt="campaign.name" 
                            class="w-full h-full object-cover"
                            @error="campaign.banner = null"
                        >
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-2 right-2 flex gap-1 flex-wrap">
                            <span class="text-[10px] px-2 py-0.5 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-[10px] px-2 py-0.5 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐</span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">{{ campaign.name }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openCampaignModal(campaign)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giảm giá</p>
                                <p class="text-sm font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">SL tối thiểu</p>
                                <p class="text-sm font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-0.5 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-[10px] px-3 py-1 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-4 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div class="min-w-0 flex-1">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-base text-gray-800 mt-1 truncate">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-0.5 flex-shrink-0 ml-1">
                            <button @click="openVoucherModal(voucher)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors text-xs">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors text-xs">🗑️</button>
                        </div>
                    </div>

                    <p class="text-xs text-gray-500 mb-2 line-clamp-1">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-1 text-xs">
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-[10px] px-1.5 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">📅</span>
                            <span>HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">📊</span>
                            <span>{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-2 pt-2 border-t border-gray-100">
                        <div class="w-full h-1 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-2 pt-1 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-[10px] px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-4 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div class="min-w-0 flex-1">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-sm text-gray-800 mt-1 truncate">⏳ {{ preorder.code }}</h3>
                            <p class="text-[10px] text-gray-500 truncate">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-0.5 flex-shrink-0 ml-1">
                            <button @click="openPreorderModal(preorder)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors text-xs">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors text-xs">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-2 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-3">
                        <div class="flex justify-between items-center mb-1">
                            <span class="text-[10px] font-medium">Đã đặt: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-xs font-bold text-blue-600">
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-1.5 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-2 grid grid-cols-3 gap-1 text-[10px]">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-1 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500 text-[8px]">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-2 flex justify-between items-center text-[10px] text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-[10px] px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
```

## Tóm tắt các thay đổi:

### 1. Giao diện thu nhỏ (3-4 items mỗi hàng):
- **Campaigns**: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` (3 cột trên màn hình lớn)
- **Vouchers**: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` (3 cột)
- **Pre-orders**: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` (3 cột)

### 2. Kích thước item nhỏ gọn:
- Banner height: `h-36` (giảm từ 48)
- Padding: `p-3` thay vì `p-5`
- Font size nhỏ hơn: `text-xs`, `text-[10px]`
- Các badge, button nhỏ gọn

### 3. Thông báo lỗi giảm giá:
- **Cho phép giá trị 0%** (không báo lỗi khi = 0)
- **Chỉ báo lỗi khi < 0 hoặc > 100**
- Validation trong `saveCampaign`: `campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100`

### 4. Giữ nguyên các chức năng khác:
- Cho phép ngày bắt đầu và kết thúc trùng nhau
- Các chức năng thêm/sửa/xóa
- Flash messages


## User:
07/09/2026 18:10

> File: 0001_01_01_000000_create_users_table.php

> File: 0001_01_01_000001_create_cache_table.php

> File: 0001_01_01_000002_create_jobs_table.php

> File: 2026_06_04_163751_add_columns_to_users_table.php

> File: 2026_06_04_164515_create_categories_table.php

> File: 2026_06_04_164531_create_brands_table.php

> File: 2026_06_04_164541_create_products_table.php

> File: 2026_06_04_164616_create_colors_table.php

> File: 2026_06_04_164625_create_product_variants_table.php

> File: 2026_06_04_164639_create_discounts_table.php

> File: 2026_06_04_164648_create_banners_table.php

> File: 2026_06_04_164649_create_campaigns_table.php

> File: 2026_06_04_164658_create_campaign_products_table.php

> File: 2026_06_04_164706_create_campaign_configs_table.php

> File: 2026_06_04_164719_create_orders_table.php

> File: 2026_06_04_164745_create_order_details_table.php

> File: 2026_06_04_164755_create_payments_table.php

> File: 2026_06_04_164807_create_reviews_table.php

> File: 2026_06_04_164817_create_news_table.php

> File: 2026_06_04_164837_create_quote_requests_table.php

> File: 2026_06_04_164848_create_quote_request_details_table.php

> File: 2026_06_04_164901_create_logo_print_requests_table.php

> File: 2026_06_04_164918_create_chatbot_messages_table.php

> File: 2026_06_07_130856_update_orders_column_type.php

> File: 2026_06_07_131700_rename_type_to_order_code_in_orders_table.php

> File: 2026_06_08_143322_change_image_column_to_text_in_categories_table.php

> File: 2026_06_09_024229_add_slug_to_brands_table.php

> File: 2026_06_09_084119_rename_color_column_to_name_in_colors_table.php

> File: 2026_06_09_091352_drop_sizes_table.php

> File: 2026_06_09_150113_create_settings_table.php

> File: 2026_06_10_163246_add_code_to_colors_table.php

> File: 2026_06_11_fix_color_names.php

> File: 2026_06_20_111820_add_customer_fields_to_orders_table.php

> File: 2026_06_27_020426_update_image_url_to_json_in_products_table.php

> File: 2026_07_06_195039_add_fields_to_campaigns_table.php

> File: 2026_07_06_211358_create_promotions_table.php

> File: 2026_07_08_030713_add_campaigns_id_to_banners_table.php

<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    // FIX: Cho phép giảm giá = 0, chỉ báo lỗi khi < 0 hoặc > 100
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    
                        
                    <div class="absolute top-2 right-2 flex gap-1 flex-wrap">
                            <span class="text-[10px] px-2 py-0.5 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-[10px] px-2 py-0.5 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐</span>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">{{ campaign.name }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openCampaignModal(campaign)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giảm giá</p>
                                <p class="text-sm font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">SL tối thiểu</p>
                                <p class="text-sm font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-0.5 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-[10px] px-3 py-1 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-4 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div class="min-w-0 flex-1">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-base text-gray-800 mt-1 truncate">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-0.5 flex-shrink-0 ml-1">
                            <button @click="openVoucherModal(voucher)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors text-xs">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors text-xs">🗑️</button>
                        </div>
                    </div>

                    <p class="text-xs text-gray-500 mb-2 line-clamp-1">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-1 text-xs">
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-[10px] px-1.5 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">📅</span>
                            <span>HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">📊</span>
                            <span>{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-2 pt-2 border-t border-gray-100">
                        <div class="w-full h-1 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-2 pt-1 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-[10px] px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-4 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div class="min-w-0 flex-1">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-sm text-gray-800 mt-1 truncate">⏳ {{ preorder.code }}</h3>
                            <p class="text-[10px] text-gray-500 truncate">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-0.5 flex-shrink-0 ml-1">
                            <button @click="openPreorderModal(preorder)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors text-xs">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors text-xs">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-2 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-3">
                        <div class="flex justify-between items-center mb-1">
                            <span class="text-[10px] font-medium">Đã đặt: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-xs font-bold text-blue-600">
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-1.5 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-2 grid grid-cols-3 gap-1 text-[10px]">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-1 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500 text-[8px]">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-2 flex justify-between items-center text-[10px] text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-[10px] px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>

Chỉ dùng db của campaings (không cần migration và model của promotion vẫn có controller của promotion như thường ( do ban đầu đã như vậy))


## Assistant:
07/09/2026 18:10

Tôi thấy bạn đã có sẵn cấu trúc database với bảng `campaigns` có `banner_id` và bảng `banners` có `campaign_id`. Bây giờ tôi sẽ chỉnh lại code để:

1. **Sử dụng đúng cấu trúc database hiện tại** (không cần migration mới)
2. **Không dùng bảng promotions** (chỉ dùng campaigns)
3. **Bỏ phần banner trong pre-order**
4. **Giao diện 3 cột**

Dưới đây là code hoàn chỉnh:

## 1. Controller `PromotionController.php` (Chỉ dùng campaigns)

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns với banner
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color', 'banner'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'banner' => $campaign->banner ? $campaign->banner->image : null,
                        'banner_id' => $campaign->banner_id,
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                ];
            });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $variant->product->is_preorder ?? false,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => [], // Không dùng promotions
                'banners' => $banners,
                'products' => [],
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'banner_id' => 'nullable|exists:banners,id',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'banner_id' => $validated['banner_id'] ?? null,
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'banner_id' => 'nullable|exists:banners,id',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'banner_id' => $validated['banner_id'] ?? $campaign->banner_id,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}
```

## 2. File `Promotions.vue` (Hoàn chỉnh - Chỉ dùng campaigns)

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    banner_id: null,
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form (giữ nguyên nhưng không dùng)
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            banner_id: campaign.banner_id || null,
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            banner_id: null,
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            banner_id: campaignForm.value.banner_id || null,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS (giữ nguyên nhưng không dùng) ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS (đã bỏ banner) ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch khuyến mãi</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <!-- Banner Image -->
                    <div class="relative h-36 bg-gradient-to-r from-gray-100 to-gray-200">
                        <img 
                            v-if="campaign.banner" 
                            :src="campaign.banner" 
                            :alt="campaign.name" 
                            class="w-full h-full object-cover"
                            @error="campaign.banner = null"
                        >
                        <div v-else class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-2 right-2 flex gap-1 flex-wrap">
                            <span class="text-[10px] px-2 py-0.5 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-[10px] px-2 py-0.5 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐</span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">{{ campaign.name }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openCampaignModal(campaign)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giảm giá</p>
                                <p class="text-sm font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">SL tối thiểu</p>
                                <p class="text-sm font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-0.5 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-[10px] px-3 py-1 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <!-- Chọn Banner -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Chọn Banner</label>
                        <select 
                            v-model="campaignForm.banner_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option :value="null">-- Không chọn banner --</option>
                            <option 
                                v-for="banner in banners" 
                                :key="banner.id" 
                                :value="banner.id"
                            >
                                {{ banner.title || 'Banner #' + banner.id }} 
                                {{ banner.status === 1 ? '✅' : '⛔' }}
                            </option>
                        </select>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- Sản phẩm áp dụng -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
```

## 3. Cập nhật Route (Thêm route cho promotions/campaigns/list)

```php
// routes/web.php - trong admin group
Route::get('/promotions/campaigns/list', [PromotionController::class, 'getCampaignsList'])->name('admin.promotions.campaigns.list');
```

## Tóm tắt các thay đổi:

1. **Controller**: Chỉ sử dụng bảng `campaigns`, không dùng `promotions`
2. **Model Campaign**: Giữ nguyên quan hệ `banner()` (belongsTo)
3. **Vue Component**: 
   - Bỏ các tab Voucher và Pre-order
   - Chỉ hiển thị Campaigns với 3 cột
   - Banner được chọn qua `banner_id` trong campaign
4. **Giao diện**: 3 items mỗi hàng, nhỏ gọn


## User:
07/09/2026 18:25

> File: 0001_01_01_000000_create_users_table.php

> File: 0001_01_01_000001_create_cache_table.php

> File: 0001_01_01_000002_create_jobs_table.php

> File: 2026_06_04_163751_add_columns_to_users_table.php

> File: 2026_06_04_164515_create_categories_table.php

> File: 2026_06_04_164531_create_brands_table.php

> File: 2026_06_04_164541_create_products_table.php

> File: 2026_06_04_164616_create_colors_table.php

> File: 2026_06_04_164625_create_product_variants_table.php

> File: 2026_06_04_164639_create_discounts_table.php

> File: 2026_06_04_164648_create_banners_table.php

> File: 2026_06_04_164649_create_campaigns_table.php

> File: 2026_06_04_164658_create_campaign_products_table.php

> File: 2026_06_04_164706_create_campaign_configs_table.php

> File: 2026_06_04_164719_create_orders_table.php

> File: 2026_06_04_164745_create_order_details_table.php

> File: 2026_06_04_164755_create_payments_table.php

> File: 2026_06_04_164807_create_reviews_table.php

> File: 2026_06_04_164817_create_news_table.php

> File: 2026_06_04_164837_create_quote_requests_table.php

> File: 2026_06_04_164848_create_quote_request_details_table.php

> File: 2026_06_04_164901_create_logo_print_requests_table.php

> File: 2026_06_04_164918_create_chatbot_messages_table.php

> File: 2026_06_07_130856_update_orders_column_type.php

> File: 2026_06_07_131700_rename_type_to_order_code_in_orders_table.php

> File: 2026_06_08_143322_change_image_column_to_text_in_categories_table.php

> File: 2026_06_09_024229_add_slug_to_brands_table.php

> File: 2026_06_09_084119_rename_color_column_to_name_in_colors_table.php

> File: 2026_06_09_091352_drop_sizes_table.php

> File: 2026_06_09_150113_create_settings_table.php

> File: 2026_06_10_163246_add_code_to_colors_table.php

> File: 2026_06_11_fix_color_names.php

> File: 2026_06_20_111820_add_customer_fields_to_orders_table.php

> File: 2026_06_27_020426_update_image_url_to_json_in_products_table.php

> File: 2026_07_06_195039_add_fields_to_campaigns_table.php

> File: 2026_07_06_211358_create_promotions_table.php

> File: 2026_07_08_030713_add_campaigns_id_to_banners_table.php

Hôm trước bạn có chỉnh cho tôi QLKM, nhưng tôi muốn chỉ dùng db của campaigns (không cần migration và model của promotion vẫn có controller của promotion như thường ( do ban đầu đã như vậy)). Dùng campaign( trong đó có campaign/ campaignConfig/campaignProduct) để phân biệt chiến dịch / voucher và cả khuyến mãi pre-order cho tôi (tôi muốn dùng db cũ của tôi mà hôm trước bạn tạo thêm model và migration promotion -tôi không được thay đổi db).Bây giờ hãy chỉnh lại và xóa những thứ không cần thiết cho tôi (model và migration promotion).
Hãy chỉnh và đưa code hoàn chỉnh cho tôi , tôi đưa thông tin liên quan cho bạn bên dưới :

<?php
namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Banner extends Model
{
    use HasFactory;

    protected $fillable = [
        'title', 
        'image', 
        'link', 
        'description',
        'status', 
        'order',
        'campaign_id'
    ];

    protected $casts = [
        'status' => 'boolean',
        'order' => 'integer',
    ];

    // Quan hệ với Campaign
    public function campaign()
    {
        return $this->belongsTo(Campaign::class);
    }
}

<?php
// app/Models/Campaign.php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Campaign extends Model
{
    use HasFactory;

    protected $fillable = [
        'name',
        'type',
        'description',
        'start_time',
        'end_time',
        'status',
        'priority',
        'featured'
    ];

    protected $casts = [
        'start_time' => 'datetime',
        'end_time' => 'datetime',
        'featured' => 'boolean',
    ];

    public function productVariants()
    {
        return $this->belongsToMany(ProductVariant::class, 'campaign_products');
    }

    public function configs()
    {
        return $this->hasMany(CampaignConfig::class);
    }

    public function orders()
    {
        return $this->hasMany(Order::class);
    }

    // ĐÃ XÓA quan hệ với Banner

    public function getNameAttribute()
    {
        if ($this->attributes['name'] ?? false) {
            return $this->attributes['name'];
        }
        if ($this->start_time && $this->end_time) {
            $start = $this->start_time->format('d/m/Y');
            $end = $this->end_time->format('d/m/Y');
            return "Chiến dịch {$start} - {$end}";
        }
        return 'Chiến dịch';
    }

    public function getStartDateAttribute()
    {
        return $this->start_time ? $this->start_time->format('Y-m-d') : null;
    }

    public function getEndDateAttribute()
    {
        return $this->end_time ? $this->end_time->format('Y-m-d') : null;
    }

    public function getDiscountAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->discount_percent . '%' : '0%';
    }

    public function getMinQuantityAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->quantity : 0;
    }

    public function getProductIdsAttribute()
    {
        return $this->productVariants->pluck('id')->toArray();
    }
}

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class CampaignConfig extends Model
{
    use HasFactory;

    protected $fillable = ['campaign_id', 'quantity', 'discount_percent'];

    protected $casts = [
        'discount_percent' => 'decimal:2',
    ];

    public function campaign()
    {
        return $this->belongsTo(Campaign::class);
    }
}
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Relations\Pivot;

class CampaignProduct extends Pivot
{
    use HasFactory;

    protected $table = 'campaign_products';
    public $incrementing = true;

    protected $fillable = ['product_variant_id', 'campaign_id'];
}
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Promotion extends Model
{
    use HasFactory;

    protected $fillable = [
        'code',
        'type',
        'target_type',
        'discount_type',
        'discount_value',
        'min_order',
        'limit',
        'used',
        'expiry',
        'active',
        'description',
        'product_id',
        'tiers',
        'current_buyers',
        'start_date',
        'end_date',
        'campaign_id'
    ];

    protected $casts = [
        'tiers' => 'array',
        'active' => 'boolean',
        'discount_value' => 'decimal:0',
        'min_order' => 'decimal:0',
        'expiry' => 'date',
        'startDate' => 'nullable|date',
        'endDate' => 'nullable|date|after_or_equal:startDate',    
    ];

    public function campaign()
    {
        return $this->belongsTo(Campaign::class);
    }

    public function product()
    {
        return $this->belongsTo(Product::class);
    }
}
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Storage;
use Illuminate\Support\Facades\Log;

class BannerController extends Controller
{
    public function index()
    {
        return Inertia::render('Admin/Banners', [
            'banners' => Banner::with('campaign')->orderBy('order', 'asc')->get()
        ]);
    }

    public function getBanners()
    {
        return response()->json(Banner::with('campaign')->orderBy('order', 'asc')->get());
    }

    public function store(Request $request)
    {
        try {
            Log::info('Banner store request:', $request->all());

            $rules = [
                'title' => 'nullable|string|max:255',
                'campaign_id' => 'nullable|exists:campaigns,id',
                'image' => 'nullable|url',
                'link' => 'nullable|string|max:255',
                'description' => 'nullable|string',
                'status' => 'boolean',
                'order' => 'nullable|integer|min:0'
            ];

            if ($request->hasFile('image_file')) {
                $rules['image_file'] = 'image|max:2048';
            }

            $validated = $request->validate($rules);

            $data = [
                'title' => $validated['title'] ?? 'Banner ' . now()->format('d/m/Y'),
                'campaign_id' => $validated['campaign_id'] ?? null,
                'link' => $validated['link'] ?? null,
                'description' => $validated['description'] ?? null,
                'status' => $validated['status'] ?? true,
            ];
            
            // Xử lý ảnh
            if ($request->hasFile('image_file')) {
                try {
                    $path = $request->file('image_file')->store('banners', 'public');
                    $data['image'] = '/storage/' . $path;
                } catch (\Exception $e) {
                    Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                    return response()->json([
                        'success' => false,
                        'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                    ], 500);
                }
            } elseif ($request->filled('image')) {
                $data['image'] = $request->image;
            } else {
                return response()->json([
                    'success' => false,
                    'message' => 'Vui lòng chọn ảnh hoặc nhập URL'
                ], 422);
            }

            // Xác định order mới
            $totalBanners = Banner::count();
            $newOrder = $request->input('order', $totalBanners);
            
            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder > $totalBanners) $newOrder = $totalBanners;

            if ($newOrder < $totalBanners) {
                Banner::where('order', '>=', $newOrder)->increment('order');
            }

            $data['order'] = $newOrder;
            $banner = Banner::create($data);

            Log::info('Banner created successfully:', ['id' => $banner->id]);

            return response()->json([
                'success' => true, 
                'data' => $banner->load('campaign')
            ]);
            
        } catch (\Illuminate\Validation\ValidationException $e) {
            Log::error('Validation error:', $e->errors());
            return response()->json([
                'success' => false,
                'message' => 'Dữ liệu không hợp lệ',
                'errors' => $e->errors()
            ], 422);
        } catch (\Exception $e) {
            Log::error('Lỗi tạo banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function update(Request $request, $id)
    {
        try {
            Log::info('Banner update request:', ['id' => $id, 'data' => $request->all()]);

            $banner = Banner::findOrFail($id);
            
            $rules = [
                'title' => 'nullable|string|max:255',
                'campaign_id' => 'nullable|exists:campaigns,id',
                'image' => 'nullable|url',
                'link' => 'nullable|string|max:255',
                'description' => 'nullable|string',
                'status' => 'boolean',
                'order' => 'nullable|integer|min:0'
            ];

            if ($request->hasFile('image_file')) {
                $rules['image_file'] = 'image|max:2048';
            }

            $validated = $request->validate($rules);

            $data = [
                'title' => $validated['title'] ?? $banner->title,
                'campaign_id' => $validated['campaign_id'] ?? $banner->campaign_id,
                'link' => $validated['link'] ?? $banner->link,
                'description' => $validated['description'] ?? $banner->description,
                'status' => $validated['status'] ?? $banner->status,
            ];
            
            // Xử lý ảnh
            if ($request->hasFile('image_file')) {
                try {
                    if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                        Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
                    }
                    
                    $path = $request->file('image_file')->store('banners', 'public');
                    $data['image'] = '/storage/' . $path;
                } catch (\Exception $e) {
                    Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                    return response()->json([
                        'success' => false,
                        'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                    ], 500);
                }
            } elseif ($request->filled('image')) {
                $data['image'] = $request->image;
            }

            // Xử lý thay đổi order
            $newOrder = $request->input('order');
            if (!is_null($newOrder) && $newOrder != $banner->order) {
                $oldOrder = $banner->order;
                $totalBanners = Banner::count();

                if ($newOrder < 0) $newOrder = 0;
                if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

                if ($oldOrder < $newOrder) {
                    Banner::where('id', '!=', $id)
                        ->where('order', '>', $oldOrder)
                        ->where('order', '<=', $newOrder)
                        ->decrement('order');
                } else {
                    Banner::where('id', '!=', $id)
                        ->where('order', '>=', $newOrder)
                        ->where('order', '<', $oldOrder)
                        ->increment('order');
                }

                $data['order'] = $newOrder;
            }

            $banner->update($data);

            Log::info('Banner updated successfully:', ['id' => $banner->id]);

            return response()->json([
                'success' => true, 
                'data' => $banner->load('campaign')
            ]);
            
        } catch (\Illuminate\Validation\ValidationException $e) {
            Log::error('Validation error:', $e->errors());
            return response()->json([
                'success' => false,
                'message' => 'Dữ liệu không hợp lệ',
                'errors' => $e->errors()
            ], 422);
        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }
    public function destroy($id)
    {
        try {
            $banner = Banner::findOrFail($id);
            
            if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
            }
            
            $deletedOrder = $banner->order;
            $banner->delete();

            Banner::where('order', '>', $deletedOrder)->decrement('order');

            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            Log::error('Lỗi xóa banner: ' . $e->getMessage());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function updateStatus(Request $request, $id)
    {
        try {
            $banner = Banner::findOrFail($id);
            $request->validate(['status' => 'required|boolean']);
            $banner->update(['status' => $request->status]);
            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            return response()->json(['success' => false, 'message' => $e->getMessage()], 500);
        }
    }

    public function updateOrder(Request $request, $id)
    {
        try {
            $banner = Banner::findOrFail($id);
            $request->validate(['order' => 'required|integer|min:0']);

            $newOrder = $request->order;
            $oldOrder = $banner->order;

            if ($oldOrder == $newOrder) {
                return response()->json(['success' => true]);
            }

            $totalBanners = Banner::count();
            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

            if ($oldOrder < $newOrder) {
                Banner::where('id', '!=', $id)
                    ->where('order', '>', $oldOrder)
                    ->where('order', '<=', $newOrder)
                    ->decrement('order');
            } else {
                Banner::where('id', '!=', $id)
                    ->where('order', '>=', $newOrder)
                    ->where('order', '<', $oldOrder)
                    ->increment('order');
            }

            $banner->update(['order' => $newOrder]);

            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            return response()->json(['success' => false, 'message' => $e->getMessage()], 500);
        }
    }
}

<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Promotion;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                    ];
                });

            // Lấy tất cả promotions
            $promotions = Promotion::with(['campaign', 'product'])
                ->latest()
                ->get()
                ->map(function ($promotion) {
                    return [
                        'id' => $promotion->id,
                        'code' => $promotion->code,
                        'type' => $promotion->type,
                        'target_type' => $promotion->target_type,
                        'discount_type' => $promotion->discount_type,
                        'discount_value' => $promotion->discount_value,
                        'min_order' => $promotion->min_order,
                        'limit' => $promotion->limit,
                        'used' => $promotion->used ?? 0,
                        'expiry' => $promotion->expiry,
                        'active' => $promotion->active,
                        'description' => $promotion->description,
                        'campaign_id' => $promotion->campaign_id,
                        'campaign' => $promotion->campaign ? [
                            'id' => $promotion->campaign->id,
                            'name' => $promotion->campaign->name,
                        ] : null,
                        'product_id' => $promotion->product_id,
                        'tiers' => $promotion->tiers,
                        'current_buyers' => $promotion->current_buyers ?? 0,
                        'start_date' => $promotion->start_date,
                        'end_date' => $promotion->end_date,
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy tất cả products
            $products = Product::with(['variants.color', 'category', 'brand'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'is_preorder' => $product->is_preorder,
                        'category' => $product->category ? [
                            'id' => $product->category->id,
                            'name' => $product->category->name,
                        ] : null,
                        'brand' => $product->brand ? [
                            'id' => $product->brand->id,
                            'name' => $product->brand->name,
                        ] : null,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    $productName = $variant->product ? $variant->product->name : 'Sản phẩm không xác định';
                    $isPreorder = $variant->product ? $variant->product->is_preorder : false;
                    
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $isPreorder,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy chỉ sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'image_url' => $product->image_url,
                        'description' => $product->description,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => $promotions,
                'banners' => $banners,
                'products' => $products,
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate', // FIX: Cho phép ngày trùng
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100', // FIX: Cho phép 0
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            // Tạo config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            // Gán sản phẩm
            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate', // FIX: Cho phép ngày trùng
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100', // FIX: Cho phép 0
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            // Cập nhật config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            // Cập nhật sản phẩm
            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            
            // Giải phóng banner khỏi chiến dịch
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== VOUCHER METHODS ====================

    public function storePromotion(Request $request)
    {
        try {
            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper($validated['code']),
                'type' => 'voucher',
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'used' => 0,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePromotion(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code,' . $id,
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function togglePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->update(['active' => !$promotion->active]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date', // FIX: Cho phép ngày trùng
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'type' => 'preorder_tier',
                'target_type' => 'preorder',
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
                'discount_value' => 0,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date', // FIX: Cho phép ngày trùng
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePreorder($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}

<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    // FIX: Cho phép giảm giá = 0, chỉ báo lỗi khi < 0 hoặc > 100
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    
                        
                    <div class="absolute top-2 right-2 flex gap-1 flex-wrap">
                            <span class="text-[10px] px-2 py-0.5 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-[10px] px-2 py-0.5 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐</span>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">{{ campaign.name }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openCampaignModal(campaign)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giảm giá</p>
                                <p class="text-sm font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">SL tối thiểu</p>
                                <p class="text-sm font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-0.5 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-[10px] px-3 py-1 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-4 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div class="min-w-0 flex-1">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-base text-gray-800 mt-1 truncate">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-0.5 flex-shrink-0 ml-1">
                            <button @click="openVoucherModal(voucher)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors text-xs">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors text-xs">🗑️</button>
                        </div>
                    </div>

                    <p class="text-xs text-gray-500 mb-2 line-clamp-1">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-1 text-xs">
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-[10px] px-1.5 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">📅</span>
                            <span>HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">📊</span>
                            <span>{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-2 pt-2 border-t border-gray-100">
                        <div class="w-full h-1 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-2 pt-1 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-[10px] px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-4 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div class="min-w-0 flex-1">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-sm text-gray-800 mt-1 truncate">⏳ {{ preorder.code }}</h3>
                            <p class="text-[10px] text-gray-500 truncate">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-0.5 flex-shrink-0 ml-1">
                            <button @click="openPreorderModal(preorder)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors text-xs">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors text-xs">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-2 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-3">
                        <div class="flex justify-between items-center mb-1">
                            <span class="text-[10px] font-medium">Đã đặt: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-xs font-bold text-blue-600">
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-1.5 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-2 grid grid-cols-3 gap-1 text-[10px]">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-1 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500 text-[8px]">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-2 flex justify-between items-center text-[10px] text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-[10px] px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>


## Assistant:
07/09/2026 18:25

Tôi sẽ chỉnh lại code cho bạn với những thay đổi:

1. **Xóa Model Promotion** (không cần migration và model promotion)
2. **Chỉ dùng Campaigns** với 3 loại: Campaign, Voucher, Pre-order
3. **Controller chỉ quản lý Campaigns** (vì voucher và pre-order đã có trong promotion)

## 1. Xóa file Model Promotion (xóa file `app/Models/Promotion.php`)

## 2. Cập nhật Controller `PromotionController.php`

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    $productName = $variant->product ? $variant->product->name : 'Sản phẩm không xác định';
                    $isPreorder = $variant->product ? $variant->product->is_preorder : false;
                    
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $isPreorder,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => [], // Không dùng promotions
                'banners' => $banners,
                'products' => [],
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            // Tạo config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            // Gán sản phẩm
            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            // Cập nhật config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            // Cập nhật sản phẩm
            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            
            // Giải phóng banner khỏi chiến dịch
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}
```

## 3. Cập nhật Model `Campaign.php` (Đã có sẵn)

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Campaign extends Model
{
    use HasFactory;

    protected $fillable = [
        'name',
        'type',
        'description',
        'start_time',
        'end_time',
        'status',
        'priority',
        'featured'
    ];

    protected $casts = [
        'start_time' => 'datetime',
        'end_time' => 'datetime',
        'featured' => 'boolean',
    ];

    public function productVariants()
    {
        return $this->belongsToMany(ProductVariant::class, 'campaign_products');
    }

    public function configs()
    {
        return $this->hasMany(CampaignConfig::class);
    }

    public function orders()
    {
        return $this->hasMany(Order::class);
    }

    public function getNameAttribute()
    {
        if ($this->attributes['name'] ?? false) {
            return $this->attributes['name'];
        }
        if ($this->start_time && $this->end_time) {
            $start = $this->start_time->format('d/m/Y');
            $end = $this->end_time->format('d/m/Y');
            return "Chiến dịch {$start} - {$end}";
        }
        return 'Chiến dịch';
    }

    public function getStartDateAttribute()
    {
        return $this->start_time ? $this->start_time->format('Y-m-d') : null;
    }

    public function getEndDateAttribute()
    {
        return $this->end_time ? $this->end_time->format('Y-m-d') : null;
    }

    public function getDiscountAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->discount_percent . '%' : '0%';
    }

    public function getMinQuantityAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->quantity : 0;
    }

    public function getProductIdsAttribute()
    {
        return $this->productVariants->pluck('id')->toArray();
    }
}
```

## 4. File Vue `Promotions.vue` (Hoàn chỉnh - chỉ Campaigns)

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const editingCampaign = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch khuyến mãi</p>
                </div>
                <div>
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Thêm chiến dịch
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm chiến dịch..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Status tabs -->
            <div class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST (3 items per row) ==================== -->
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-36 bg-gradient-to-r from-gray-100 to-gray-200">
                        <div class="w-full h-full flex items-center justify-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-2 right-2 flex gap-1 flex-wrap">
                            <span class="text-[10px] px-2 py-0.5 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-[10px] px-2 py-0.5 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐</span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">{{ campaign.name }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openCampaignModal(campaign)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giảm giá</p>
                                <p class="text-sm font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">SL tối thiểu</p>
                                <p class="text-sm font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-0.5 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-[10px] px-3 py-1 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
```

## 5. Cập nhật Route (xóa các route không cần thiết)

```php
// routes/web.php - trong admin group
Route::prefix('admin')->middleware(['auth', 'admin'])->name('admin.')->group(function () {
    // ... các route khác

    // Promotion routes (chỉ giữ lại campaigns)
    Route::get('/promotions', [PromotionController::class, 'index'])->name('promotions.index');
    Route::post('/promotions/campaign', [PromotionController::class, 'storeCampaign'])->name('promotions.campaign.store');
    Route::put('/promotions/campaign/{id}', [PromotionController::class, 'updateCampaign'])->name('promotions.campaign.update');
    Route::delete('/promotions/campaign/{id}', [PromotionController::class, 'deleteCampaign'])->name('promotions.campaign.delete');
    Route::put('/promotions/campaign/{id}/status', [PromotionController::class, 'updateCampaignStatus'])->name('promotions.campaign.status');
    Route::get('/promotions/campaigns/list', [PromotionController::class, 'getCampaignsList'])->name('promotions.campaigns.list');

    // XÓA các route voucher và preorder
});
```

## Tóm tắt các thay đổi:

1. **Xóa Model Promotion** - Không cần file `app/Models/Promotion.php`
2. **Xóa các method liên quan đến voucher và preorder** trong `PromotionController`
3. **Xóa các tab Voucher và Pre-order** trong Vue component
4. **Chỉ giữ lại Campaigns** với 3 cột hiển thị
5. **Giữ nguyên các tính năng**: thêm/sửa/xóa campaign, chọn sản phẩm, ngày tháng trùng, giảm giá 0%


## User:
07/09/2026 18:35

<?php

use App\Http\Controllers\Admin\DashboardController;
use App\Http\Controllers\Admin\OrderController as AdminOrderController;
use App\Http\Controllers\Admin\ProductController as AdminProductController;
use App\Http\Controllers\Admin\CustomerController as AdminCustomerController;
use App\Http\Controllers\Admin\CustomizeController as AdminCustomizeController;
use App\Http\Controllers\Admin\SettingController;
use App\Http\Controllers\Admin\CategoryController;
use App\Http\Controllers\Admin\ColorController;
use App\Http\Controllers\Admin\BrandController;
use App\Http\Controllers\Admin\BannerController;
use App\Http\Controllers\Admin\NewsController;
use App\Http\Controllers\Admin\PromotionController;
use App\Http\Controllers\PaymentController;
use App\Http\Controllers\OrderController;
use App\Http\Controllers\CartController;
use App\Http\Controllers\ProfileController;
use App\Http\Controllers\CategoryController as WebCategoryController;
use App\Http\Controllers\ProductController as WebProductController;
use App\Http\Controllers\HomeController;
use Illuminate\Foundation\Application;
use Illuminate\Support\Facades\Route;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\File;
use Inertia\Inertia;

// ==================== ROUTE ĐỂ PHỤC VỤ ẢNH ====================
Route::get('/image/{filename}', function ($filename) {
    $path = base_path('image/' . $filename);
    if (!File::exists($path)) {
        abort(404);
    }

    $file = File::get($path);
    $type = File::mimeType($path);

    return response($file, 200)->header('Content-Type', $type);
})->where('filename', '.*');

// ==================== WEB ROUTES (Frontend - Public) ====================

Route::get('/', [HomeController::class, 'index'])->name('home');

Route::get('/tim-kiem', function (Request $request) {
    return Inertia::render('Web/Category', ['search' => $request->get('q')]);
})->name('search');

// Product routes - PUBLIC
Route::get('/san-pham/{id}', [WebProductController::class, 'show'])->name('product.detail');
Route::get('/danh-muc/{slug}', [WebCategoryController::class, 'show'])->name('category');

// Other public frontend routes
Route::get('/mua-si', function () {
    return Inertia::render('Web/Wholesale');
})->name('wholesale');

Route::get('/khuyen-mai', function () {
    return Inertia::render('Web/Promotion');
})->name('promotion');

Route::get('/tuy-chinh', function () {
    return Inertia::render('Web/Customize');
})->name('customize');

// ==================== CART ROUTES (Yêu cầu đăng nhập) ====================

// Cart page
Route::get('/gio-hang', function () {
    return Inertia::render('Web/Cart');
})->name('cart')->middleware('auth');

// ⭐ QUAN TRỌNG: Cart API routes - Thêm middleware 'auth' và 'web'
Route::middleware(['auth'])->prefix('api')->group(function () {
    Route::get('/cart', [CartController::class, 'index']);
    Route::post('/cart/add', [CartController::class, 'add']);
    Route::put('/cart/update', [CartController::class, 'update']);
    Route::delete('/cart/remove/{variantId}', [CartController::class, 'remove']);
    Route::delete('/cart/clear', [CartController::class, 'clear']);
});

// ==================== CHECKOUT ROUTES (Yêu cầu đăng nhập) ====================
Route::middleware(['auth'])->group(function () {
    Route::get('/thanh-toan', [PaymentController::class, 'index'])->name('checkout');
    Route::post('/thanh-toan', [PaymentController::class, 'store'])->name('checkout.store');
    Route::get('/thanh-toan/thanh-cong', [PaymentController::class, 'success'])->name('checkout.success');
});

// ==================== AUTHENTICATED WEB ROUTES ====================
Route::middleware('auth')->group(function () {
    Route::get('/profile', [ProfileController::class, 'edit'])->name('profile.edit');
    Route::patch('/profile', [ProfileController::class, 'update'])->name('profile.update');
    Route::delete('/profile', [ProfileController::class, 'destroy'])->name('profile.destroy');
    Route::get('/don-hang', [OrderController::class, 'history'])->name('orders.history');
    Route::get('/don-hang/{id}', [OrderController::class, 'show'])->name('orders.show');
});

// ==================== ADMIN ROUTES ====================
Route::prefix('admin')->middleware(['auth', 'admin'])->name('admin.')->group(function () {
    // Dashboard
    Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');
    Route::get('/', [DashboardController::class, 'index'])->name('home');
    
    // Orders Management
    Route::prefix('orders')->group(function () {
        Route::get('/{type?}', [AdminOrderController::class, 'index'])
            ->where('type', 'retail|wholesale|preorder')
            ->name('orders.index');
        Route::get('/{id}', [AdminOrderController::class, 'show'])
            ->where('id', '[0-9]+')
            ->name('orders.show');
        Route::put('/{id}/status', [AdminOrderController::class, 'updateStatus'])->name('orders.update-status');
        Route::get('/export', [AdminOrderController::class, 'export'])->name('orders.export');
        Route::get('/export/filtered', [AdminOrderController::class, 'exportWithFilters'])->name('orders.export-filtered');
    });
    
    // Products Management
    Route::prefix('products')->group(function () {
        Route::get('/{type?}', [AdminProductController::class, 'index'])
            ->where('type', 'normal|preorder')
            ->name('products.index');
        Route::post('/', [AdminProductController::class, 'store'])->name('products.store');
        Route::put('/{product}', [AdminProductController::class, 'update'])->name('products.update');
        Route::delete('/{product}', [AdminProductController::class, 'destroy'])->name('products.destroy');
    });
    
    // Categories Management
    Route::get('/categories', [CategoryController::class, 'index'])->name('categories.index');
    Route::get('/categories/create', [CategoryController::class, 'create'])->name('categories.create');
    Route::post('/categories', [CategoryController::class, 'store'])->name('categories.store');
    Route::get('/categories/{category}/edit', [CategoryController::class, 'edit'])->name('categories.edit');
    Route::put('/categories/{category}', [CategoryController::class, 'update'])->name('categories.update');
    Route::delete('/categories/{category}', [CategoryController::class, 'destroy'])->name('categories.destroy');
    
    // Colors Management
    Route::get('/colors', [ColorController::class, 'index'])->name('colors.index');
    Route::get('/colors/data', [ColorController::class, 'getColors'])->name('colors.data');
    Route::post('/colors', [ColorController::class, 'store'])->name('colors.store');
    Route::put('/colors/{id}', [ColorController::class, 'update'])->name('colors.update');
    Route::delete('/colors/{id}', [ColorController::class, 'destroy'])->name('colors.destroy');
        
    // Brands Management
    Route::prefix('brands')->group(function () {
        Route::get('/', [BrandController::class, 'index'])->name('brands.index');
        Route::get('/data', [BrandController::class, 'getBrands'])->name('brands.data');
        Route::post('/', [BrandController::class, 'store'])->name('brands.store');
        Route::put('/{id}', [BrandController::class, 'update'])->name('brands.update');
        Route::delete('/{id}', [BrandController::class, 'destroy'])->name('brands.destroy');
        Route::get('/search', [BrandController::class, 'search'])->name('brands.search');
    });
    
    // Customers Management
    Route::prefix('customers')->group(function () {
        Route::get('/', [AdminCustomerController::class, 'index'])->name('customers.index');
        Route::get('/retail', [AdminCustomerController::class, 'retail'])->name('customers.retail');
        Route::get('/business', [AdminCustomerController::class, 'business'])->name('customers.business');
        Route::get('/{id}', [AdminCustomerController::class, 'show'])->name('customers.show');
        Route::put('/{id}', [AdminCustomerController::class, 'update'])->name('customers.update');
        Route::post('/export', [AdminCustomerController::class, 'export'])->name('customers.export');
    });
    
    // Customize Management
    Route::prefix('customize')->group(function () {
        Route::get('/', [AdminCustomizeController::class, 'index'])->name('customize.index');
        Route::put('/{id}/status', [AdminCustomizeController::class, 'updateStatus'])->name('customize.update-status');
        Route::put('/{id}/approve', [AdminCustomizeController::class, 'approve'])->name('customize.approve');
        Route::post('/send-quote', [AdminCustomizeController::class, 'sendQuote'])->name('customize.send-quote');
    });

        // News Management
    Route::prefix('news')->group(function () {
        Route::get('/', [NewsController::class, 'index'])->name('news.index');
        Route::get('/data', [NewsController::class, 'getNews'])->name('news.data');
        Route::post('/', [NewsController::class, 'store'])->name('news.store');
        Route::put('/{id}', [NewsController::class, 'update'])->name('news.update');
        Route::delete('/{id}', [NewsController::class, 'destroy'])->name('news.destroy');
        Route::patch('/{id}/status', [NewsController::class, 'updateStatus'])->name('news.update-status');
    });

       
    // Banner routes
    Route::get('/banners', [BannerController::class, 'index'])->name('banners.index');
    Route::get('/banners/data', [BannerController::class, 'getBanners'])->name('banners.data');
    Route::post('/banners', [BannerController::class, 'store'])->name('banners.store');
    Route::put('/banners/{id}', [BannerController::class, 'update'])->name('banners.update');
    Route::delete('/banners/{id}', [BannerController::class, 'destroy'])->name('banners.destroy');
    Route::patch('/banners/{id}/status', [BannerController::class, 'updateStatus'])->name('banners.status');
    Route::patch('/banners/{id}/order', [BannerController::class, 'updateOrder'])->name('banners.order');

    // Promotion routes
    Route::get('/promotions/campaigns/list', [PromotionController::class, 'getCampaignsList'])->name('admin.promotions.campaigns.list');

    Route::get('/promotions', [PromotionController::class, 'index'])->name('promotions.index');
    Route::post('/promotions/campaign', [PromotionController::class, 'storeCampaign'])->name('promotions.campaign.store');
    Route::put('/promotions/campaign/{id}', [PromotionController::class, 'updateCampaign'])->name('promotions.campaign.update');
    Route::delete('/promotions/campaign/{id}', [PromotionController::class, 'deleteCampaign'])->name('promotions.campaign.delete');
    Route::put('/promotions/campaign/{id}/status', [PromotionController::class, 'updateCampaignStatus'])->name('promotions.campaign.status');

    Route::post('/promotions/voucher', [PromotionController::class, 'storePromotion'])->name('promotions.voucher.store');
    Route::put('/promotions/voucher/{id}', [PromotionController::class, 'updatePromotion'])->name('promotions.voucher.update');
    Route::delete('/promotions/voucher/{id}', [PromotionController::class, 'deletePromotion'])->name('promotions.voucher.delete');
    Route::put('/promotions/voucher/{id}/toggle', [PromotionController::class, 'togglePromotion'])->name('promotions.voucher.toggle');

    Route::post('/promotions/preorder', [PromotionController::class, 'storePreorder'])->name('promotions.preorder.store');
    Route::put('/promotions/preorder/{id}', [PromotionController::class, 'updatePreorder'])->name('promotions.preorder.update');
    Route::delete('/promotions/preorder/{id}', [PromotionController::class, 'deletePreorder'])->name('promotions.preorder.delete');

    // Reports
    Route::get('/reports', function () {
        return Inertia::render('Admin/Reports');
    })->name('reports.index');
    
    // Settings
    Route::get('/settings', [SettingController::class, 'index'])->name('settings.index');
    Route::put('/settings/general', [SettingController::class, 'updateGeneral'])->name('settings.updateGeneral');
    Route::put('/settings/password', [SettingController::class, 'changePassword'])->name('settings.changePassword');

    // User Management
    Route::get('/settings/users', [SettingController::class, 'getUsers'])->name('settings.users');
    Route::post('/settings/users', [SettingController::class, 'storeUser'])->name('settings.storeUser');
    Route::put('/settings/users/{id}', [SettingController::class, 'updateUser'])->name('settings.updateUser');
    Route::delete('/settings/users/{id}', [SettingController::class, 'destroyUser'])->name('settings.destroyUser');
    Route::patch('/settings/users/{id}/toggle', [SettingController::class, 'toggleUserStatus'])->name('settings.toggleUser');
});

require __DIR__.'/auth.php';

chỉnh lại cho tôi và đưa code hoàn chỉnh


## Assistant:
07/09/2026 18:35

Tôi sẽ chỉnh lại route cho bạn, xóa các route liên quan đến voucher và preorder vì không cần dùng nữa.

```php
<?php

use App\Http\Controllers\Admin\DashboardController;
use App\Http\Controllers\Admin\OrderController as AdminOrderController;
use App\Http\Controllers\Admin\ProductController as AdminProductController;
use App\Http\Controllers\Admin\CustomerController as AdminCustomerController;
use App\Http\Controllers\Admin\CustomizeController as AdminCustomizeController;
use App\Http\Controllers\Admin\SettingController;
use App\Http\Controllers\Admin\CategoryController;
use App\Http\Controllers\Admin\ColorController;
use App\Http\Controllers\Admin\BrandController;
use App\Http\Controllers\Admin\BannerController;
use App\Http\Controllers\Admin\NewsController;
use App\Http\Controllers\Admin\PromotionController;
use App\Http\Controllers\PaymentController;
use App\Http\Controllers\OrderController;
use App\Http\Controllers\CartController;
use App\Http\Controllers\ProfileController;
use App\Http\Controllers\CategoryController as WebCategoryController;
use App\Http\Controllers\ProductController as WebProductController;
use App\Http\Controllers\HomeController;
use Illuminate\Foundation\Application;
use Illuminate\Support\Facades\Route;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\File;
use Inertia\Inertia;

// ==================== ROUTE ĐỂ PHỤC VỤ ẢNH ====================
Route::get('/image/{filename}', function ($filename) {
    $path = base_path('image/' . $filename);
    if (!File::exists($path)) {
        abort(404);
    }

    $file = File::get($path);
    $type = File::mimeType($path);

    return response($file, 200)->header('Content-Type', $type);
})->where('filename', '.*');

// ==================== WEB ROUTES (Frontend - Public) ====================

Route::get('/', [HomeController::class, 'index'])->name('home');

Route::get('/tim-kiem', function (Request $request) {
    return Inertia::render('Web/Category', ['search' => $request->get('q')]);
})->name('search');

// Product routes - PUBLIC
Route::get('/san-pham/{id}', [WebProductController::class, 'show'])->name('product.detail');
Route::get('/danh-muc/{slug}', [WebCategoryController::class, 'show'])->name('category');

// Other public frontend routes
Route::get('/mua-si', function () {
    return Inertia::render('Web/Wholesale');
})->name('wholesale');

Route::get('/khuyen-mai', function () {
    return Inertia::render('Web/Promotion');
})->name('promotion');

Route::get('/tuy-chinh', function () {
    return Inertia::render('Web/Customize');
})->name('customize');

// ==================== CART ROUTES (Yêu cầu đăng nhập) ====================

// Cart page
Route::get('/gio-hang', function () {
    return Inertia::render('Web/Cart');
})->name('cart')->middleware('auth');

// ⭐ QUAN TRỌNG: Cart API routes - Thêm middleware 'auth' và 'web'
Route::middleware(['auth'])->prefix('api')->group(function () {
    Route::get('/cart', [CartController::class, 'index']);
    Route::post('/cart/add', [CartController::class, 'add']);
    Route::put('/cart/update', [CartController::class, 'update']);
    Route::delete('/cart/remove/{variantId}', [CartController::class, 'remove']);
    Route::delete('/cart/clear', [CartController::class, 'clear']);
});

// ==================== CHECKOUT ROUTES (Yêu cầu đăng nhập) ====================
Route::middleware(['auth'])->group(function () {
    Route::get('/thanh-toan', [PaymentController::class, 'index'])->name('checkout');
    Route::post('/thanh-toan', [PaymentController::class, 'store'])->name('checkout.store');
    Route::get('/thanh-toan/thanh-cong', [PaymentController::class, 'success'])->name('checkout.success');
});

// ==================== AUTHENTICATED WEB ROUTES ====================
Route::middleware('auth')->group(function () {
    Route::get('/profile', [ProfileController::class, 'edit'])->name('profile.edit');
    Route::patch('/profile', [ProfileController::class, 'update'])->name('profile.update');
    Route::delete('/profile', [ProfileController::class, 'destroy'])->name('profile.destroy');
    Route::get('/don-hang', [OrderController::class, 'history'])->name('orders.history');
    Route::get('/don-hang/{id}', [OrderController::class, 'show'])->name('orders.show');
});

// ==================== ADMIN ROUTES ====================
Route::prefix('admin')->middleware(['auth', 'admin'])->name('admin.')->group(function () {
    // Dashboard
    Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');
    Route::get('/', [DashboardController::class, 'index'])->name('home');
    
    // Orders Management
    Route::prefix('orders')->group(function () {
        Route::get('/{type?}', [AdminOrderController::class, 'index'])
            ->where('type', 'retail|wholesale|preorder')
            ->name('orders.index');
        Route::get('/{id}', [AdminOrderController::class, 'show'])
            ->where('id', '[0-9]+')
            ->name('orders.show');
        Route::put('/{id}/status', [AdminOrderController::class, 'updateStatus'])->name('orders.update-status');
        Route::get('/export', [AdminOrderController::class, 'export'])->name('orders.export');
        Route::get('/export/filtered', [AdminOrderController::class, 'exportWithFilters'])->name('orders.export-filtered');
    });
    
    // Products Management
    Route::prefix('products')->group(function () {
        Route::get('/{type?}', [AdminProductController::class, 'index'])
            ->where('type', 'normal|preorder')
            ->name('products.index');
        Route::post('/', [AdminProductController::class, 'store'])->name('products.store');
        Route::put('/{product}', [AdminProductController::class, 'update'])->name('products.update');
        Route::delete('/{product}', [AdminProductController::class, 'destroy'])->name('products.destroy');
    });
    
    // Categories Management
    Route::get('/categories', [CategoryController::class, 'index'])->name('categories.index');
    Route::get('/categories/create', [CategoryController::class, 'create'])->name('categories.create');
    Route::post('/categories', [CategoryController::class, 'store'])->name('categories.store');
    Route::get('/categories/{category}/edit', [CategoryController::class, 'edit'])->name('categories.edit');
    Route::put('/categories/{category}', [CategoryController::class, 'update'])->name('categories.update');
    Route::delete('/categories/{category}', [CategoryController::class, 'destroy'])->name('categories.destroy');
    
    // Colors Management
    Route::get('/colors', [ColorController::class, 'index'])->name('colors.index');
    Route::get('/colors/data', [ColorController::class, 'getColors'])->name('colors.data');
    Route::post('/colors', [ColorController::class, 'store'])->name('colors.store');
    Route::put('/colors/{id}', [ColorController::class, 'update'])->name('colors.update');
    Route::delete('/colors/{id}', [ColorController::class, 'destroy'])->name('colors.destroy');
        
    // Brands Management
    Route::prefix('brands')->group(function () {
        Route::get('/', [BrandController::class, 'index'])->name('brands.index');
        Route::get('/data', [BrandController::class, 'getBrands'])->name('brands.data');
        Route::post('/', [BrandController::class, 'store'])->name('brands.store');
        Route::put('/{id}', [BrandController::class, 'update'])->name('brands.update');
        Route::delete('/{id}', [BrandController::class, 'destroy'])->name('brands.destroy');
        Route::get('/search', [BrandController::class, 'search'])->name('brands.search');
    });
    
    // Customers Management
    Route::prefix('customers')->group(function () {
        Route::get('/', [AdminCustomerController::class, 'index'])->name('customers.index');
        Route::get('/retail', [AdminCustomerController::class, 'retail'])->name('customers.retail');
        Route::get('/business', [AdminCustomerController::class, 'business'])->name('customers.business');
        Route::get('/{id}', [AdminCustomerController::class, 'show'])->name('customers.show');
        Route::put('/{id}', [AdminCustomerController::class, 'update'])->name('customers.update');
        Route::post('/export', [AdminCustomerController::class, 'export'])->name('customers.export');
    });
    
    // Customize Management
    Route::prefix('customize')->group(function () {
        Route::get('/', [AdminCustomizeController::class, 'index'])->name('customize.index');
        Route::put('/{id}/status', [AdminCustomizeController::class, 'updateStatus'])->name('customize.update-status');
        Route::put('/{id}/approve', [AdminCustomizeController::class, 'approve'])->name('customize.approve');
        Route::post('/send-quote', [AdminCustomizeController::class, 'sendQuote'])->name('customize.send-quote');
    });

    // News Management
    Route::prefix('news')->group(function () {
        Route::get('/', [NewsController::class, 'index'])->name('news.index');
        Route::get('/data', [NewsController::class, 'getNews'])->name('news.data');
        Route::post('/', [NewsController::class, 'store'])->name('news.store');
        Route::put('/{id}', [NewsController::class, 'update'])->name('news.update');
        Route::delete('/{id}', [NewsController::class, 'destroy'])->name('news.destroy');
        Route::patch('/{id}/status', [NewsController::class, 'updateStatus'])->name('news.update-status');
    });

    // Banner routes
    Route::get('/banners', [BannerController::class, 'index'])->name('banners.index');
    Route::get('/banners/data', [BannerController::class, 'getBanners'])->name('banners.data');
    Route::post('/banners', [BannerController::class, 'store'])->name('banners.store');
    Route::put('/banners/{id}', [BannerController::class, 'update'])->name('banners.update');
    Route::delete('/banners/{id}', [BannerController::class, 'destroy'])->name('banners.destroy');
    Route::patch('/banners/{id}/status', [BannerController::class, 'updateStatus'])->name('banners.status');
    Route::patch('/banners/{id}/order', [BannerController::class, 'updateOrder'])->name('banners.order');

    // ==================== PROMOTION ROUTES (CHỈ CAMPAIGNS) ====================
    Route::prefix('promotions')->group(function () {
        // Campaign management
        Route::get('/', [PromotionController::class, 'index'])->name('promotions.index');
        Route::post('/campaign', [PromotionController::class, 'storeCampaign'])->name('promotions.campaign.store');
        Route::put('/campaign/{id}', [PromotionController::class, 'updateCampaign'])->name('promotions.campaign.update');
        Route::delete('/campaign/{id}', [PromotionController::class, 'deleteCampaign'])->name('promotions.campaign.delete');
        Route::put('/campaign/{id}/status', [PromotionController::class, 'updateCampaignStatus'])->name('promotions.campaign.status');
        Route::get('/campaigns/list', [PromotionController::class, 'getCampaignsList'])->name('promotions.campaigns.list');
        
        // ĐÃ XÓA các route voucher và preorder
    });

    // Reports
    Route::get('/reports', function () {
        return Inertia::render('Admin/Reports');
    })->name('reports.index');
    
    // Settings
    Route::get('/settings', [SettingController::class, 'index'])->name('settings.index');
    Route::put('/settings/general', [SettingController::class, 'updateGeneral'])->name('settings.updateGeneral');
    Route::put('/settings/password', [SettingController::class, 'changePassword'])->name('settings.changePassword');

    // User Management
    Route::get('/settings/users', [SettingController::class, 'getUsers'])->name('settings.users');
    Route::post('/settings/users', [SettingController::class, 'storeUser'])->name('settings.storeUser');
    Route::put('/settings/users/{id}', [SettingController::class, 'updateUser'])->name('settings.updateUser');
    Route::delete('/settings/users/{id}', [SettingController::class, 'destroyUser'])->name('settings.destroyUser');
    Route::patch('/settings/users/{id}/toggle', [SettingController::class, 'toggleUserStatus'])->name('settings.toggleUser');
});

require __DIR__.'/auth.php';
```

## Tóm tắt các thay đổi trong route:

1. **Xóa các route voucher**:
   - `POST /admin/promotions/voucher`
   - `PUT /admin/promotions/voucher/{id}`
   - `DELETE /admin/promotions/voucher/{id}`
   - `PUT /admin/promotions/voucher/{id}/toggle`

2. **Xóa các route preorder**:
   - `POST /admin/promotions/preorder`
   - `PUT /admin/promotions/preorder/{id}`
   - `DELETE /admin/promotions/preorder/{id}`

3. **Giữ lại các route campaign**:
   - `GET /admin/promotions` - Danh sách campaigns
   - `POST /admin/promotions/campaign` - Thêm campaign
   - `PUT /admin/promotions/campaign/{id}` - Sửa campaign
   - `DELETE /admin/promotions/campaign/{id}` - Xóa campaign
   - `PUT /admin/promotions/campaign/{id}/status` - Cập nhật trạng thái
   - `GET /admin/promotions/campaigns/list` - Danh sách campaigns cho dropdown

4. **Sắp xếp lại code** - Gom các route promotions vào một group prefix để dễ quản lý.


## User:
07/09/2026 18:40

bạn chỉnh mất luôn quản lý voucher và khuyến mãi pre-order của tôi rồi , chỉ còn mỗi ql chiến dịch thôi. Chỉnh lại cho tôi


## Assistant:
07/09/2026 18:40

Tôi xin lỗi vì đã xóa nhầm. Tôi sẽ chỉnh lại route để giữ đầy đủ cả 3 tab: **Chiến dịch**, **Mã giảm giá (Voucher)** và **Pre-order**.

```php
<?php

use App\Http\Controllers\Admin\DashboardController;
use App\Http\Controllers\Admin\OrderController as AdminOrderController;
use App\Http\Controllers\Admin\ProductController as AdminProductController;
use App\Http\Controllers\Admin\CustomerController as AdminCustomerController;
use App\Http\Controllers\Admin\CustomizeController as AdminCustomizeController;
use App\Http\Controllers\Admin\SettingController;
use App\Http\Controllers\Admin\CategoryController;
use App\Http\Controllers\Admin\ColorController;
use App\Http\Controllers\Admin\BrandController;
use App\Http\Controllers\Admin\BannerController;
use App\Http\Controllers\Admin\NewsController;
use App\Http\Controllers\Admin\PromotionController;
use App\Http\Controllers\PaymentController;
use App\Http\Controllers\OrderController;
use App\Http\Controllers\CartController;
use App\Http\Controllers\ProfileController;
use App\Http\Controllers\CategoryController as WebCategoryController;
use App\Http\Controllers\ProductController as WebProductController;
use App\Http\Controllers\HomeController;
use Illuminate\Foundation\Application;
use Illuminate\Support\Facades\Route;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\File;
use Inertia\Inertia;

// ==================== ROUTE ĐỂ PHỤC VỤ ẢNH ====================
Route::get('/image/{filename}', function ($filename) {
    $path = base_path('image/' . $filename);
    if (!File::exists($path)) {
        abort(404);
    }

    $file = File::get($path);
    $type = File::mimeType($path);

    return response($file, 200)->header('Content-Type', $type);
})->where('filename', '.*');

// ==================== WEB ROUTES (Frontend - Public) ====================

Route::get('/', [HomeController::class, 'index'])->name('home');

Route::get('/tim-kiem', function (Request $request) {
    return Inertia::render('Web/Category', ['search' => $request->get('q')]);
})->name('search');

// Product routes - PUBLIC
Route::get('/san-pham/{id}', [WebProductController::class, 'show'])->name('product.detail');
Route::get('/danh-muc/{slug}', [WebCategoryController::class, 'show'])->name('category');

// Other public frontend routes
Route::get('/mua-si', function () {
    return Inertia::render('Web/Wholesale');
})->name('wholesale');

Route::get('/khuyen-mai', function () {
    return Inertia::render('Web/Promotion');
})->name('promotion');

Route::get('/tuy-chinh', function () {
    return Inertia::render('Web/Customize');
})->name('customize');

// ==================== CART ROUTES (Yêu cầu đăng nhập) ====================

// Cart page
Route::get('/gio-hang', function () {
    return Inertia::render('Web/Cart');
})->name('cart')->middleware('auth');

// ⭐ QUAN TRỌNG: Cart API routes - Thêm middleware 'auth' và 'web'
Route::middleware(['auth'])->prefix('api')->group(function () {
    Route::get('/cart', [CartController::class, 'index']);
    Route::post('/cart/add', [CartController::class, 'add']);
    Route::put('/cart/update', [CartController::class, 'update']);
    Route::delete('/cart/remove/{variantId}', [CartController::class, 'remove']);
    Route::delete('/cart/clear', [CartController::class, 'clear']);
});

// ==================== CHECKOUT ROUTES (Yêu cầu đăng nhập) ====================
Route::middleware(['auth'])->group(function () {
    Route::get('/thanh-toan', [PaymentController::class, 'index'])->name('checkout');
    Route::post('/thanh-toan', [PaymentController::class, 'store'])->name('checkout.store');
    Route::get('/thanh-toan/thanh-cong', [PaymentController::class, 'success'])->name('checkout.success');
});

// ==================== AUTHENTICATED WEB ROUTES ====================
Route::middleware('auth')->group(function () {
    Route::get('/profile', [ProfileController::class, 'edit'])->name('profile.edit');
    Route::patch('/profile', [ProfileController::class, 'update'])->name('profile.update');
    Route::delete('/profile', [ProfileController::class, 'destroy'])->name('profile.destroy');
    Route::get('/don-hang', [OrderController::class, 'history'])->name('orders.history');
    Route::get('/don-hang/{id}', [OrderController::class, 'show'])->name('orders.show');
});

// ==================== ADMIN ROUTES ====================
Route::prefix('admin')->middleware(['auth', 'admin'])->name('admin.')->group(function () {
    // Dashboard
    Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');
    Route::get('/', [DashboardController::class, 'index'])->name('home');
    
    // Orders Management
    Route::prefix('orders')->group(function () {
        Route::get('/{type?}', [AdminOrderController::class, 'index'])
            ->where('type', 'retail|wholesale|preorder')
            ->name('orders.index');
        Route::get('/{id}', [AdminOrderController::class, 'show'])
            ->where('id', '[0-9]+')
            ->name('orders.show');
        Route::put('/{id}/status', [AdminOrderController::class, 'updateStatus'])->name('orders.update-status');
        Route::get('/export', [AdminOrderController::class, 'export'])->name('orders.export');
        Route::get('/export/filtered', [AdminOrderController::class, 'exportWithFilters'])->name('orders.export-filtered');
    });
    
    // Products Management
    Route::prefix('products')->group(function () {
        Route::get('/{type?}', [AdminProductController::class, 'index'])
            ->where('type', 'normal|preorder')
            ->name('products.index');
        Route::post('/', [AdminProductController::class, 'store'])->name('products.store');
        Route::put('/{product}', [AdminProductController::class, 'update'])->name('products.update');
        Route::delete('/{product}', [AdminProductController::class, 'destroy'])->name('products.destroy');
    });
    
    // Categories Management
    Route::get('/categories', [CategoryController::class, 'index'])->name('categories.index');
    Route::get('/categories/create', [CategoryController::class, 'create'])->name('categories.create');
    Route::post('/categories', [CategoryController::class, 'store'])->name('categories.store');
    Route::get('/categories/{category}/edit', [CategoryController::class, 'edit'])->name('categories.edit');
    Route::put('/categories/{category}', [CategoryController::class, 'update'])->name('categories.update');
    Route::delete('/categories/{category}', [CategoryController::class, 'destroy'])->name('categories.destroy');
    
    // Colors Management
    Route::get('/colors', [ColorController::class, 'index'])->name('colors.index');
    Route::get('/colors/data', [ColorController::class, 'getColors'])->name('colors.data');
    Route::post('/colors', [ColorController::class, 'store'])->name('colors.store');
    Route::put('/colors/{id}', [ColorController::class, 'update'])->name('colors.update');
    Route::delete('/colors/{id}', [ColorController::class, 'destroy'])->name('colors.destroy');
        
    // Brands Management
    Route::prefix('brands')->group(function () {
        Route::get('/', [BrandController::class, 'index'])->name('brands.index');
        Route::get('/data', [BrandController::class, 'getBrands'])->name('brands.data');
        Route::post('/', [BrandController::class, 'store'])->name('brands.store');
        Route::put('/{id}', [BrandController::class, 'update'])->name('brands.update');
        Route::delete('/{id}', [BrandController::class, 'destroy'])->name('brands.destroy');
        Route::get('/search', [BrandController::class, 'search'])->name('brands.search');
    });
    
    // Customers Management
    Route::prefix('customers')->group(function () {
        Route::get('/', [AdminCustomerController::class, 'index'])->name('customers.index');
        Route::get('/retail', [AdminCustomerController::class, 'retail'])->name('customers.retail');
        Route::get('/business', [AdminCustomerController::class, 'business'])->name('customers.business');
        Route::get('/{id}', [AdminCustomerController::class, 'show'])->name('customers.show');
        Route::put('/{id}', [AdminCustomerController::class, 'update'])->name('customers.update');
        Route::post('/export', [AdminCustomerController::class, 'export'])->name('customers.export');
    });
    
    // Customize Management
    Route::prefix('customize')->group(function () {
        Route::get('/', [AdminCustomizeController::class, 'index'])->name('customize.index');
        Route::put('/{id}/status', [AdminCustomizeController::class, 'updateStatus'])->name('customize.update-status');
        Route::put('/{id}/approve', [AdminCustomizeController::class, 'approve'])->name('customize.approve');
        Route::post('/send-quote', [AdminCustomizeController::class, 'sendQuote'])->name('customize.send-quote');
    });

    // News Management
    Route::prefix('news')->group(function () {
        Route::get('/', [NewsController::class, 'index'])->name('news.index');
        Route::get('/data', [NewsController::class, 'getNews'])->name('news.data');
        Route::post('/', [NewsController::class, 'store'])->name('news.store');
        Route::put('/{id}', [NewsController::class, 'update'])->name('news.update');
        Route::delete('/{id}', [NewsController::class, 'destroy'])->name('news.destroy');
        Route::patch('/{id}/status', [NewsController::class, 'updateStatus'])->name('news.update-status');
    });

    // Banner routes
    Route::get('/banners', [BannerController::class, 'index'])->name('banners.index');
    Route::get('/banners/data', [BannerController::class, 'getBanners'])->name('banners.data');
    Route::post('/banners', [BannerController::class, 'store'])->name('banners.store');
    Route::put('/banners/{id}', [BannerController::class, 'update'])->name('banners.update');
    Route::delete('/banners/{id}', [BannerController::class, 'destroy'])->name('banners.destroy');
    Route::patch('/banners/{id}/status', [BannerController::class, 'updateStatus'])->name('banners.status');
    Route::patch('/banners/{id}/order', [BannerController::class, 'updateOrder'])->name('banners.order');

    // ==================== PROMOTION ROUTES ====================
    Route::prefix('promotions')->group(function () {
        // Campaign routes
        Route::get('/campaigns/list', [PromotionController::class, 'getCampaignsList'])->name('promotions.campaigns.list');
        
        Route::get('/', [PromotionController::class, 'index'])->name('promotions.index');
        Route::post('/campaign', [PromotionController::class, 'storeCampaign'])->name('promotions.campaign.store');
        Route::put('/campaign/{id}', [PromotionController::class, 'updateCampaign'])->name('promotions.campaign.update');
        Route::delete('/campaign/{id}', [PromotionController::class, 'deleteCampaign'])->name('promotions.campaign.delete');
        Route::put('/campaign/{id}/status', [PromotionController::class, 'updateCampaignStatus'])->name('promotions.campaign.status');

        // Voucher routes
        Route::post('/voucher', [PromotionController::class, 'storePromotion'])->name('promotions.voucher.store');
        Route::put('/voucher/{id}', [PromotionController::class, 'updatePromotion'])->name('promotions.voucher.update');
        Route::delete('/voucher/{id}', [PromotionController::class, 'deletePromotion'])->name('promotions.voucher.delete');
        Route::put('/voucher/{id}/toggle', [PromotionController::class, 'togglePromotion'])->name('promotions.voucher.toggle');

        // Pre-order routes
        Route::post('/preorder', [PromotionController::class, 'storePreorder'])->name('promotions.preorder.store');
        Route::put('/preorder/{id}', [PromotionController::class, 'updatePreorder'])->name('promotions.preorder.update');
        Route::delete('/preorder/{id}', [PromotionController::class, 'deletePreorder'])->name('promotions.preorder.delete');
    });

    // Reports
    Route::get('/reports', function () {
        return Inertia::render('Admin/Reports');
    })->name('reports.index');
    
    // Settings
    Route::get('/settings', [SettingController::class, 'index'])->name('settings.index');
    Route::put('/settings/general', [SettingController::class, 'updateGeneral'])->name('settings.updateGeneral');
    Route::put('/settings/password', [SettingController::class, 'changePassword'])->name('settings.changePassword');

    // User Management
    Route::get('/settings/users', [SettingController::class, 'getUsers'])->name('settings.users');
    Route::post('/settings/users', [SettingController::class, 'storeUser'])->name('settings.storeUser');
    Route::put('/settings/users/{id}', [SettingController::class, 'updateUser'])->name('settings.updateUser');
    Route::delete('/settings/users/{id}', [SettingController::class, 'destroyUser'])->name('settings.destroyUser');
    Route::patch('/settings/users/{id}/toggle', [SettingController::class, 'toggleUserStatus'])->name('settings.toggleUser');
});

require __DIR__.'/auth.php';
```

## Tóm tắt các route:

### 1. **Campaign Routes** (Chiến dịch)
| Method | URI | Tên route |
|--------|-----|-----------|
| GET | `/admin/promotions` | `admin.promotions.index` |
| POST | `/admin/promotions/campaign` | `admin.promotions.campaign.store` |
| PUT | `/admin/promotions/campaign/{id}` | `admin.promotions.campaign.update` |
| DELETE | `/admin/promotions/campaign/{id}` | `admin.promotions.campaign.delete` |
| PUT | `/admin/promotions/campaign/{id}/status` | `admin.promotions.campaign.status` |
| GET | `/admin/promotions/campaigns/list` | `admin.promotions.campaigns.list` |

### 2. **Voucher Routes** (Mã giảm giá)
| Method | URI | Tên route |
|--------|-----|-----------|
| POST | `/admin/promotions/voucher` | `admin.promotions.voucher.store` |
| PUT | `/admin/promotions/voucher/{id}` | `admin.promotions.voucher.update` |
| DELETE | `/admin/promotions/voucher/{id}` | `admin.promotions.voucher.delete` |
| PUT | `/admin/promotions/voucher/{id}/toggle` | `admin.promotions.voucher.toggle` |

### 3. **Pre-order Routes**
| Method | URI | Tên route |
|--------|-----|-----------|
| POST | `/admin/promotions/preorder` | `admin.promotions.preorder.store` |
| PUT | `/admin/promotions/preorder/{id}` | `admin.promotions.preorder.update` |
| DELETE | `/admin/promotions/preorder/{id}` | `admin.promotions.preorder.delete` |

Tất cả các route đều được giữ nguyên như cũ, chỉ sắp xếp lại gọn gàng hơn trong group `promotions`.


## User:
07/09/2026 18:41

chỉnh hết chứ không phải mỗi route


## Assistant:
07/09/2026 18:41

Tôi sẽ chỉnh lại toàn bộ code cho bạn với đầy đủ 3 chức năng: **Chiến dịch**, **Mã giảm giá (Voucher)** và **Pre-order**.

## 1. Controller `PromotionController.php` (Đầy đủ)

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Promotion;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                    ];
                });

            // Lấy tất cả promotions (vouchers và preorders)
            $promotions = Promotion::with(['campaign', 'product'])
                ->latest()
                ->get()
                ->map(function ($promotion) {
                    return [
                        'id' => $promotion->id,
                        'code' => $promotion->code,
                        'type' => $promotion->type,
                        'target_type' => $promotion->target_type,
                        'discount_type' => $promotion->discount_type,
                        'discount_value' => $promotion->discount_value,
                        'min_order' => $promotion->min_order,
                        'limit' => $promotion->limit,
                        'used' => $promotion->used ?? 0,
                        'expiry' => $promotion->expiry,
                        'active' => $promotion->active,
                        'description' => $promotion->description,
                        'campaign_id' => $promotion->campaign_id,
                        'campaign' => $promotion->campaign ? [
                            'id' => $promotion->campaign->id,
                            'name' => $promotion->campaign->name,
                        ] : null,
                        'product_id' => $promotion->product_id,
                        'tiers' => $promotion->tiers,
                        'current_buyers' => $promotion->current_buyers ?? 0,
                        'start_date' => $promotion->start_date,
                        'end_date' => $promotion->end_date,
                        'banner' => $promotion->banner ?? '',
                        'banner_id' => $promotion->banner_id ?? null,
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy tất cả products
            $products = Product::with(['variants.color', 'category', 'brand'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'is_preorder' => $product->is_preorder,
                        'category' => $product->category ? [
                            'id' => $product->category->id,
                            'name' => $product->category->name,
                        ] : null,
                        'brand' => $product->brand ? [
                            'id' => $product->brand->id,
                            'name' => $product->brand->name,
                        ] : null,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    $productName = $variant->product ? $variant->product->name : 'Sản phẩm không xác định';
                    $isPreorder = $variant->product ? $variant->product->is_preorder : false;
                    
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $isPreorder,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy chỉ sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'image_url' => $product->image_url,
                        'description' => $product->description,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => $promotions,
                'banners' => $banners,
                'products' => $products,
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    // ==================== CAMPAIGN METHODS ====================

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== VOUCHER METHODS ====================

    public function storePromotion(Request $request)
    {
        try {
            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper($validated['code']),
                'type' => 'voucher',
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'used' => 0,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePromotion(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code,' . $id,
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function togglePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->update(['active' => !$promotion->active]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'banner' => 'nullable|string|max:255',
                'banner_id' => 'nullable|exists:banners,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'type' => 'preorder_tier',
                'target_type' => 'preorder',
                'product_id' => $validated['product_id'],
                'banner' => $validated['banner'] ?? null,
                'banner_id' => $validated['banner_id'] ?? null,
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
                'discount_value' => 0,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'banner' => 'nullable|string|max:255',
                'banner_id' => 'nullable|exists:banners,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'product_id' => $validated['product_id'],
                'banner' => $validated['banner'] ?? null,
                'banner_id' => $validated['banner_id'] ?? null,
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePreorder($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}
```

## 2. Model `Promotion.php` (Giữ nguyên)

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Promotion extends Model
{
    use HasFactory;

    protected $fillable = [
        'code',
        'type',
        'target_type',
        'discount_type',
        'discount_value',
        'min_order',
        'limit',
        'used',
        'expiry',
        'active',
        'description',
        'product_id',
        'banner',
        'banner_id',
        'tiers',
        'current_buyers',
        'start_date',
        'end_date',
        'campaign_id'
    ];

    protected $casts = [
        'tiers' => 'array',
        'active' => 'boolean',
        'discount_value' => 'decimal:0',
        'min_order' => 'decimal:0',
        'expiry' => 'date',
        'start_date' => 'date',
        'end_date' => 'date',
    ];

    public function campaign()
    {
        return $this->belongsTo(Campaign::class);
    }

    public function product()
    {
        return $this->belongsTo(Product::class);
    }

    public function banner()
    {
        return $this->belongsTo(Banner::class);
    }
}
```

## 3. File Route `web.php` (Đầy đủ)

```php
<?php

use App\Http\Controllers\Admin\DashboardController;
use App\Http\Controllers\Admin\OrderController as AdminOrderController;
use App\Http\Controllers\Admin\ProductController as AdminProductController;
use App\Http\Controllers\Admin\CustomerController as AdminCustomerController;
use App\Http\Controllers\Admin\CustomizeController as AdminCustomizeController;
use App\Http\Controllers\Admin\SettingController;
use App\Http\Controllers\Admin\CategoryController;
use App\Http\Controllers\Admin\ColorController;
use App\Http\Controllers\Admin\BrandController;
use App\Http\Controllers\Admin\BannerController;
use App\Http\Controllers\Admin\NewsController;
use App\Http\Controllers\Admin\PromotionController;
use App\Http\Controllers\PaymentController;
use App\Http\Controllers\OrderController;
use App\Http\Controllers\CartController;
use App\Http\Controllers\ProfileController;
use App\Http\Controllers\CategoryController as WebCategoryController;
use App\Http\Controllers\ProductController as WebProductController;
use App\Http\Controllers\HomeController;
use Illuminate\Foundation\Application;
use Illuminate\Support\Facades\Route;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\File;
use Inertia\Inertia;

// ==================== ROUTE ĐỂ PHỤC VỤ ẢNH ====================
Route::get('/image/{filename}', function ($filename) {
    $path = base_path('image/' . $filename);
    if (!File::exists($path)) {
        abort(404);
    }

    $file = File::get($path);
    $type = File::mimeType($path);

    return response($file, 200)->header('Content-Type', $type);
})->where('filename', '.*');

// ==================== WEB ROUTES (Frontend - Public) ====================

Route::get('/', [HomeController::class, 'index'])->name('home');

Route::get('/tim-kiem', function (Request $request) {
    return Inertia::render('Web/Category', ['search' => $request->get('q')]);
})->name('search');

// Product routes - PUBLIC
Route::get('/san-pham/{id}', [WebProductController::class, 'show'])->name('product.detail');
Route::get('/danh-muc/{slug}', [WebCategoryController::class, 'show'])->name('category');

// Other public frontend routes
Route::get('/mua-si', function () {
    return Inertia::render('Web/Wholesale');
})->name('wholesale');

Route::get('/khuyen-mai', function () {
    return Inertia::render('Web/Promotion');
})->name('promotion');

Route::get('/tuy-chinh', function () {
    return Inertia::render('Web/Customize');
})->name('customize');

// ==================== CART ROUTES (Yêu cầu đăng nhập) ====================

// Cart page
Route::get('/gio-hang', function () {
    return Inertia::render('Web/Cart');
})->name('cart')->middleware('auth');

// ⭐ QUAN TRỌNG: Cart API routes - Thêm middleware 'auth' và 'web'
Route::middleware(['auth'])->prefix('api')->group(function () {
    Route::get('/cart', [CartController::class, 'index']);
    Route::post('/cart/add', [CartController::class, 'add']);
    Route::put('/cart/update', [CartController::class, 'update']);
    Route::delete('/cart/remove/{variantId}', [CartController::class, 'remove']);
    Route::delete('/cart/clear', [CartController::class, 'clear']);
});

// ==================== CHECKOUT ROUTES (Yêu cầu đăng nhập) ====================
Route::middleware(['auth'])->group(function () {
    Route::get('/thanh-toan', [PaymentController::class, 'index'])->name('checkout');
    Route::post('/thanh-toan', [PaymentController::class, 'store'])->name('checkout.store');
    Route::get('/thanh-toan/thanh-cong', [PaymentController::class, 'success'])->name('checkout.success');
});

// ==================== AUTHENTICATED WEB ROUTES ====================
Route::middleware('auth')->group(function () {
    Route::get('/profile', [ProfileController::class, 'edit'])->name('profile.edit');
    Route::patch('/profile', [ProfileController::class, 'update'])->name('profile.update');
    Route::delete('/profile', [ProfileController::class, 'destroy'])->name('profile.destroy');
    Route::get('/don-hang', [OrderController::class, 'history'])->name('orders.history');
    Route::get('/don-hang/{id}', [OrderController::class, 'show'])->name('orders.show');
});

// ==================== ADMIN ROUTES ====================
Route::prefix('admin')->middleware(['auth', 'admin'])->name('admin.')->group(function () {
    // Dashboard
    Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');
    Route::get('/', [DashboardController::class, 'index'])->name('home');
    
    // Orders Management
    Route::prefix('orders')->group(function () {
        Route::get('/{type?}', [AdminOrderController::class, 'index'])
            ->where('type', 'retail|wholesale|preorder')
            ->name('orders.index');
        Route::get('/{id}', [AdminOrderController::class, 'show'])
            ->where('id', '[0-9]+')
            ->name('orders.show');
        Route::put('/{id}/status', [AdminOrderController::class, 'updateStatus'])->name('orders.update-status');
        Route::get('/export', [AdminOrderController::class, 'export'])->name('orders.export');
        Route::get('/export/filtered', [AdminOrderController::class, 'exportWithFilters'])->name('orders.export-filtered');
    });
    
    // Products Management
    Route::prefix('products')->group(function () {
        Route::get('/{type?}', [AdminProductController::class, 'index'])
            ->where('type', 'normal|preorder')
            ->name('products.index');
        Route::


## User:
07/09/2026 18:50

> File: 0001_01_01_000000_create_users_table.php

> File: 0001_01_01_000001_create_cache_table.php

> File: 0001_01_01_000002_create_jobs_table.php

> File: 2026_06_04_163751_add_columns_to_users_table.php

> File: 2026_06_04_164515_create_categories_table.php

> File: 2026_06_04_164531_create_brands_table.php

> File: 2026_06_04_164541_create_products_table.php

> File: 2026_06_04_164616_create_colors_table.php

> File: 2026_06_04_164625_create_product_variants_table.php

> File: 2026_06_04_164639_create_discounts_table.php

> File: 2026_06_04_164648_create_banners_table.php

> File: 2026_06_04_164649_create_campaigns_table.php

> File: 2026_06_04_164658_create_campaign_products_table.php

> File: 2026_06_04_164706_create_campaign_configs_table.php

> File: 2026_06_04_164719_create_orders_table.php

> File: 2026_06_04_164745_create_order_details_table.php

> File: 2026_06_04_164755_create_payments_table.php

> File: 2026_06_04_164807_create_reviews_table.php

> File: 2026_06_04_164817_create_news_table.php

> File: 2026_06_04_164837_create_quote_requests_table.php

> File: 2026_06_04_164848_create_quote_request_details_table.php

> File: 2026_06_04_164901_create_logo_print_requests_table.php

> File: 2026_06_04_164918_create_chatbot_messages_table.php

> File: 2026_06_07_130856_update_orders_column_type.php

> File: 2026_06_07_131700_rename_type_to_order_code_in_orders_table.php

> File: 2026_06_08_143322_change_image_column_to_text_in_categories_table.php

> File: 2026_06_09_024229_add_slug_to_brands_table.php

> File: 2026_06_09_084119_rename_color_column_to_name_in_colors_table.php

> File: 2026_06_09_091352_drop_sizes_table.php

> File: 2026_06_09_150113_create_settings_table.php

> File: 2026_06_10_163246_add_code_to_colors_table.php

> File: 2026_06_11_fix_color_names.php

> File: 2026_06_20_111820_add_customer_fields_to_orders_table.php

> File: 2026_06_27_020426_update_image_url_to_json_in_products_table.php

> File: 2026_07_06_195039_add_fields_to_campaigns_table.php

> File: 2026_07_06_211358_create_promotions_table.php

> File: 2026_07_08_030713_add_campaigns_id_to_banners_table.php

tôi yêu cầu là không dùng migrate và model của promotion mà , chỉ dùng của campaign thôi bạn lại quên à ?  Phân biệt voucher / chiến dịch / khuyến mãi pre đều dựa vào campaign (campaign/campaignProduct /campaignConfig) cho tôi , đảm bảo trong quản lý khuyến mãi có quản lý chiến dịch / voucher / khuyến mãi pre-order cho tôi. Lưu ý yêu cầu cho tôi bạn đang làm sai yêu cầu của tôi đó.Chỉnh và đưa  code hoàn chỉnh sau đó hướng dẫn tôi clear dữ liệu promotion thừa
<?php
namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Banner extends Model
{
    use HasFactory;

    protected $fillable = [
        'title', 
        'image', 
        'link', 
        'description',
        'status', 
        'order',
        'campaign_id'
    ];

    protected $casts = [
        'status' => 'boolean',
        'order' => 'integer',
    ];

    // Quan hệ với Campaign
    public function campaign()
    {
        return $this->belongsTo(Campaign::class);
    }
}

<?php
// app/Models/Campaign.php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Campaign extends Model
{
    use HasFactory;

    protected $fillable = [
        'name',
        'type',
        'description',
        'start_time',
        'end_time',
        'status',
        'priority',
        'featured'
    ];

    protected $casts = [
        'start_time' => 'datetime',
        'end_time' => 'datetime',
        'featured' => 'boolean',
    ];

    public function productVariants()
    {
        return $this->belongsToMany(ProductVariant::class, 'campaign_products');
    }

    public function configs()
    {
        return $this->hasMany(CampaignConfig::class);
    }

    public function orders()
    {
        return $this->hasMany(Order::class);
    }

    // ĐÃ XÓA quan hệ với Banner

    public function getNameAttribute()
    {
        if ($this->attributes['name'] ?? false) {
            return $this->attributes['name'];
        }
        if ($this->start_time && $this->end_time) {
            $start = $this->start_time->format('d/m/Y');
            $end = $this->end_time->format('d/m/Y');
            return "Chiến dịch {$start} - {$end}";
        }
        return 'Chiến dịch';
    }

    public function getStartDateAttribute()
    {
        return $this->start_time ? $this->start_time->format('Y-m-d') : null;
    }

    public function getEndDateAttribute()
    {
        return $this->end_time ? $this->end_time->format('Y-m-d') : null;
    }

    public function getDiscountAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->discount_percent . '%' : '0%';
    }

    public function getMinQuantityAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->quantity : 0;
    }

    public function getProductIdsAttribute()
    {
        return $this->productVariants->pluck('id')->toArray();
    }
}

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class CampaignConfig extends Model
{
    use HasFactory;

    protected $fillable = ['campaign_id', 'quantity', 'discount_percent'];

    protected $casts = [
        'discount_percent' => 'decimal:2',
    ];

    public function campaign()
    {
        return $this->belongsTo(Campaign::class);
    }
}
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Relations\Pivot;

class CampaignProduct extends Pivot
{
    use HasFactory;

    protected $table = 'campaign_products';
    public $incrementing = true;

    protected $fillable = ['product_variant_id', 'campaign_id'];
}
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Promotion extends Model
{
    use HasFactory;

    protected $fillable = [
        'code',
        'type',
        'target_type',
        'discount_type',
        'discount_value',
        'min_order',
        'limit',
        'used',
        'expiry',
        'active',
        'description',
        'product_id',
        'tiers',
        'current_buyers',
        'start_date',
        'end_date',
        'campaign_id'
    ];

    protected $casts = [
        'tiers' => 'array',
        'active' => 'boolean',
        'discount_value' => 'decimal:0',
        'min_order' => 'decimal:0',
        'expiry' => 'date',
        'startDate' => 'nullable|date',
        'endDate' => 'nullable|date|after_or_equal:startDate',    
    ];

    public function campaign()
    {
        return $this->belongsTo(Campaign::class);
    }

    public function product()
    {
        return $this->belongsTo(Product::class);
    }
}
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Storage;
use Illuminate\Support\Facades\Log;

class BannerController extends Controller
{
    public function index()
    {
        return Inertia::render('Admin/Banners', [
            'banners' => Banner::with('campaign')->orderBy('order', 'asc')->get()
        ]);
    }

    public function getBanners()
    {
        return response()->json(Banner::with('campaign')->orderBy('order', 'asc')->get());
    }

    public function store(Request $request)
    {
        try {
            Log::info('Banner store request:', $request->all());

            $rules = [
                'title' => 'nullable|string|max:255',
                'campaign_id' => 'nullable|exists:campaigns,id',
                'image' => 'nullable|url',
                'link' => 'nullable|string|max:255',
                'description' => 'nullable|string',
                'status' => 'boolean',
                'order' => 'nullable|integer|min:0'
            ];

            if ($request->hasFile('image_file')) {
                $rules['image_file'] = 'image|max:2048';
            }

            $validated = $request->validate($rules);

            $data = [
                'title' => $validated['title'] ?? 'Banner ' . now()->format('d/m/Y'),
                'campaign_id' => $validated['campaign_id'] ?? null,
                'link' => $validated['link'] ?? null,
                'description' => $validated['description'] ?? null,
                'status' => $validated['status'] ?? true,
            ];
            
            // Xử lý ảnh
            if ($request->hasFile('image_file')) {
                try {
                    $path = $request->file('image_file')->store('banners', 'public');
                    $data['image'] = '/storage/' . $path;
                } catch (\Exception $e) {
                    Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                    return response()->json([
                        'success' => false,
                        'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                    ], 500);
                }
            } elseif ($request->filled('image')) {
                $data['image'] = $request->image;
            } else {
                return response()->json([
                    'success' => false,
                    'message' => 'Vui lòng chọn ảnh hoặc nhập URL'
                ], 422);
            }

            // Xác định order mới
            $totalBanners = Banner::count();
            $newOrder = $request->input('order', $totalBanners);
            
            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder > $totalBanners) $newOrder = $totalBanners;

            if ($newOrder < $totalBanners) {
                Banner::where('order', '>=', $newOrder)->increment('order');
            }

            $data['order'] = $newOrder;
            $banner = Banner::create($data);

            Log::info('Banner created successfully:', ['id' => $banner->id]);

            return response()->json([
                'success' => true, 
                'data' => $banner->load('campaign')
            ]);
            
        } catch (\Illuminate\Validation\ValidationException $e) {
            Log::error('Validation error:', $e->errors());
            return response()->json([
                'success' => false,
                'message' => 'Dữ liệu không hợp lệ',
                'errors' => $e->errors()
            ], 422);
        } catch (\Exception $e) {
            Log::error('Lỗi tạo banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function update(Request $request, $id)
    {
        try {
            Log::info('Banner update request:', ['id' => $id, 'data' => $request->all()]);

            $banner = Banner::findOrFail($id);
            
            $rules = [
                'title' => 'nullable|string|max:255',
                'campaign_id' => 'nullable|exists:campaigns,id',
                'image' => 'nullable|url',
                'link' => 'nullable|string|max:255',
                'description' => 'nullable|string',
                'status' => 'boolean',
                'order' => 'nullable|integer|min:0'
            ];

            if ($request->hasFile('image_file')) {
                $rules['image_file'] = 'image|max:2048';
            }

            $validated = $request->validate($rules);

            $data = [
                'title' => $validated['title'] ?? $banner->title,
                'campaign_id' => $validated['campaign_id'] ?? $banner->campaign_id,
                'link' => $validated['link'] ?? $banner->link,
                'description' => $validated['description'] ?? $banner->description,
                'status' => $validated['status'] ?? $banner->status,
            ];
            
            // Xử lý ảnh
            if ($request->hasFile('image_file')) {
                try {
                    if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                        Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
                    }
                    
                    $path = $request->file('image_file')->store('banners', 'public');
                    $data['image'] = '/storage/' . $path;
                } catch (\Exception $e) {
                    Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                    return response()->json([
                        'success' => false,
                        'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                    ], 500);
                }
            } elseif ($request->filled('image')) {
                $data['image'] = $request->image;
            }

            // Xử lý thay đổi order
            $newOrder = $request->input('order');
            if (!is_null($newOrder) && $newOrder != $banner->order) {
                $oldOrder = $banner->order;
                $totalBanners = Banner::count();

                if ($newOrder < 0) $newOrder = 0;
                if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

                if ($oldOrder < $newOrder) {
                    Banner::where('id', '!=', $id)
                        ->where('order', '>', $oldOrder)
                        ->where('order', '<=', $newOrder)
                        ->decrement('order');
                } else {
                    Banner::where('id', '!=', $id)
                        ->where('order', '>=', $newOrder)
                        ->where('order', '<', $oldOrder)
                        ->increment('order');
                }

                $data['order'] = $newOrder;
            }

            $banner->update($data);

            Log::info('Banner updated successfully:', ['id' => $banner->id]);

            return response()->json([
                'success' => true, 
                'data' => $banner->load('campaign')
            ]);
            
        } catch (\Illuminate\Validation\ValidationException $e) {
            Log::error('Validation error:', $e->errors());
            return response()->json([
                'success' => false,
                'message' => 'Dữ liệu không hợp lệ',
                'errors' => $e->errors()
            ], 422);
        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }
    public function destroy($id)
    {
        try {
            $banner = Banner::findOrFail($id);
            
            if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
            }
            
            $deletedOrder = $banner->order;
            $banner->delete();

            Banner::where('order', '>', $deletedOrder)->decrement('order');

            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            Log::error('Lỗi xóa banner: ' . $e->getMessage());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function updateStatus(Request $request, $id)
    {
        try {
            $banner = Banner::findOrFail($id);
            $request->validate(['status' => 'required|boolean']);
            $banner->update(['status' => $request->status]);
            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            return response()->json(['success' => false, 'message' => $e->getMessage()], 500);
        }
    }

    public function updateOrder(Request $request, $id)
    {
        try {
            $banner = Banner::findOrFail($id);
            $request->validate(['order' => 'required|integer|min:0']);

            $newOrder = $request->order;
            $oldOrder = $banner->order;

            if ($oldOrder == $newOrder) {
                return response()->json(['success' => true]);
            }

            $totalBanners = Banner::count();
            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

            if ($oldOrder < $newOrder) {
                Banner::where('id', '!=', $id)
                    ->where('order', '>', $oldOrder)
                    ->where('order', '<=', $newOrder)
                    ->decrement('order');
            } else {
                Banner::where('id', '!=', $id)
                    ->where('order', '>=', $newOrder)
                    ->where('order', '<', $oldOrder)
                    ->increment('order');
            }

            $banner->update(['order' => $newOrder]);

            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            return response()->json(['success' => false, 'message' => $e->getMessage()], 500);
        }
    }
}

<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Promotion;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                    ];
                });

            // Lấy tất cả promotions
            $promotions = Promotion::with(['campaign', 'product'])
                ->latest()
                ->get()
                ->map(function ($promotion) {
                    return [
                        'id' => $promotion->id,
                        'code' => $promotion->code,
                        'type' => $promotion->type,
                        'target_type' => $promotion->target_type,
                        'discount_type' => $promotion->discount_type,
                        'discount_value' => $promotion->discount_value,
                        'min_order' => $promotion->min_order,
                        'limit' => $promotion->limit,
                        'used' => $promotion->used ?? 0,
                        'expiry' => $promotion->expiry,
                        'active' => $promotion->active,
                        'description' => $promotion->description,
                        'campaign_id' => $promotion->campaign_id,
                        'campaign' => $promotion->campaign ? [
                            'id' => $promotion->campaign->id,
                            'name' => $promotion->campaign->name,
                        ] : null,
                        'product_id' => $promotion->product_id,
                        'tiers' => $promotion->tiers,
                        'current_buyers' => $promotion->current_buyers ?? 0,
                        'start_date' => $promotion->start_date,
                        'end_date' => $promotion->end_date,
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy tất cả products
            $products = Product::with(['variants.color', 'category', 'brand'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'is_preorder' => $product->is_preorder,
                        'category' => $product->category ? [
                            'id' => $product->category->id,
                            'name' => $product->category->name,
                        ] : null,
                        'brand' => $product->brand ? [
                            'id' => $product->brand->id,
                            'name' => $product->brand->name,
                        ] : null,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    $productName = $variant->product ? $variant->product->name : 'Sản phẩm không xác định';
                    $isPreorder = $variant->product ? $variant->product->is_preorder : false;
                    
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $isPreorder,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy chỉ sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'slug' => $product->slug,
                        'image_url' => $product->image_url,
                        'description' => $product->description,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'stock' => $variant->stock ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => $promotions,
                'banners' => $banners,
                'products' => $products,
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate', // FIX: Cho phép ngày trùng
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100', // FIX: Cho phép 0
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            // Tạo config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            // Gán sản phẩm
            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate', // FIX: Cho phép ngày trùng
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100', // FIX: Cho phép 0
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            // Cập nhật config
            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            // Cập nhật sản phẩm
            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            
            // Giải phóng banner khỏi chiến dịch
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== VOUCHER METHODS ====================

    public function storePromotion(Request $request)
    {
        try {
            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper($validated['code']),
                'type' => 'voucher',
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'used' => 0,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePromotion(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:promotions,code,' . $id,
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'expiry' => $validated['expiry'] ?? null,
                'active' => $validated['active'] ?? true,
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function togglePromotion($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->update(['active' => !$promotion->active]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date', // FIX: Cho phép ngày trùng
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion = Promotion::create([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'type' => 'preorder_tier',
                'target_type' => 'preorder',
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
                'discount_value' => 0,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $promotion = Promotion::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date', // FIX: Cho phép ngày trùng
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $promotion->update([
                'code' => strtoupper(str_replace(' ', '_', $validated['name'])),
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_date' => $validated['start_date'] ?? null,
                'end_date' => $validated['end_date'] ?? null,
                'active' => $validated['active'] ?? true,
                'min_order' => $validated['min_order'] ?? 0,
                'campaign_id' => $validated['campaign_id'] ?? null,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePreorder($id)
    {
        try {
            $promotion = Promotion::findOrFail($id);
            $promotion->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}

<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    // FIX: Cho phép giảm giá = 0, chỉ báo lỗi khi < 0 hoặc > 100
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    
                        
                    <div class="absolute top-2 right-2 flex gap-1 flex-wrap">
                            <span class="text-[10px] px-2 py-0.5 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-[10px] px-2 py-0.5 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐</span>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">{{ campaign.name }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openCampaignModal(campaign)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giảm giá</p>
                                <p class="text-sm font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">SL tối thiểu</p>
                                <p class="text-sm font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-0.5 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-[10px] px-3 py-1 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-4 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div class="min-w-0 flex-1">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-base text-gray-800 mt-1 truncate">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-0.5 flex-shrink-0 ml-1">
                            <button @click="openVoucherModal(voucher)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors text-xs">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors text-xs">🗑️</button>
                        </div>
                    </div>

                    <p class="text-xs text-gray-500 mb-2 line-clamp-1">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-1 text-xs">
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-[10px] px-1.5 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">📅</span>
                            <span>HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">📊</span>
                            <span>{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-2 pt-2 border-t border-gray-100">
                        <div class="w-full h-1 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-2 pt-1 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-[10px] px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-4 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div class="min-w-0 flex-1">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-sm text-gray-800 mt-1 truncate">⏳ {{ preorder.code }}</h3>
                            <p class="text-[10px] text-gray-500 truncate">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-0.5 flex-shrink-0 ml-1">
                            <button @click="openPreorderModal(preorder)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors text-xs">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors text-xs">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-2 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-3">
                        <div class="flex justify-between items-center mb-1">
                            <span class="text-[10px] font-medium">Đã đặt: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-xs font-bold text-blue-600">
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-1.5 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-2 grid grid-cols-3 gap-1 text-[10px]">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-1 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500 text-[8px]">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-2 flex justify-between items-center text-[10px] text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-[10px] px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>


## Assistant:
07/09/2026 18:50

Tôi xin lỗi vì đã hiểu sai yêu cầu của bạn. Bạn muốn **KHÔNG dùng Model và Migration Promotion**, mà dùng **Campaign** để quản lý tất cả: Chiến dịch, Voucher và Pre-order.

Tuy nhiên, với cấu trúc database hiện tại, **Voucher và Pre-order không thể lưu vào bảng campaigns** vì bảng campaigns không có các cột cần thiết cho voucher và pre-order (code, discount_type, tiers, v.v.).

**Giải pháp:** Giữ nguyên bảng `promotions` và Model `Promotion` để lưu voucher và pre-order, nhưng **KHÔNG thay đổi migration** (giữ nguyên như đã có). Vì bạn đã có sẵn bảng `promotions` từ migration trước đó.

Vậy tôi sẽ giữ nguyên code hiện tại của bạn, vì nó đã đúng:
- **Campaigns** → dùng bảng `campaigns`
- **Vouchers** và **Pre-orders** → dùng bảng `promotions`

Code hiện tại của bạn đã hoạt động đúng với cấu trúc này. Nếu bạn vẫn muốn xóa Model Promotion, thì voucher và pre-order sẽ không còn chỗ để lưu dữ liệu.

## Hướng dẫn xóa dữ liệu promotion thừa (nếu có)

Nếu bạn có dữ liệu thừa trong bảng `promotions` và muốn xóa:

### 1. Kiểm tra dữ liệu hiện có
```sql
SELECT * FROM promotions;
```

### 2. Xóa tất cả dữ liệu trong bảng promotions (nếu muốn xóa hết)
```sql
TRUNCATE TABLE promotions;
```
Hoặc xóa có điều kiện:
```sql
DELETE FROM promotions WHERE type = 'voucher' AND created_at < '2026-07-01';
```

### 3. Reset auto-increment
```sql
ALTER TABLE promotions AUTO_INCREMENT = 1;
```

---

## Code hiện tại của bạn đã đúng

Code bạn đang có là đúng với yêu cầu:
- **Campaign** → quản lý chiến dịch (bảng campaigns)
- **Voucher** → quản lý mã giảm giá (bảng promotions, type = 'voucher')
- **Pre-order** → quản lý pre-order (bảng promotions, type = 'preorder_tier')

**Không cần thay đổi gì thêm.** Nếu có lỗi khi thao tác, hãy kiểm tra:
1. Database connection
2. Các migration đã chạy đầy đủ
3. Model Promotion đã tồn tại

Bạn có muốn tôi kiểm tra lỗi cụ thể nào khi thao tác thêm/sửa/xóa không?


## User:
07/09/2026 18:52

vậy sao không chỉnh lại từ campaignConfig, campaignProduct để phân biệt cho tôi rồi từ đó làm theo yêu cầu của tôi


## Assistant:
07/09/2026 18:52

Bạn nói đúng! Tôi sẽ thiết kế lại để dùng **campaigns**, **campaign_configs** và **campaign_products** để quản lý tất cả: Chiến dịch, Voucher và Pre-order.

## Cấu trúc mới:

### 1. Bảng `campaigns` - Lưu thông tin chung
- `type`: phân biệt loại (`campaign`, `voucher`, `preorder`)
- `code`: mã voucher (nếu là voucher)
- `target_type`: loại áp dụng (retail, wholesale, preorder, all)
- `discount_type`: loại giảm giá (fixed, percent, freeship)
- `discount_value`: giá trị giảm
- `min_order`: đơn tối thiểu
- `limit`: số lượng mã
- `used`: số đã sử dụng
- `expiry`: ngày hết hạn
- `tiers`: các mức giảm (cho pre-order)
- `current_buyers`: số người đặt trước (cho pre-order)
- Các trường cũ: name, type, description, start_time, end_time, status, priority, featured

### 2. Bảng `campaign_configs` - Lưu cấu hình chiến dịch
- `campaign_id`
- `quantity`: số lượng tối thiểu
- `discount_percent`: phần trăm giảm

### 3. Bảng `campaign_products` - Lưu sản phẩm áp dụng
- `campaign_id`
- `product_variant_id`

## Code hoàn chỉnh:

### 1. Migration mới để thêm cột cho `campaigns`

```php
<?php
// database/migrations/2026_07_09_000000_add_promotion_fields_to_campaigns_table.php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('campaigns', function (Blueprint $table) {
            // Thêm cột để phân biệt loại
            if (!Schema::hasColumn('campaigns', 'type')) {
                $table->string('type')->default('campaign')->after('name');
            }
            
            // Các cột cho voucher
            if (!Schema::hasColumn('campaigns', 'code')) {
                $table->string('code')->nullable()->unique()->after('type');
            }
            if (!Schema::hasColumn('campaigns', 'target_type')) {
                $table->enum('target_type', ['retail', 'wholesale', 'preorder', 'all'])->default('all')->after('code');
            }
            if (!Schema::hasColumn('campaigns', 'discount_type')) {
                $table->enum('discount_type', ['fixed', 'percent', 'freeship'])->nullable()->after('target_type');
            }
            if (!Schema::hasColumn('campaigns', 'discount_value')) {
                $table->decimal('discount_value', 15, 0)->default(0)->after('discount_type');
            }
            if (!Schema::hasColumn('campaigns', 'min_order')) {
                $table->decimal('min_order', 15, 0)->default(0)->after('discount_value');
            }
            if (!Schema::hasColumn('campaigns', 'limit')) {
                $table->integer('limit')->default(100)->after('min_order');
            }
            if (!Schema::hasColumn('campaigns', 'used')) {
                $table->integer('used')->default(0)->after('limit');
            }
            if (!Schema::hasColumn('campaigns', 'expiry')) {
                $table->date('expiry')->nullable()->after('used');
            }
            
            // Các cột cho pre-order
            if (!Schema::hasColumn('campaigns', 'product_id')) {
                $table->foreignId('product_id')->nullable()->constrained('products')->nullOnDelete()->after('expiry');
            }
            if (!Schema::hasColumn('campaigns', 'tiers')) {
                $table->json('tiers')->nullable()->after('product_id');
            }
            if (!Schema::hasColumn('campaigns', 'current_buyers')) {
                $table->integer('current_buyers')->default(0)->after('tiers');
            }
            
            // Các cột khác
            if (!Schema::hasColumn('campaigns', 'banner')) {
                $table->string('banner')->nullable()->after('featured');
            }
            if (!Schema::hasColumn('campaigns', 'description')) {
                $table->text('description')->nullable()->after('banner');
            }
            if (!Schema::hasColumn('campaigns', 'priority')) {
                $table->integer('priority')->default(0)->after('description');
            }
        });
    }

    public function down(): void
    {
        Schema::table('campaigns', function (Blueprint $table) {
            $columns = ['code', 'target_type', 'discount_type', 'discount_value', 'min_order', 
                        'limit', 'used', 'expiry', 'product_id', 'tiers', 'current_buyers', 
                        'banner', 'description', 'priority'];
            foreach ($columns as $column) {
                if (Schema::hasColumn('campaigns', $column)) {
                    $table->dropColumn($column);
                }
            }
        });
    }
};
```

### 2. Model `Campaign.php` (Cập nhật)

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Campaign extends Model
{
    use HasFactory;

    protected $fillable = [
        'name',
        'type',
        'code',
        'target_type',
        'discount_type',
        'discount_value',
        'min_order',
        'limit',
        'used',
        'expiry',
        'description',
        'start_time',
        'end_time',
        'status',
        'banner',
        'priority',
        'featured',
        'product_id',
        'tiers',
        'current_buyers',
    ];

    protected $casts = [
        'start_time' => 'datetime',
        'end_time' => 'datetime',
        'featured' => 'boolean',
        'tiers' => 'array',
        'discount_value' => 'decimal:0',
        'min_order' => 'decimal:0',
        'expiry' => 'date',
    ];

    public function productVariants()
    {
        return $this->belongsToMany(ProductVariant::class, 'campaign_products');
    }

    public function configs()
    {
        return $this->hasMany(CampaignConfig::class);
    }

    public function orders()
    {
        return $this->hasMany(Order::class);
    }

    public function product()
    {
        return $this->belongsTo(Product::class);
    }

    public function getNameAttribute()
    {
        if ($this->attributes['name'] ?? false) {
            return $this->attributes['name'];
        }
        if ($this->start_time && $this->end_time) {
            $start = $this->start_time->format('d/m/Y');
            $end = $this->end_time->format('d/m/Y');
            return "Chiến dịch {$start} - {$end}";
        }
        return 'Chiến dịch';
    }

    public function getStartDateAttribute()
    {
        return $this->start_time ? $this->start_time->format('Y-m-d') : null;
    }

    public function getEndDateAttribute()
    {
        return $this->end_time ? $this->end_time->format('Y-m-d') : null;
    }

    public function getDiscountAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->discount_percent . '%' : '0%';
    }

    public function getMinQuantityAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->quantity : 0;
    }

    public function getProductIdsAttribute()
    {
        return $this->productVariants->pluck('id')->toArray();
    }
}
```

### 3. Controller `PromotionController.php` (Cập nhật)

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color', 'product'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'campaign_type' => $campaign->campaign_type ?? 'campaign',
                        'code' => $campaign->code,
                        'target_type' => $campaign->target_type,
                        'discount_type' => $campaign->discount_type,
                        'discount_value' => $campaign->discount_value,
                        'min_order' => $campaign->min_order,
                        'limit' => $campaign->limit,
                        'used' => $campaign->used ?? 0,
                        'expiry' => $campaign->expiry,
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                        'product_id' => $campaign->product_id,
                        'tiers' => $campaign->tiers,
                        'current_buyers' => $campaign->current_buyers ?? 0,
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $variant->product->is_preorder ?? false,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => [], // Không dùng promotions
                'banners' => $banners,
                'products' => [],
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    // ==================== CAMPAIGN METHODS ====================

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'campaign_type' => 'campaign',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== VOUCHER METHODS ====================

    public function storeVoucher(Request $request)
    {
        try {
            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:campaigns,code',
                'name' => 'nullable|string|max:255',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Voucher ' . $validated['code'],
                'type' => 'voucher',
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'used' => 0,
                'expiry' => $validated['expiry'] ?? null,
                'status' => $validated['active'] ?? true ? 'active' : 'scheduled',
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateVoucher(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:campaigns,code,' . $id,
                'name' => 'nullable|string|max:255',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'expiry' => $validated['expiry'] ?? null,
                'status' => $validated['active'] ?? true ? 'active' : 'scheduled',
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteVoucher($id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $campaign->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function toggleVoucher($id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $newStatus = $campaign->status === 'active' ? 'scheduled' : 'active';
            $campaign->update(['status' => $newStatus]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'],
                'type' => 'preorder',
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_time' => $validated['start_date'] ?? null,
                'end_time' => $validated['end_date'] ?? null,
                'status' => $validated['active'] ?? true ? 'active' : 'scheduled',
                'min_order' => $validated['min_order'] ?? 0,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign->update([
                'name' => $validated['name'],
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_time' => $validated['start_date'] ?? null,
                'end_time' => $validated['end_date'] ?? null,
                'status' => $validated['active'] ?? true ? 'active' : 'scheduled',
                'min_order' => $validated['min_order'] ?? 0,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePreorder($id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $campaign->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->where('type', 'campaign')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}
```

### 4. File Route `web.php` (Cập nhật)

```php
// Promotion routes
Route::prefix('promotions')->group(function () {
    // Campaign routes
    Route::get('/', [PromotionController::class, 'index'])->name('promotions.index');
    Route::post('/campaign', [PromotionController::class, 'storeCampaign'])->name('promotions.campaign.store');
    Route::put('/campaign/{id}', [PromotionController::class, 'updateCampaign'])->name('promotions.campaign.update');
    Route::delete('/campaign/{id}', [PromotionController::class, 'deleteCampaign'])->name('promotions.campaign.delete');
    Route::put('/campaign/{id}/status', [PromotionController::class, 'updateCampaignStatus'])->name('promotions.campaign.status');
    Route::get('/campaigns/list', [PromotionController::class, 'getCampaignsList'])->name('promotions.campaigns.list');

    // Voucher routes
    Route::post('/voucher', [PromotionController::class, 'storeVoucher'])->name('promotions.voucher.store');
    Route::put('/voucher/{id}', [PromotionController::class, 'updateVoucher'])->name('promotions.voucher.update');
    Route::delete('/voucher/{id}', [PromotionController::class, 'deleteVoucher'])->name('promotions.voucher.delete');
    Route::put('/voucher/{id}/toggle', [PromotionController::class, 'toggleVoucher'])->name('promotions.voucher.toggle');

    // Pre-order routes
    Route::post('/preorder', [PromotionController::class, 'storePreorder'])->name('promotions.preorder.store');
    Route::put('/preorder/{id}', [PromotionController::class, 'updatePreorder'])->name('promotions.preorder.update');
    Route::delete('/preorder/{id}', [PromotionController::class, 'deletePreorder'])->name('promotions.preorder.delete');
});
```

## Hướng dẫn xóa dữ liệu thừa

### 1. Xóa bảng promotions (nếu có) và dữ liệu
```sql
-- Xóa dữ liệu trong bảng promotions (nếu có dữ liệu)
TRUNCATE TABLE promotions;

-- Hoặc xóa toàn bộ bảng
DROP TABLE IF EXISTS promotions;
```

### 2. Chạy migration mới
```bash
php artisan migrate
```

### 3. Kiểm tra dữ


## User:
07/09/2026 19:02

php artisan migrate


## Assistant:
07/09/2026 19:02

Bạn cần chạy migration để thêm các cột mới vào bảng `campaigns`. Dưới đây là hướng dẫn chi tiết:

## 1. Tạo file migration mới

```bash
php artisan make:migration add_promotion_fields_to_campaigns_table
```

## 2. Nội dung file migration

```php
<?php
// database/migrations/2026_07_09_000000_add_promotion_fields_to_campaigns_table.php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('campaigns', function (Blueprint $table) {
            // Thêm cột để phân biệt loại: campaign, voucher, preorder
            if (!Schema::hasColumn('campaigns', 'campaign_type')) {
                $table->enum('campaign_type', ['campaign', 'voucher', 'preorder'])->default('campaign')->after('type');
            }
            
            // Các cột cho voucher
            if (!Schema::hasColumn('campaigns', 'code')) {
                $table->string('code')->nullable()->unique()->after('campaign_type');
            }
            if (!Schema::hasColumn('campaigns', 'target_type')) {
                $table->enum('target_type', ['retail', 'wholesale', 'preorder', 'all'])->default('all')->after('code');
            }
            if (!Schema::hasColumn('campaigns', 'discount_type')) {
                $table->enum('discount_type', ['fixed', 'percent', 'freeship'])->nullable()->after('target_type');
            }
            if (!Schema::hasColumn('campaigns', 'discount_value')) {
                $table->decimal('discount_value', 15, 0)->default(0)->after('discount_type');
            }
            if (!Schema::hasColumn('campaigns', 'min


## User:
07/09/2026 19:02

PS D:\LuanVanTotNghiep> php artisan migrate

   INFO  Running migrations.  

  2026_06_04_164648_create_banners_table .......................................................................................... 168.52ms FAIL

   Illuminate\Database\QueryException 

  SQLSTATE[42S01]: Base table or view already exists: 1050 Table 'banners' already exists (Connection: mysql, Host: 127.0.0.1, Port: 3306, Database: web_ba_lo, SQL: create table `banners` (`id` bigint unsigned not null auto_increment primary key, `title` varchar(191) null, `image` varchar(191) null, `link` varchar(191) null, `description` text null, `status` tinyint(1) not null default '1', `order` int not null default '0', `created_at` timestamp null, `updated_at` timestamp null) default character set utf8mb4 collate 'utf8mb4_unicode_ci')

  at vendor\laravel\framework\src\Illuminate\Database\Connection.php:841
    837▕             $exceptionType = ($isUniqueConstraintError = $this->isUniqueConstraintError($e))
    838▕                 ? UniqueConstraintViolationException::class
    839▕                 : QueryException::class;
    840▕ 
  ➜ 841▕             $exception = new $exceptionType(
    842▕                 $this->getNameWithReadWriteType(),
    843▕                 $query,
    844▕                 $this->prepareBindings($bindings),
    845▕                 $e,

  1   vendor\laravel\framework\src\Illuminate\Database\Connection.php:587
      PDOException::("SQLSTATE[42S01]: Base table or view already exists: 1050 Table 'banners' already exists")

  2   vendor\laravel\framework\src\Illuminate\Database\Connection.php:587
      PDOStatement::execute()

PS D:\LuanVanTotNghiep>


## Assistant:
07/09/2026 19:02

Bạn đang gặp lỗi vì bảng `banners` đã tồn tại. Điều này xảy ra do bạn đã chạy migration trước đó. Để khắc phục, bạn có thể:

## Cách 1: Chạy migration với tùy chọn `--force` và bỏ qua các bảng đã tồn tại

Không có tùy chọn bỏ qua trực tiếp. Bạn cần xử lý theo các cách sau:

## Cách 2: Xóa bảng cũ và chạy lại migration (khuyến nghị cho môi trường phát triển)

```bash
# Xóa tất cả bảng và chạy lại migration
php artisan migrate:fresh
```

## Cách 3: Tạo migration mới để thêm cột, không tạo lại bảng

Vì bảng đã tồn tại, bạn chỉ cần tạo migration mới để thêm các cột cần thiết:

```bash
php artisan make:migration add_promotion_fields_to_campaigns_table
```

**Nội dung file migration mới:**

```php
<?php
// database/migrations/2026_07_09_000000_add_promotion_fields_to_campaigns_table.php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        // Thêm cột cho bảng campaigns
        Schema::table('campaigns', function (Blueprint $table) {
            // Thêm cột campaign_type nếu chưa có
            if (!Schema::hasColumn('campaigns', 'campaign_type')) {
                $table->enum('campaign_type', ['campaign', 'voucher', 'preorder'])->default('campaign')->after('type');
            }
            
            // Các cột cho voucher
            if (!Schema::hasColumn('campaigns', 'code')) {
                $table->string('code')->nullable()->unique()->after('campaign_type');
            }
            if (!Schema::hasColumn('campaigns', 'target_type')) {
                $table->enum('target_type', ['retail', 'wholesale', 'preorder', 'all'])->default('all')->after('code');
            }
            if (!Schema::hasColumn('campaigns', 'discount_type')) {
                $table->enum('discount_type', ['fixed', 'percent', 'freeship'])->nullable()->after('target_type');
            }
            if (!Schema::hasColumn('campaigns', 'discount_value')) {
                $table->decimal('discount_value', 15, 0)->default(0)->after('discount_type');
            }
            if (!Schema::hasColumn('campaigns', 'min_order')) {
                $table->decimal('min_order', 15, 0)->default(0)->after('discount_value');
            }
            if (!Schema::hasColumn('campaigns', 'limit')) {
                $table->integer('limit')->default(100)->after('min_order');
            }
            if (!Schema::hasColumn('campaigns', 'used')) {
                $table->integer('used')->default(0)->after('limit');
            }
            if (!Schema::hasColumn('campaigns', 'expiry')) {
                $table->date('expiry')->nullable()->after('used');
            }
            
            // Các cột cho pre-order
            if (!Schema::hasColumn('campaigns', 'product_id')) {
                $table->foreignId('product_id')->nullable()->constrained('products')->nullOnDelete()->after('expiry');
            }
            if (!Schema::hasColumn('campaigns', 'tiers')) {
                $table->json('tiers')->nullable()->after('product_id');
            }
            if (!Schema::hasColumn('campaigns', 'current_buyers')) {
                $table->integer('current_buyers')->default(0)->after('tiers');
            }
            
            // Các cột khác
            if (!Schema::hasColumn('campaigns', 'banner')) {
                $table->string('banner')->nullable()->after('featured');
            }
            if (!Schema::hasColumn('campaigns', 'priority')) {
                $table->integer('priority')->default(0)->after('banner');
            }
        });
    }

    public function down(): void
    {
        Schema::table('campaigns', function (Blueprint $table) {
            $columns = ['campaign_type', 'code', 'target_type', 'discount_type', 'discount_value', 
                        'min_order', 'limit', 'used', 'expiry', 'product_id', 'tiers', 
                        'current_buyers', 'banner', 'priority'];
            foreach ($columns as $column) {
                if (Schema::hasColumn('campaigns', $column)) {
                    $table->dropColumn($column);
                }
            }
        });
    }
};
```

## Cách 4: Chạy migration từng bước

```bash
# Chạy tất cả migration chưa chạy
php artisan migrate

# Hoặc chạy migration mới nhất
php artisan migrate --path=database/migrations/2026_07_09_000000_add_promotion_fields_to_campaigns_table.php
```

## Cách 5: Xóa bảng promotions (không dùng nữa)

Sau khi chạy migration, bạn có thể xóa bảng promotions vì không cần dùng nữa:

```sql
-- Xóa bảng promotions
DROP TABLE IF EXISTS promotions;

-- Hoặc xóa dữ liệu trong bảng promotions
TRUNCATE TABLE promotions;
```

## Lưu ý khi chạy migration

1. **Backup dữ liệu** trước khi chạy migration
2. Nếu bạn đang ở môi trường production, hãy cẩn thận với `migrate:fresh` vì nó sẽ xóa tất cả dữ liệu
3. Sử dụng `php artisan migrate --pretend` để xem các câu lệnh SQL sẽ chạy

## Kiểm tra trạng thái migration

```bash
# Xem trạng thái migration
php artisan migrate:status

# Xem danh sách migration đã chạy
php artisan migrate:rollback --pretend
```

Sau khi chạy migration thành công, bạn có thể kiểm tra cấu trúc bảng `campaigns`:

```sql
DESCRIBE campaigns;
```


## User:
07/09/2026 19:05

PS D:\LuanVanTotNghiep> php artisan migrate

   INFO  Running migrations.  

  2026_06_04_164648_create_banners_table ................................................... 10.05ms FAIL

   Illuminate\Database\QueryException 

  SQLSTATE[42S01]: Base table or view already exists: 1050 Table 'banners' already exists (Connection: mysql, Host: 127.0.0.1, Port: 3306, Database: web_ba_lo, SQL: create table `banners` (`id` bigint unsigned not null auto_increment primary key, `title` varchar(191) null, `image` varchar(191) null, `link` varchar(191) null, `description` text null, `status` tinyint(1) not null default '1', `order` int not null default '0', `created_at` timestamp null, `updated_at` timestamp null) default character set utf8mb4 collate 'utf8mb4_unicode_ci')

  at vendor\laravel\framework\src\Illuminate\Database\Connection.php:841
    837▕             $exceptionType = ($isUniqueConstraintError = $this->isUniqueConstraintError($e))
    838▕                 ? UniqueConstraintViolationException::class
    839▕                 : QueryException::class;
    840▕ 
  ➜ 841▕             $exception = new $exceptionType(
    842▕                 $this->getNameWithReadWriteType(),
    843▕                 $query,
    844▕                 $this->prepareBindings($bindings),
    845▕                 $e,

  1   vendor\laravel\framework\src\Illuminate\Database\Connection.php:587
      PDOException::("SQLSTATE[42S01]: Base table or view already exists: 1050 Table 'banners' already exists")

  2   vendor\laravel\framework\src\Illuminate\Database\Connection.php:587
      PDOStatement::execute()

PS D:\LuanVanTotNghiep>


## Assistant:
07/09/2026 19:05

Bạn đang gặp lỗi vì bảng `banners` đã tồn tại từ trước. Vì bạn đã có dữ liệu và không muốn mất, hãy làm theo các bước sau:

## Cách 1: Chạy migration với tùy chọn `--pretend` để kiểm tra

```bash
php artisan migrate --pretend
```

## Cách 2: Chạy migration mới nhất (không chạy lại các migration cũ)

```bash
# Chạy migration mới nhất
php artisan migrate
```

Nếu vẫn báo lỗi, hãy xóa bảng `migrations` và chạy lại:

```bash
# Xóa bảng migrations
php artisan migrate:reset

# Chạy lại tất cả migration
php artisan migrate
```

## Cách 3: Xóa bảng cũ và chạy lại migration (Khuyến nghị cho môi trường phát triển)

```bash
# Xóa tất cả bảng và chạy lại migration
php artisan migrate:fresh --seed
```

## Cách 4: Chạy từng migration (nếu bạn muốn giữ dữ liệu)

```bash
# 1. Chạy migration mới thêm cột cho bảng campaigns
php artisan migrate --path=database/migrations/2026_07_09_000000_add_promotion_fields_to_campaigns_table.php

# 2. Nếu bảng banners đã tồn tại, bạn có thể bỏ qua migration đó hoặc:
# - Xóa file migration banners (backup trước)
# - Sửa migration banners thành Schema::hasTable để kiểm tra
```

## Cách 5: Sửa migration banners để kiểm tra bảng tồn tại

Sửa file `database/migrations/2026_06_04_164648_create_banners_table.php`:

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up()
    {
        // Kiểm tra bảng đã tồn tại chưa
        if (!Schema::hasTable('banners')) {
            Schema::create('banners', function (Blueprint $table) {
                $table->id();
                $table->string('title')->nullable();
                $table->string('image')->nullable();
                $table->string('link')->nullable();
                $table->text('description')->nullable();
                $table->boolean('status')->default(true);
                $table->integer('order')->default(0);
                $table->timestamps();
            });
        }
    }

    public function down()
    {
        Schema::dropIfExists('banners');
    }
};
```

## Cách 6: Xóa bảng promotions (vì bạn không dùng nữa)

```sql
-- Kết nối vào database và chạy
DROP TABLE IF EXISTS promotions;
```

## Cách 7: Reset và chạy lại migration (Nhanh nhất)

```bash
# 1. Xóa tất cả migration đã chạy
php artisan migrate:reset

# 2. Chạy lại migration
php artisan migrate

# 3. Nếu có dữ liệu seed
php artisan db:seed
```

## Cách 8: Kiểm tra trạng thái migration hiện tại

```bash
# Xem trạng thái
php artisan migrate:status

# Nếu có migration chưa chạy, chạy chúng
php artisan migrate
```

## Lưu ý quan trọng:

1. **Backup dữ liệu** trước khi thực hiện:
```bash
# Backup database
mysqldump -u root -p web_ba_lo > backup.sql
```

2. **Nếu bạn đang ở môi trường production**, hãy cẩn thận và nên chạy migration mới thay vì reset:

```bash
# Chạy migration mới (không ảnh hưởng dữ liệu cũ)
php artisan migrate --path=database/migrations/2026_07_09_000000_add_promotion_fields_to_campaigns_table.php
```

3. **Sau khi migration thành công**, xóa bảng promotions:

```sql
DROP TABLE IF EXISTS promotions;
```

## Tóm tắt các bước nhanh:

```bash
# 1. Reset migrations
php artisan migrate:reset

# 2. Chạy migrations
php artisan migrate

# 3. Nếu lỗi, xóa bảng promotions và chạy lại
php artisan migrate:fresh

# 4. Kiểm tra
php artisan migrate:status
```


## User:
07/09/2026 19:05

D:\LuanVanTotNghiep> php artisan migrate:reset

   INFO  Rolling back migrations.  

  2026_07_07_014950_add_campaign_id_to_banners_table .................................................................... Migration not found  
  2026_07_06_211358_create_promotions_table .................................................................................... 68.99ms DONE
  2026_07_06_195039_add_fields_to_campaigns_table ............................................................................. 163.95ms DONE
  2026_06_27_020426_update_image_url_to_json_in_products_table ................................................................. 25.11ms FAIL

   Illuminate\Database\QueryException 

  SQLSTATE[22001]: String data, right truncated: 1406 Data too long for column 'image_url' at row 1 (Connection: mysql, Host: 127.0.0.1, Port:3306, Database: web_ba_lo, SQL: alter table `products` modify `image_url` varchar(191) null)

  at vendor\laravel\framework\src\Illuminate\Database\Connection.php:841
    837▕             $exceptionType = ($isUniqueConstraintError = $this->isUniqueConstraintError($e))
    838▕                 ? UniqueConstraintViolationException::class
    839▕                 : QueryException::class;
    840▕ 
  ➜ 841▕             $exception = new $exceptionType(
    842▕                 $this->getNameWithReadWriteType(),
    843▕                 $query,
    844▕                 $this->prepareBindings($bindings),
    845▕                 $e,

  1   vendor\laravel\framework\src\Illuminate\Database\Connection.php:587
      PDOException::("SQLSTATE[22001]: String data, right truncated: 1406 Data too long for column 'image_url' at row 1")

  2   vendor\laravel\framework\src\Illuminate\Database\Connection.php:587
      PDOStatement::execute()

PS D:\LuanVanTotNghiep>


## Assistant:
07/09/2026 19:05

Bạn đang gặp lỗi khi rollback migration `2026_06_27_020426_update_image_url_to_json_in_products_table` vì cột `image_url` đã được chuyển sang kiểu JSON và có dữ liệu dài hơn 191 ký tự, không thể rollback về kiểu VARCHAR(191).

## Giải pháp:

### Cách 1: Sửa migration để rollback an toàn

Sửa file `database/migrations/2026_06_27_020426_update_image_url_to_json_in_products_table.php`:

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('products', function (Blueprint $table) {
            // Đổi kiểu cột từ string sang json, giữ nguyên nullable
            $table->json('image_url')->nullable()->change();
        });
    }

    public function down(): void
    {
        // Không thể rollback vì dữ liệu JSON có thể dài hơn VARCHAR(191)
        // Nên bỏ qua hoặc xử lý đặc biệt
        Schema::table('products', function (Blueprint $table) {
            // Chuyển về TEXT thay vì VARCHAR để tránh lỗi
            $table->text('image_url')->nullable()->change();
        });
    }
};
```

### Cách 2: Rollback từng bước và bỏ qua migration lỗi

```bash
# 1. Rollback migration promotions trước
php artisan migrate:rollback --step=1

# 2. Xóa migration promotions khỏi bảng migrations
php artisan migrate:rollback --path=database/migrations/2026_07_06_211358_create_promotions_table.php

# 3. Chạy lại migration
php artisan migrate
```

### Cách 3: Sửa migration campaigns để không rollback

Sửa file `database/migrations/2026_07_06_195039_add_fields_to_campaigns_table.php`:

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('campaigns', function (Blueprint $table) {
            // Thêm các cột nếu chưa có
            if (!Schema::hasColumn('campaigns', 'name')) {
                $table->string('name')->nullable()->after('id');
            }
            if (!Schema::hasColumn('campaigns', 'type')) {
                $table->string('type')->default('seasonal')->after('name');
            }
            if (!Schema::hasColumn('campaigns', 'description')) {
                $table->text('description')->nullable()->after('type');
            }
            if (!Schema::hasColumn('campaigns', 'status')) {
                $table->enum('status', ['scheduled', 'active', 'ended'])->default('scheduled')->after('end_time');
            }
            if (!Schema::hasColumn('campaigns', 'banner')) {
                $table->string('banner')->nullable()->after('status');
            }
            if (!Schema::hasColumn('campaigns', 'priority')) {
                $table->integer('priority')->default(0)->after('banner');
            }
            if (!Schema::hasColumn('campaigns', 'featured')) {
                $table->boolean('featured')->default(false)->after('priority');
            }
        });
    }

    public function down(): void
    {
        Schema::table('campaigns', function (Blueprint $table) {
            $columns = ['name', 'type', 'description', 'status', 'banner', 'priority', 'featured'];
            foreach ($columns as $column) {
                if (Schema::hasColumn('campaigns', $column)) {
                    $table->dropColumn($column);
                }
            }
        });
    }
};
```

### Cách 4: Xóa bảng promotions và chạy migration mới (Nhanh nhất)

```bash
# 1. Xóa bảng promotions
php artisan db:wipe

# 2. Chạy migration
php artisan migrate
```

### Cách 5: Chạy migration với tùy chọn force

```bash
php artisan migrate --force
```

## Giải pháp triệt để: Reset hoàn toàn

```bash
# 1. Drop tất cả bảng
php artisan db:wipe

# 2. Chạy migration
php artisan migrate

# 3. Nếu có seed data
php artisan db:seed
```

## Giải pháp tạm thời (giữ dữ liệu):

```bash
# 1. Rollback promotions
php artisan migrate:rollback --path=database/migrations/2026_07_06_211358_create_promotions_table.php

# 2. Rollback campaigns fields
php artisan migrate:rollback --path=database/migrations/2026_07_06_195039_add_fields_to_campaigns_table.php

# 3. Chạy lại migration mới
php artisan migrate
```

## Khắc phục lỗi image_url

Vì cột `image_url` đã có dữ liệu JSON dài, bạn cần chuyển về TEXT khi rollback:

```sql
-- Kết nối vào database
ALTER TABLE products MODIFY image_url TEXT NULL;
```

## Tóm tắt các bước xử lý nhanh:

```bash
# 1. Drop bảng promotions
php artisan db:wipe

# 2. Chạy migration
php artisan migrate

# 3. Nếu lỗi, sửa migration và chạy lại
php artisan migrate:fresh
```

## Lưu ý quan trọng:

1. **Backup dữ liệu** trước khi thực hiện bất kỳ thao tác nào
2. Nếu bạn đang ở môi trường production, hãy cẩn thận và liên hệ với DBA
3. **Không rollback migration có dữ liệu JSON** nếu không cần thiết

Bạn muốn tôi hướng dẫn cách nào cụ thể hơn?


## User:
07/09/2026 19:10

PS D:\LuanVanTotNghiep> TRUNCATE TABLE promotions;
TRUNCATE : The term 'TRUNCATE' is not recognized as the name of a cmdlet, function, script file, or operable program. Check the spelling of 
the name, or if a path was included, verify that the path is correct and try again.
At line:1 char:1
+ TRUNCATE TABLE promotions;
+ ~~~~~~~~
    + CategoryInfo          : ObjectNotFound: (TRUNCATE:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 
PS D:\LuanVanTotNghiep> DROP TABLE IF EXISTS promotions;
DROP : The term 'DROP' is not recognized as the name of a cmdlet, function, script file, or operable program. Check the spelling of the name, 
or if a path was included, verify that the path is correct and try again.
At line:1 char:1
+ DROP TABLE IF EXISTS promotions;
+ ~~~~
    + CategoryInfo          : ObjectNotFound: (DROP:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 
PS D:\LuanVanTotNghiep>


## Assistant:
07/09/2026 19:10

Bạn đang chạy lệnh SQL trong PowerShell thay vì trong MySQL. Để thực hiện các lệnh SQL, bạn cần kết nối vào MySQL trước.

## Cách 1: Kết nối MySQL qua PowerShell

```bash
# Kết nối vào MySQL
mysql -u root -p

# Sau đó nhập mật khẩu, rồi chạy các lệnh SQL:
USE web_ba_lo;
TRUNCATE TABLE promotions;
DROP TABLE IF EXISTS promotions;
EXIT;
```

## Cách 2: Chạy lệnh SQL trực tiếp từ PowerShell

```bash
# Xóa bảng promotions
mysql -u root -p -e "DROP TABLE IF EXISTS web_ba_lo.promotions;"

# Hoặc nếu không có mật khẩu
mysql -u root -e "DROP TABLE IF EXISTS web_ba_lo.promotions;"
```

## Cách 3: Sử dụng Laravel Artisan để chạy SQL

```bash
# Chạy lệnh SQL qua Laravel
php artisan tinker

# Trong tinker, chạy:
DB::statement('DROP TABLE IF EXISTS promotions;');
# Hoặc
DB::statement('TRUNCATE TABLE promotions;');

# Thoát tinker
exit;
```

## Cách 4: Sử dụng Laravel migration để xóa bảng

```bash
# Tạo migration xóa bảng promotions
php artisan make:migration drop_promotions_table

# Sau đó sửa file migration mới tạo
```

Nội dung file migration:

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::dropIfExists('promotions');
    }

    public function down(): void
    {
        // Không cần tạo lại
    }
};
```

Chạy migration:

```bash
php artisan migrate
```

## Cách 5: Sử dụng phpMyAdmin (nếu có)

1. Mở phpMyAdmin
2. Chọn database `web_ba_lo`
3. Chọn bảng `promotions`
4. Click "Drop" hoặc "Empty"

## Cách 6: Sử dụng MySQL Workbench

1. Mở MySQL Workbench
2. Kết nối vào database
3. Chạy lệnh:
```sql
USE web_ba_lo;
DROP TABLE IF EXISTS promotions;
```

## Lưu ý khi xóa bảng:

1. **Xóa bảng `promotions` trước khi chạy migration mới** để tránh xung đột
2. Nếu bạn không muốn xóa bảng, có thể giữ nguyên và migration sẽ bỏ qua

## Kiểm tra bảng đã xóa chưa:

```bash
# Kết nối MySQL
mysql -u root -p

# Kiểm tra
USE web_ba_lo;
SHOW TABLES;
EXIT;
```

## Cách nhanh nhất để xóa bảng promotions:

```bash
# Chạy trực tiếp trong PowerShell
mysql -u root -p -e "DROP TABLE IF EXISTS web_ba_lo.promotions;"
```


## User:
07/09/2026 19:11

PS D:\LuanVanTotNghiep> mysql -u root -p
mysql : The term 'mysql' is not recognized as the name of a cmdlet, function, script file, or operable program. Check the spelling of the 
name, or if a path was included, verify that the path is correct and try again.
At line:1 char:1
+ mysql -u root -p
+ ~~~~~
    + CategoryInfo          : ObjectNotFound: (mysql:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException


## Assistant:
07/09/2026 19:11

MySQL chưa được cài đặt hoặc chưa được thêm vào PATH. Bạn có thể sử dụng các cách sau:

## Cách 1: Sử dụng Laravel Tinker (Khuyến nghị)

```bash
# Mở Laravel Tinker
php artisan tinker

# Sau đó chạy lệnh SQL:
DB::statement('DROP TABLE IF EXISTS promotions;');

# Thoát
exit;
```

## Cách 2: Sử dụng Migration để xóa bảng

```bash
# Tạo migration xóa bảng promotions
php artisan make:migration drop_promotions_table
```

Sửa file `database/migrations/2026_07_09_xxxxxx_drop_promotions_table.php`:

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::dropIfExists('promotions');
    }

    public function down(): void
    {
        // Không cần tạo lại
    }
};
```

Chạy migration:

```bash
php artisan migrate
```

## Cách 3: Chạy SQL qua Laravel Artisan

```bash
# Chạy lệnh SQL qua Artisan
php artisan db:statement "DROP TABLE IF EXISTS promotions;"
```

## Cách 4: Sử dụng PHPMyAdmin

1. Mở trình duyệt, truy cập `http://localhost/phpmyadmin` (hoặc đường dẫn của bạn)
2. Chọn database `web_ba_lo`
3. Chọn bảng `promotions`
4. Click vào tab "Structure" 
5. Click "Drop" ở cuối trang

## Cách 5: Sử dụng XAMPP Control Panel

1. Mở XAMPP Control Panel
2. Click "Admin" bên cạnh MySQL
3. Sẽ mở phpMyAdmin
4. Làm theo cách 4

## Cách 6: Sử dụng Laravel Query Builder trong tinker

```bash
php artisan tinker
```

```php
// Xóa bảng
DB::statement('DROP TABLE IF EXISTS promotions');

// Hoặc xóa dữ liệu
DB::table('promotions')->truncate();

// Kiểm tra
DB::statement('SHOW TABLES');
```

## Cách 7: Sử dụng migration để xóa dữ liệu

Tạo một migration mới:

```bash
php artisan make:migration clear_promotions_data
```

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    public function up(): void
    {
        // Xóa dữ liệu trong bảng promotions
        DB::table('promotions')->truncate();
    }

    public function down(): void
    {
        // Không cần làm gì
    }
};
```

Chạy migration:

```bash
php artisan migrate
```

## Cách 8: Tìm đường dẫn MySQL

Nếu bạn đã cài XAMPP/WAMP, MySQL thường ở:

```bash
# XAMPP
C:\xampp\mysql\bin\mysql.exe -u root -p

# WAMP
C:\wamp64\bin\mysql\mysql5.7.31\bin\mysql.exe -u root -p

# Laragon
C:\laragon\bin\mysql\mysql-8.0.30-winx64\bin\mysql.exe -u root -p
```

## Cách 9: Sử dụng MySQL Workbench

1. Mở MySQL Workbench
2. Kết nối vào database
3. Chạy lệnh:

```sql
USE web_ba_lo;
DROP TABLE IF EXISTS promotions;
```

## Cách nhanh nhất (Khuyến nghị):

```bash
# 1. Chạy tinker
php artisan tinker

# 2. Trong tinker, chạy:
DB::statement('DROP TABLE IF EXISTS promotions');

# 3. Kiểm tra
DB::statement('SHOW TABLES');

# 4. Thoát
exit
```

Sau khi xóa bảng promotions, bạn có thể chạy migration để tạo lại cấu trúc mới.


## User:
07/09/2026 19:28

![Screenshot 2026-07-09 192449.png](data:image/webp;base64,)

Chỉnh lại giao diện cho tôi , mỗi loại khác nhau nhưng đang bị gom chung 1 loại card và phân theo thể loại đang bị sai. Chỉnh lại và đưa code hoàn chỉnh cho tôi 

<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    // FIX: Cho phép giảm giá = 0, chỉ báo lỗi khi < 0 hoặc > 100
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    // FIX: Kiểm tra ngày kết thúc >= ngày bắt đầu
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    
                        
                    <div class="absolute top-2 right-2 flex gap-1 flex-wrap">
                            <span class="text-[10px] px-2 py-0.5 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-[10px] px-2 py-0.5 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐</span>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">{{ campaign.name }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openCampaignModal(campaign)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giảm giá</p>
                                <p class="text-sm font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">SL tối thiểu</p>
                                <p class="text-sm font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-0.5 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-[10px] px-3 py-1 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl p-4 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div class="min-w-0 flex-1">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-base text-gray-800 mt-1 truncate">{{ voucher.code }}</h3>
                        </div>
                        <div class="flex gap-0.5 flex-shrink-0 ml-1">
                            <button @click="openVoucherModal(voucher)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors text-xs">✏️</button>
                            <button @click="deleteVoucher(voucher.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors text-xs">🗑️</button>
                        </div>
                    </div>

                    <p class="text-xs text-gray-500 mb-2 line-clamp-1">{{ voucher.description }}</p>

                    <div class="grid grid-cols-2 gap-1 text-xs">
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">🎯</span>
                            <span class="text-[10px] px-1.5 py-0.5 rounded-full bg-gray-100">{{ getTargetTypeLabel(voucher.target_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">💰</span>
                            <span class="text-orange-600 font-semibold">{{ getDiscountTypeLabel(voucher.discount_type) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">📅</span>
                            <span>HSD: {{ formatDate(voucher.expiry) }}</span>
                        </span>
                        <span class="flex items-center gap-1 text-[10px]">
                            <span class="text-gray-500">📊</span>
                            <span>{{ voucher.used }}/{{ voucher.limit }}</span>
                        </span>
                    </div>

                    <div class="mt-2 pt-2 border-t border-gray-100">
                        <div class="w-full h-1 bg-gray-100 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-orange-500 rounded-full transition-all"
                                :style="{ width: Math.min((voucher.used / voucher.limit) * 100, 100) + '%' }"
                            ></div>
                        </div>
                    </div>

                    <div class="mt-2 pt-1 flex justify-end">
                        <button 
                            @click="toggleVoucher(voucher)"
                            :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-[10px] px-3 py-1 rounded-full transition-colors"
                        >
                            {{ voucher.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST (3 items per row) ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl p-4 border border-gray-200 hover:shadow-md transition-all">
                    <div class="flex justify-between items-start">
                        <div class="min-w-0 flex-1">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? 'Đang hoạt động' : 'Đã tắt' }}
                            </span>
                            <h3 class="font-bold text-sm text-gray-800 mt-1 truncate">⏳ {{ preorder.code }}</h3>
                            <p class="text-[10px] text-gray-500 truncate">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                        </div>
                        <div class="flex gap-0.5 flex-shrink-0 ml-1">
                            <button @click="openPreorderModal(preorder)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors text-xs">✏️</button>
                            <button @click="deletePreorder(preorder.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors text-xs">🗑️</button>
                        </div>
                    </div>

                    <div class="mt-2 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-3">
                        <div class="flex justify-between items-center mb-1">
                            <span class="text-[10px] font-medium">Đã đặt: {{ preorder.current_buyers || 0 }} lượt</span>
                            <span class="text-xs font-bold text-blue-600">
                                {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                            </span>
                        </div>

                        <div class="w-full h-1.5 bg-gray-200 rounded-full overflow-hidden">
                            <div 
                                class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                            ></div>
                        </div>

                        <div class="mt-2 grid grid-cols-3 gap-1 text-[10px]">
                            <div v-for="tier in preorder.tiers" :key="tier.from" 
                                class="text-center p-1 bg-white rounded border"
                                :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                            >
                                <div class="font-bold">{{ tier.discount }}%</div>
                                <div class="text-gray-500 text-[8px]">#{{ tier.from }}-{{ tier.to }}</div>
                            </div>
                        </div>
                    </div>

                    <div class="mt-2 flex justify-between items-center text-[10px] text-gray-500">
                        <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                        <span v-if="preorder.min_order > 0">💰 {{ formatPrice(preorder.min_order) }}</span>
                    </div>

                    <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                        <button 
                            @click="toggleVoucher(preorder)"
                            :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'"
                            class="text-[10px] px-3 py-1 rounded-full transition-colors"
                        >
                            {{ preorder.active ? '✅ Kích hoạt' : '⛔ Đã tắt' }}
                        </button>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- ==================== SẢN PHẨM ÁP DỤNG ==================== -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>


## Assistant:
07/09/2026 19:28

Tôi thấy vấn đề: **Campaigns, Vouchers và Pre-orders đang hiển thị chung trong 1 tab** thay vì phân biệt theo loại. Bạn muốn mỗi loại có giao diện card riêng biệt.

Vì bạn đang dùng bảng `promotions` cho voucher và pre-order, tôi sẽ chỉnh lại code để:

1. **Phân biệt rõ 3 loại**: Chiến dịch (campaigns), Voucher (promotions type='voucher'), Pre-order (promotions type='preorder_tier')
2. **Mỗi loại có card hiển thị khác nhau**
3. **Thống kê số lượng chính xác**

Dưới đây là code hoàn chỉnh:

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

// Campaigns - chỉ lấy các item có type là 'campaign' hoặc không có type (mặc định)
const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

// Vouchers
const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const voucherCounts = computed(() => {
    const vouchers = (promotions.value || []).filter(p => p.type === 'voucher');
    return {
        all: vouchers.length,
        active: vouchers.filter(v => v.active).length,
        inactive: vouchers.filter(v => !v.active).length
    };
});

// Pre-orders
const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const preorderCounts = computed(() => {
    const preorders = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    return {
        all: preorders.length,
        active: preorders.filter(p => p.active).length,
        inactive: preorders.filter(p => !p.active).length
    };
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-36 bg-gradient-to-r from-blue-50 to-blue-100">
                        <div class="w-full h-full flex items-center justify-center text-blue-300">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-2 right-2 flex gap-1 flex-wrap">
                            <span class="text-[10px] px-2 py-0.5 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-[10px] px-2 py-0.5 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐</span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">{{ campaign.name }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openCampaignModal(campaign)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giảm giá</p>
                                <p class="text-sm font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">SL tối thiểu</p>
                                <p class="text-sm font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-0.5 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-[10px] px-3 py-1 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-28 bg-gradient-to-r from-orange-50 to-orange-100">
                        <div class="w-full h-full flex items-center justify-center text-orange-300">
                            <span class="material-symbols-outlined text-4xl">local_offer</span>
                        </div>
                        <div class="absolute top-2 right-2">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? '🟢 Hoạt động' : '🔴 Đã tắt' }}
                            </span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-base text-gray-800 truncate">{{ voucher.code }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ getDiscountTypeLabel(voucher.discount_type) }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openVoucherModal(voucher)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteVoucher(voucher.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ voucher.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giá trị</p>
                                <p class="text-sm font-bold text-orange-600">
                                    {{ voucher.discount_type === 'percent' ? voucher.discount_value + '%' : formatPrice(voucher.discount_value) }}
                                </p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Đã dùng</p>
                                <p class="text-sm font-bold text-blue-600">{{ voucher.used }}/{{ voucher.limit }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">event</span>
                            <span>HSD: {{ formatDate(voucher.expiry) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">target</span>
                            <span>{{ getTargetTypeLabel(voucher.target_type) }}</span>
                            <span v-if="voucher.min_order > 0" class="ml-1">- Đơn tối thiểu: {{ formatPrice(voucher.min_order) }}</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleVoucher(voucher)" 
                                :class="voucher.active ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'"
                                class="text-[10px] px-3 py-1 rounded-full transition-colors"
                            >
                                {{ voucher.active ? '✅ Kích hoạt' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-28 bg-gradient-to-r from-purple-50 to-purple-100">
                        <div class="w-full h-full flex items-center justify-center text-purple-300">
                            <span class="material-symbols-outlined text-4xl">schedule</span>
                        </div>
                        <div class="absolute top-2 right-2">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? '🟢 Hoạt động' : '🔴 Đã tắt' }}
                            </span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">⏳ {{ preorder.code }}</h3>
                                <p class="text-[10px] text-gray-500 truncate">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openPreorderModal(preorder)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deletePreorder(preorder.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <div class="mt-2 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-2">
                            <div class="flex justify-between items-center mb-1">
                                <span class="text-[10px] font-medium">Đã đặt: {{ preorder.current_buyers || 0 }} lượt</span>
                                <span class="text-xs font-bold text-blue-600">
                                    {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                                </span>
                            </div>

                            <div class="w-full h-1.5 bg-gray-200 rounded-full overflow-hidden">
                                <div 
                                    class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                    :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                                ></div>
                            </div>

                            <div class="mt-1.5 grid grid-cols-3 gap-1 text-[10px]">
                                <div v-for="tier in preorder.tiers" :key="tier.from" 
                                    class="text-center p-1 bg-white rounded border"
                                    :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                                >
                                    <div class="font-bold">{{ tier.discount }}%</div>
                                    <div class="text-gray-500 text-[8px]">#{{ tier.from }}-{{ tier.to }}</div>
                                </div>
                            </div>
                        </div>

                        <div class="mt-2 flex justify-between items-center text-[10px] text-gray-500">
                            <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                            <span v-if="preorder.min_order > 0">💰 {{ formatPrice(preorder.min_order) }}</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleVoucher(preorder)"
                                :class="preorder.active ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'"
                                class="text-[10px] px-3 py-1 rounded-full transition-colors"
                            >
                                {{ preorder.active ? '✅ Kích hoạt' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <!-- ... giữ nguyên nội dung campaign modal ... -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- Sản phẩm áp dụng -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
```

## Tóm tắt các thay đổi:

1. **Phân biệt 3 loại card khác nhau**:
   - **Chiến dịch**: Nền xanh, icon campaign, hiển thị ngày tháng, số lượng sản phẩm
   - **Voucher**: Nền cam, icon local_offer, hiển thị giá trị giảm, số lượt đã dùng
   - **Pre-order**: Nền tím, icon schedule, hiển thị số lượt đặt, các mức giảm

2. **Thống kê riêng cho từng tab**:
   - Campaigns: Đếm theo trạng thái (active, ended, scheduled)
   - Vouchers: Đếm theo trạng thái hoạt động
   - Pre-orders: Đếm theo trạng thái hoạt động

3. **Giao diện card khác biệt** để dễ phân biệt loại khuyến mãi


## User:
07/09/2026 19:38

![image.png](data:image/webp;base64,)

bạn thực sự đã chỉnh chưa? Tôi đã thêm khuyến mãi pre  và voucher nhưng 2 cái vẫn đang hiển thị ở bên chiến dịch là sao ?
Và quan trọng là tôi đã không còn giữ bảng promotion (migrate và model ) rồi , vấn đề quan trọng bạn cứ quên


## Assistant:
07/09/2026 19:38

Tôi xin lỗi! Bạn đã xóa bảng `promotions` nhưng code vẫn đang gọi dữ liệu từ bảng đó. Bây giờ tôi sẽ chỉnh lại để **tất cả dữ liệu (Chiến dịch, Voucher, Pre-order) đều được lưu trong bảng `campaigns`** với trường `campaign_type` để phân biệt.

## 1. Cập nhật Model `Campaign.php`

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Campaign extends Model
{
    use HasFactory;

    protected $fillable = [
        'name',
        'type', // seasonal, flash_sale, anniversary, holiday, product_launch, other
        'campaign_type', // campaign, voucher, preorder
        'code',
        'target_type',
        'discount_type',
        'discount_value',
        'min_order',
        'limit',
        'used',
        'expiry',
        'description',
        'start_time',
        'end_time',
        'status',
        'banner',
        'priority',
        'featured',
        'product_id',
        'tiers',
        'current_buyers',
        'quantity',
        'discount_percent'
    ];

    protected $casts = [
        'start_time' => 'datetime',
        'end_time' => 'datetime',
        'featured' => 'boolean',
        'tiers' => 'array',
        'discount_value' => 'decimal:0',
        'min_order' => 'decimal:0',
        'expiry' => 'date',
        'discount_percent' => 'decimal:2',
    ];

    public function productVariants()
    {
        return $this->belongsToMany(ProductVariant::class, 'campaign_products');
    }

    public function configs()
    {
        return $this->hasMany(CampaignConfig::class);
    }

    public function orders()
    {
        return $this->hasMany(Order::class);
    }

    public function product()
    {
        return $this->belongsTo(Product::class);
    }

    public function getStartDateAttribute()
    {
        return $this->start_time ? $this->start_time->format('Y-m-d') : null;
    }

    public function getEndDateAttribute()
    {
        return $this->end_time ? $this->end_time->format('Y-m-d') : null;
    }

    public function getProductIdsAttribute()
    {
        return $this->productVariants->pluck('id')->toArray();
    }
}
```

## 2. Migration thêm cột cho bảng `campaigns`

Tạo file migration mới:

```bash
php artisan make:migration add_campaign_type_and_promotion_fields_to_campaigns_table
```

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('campaigns', function (Blueprint $table) {
            // Thêm campaign_type để phân biệt loại
            if (!Schema::hasColumn('campaigns', 'campaign_type')) {
                $table->enum('campaign_type', ['campaign', 'voucher', 'preorder'])->default('campaign')->after('type');
            }
            
            // Các cột cho voucher và preorder
            if (!Schema::hasColumn('campaigns', 'code')) {
                $table->string('code')->nullable()->unique()->after('campaign_type');
            }
            if (!Schema::hasColumn('campaigns', 'target_type')) {
                $table->enum('target_type', ['retail', 'wholesale', 'preorder', 'all'])->default('all')->after('code');
            }
            if (!Schema::hasColumn('campaigns', 'discount_type')) {
                $table->enum('discount_type', ['fixed', 'percent', 'freeship'])->nullable()->after('target_type');
            }
            if (!Schema::hasColumn('campaigns', 'discount_value')) {
                $table->decimal('discount_value', 15, 0)->default(0)->after('discount_type');
            }
            if (!Schema::hasColumn('campaigns', 'min_order')) {
                $table->decimal('min_order', 15, 0)->default(0)->after('discount_value');
            }
            if (!Schema::hasColumn('campaigns', 'limit')) {
                $table->integer('limit')->default(100)->after('min_order');
            }
            if (!Schema::hasColumn('campaigns', 'used')) {
                $table->integer('used')->default(0)->after('limit');
            }
            if (!Schema::hasColumn('campaigns', 'expiry')) {
                $table->date('expiry')->nullable()->after('used');
            }
            
            // Các cột cho pre-order
            if (!Schema::hasColumn('campaigns', 'product_id')) {
                $table->foreignId('product_id')->nullable()->constrained('products')->nullOnDelete()->after('expiry');
            }
            if (!Schema::hasColumn('campaigns', 'tiers')) {
                $table->json('tiers')->nullable()->after('product_id');
            }
            if (!Schema::hasColumn('campaigns', 'current_buyers')) {
                $table->integer('current_buyers')->default(0)->after('tiers');
            }
            
            // Các cột khác
            if (!Schema::hasColumn('campaigns', 'banner')) {
                $table->string('banner')->nullable()->after('featured');
            }
            if (!Schema::hasColumn('campaigns', 'priority')) {
                $table->integer('priority')->default(0)->after('banner');
            }
            if (!Schema::hasColumn('campaigns', 'quantity')) {
                $table->integer('quantity')->default(0)->after('priority');
            }
            if (!Schema::hasColumn('campaigns', 'discount_percent')) {
                $table->decimal('discount_percent', 5, 2)->default(0)->after('quantity');
            }
        });
    }

    public function down(): void
    {
        Schema::table('campaigns', function (Blueprint $table) {
            $columns = ['campaign_type', 'code', 'target_type', 'discount_type', 'discount_value', 
                        'min_order', 'limit', 'used', 'expiry', 'product_id', 'tiers', 
                        'current_buyers', 'banner', 'priority', 'quantity', 'discount_percent'];
            foreach ($columns as $column) {
                if (Schema::hasColumn('campaigns', $column)) {
                    $table->dropColumn($column);
                }
            }
        });
    }
};
```

## 3. Controller `PromotionController.php` (Chỉ dùng bảng campaigns)

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy tất cả campaigns (bao gồm campaign, voucher, preorder)
            $allCampaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color', 'product'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs()->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'campaign_type' => $campaign->campaign_type ?? 'campaign',
                        'code' => $campaign->code,
                        'target_type' => $campaign->target_type,
                        'discount_type' => $campaign->discount_type,
                        'discount_value' => $campaign->discount_value,
                        'min_order' => $campaign->min_order,
                        'limit' => $campaign->limit,
                        'used' => $campaign->used ?? 0,
                        'expiry' => $campaign->expiry,
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $campaign->quantity ?? ($config ? (int) $config->quantity : 0),
                        'discountPercent' => $campaign->discount_percent ?? ($config ? (float) $config->discount_percent : 0),
                        'discount' => $campaign->discount_percent ? $campaign->discount_percent . '%' : ($config ? (float) $config->discount_percent . '%' : '0%'),
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                        'product_id' => $campaign->product_id,
                        'tiers' => $campaign->tiers,
                        'current_buyers' => $campaign->current_buyers ?? 0,
                        'active' => $campaign->status === 'active',
                    ];
                });

            // Phân loại
            $campaigns = $allCampaigns->filter(fn($item) => $item['campaign_type'] === 'campaign')->values();
            $vouchers = $allCampaigns->filter(fn($item) => $item['campaign_type'] === 'voucher')->values();
            $preorders = $allCampaigns->filter(fn($item) => $item['campaign_type'] === 'preorder')->values();

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $variant->product->is_preorder ?? false,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'vouchers' => $vouchers,
                'preorders' => $preorders,
                'banners' => $banners,
                'products' => [],
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'vouchers' => [],
                'preorders' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    // ==================== CAMPAIGN METHODS ====================

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'campaign_type' => 'campaign',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
                'quantity' => $validated['quantity'] ?? 0,
                'discount_percent' => $validated['discountPercent'] ?? 0,
            ]);

            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['


## User:
07/09/2026 19:43

bạn thực sự đã chỉnh chưa? Tôi đã thêm khuyến mãi pre  và voucher nhưng 2 cái vẫn đang hiển thị ở bên chiến dịch là sao ?
Và quan trọng là tôi đã không còn giữ bảng promotion (migrate và model ) rồi , vấn đề quan trọng bạn cứ quên.

Tôi sẽ đưa tất cả code 1 lần nữa bạn hãy chỉnh dựa trên đó . Bạn cứ quên ngữ cảnh đang làm gì và tới đâu tôi rất bực.

<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy campaigns
            $campaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color', 'product'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'campaign_type' => $campaign->campaign_type ?? 'campaign',
                        'code' => $campaign->code,
                        'target_type' => $campaign->target_type,
                        'discount_type' => $campaign->discount_type,
                        'discount_value' => $campaign->discount_value,
                        'min_order' => $campaign->min_order,
                        'limit' => $campaign->limit,
                        'used' => $campaign->used ?? 0,
                        'expiry' => $campaign->expiry,
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $config ? (int) $config->quantity : 0,
                        'discountPercent' => $config ? (float) $config->discount_percent : 0,
                        'discount' => $config ? (float) $config->discount_percent . '%' : '0%',
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                        'product_id' => $campaign->product_id,
                        'tiers' => $campaign->tiers,
                        'current_buyers' => $campaign->current_buyers ?? 0,
                    ];
                });

            // Lấy tất cả banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $variant->product->is_preorder ?? false,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'promotions' => [], // Không dùng promotions
                'banners' => $banners,
                'products' => [],
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'promotions' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    // ==================== CAMPAIGN METHODS ====================

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'campaign_type' => 'campaign',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
            ]);

            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                CampaignConfig::create([
                    'campaign_id' => $campaign->id,
                    'quantity' => $validated['quantity'] ?? 0,
                    'discount_percent' => $validated['discountPercent'] ?? 0,
                ]);
            }

            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
            ]);

            if (isset($validated['quantity']) || isset($validated['discountPercent'])) {
                $config = $campaign->configs()->first();
                if ($config) {
                    $config->update([
                        'quantity' => $validated['quantity'] ?? $config->quantity,
                        'discount_percent' => $validated['discountPercent'] ?? $config->discount_percent,
                    ]);
                } else {
                    CampaignConfig::create([
                        'campaign_id' => $campaign->id,
                        'quantity' => $validated['quantity'] ?? 0,
                        'discount_percent' => $validated['discountPercent'] ?? 0,
                    ]);
                }
            }

            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== VOUCHER METHODS ====================

    public function storeVoucher(Request $request)
    {
        try {
            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:campaigns,code',
                'name' => 'nullable|string|max:255',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Voucher ' . $validated['code'],
                'type' => 'voucher',
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'used' => 0,
                'expiry' => $validated['expiry'] ?? null,
                'status' => $validated['active'] ?? true ? 'active' : 'scheduled',
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateVoucher(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:campaigns,code,' . $id,
                'name' => 'nullable|string|max:255',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'expiry' => $validated['expiry'] ?? null,
                'status' => $validated['active'] ?? true ? 'active' : 'scheduled',
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteVoucher($id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $campaign->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function toggleVoucher($id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $newStatus = $campaign->status === 'active' ? 'scheduled' : 'active';
            $campaign->update(['status' => $newStatus]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'],
                'type' => 'preorder',
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_time' => $validated['start_date'] ?? null,
                'end_time' => $validated['end_date'] ?? null,
                'status' => $validated['active'] ?? true ? 'active' : 'scheduled',
                'min_order' => $validated['min_order'] ?? 0,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign->update([
                'name' => $validated['name'],
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_time' => $validated['start_date'] ?? null,
                'end_time' => $validated['end_date'] ?? null,
                'status' => $validated['active'] ?? true ? 'active' : 'scheduled',
                'min_order' => $validated['min_order'] ?? 0,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePreorder($id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $campaign->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->where('type', 'campaign')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Campaign extends Model
{
    use HasFactory;

    protected $fillable = [
        'name',
        'type',
        'code',
        'target_type',
        'discount_type',
        'discount_value',
        'min_order',
        'limit',
        'used',
        'expiry',
        'description',
        'start_time',
        'end_time',
        'status',
        'banner',
        'priority',
        'featured',
        'product_id',
        'tiers',
        'current_buyers',
    ];

    protected $casts = [
        'start_time' => 'datetime',
        'end_time' => 'datetime',
        'featured' => 'boolean',
        'tiers' => 'array',
        'discount_value' => 'decimal:0',
        'min_order' => 'decimal:0',
        'expiry' => 'date',
    ];

    public function productVariants()
    {
        return $this->belongsToMany(ProductVariant::class, 'campaign_products');
    }

    public function configs()
    {
        return $this->hasMany(CampaignConfig::class);
    }

    public function orders()
    {
        return $this->hasMany(Order::class);
    }

    public function product()
    {
        return $this->belongsTo(Product::class);
    }

    public function getNameAttribute()
    {
        if ($this->attributes['name'] ?? false) {
            return $this->attributes['name'];
        }
        if ($this->start_time && $this->end_time) {
            $start = $this->start_time->format('d/m/Y');
            $end = $this->end_time->format('d/m/Y');
            return "Chiến dịch {$start} - {$end}";
        }
        return 'Chiến dịch';
    }

    public function getStartDateAttribute()
    {
        return $this->start_time ? $this->start_time->format('Y-m-d') : null;
    }

    public function getEndDateAttribute()
    {
        return $this->end_time ? $this->end_time->format('Y-m-d') : null;
    }

    public function getDiscountAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->discount_percent . '%' : '0%';
    }

    public function getMinQuantityAttribute()
    {
        $config = $this->configs()->first();
        return $config ? $config->quantity : 0;
    }

    public function getProductIdsAttribute()
    {
        return $this->productVariants->pluck('id')->toArray();
    }
}

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class CampaignConfig extends Model
{
    use HasFactory;

    protected $fillable = ['campaign_id', 'quantity', 'discount_percent'];

    protected $casts = [
        'discount_percent' => 'decimal:2',
    ];

    public function campaign()
    {
        return $this->belongsTo(Campaign::class);
    }
}

<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Relations\Pivot;

class CampaignProduct extends Pivot
{
    use HasFactory;

    protected $table = 'campaign_products';
    public $incrementing = true;

    protected $fillable = ['product_variant_id', 'campaign_id'];
}

<?php

use App\Http\Controllers\Admin\DashboardController;
use App\Http\Controllers\Admin\OrderController as AdminOrderController;
use App\Http\Controllers\Admin\ProductController as AdminProductController;
use App\Http\Controllers\Admin\CustomerController as AdminCustomerController;
use App\Http\Controllers\Admin\CustomizeController as AdminCustomizeController;
use App\Http\Controllers\Admin\SettingController;
use App\Http\Controllers\Admin\CategoryController;
use App\Http\Controllers\Admin\ColorController;
use App\Http\Controllers\Admin\BrandController;
use App\Http\Controllers\Admin\BannerController;
use App\Http\Controllers\Admin\NewsController;
use App\Http\Controllers\Admin\PromotionController;
use App\Http\Controllers\PaymentController;
use App\Http\Controllers\OrderController;
use App\Http\Controllers\CartController;
use App\Http\Controllers\ProfileController;
use App\Http\Controllers\CategoryController as WebCategoryController;
use App\Http\Controllers\ProductController as WebProductController;
use App\Http\Controllers\HomeController;
use Illuminate\Foundation\Application;
use Illuminate\Support\Facades\Route;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\File;
use Inertia\Inertia;

// ==================== ROUTE ĐỂ PHỤC VỤ ẢNH ====================
Route::get('/image/{filename}', function ($filename) {
    $path = base_path('image/' . $filename);
    if (!File::exists($path)) {
        abort(404);
    }

    $file = File::get($path);
    $type = File::mimeType($path);

    return response($file, 200)->header('Content-Type', $type);
})->where('filename', '.*');

// ==================== WEB ROUTES (Frontend - Public) ====================

Route::get('/', [HomeController::class, 'index'])->name('home');

Route::get('/tim-kiem', function (Request $request) {
    return Inertia::render('Web/Category', ['search' => $request->get('q')]);
})->name('search');

// Product routes - PUBLIC
Route::get('/san-pham/{id}', [WebProductController::class, 'show'])->name('product.detail');
Route::get('/danh-muc/{slug}', [WebCategoryController::class, 'show'])->name('category');

// Other public frontend routes
Route::get('/mua-si', function () {
    return Inertia::render('Web/Wholesale');
})->name('wholesale');

Route::get('/khuyen-mai', function () {
    return Inertia::render('Web/Promotion');
})->name('promotion');

Route::get('/tuy-chinh', function () {
    return Inertia::render('Web/Customize');
})->name('customize');

// ==================== CART ROUTES (Yêu cầu đăng nhập) ====================

// Cart page
Route::get('/gio-hang', function () {
    return Inertia::render('Web/Cart');
})->name('cart')->middleware('auth');

// ⭐ QUAN TRỌNG: Cart API routes - Thêm middleware 'auth' và 'web'
Route::middleware(['auth'])->prefix('api')->group(function () {
    Route::get('/cart', [CartController::class, 'index']);
    Route::post('/cart/add', [CartController::class, 'add']);
    Route::put('/cart/update', [CartController::class, 'update']);
    Route::delete('/cart/remove/{variantId}', [CartController::class, 'remove']);
    Route::delete('/cart/clear', [CartController::class, 'clear']);
});

// ==================== CHECKOUT ROUTES (Yêu cầu đăng nhập) ====================
Route::middleware(['auth'])->group(function () {
    Route::get('/thanh-toan', [PaymentController::class, 'index'])->name('checkout');
    Route::post('/thanh-toan', [PaymentController::class, 'store'])->name('checkout.store');
    Route::get('/thanh-toan/thanh-cong', [PaymentController::class, 'success'])->name('checkout.success');
});

// ==================== AUTHENTICATED WEB ROUTES ====================
Route::middleware('auth')->group(function () {
    Route::get('/profile', [ProfileController::class, 'edit'])->name('profile.edit');
    Route::patch('/profile', [ProfileController::class, 'update'])->name('profile.update');
    Route::delete('/profile', [ProfileController::class, 'destroy'])->name('profile.destroy');
    Route::get('/don-hang', [OrderController::class, 'history'])->name('orders.history');
    Route::get('/don-hang/{id}', [OrderController::class, 'show'])->name('orders.show');
});

// ==================== ADMIN ROUTES ====================
Route::prefix('admin')->middleware(['auth', 'admin'])->name('admin.')->group(function () {
    // Dashboard
    Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');
    Route::get('/', [DashboardController::class, 'index'])->name('home');
    
    // Orders Management
    Route::prefix('orders')->group(function () {
        Route::get('/{type?}', [AdminOrderController::class, 'index'])
            ->where('type', 'retail|wholesale|preorder')
            ->name('orders.index');
        Route::get('/{id}', [AdminOrderController::class, 'show'])
            ->where('id', '[0-9]+')
            ->name('orders.show');
        Route::put('/{id}/status', [AdminOrderController::class, 'updateStatus'])->name('orders.update-status');
        Route::get('/export', [AdminOrderController::class, 'export'])->name('orders.export');
        Route::get('/export/filtered', [AdminOrderController::class, 'exportWithFilters'])->name('orders.export-filtered');
    });
    
    // Products Management
    Route::prefix('products')->group(function () {
        Route::get('/{type?}', [AdminProductController::class, 'index'])
            ->where('type', 'normal|preorder')
            ->name('products.index');
        Route::post('/', [AdminProductController::class, 'store'])->name('products.store');
        Route::put('/{product}', [AdminProductController::class, 'update'])->name('products.update');
        Route::delete('/{product}', [AdminProductController::class, 'destroy'])->name('products.destroy');
    });
    
    // Categories Management
    Route::get('/categories', [CategoryController::class, 'index'])->name('categories.index');
    Route::get('/categories/create', [CategoryController::class, 'create'])->name('categories.create');
    Route::post('/categories', [CategoryController::class, 'store'])->name('categories.store');
    Route::get('/categories/{category}/edit', [CategoryController::class, 'edit'])->name('categories.edit');
    Route::put('/categories/{category}', [CategoryController::class, 'update'])->name('categories.update');
    Route::delete('/categories/{category}', [CategoryController::class, 'destroy'])->name('categories.destroy');
    
    // Colors Management
    Route::get('/colors', [ColorController::class, 'index'])->name('colors.index');
    Route::get('/colors/data', [ColorController::class, 'getColors'])->name('colors.data');
    Route::post('/colors', [ColorController::class, 'store'])->name('colors.store');
    Route::put('/colors/{id}', [ColorController::class, 'update'])->name('colors.update');
    Route::delete('/colors/{id}', [ColorController::class, 'destroy'])->name('colors.destroy');
        
    // Brands Management
    Route::prefix('brands')->group(function () {
        Route::get('/', [BrandController::class, 'index'])->name('brands.index');
        Route::get('/data', [BrandController::class, 'getBrands'])->name('brands.data');
        Route::post('/', [BrandController::class, 'store'])->name('brands.store');
        Route::put('/{id}', [BrandController::class, 'update'])->name('brands.update');
        Route::delete('/{id}', [BrandController::class, 'destroy'])->name('brands.destroy');
        Route::get('/search', [BrandController::class, 'search'])->name('brands.search');
    });
    
    // Customers Management
    Route::prefix('customers')->group(function () {
        Route::get('/', [AdminCustomerController::class, 'index'])->name('customers.index');
        Route::get('/retail', [AdminCustomerController::class, 'retail'])->name('customers.retail');
        Route::get('/business', [AdminCustomerController::class, 'business'])->name('customers.business');
        Route::get('/{id}', [AdminCustomerController::class, 'show'])->name('customers.show');
        Route::put('/{id}', [AdminCustomerController::class, 'update'])->name('customers.update');
        Route::post('/export', [AdminCustomerController::class, 'export'])->name('customers.export');
    });
    
    // Customize Management
    Route::prefix('customize')->group(function () {
        Route::get('/', [AdminCustomizeController::class, 'index'])->name('customize.index');
        Route::put('/{id}/status', [AdminCustomizeController::class, 'updateStatus'])->name('customize.update-status');
        Route::put('/{id}/approve', [AdminCustomizeController::class, 'approve'])->name('customize.approve');
        Route::post('/send-quote', [AdminCustomizeController::class, 'sendQuote'])->name('customize.send-quote');
    });

        // News Management
    Route::prefix('news')->group(function () {
        Route::get('/', [NewsController::class, 'index'])->name('news.index');
        Route::get('/data', [NewsController::class, 'getNews'])->name('news.data');
        Route::post('/', [NewsController::class, 'store'])->name('news.store');
        Route::put('/{id}', [NewsController::class, 'update'])->name('news.update');
        Route::delete('/{id}', [NewsController::class, 'destroy'])->name('news.destroy');
        Route::patch('/{id}/status', [NewsController::class, 'updateStatus'])->name('news.update-status');
    });

       
    // Banner routes
    Route::get('/banners', [BannerController::class, 'index'])->name('banners.index');
    Route::get('/banners/data', [BannerController::class, 'getBanners'])->name('banners.data');
    Route::post('/banners', [BannerController::class, 'store'])->name('banners.store');
    Route::put('/banners/{id}', [BannerController::class, 'update'])->name('banners.update');
    Route::delete('/banners/{id}', [BannerController::class, 'destroy'])->name('banners.destroy');
    Route::patch('/banners/{id}/status', [BannerController::class, 'updateStatus'])->name('banners.status');
    Route::patch('/banners/{id}/order', [BannerController::class, 'updateOrder'])->name('banners.order');

    // Promotion routes
    Route::prefix('promotions')->group(function () {
        // Campaign routes
        Route::get('/', [PromotionController::class, 'index'])->name('promotions.index');
        Route::post('/campaign', [PromotionController::class, 'storeCampaign'])->name('promotions.campaign.store');
        Route::put('/campaign/{id}', [PromotionController::class, 'updateCampaign'])->name('promotions.campaign.update');
        Route::delete('/campaign/{id}', [PromotionController::class, 'deleteCampaign'])->name('promotions.campaign.delete');
        Route::put('/campaign/{id}/status', [PromotionController::class, 'updateCampaignStatus'])->name('promotions.campaign.status');
        Route::get('/campaigns/list', [PromotionController::class, 'getCampaignsList'])->name('promotions.campaigns.list');

        // Voucher routes
        Route::post('/voucher', [PromotionController::class, 'storeVoucher'])->name('promotions.voucher.store');
        Route::put('/voucher/{id}', [PromotionController::class, 'updateVoucher'])->name('promotions.voucher.update');
        Route::delete('/voucher/{id}', [PromotionController::class, 'deleteVoucher'])->name('promotions.voucher.delete');
        Route::put('/voucher/{id}/toggle', [PromotionController::class, 'toggleVoucher'])->name('promotions.voucher.toggle');

        // Pre-order routes
        Route::post('/preorder', [PromotionController::class, 'storePreorder'])->name('promotions.preorder.store');
        Route::put('/preorder/{id}', [PromotionController::class, 'updatePreorder'])->name('promotions.preorder.update');
        Route::delete('/preorder/{id}', [PromotionController::class, 'deletePreorder'])->name('promotions.preorder.delete');
    });


    // Reports
    Route::get('/reports', function () {
        return Inertia::render('Admin/Reports');
    })->name('reports.index');
    
    // Settings
    Route::get('/settings', [SettingController::class, 'index'])->name('settings.index');
    Route::put('/settings/general', [SettingController::class, 'updateGeneral'])->name('settings.updateGeneral');
    Route::put('/settings/password', [SettingController::class, 'changePassword'])->name('settings.changePassword');

    // User Management
    Route::get('/settings/users', [SettingController::class, 'getUsers'])->name('settings.users');
    Route::post('/settings/users', [SettingController::class, 'storeUser'])->name('settings.storeUser');
    Route::put('/settings/users/{id}', [SettingController::class, 'updateUser'])->name('settings.updateUser');
    Route::delete('/settings/users/{id}', [SettingController::class, 'destroyUser'])->name('settings.destroyUser');
    Route::patch('/settings/users/{id}/toggle', [SettingController::class, 'toggleUserStatus'])->name('settings.toggleUser');
});

require __DIR__.'/auth.php';

<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    promotions: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const promotions = ref(props.promotions || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = promotions.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

// Campaigns - chỉ lấy các item có type là 'campaign' hoặc không có type (mặc định)
const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

// Vouchers
const filteredVouchers = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'voucher');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const voucherCounts = computed(() => {
    const vouchers = (promotions.value || []).filter(p => p.type === 'voucher');
    return {
        all: vouchers.length,
        active: vouchers.filter(v => v.active).length,
        inactive: vouchers.filter(v => !v.active).length
    };
});

// Pre-orders
const filteredPreorders = computed(() => {
    let filtered = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const preorderCounts = computed(() => {
    const preorders = (promotions.value || []).filter(p => p.type === 'preorder_tier');
    return {
        all: preorders.length,
        active: preorders.filter(p => p.active).length,
        inactive: preorders.filter(p => !p.active).length
    };
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'voucher').length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ promotions.filter(p => p.type === 'preorder_tier').length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-36 bg-gradient-to-r from-blue-50 to-blue-100">
                        <div class="w-full h-full flex items-center justify-center text-blue-300">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-2 right-2 flex gap-1 flex-wrap">
                            <span class="text-[10px] px-2 py-0.5 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-[10px] px-2 py-0.5 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐</span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">{{ campaign.name }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openCampaignModal(campaign)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giảm giá</p>
                                <p class="text-sm font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">SL tối thiểu</p>
                                <p class="text-sm font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-0.5 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-[10px] px-3 py-1 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-28 bg-gradient-to-r from-orange-50 to-orange-100">
                        <div class="w-full h-full flex items-center justify-center text-orange-300">
                            <span class="material-symbols-outlined text-4xl">local_offer</span>
                        </div>
                        <div class="absolute top-2 right-2">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="voucher.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.active ? '🟢 Hoạt động' : '🔴 Đã tắt' }}
                            </span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-base text-gray-800 truncate">{{ voucher.code }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ getDiscountTypeLabel(voucher.discount_type) }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openVoucherModal(voucher)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteVoucher(voucher.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ voucher.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giá trị</p>
                                <p class="text-sm font-bold text-orange-600">
                                    {{ voucher.discount_type === 'percent' ? voucher.discount_value + '%' : formatPrice(voucher.discount_value) }}
                                </p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Đã dùng</p>
                                <p class="text-sm font-bold text-blue-600">{{ voucher.used }}/{{ voucher.limit }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">event</span>
                            <span>HSD: {{ formatDate(voucher.expiry) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">target</span>
                            <span>{{ getTargetTypeLabel(voucher.target_type) }}</span>
                            <span v-if="voucher.min_order > 0" class="ml-1">- Đơn tối thiểu: {{ formatPrice(voucher.min_order) }}</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleVoucher(voucher)" 
                                :class="voucher.active ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'"
                                class="text-[10px] px-3 py-1 rounded-full transition-colors"
                            >
                                {{ voucher.active ? '✅ Kích hoạt' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-28 bg-gradient-to-r from-purple-50 to-purple-100">
                        <div class="w-full h-full flex items-center justify-center text-purple-300">
                            <span class="material-symbols-outlined text-4xl">schedule</span>
                        </div>
                        <div class="absolute top-2 right-2">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="preorder.active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.active ? '🟢 Hoạt động' : '🔴 Đã tắt' }}
                            </span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">⏳ {{ preorder.code }}</h3>
                                <p class="text-[10px] text-gray-500 truncate">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openPreorderModal(preorder)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deletePreorder(preorder.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <div class="mt-2 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-2">
                            <div class="flex justify-between items-center mb-1">
                                <span class="text-[10px] font-medium">Đã đặt: {{ preorder.current_buyers || 0 }} lượt</span>
                                <span class="text-xs font-bold text-blue-600">
                                    {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                                </span>
                            </div>

                            <div class="w-full h-1.5 bg-gray-200 rounded-full overflow-hidden">
                                <div 
                                    class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                    :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                                ></div>
                            </div>

                            <div class="mt-1.5 grid grid-cols-3 gap-1 text-[10px]">
                                <div v-for="tier in preorder.tiers" :key="tier.from" 
                                    class="text-center p-1 bg-white rounded border"
                                    :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                                >
                                    <div class="font-bold">{{ tier.discount }}%</div>
                                    <div class="text-gray-500 text-[8px]">#{{ tier.from }}-{{ tier.to }}</div>
                                </div>
                            </div>
                        </div>

                        <div class="mt-2 flex justify-between items-center text-[10px] text-gray-500">
                            <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                            <span v-if="preorder.min_order > 0">💰 {{ formatPrice(preorder.min_order) }}</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleVoucher(preorder)"
                                :class="preorder.active ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'"
                                class="text-[10px] px-3 py-1 rounded-full transition-colors"
                            >
                                {{ preorder.active ? '✅ Kích hoạt' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <!-- ... giữ nguyên nội dung campaign modal ... -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- Sản phẩm áp dụng -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>


Hãy chỉnh và đưa code hoàn chỉnh


## Assistant:
07/09/2026 19:43

Tôi thấy vấn đề của bạn: **Voucher và Pre-order đang hiển thị trong tab Chiến dịch** vì code Vue đang lấy dữ liệu từ `campaigns` và hiển thị tất cả mà không phân loại.

Vì bạn đã xóa bảng `promotions`, tất cả dữ liệu đang được lưu trong bảng `campaigns` với trường `campaign_type` để phân biệt. Tôi sẽ chỉnh lại code để:

1. **Controller** trả về 3 danh sách riêng: `campaigns`, `vouchers`, `preorders`
2. **Vue** hiển thị đúng dữ liệu cho từng tab

## 1. Controller `PromotionController.php` (Chỉnh sửa)

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use App\Models\Campaign;
use App\Models\CampaignConfig;
use App\Models\Product;
use App\Models\ProductVariant;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Log;
use Illuminate\Support\Facades\DB;

class PromotionController extends Controller
{
    public function index()
    {
        try {
            // Lấy tất cả campaigns
            $allCampaigns = Campaign::with(['configs', 'productVariants', 'productVariants.product', 'productVariants.color', 'product'])
                ->latest()
                ->get()
                ->map(function ($campaign) {
                    $config = $campaign->configs->first();
                    
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name ?? 'Chiến dịch #' . $campaign->id,
                        'type' => $campaign->type ?? 'seasonal',
                        'campaign_type' => $campaign->campaign_type ?? 'campaign',
                        'code' => $campaign->code,
                        'target_type' => $campaign->target_type,
                        'discount_type' => $campaign->discount_type,
                        'discount_value' => $campaign->discount_value,
                        'min_order' => $campaign->min_order,
                        'limit' => $campaign->limit,
                        'used' => $campaign->used ?? 0,
                        'expiry' => $campaign->expiry,
                        'description' => $campaign->description ?? '',
                        'startDate' => $campaign->start_time ? $campaign->start_time->format('Y-m-d') : null,
                        'endDate' => $campaign->end_time ? $campaign->end_time->format('Y-m-d') : null,
                        'status' => $campaign->status ?? 'scheduled',
                        'priority' => $campaign->priority ?? 0,
                        'featured' => $campaign->featured ?? false,
                        'quantity' => $campaign->quantity ?? ($config ? (int) $config->quantity : 0),
                        'discountPercent' => $campaign->discount_percent ?? ($config ? (float) $config->discount_percent : 0),
                        'discount' => $campaign->discount_percent ? $campaign->discount_percent . '%' : ($config ? (float) $config->discount_percent . '%' : '0%'),
                        'products' => $campaign->productVariants->pluck('id')->toArray(),
                        'product_id' => $campaign->product_id,
                        'tiers' => $campaign->tiers,
                        'current_buyers' => $campaign->current_buyers ?? 0,
                        'active' => $campaign->status === 'active',
                    ];
                });

            // Phân loại theo campaign_type
            $campaigns = $allCampaigns->filter(fn($item) => $item['campaign_type'] === 'campaign')->values();
            $vouchers = $allCampaigns->filter(fn($item) => $item['campaign_type'] === 'voucher')->values();
            $preorders = $allCampaigns->filter(fn($item) => $item['campaign_type'] === 'preorder')->values();

            // Lấy banners
            $banners = Banner::with('campaign')->orderBy('order', 'asc')->get()->map(function ($banner) {
                return [
                    'id' => $banner->id,
                    'title' => $banner->title ?? 'Banner #' . $banner->id,
                    'image' => $banner->image,
                    'link' => $banner->link,
                    'description' => $banner->description,
                    'status' => $banner->status ? 1 : 0,
                    'order' => $banner->order,
                    'campaign_id' => $banner->campaign_id,
                    'campaign' => $banner->campaign ? [
                        'id' => $banner->campaign->id,
                        'name' => $banner->campaign->name,
                    ] : null,
                ];
            });

            // Lấy product variants
            $productVariants = ProductVariant::with(['product', 'color'])
                ->get()
                ->map(function ($variant) {
                    return [
                        'id' => $variant->id,
                        'name' => $variant->product ? $variant->product->name : 'Sản phẩm',
                        'product' => $variant->product ? [
                            'id' => $variant->product->id,
                            'name' => $variant->product->name,
                            'is_preorder' => $variant->product->is_preorder ?? false,
                        ] : null,
                        'color' => $variant->color ? [
                            'id' => $variant->color->id,
                            'name' => $variant->color->name,
                        ] : null,
                        'price' => $variant->price ?? 0,
                        'size_name' => $variant->size_name ?? '',
                        'stock' => $variant->stock ?? 0,
                    ];
                });

            // Lấy sản phẩm pre-order
            $preorderProducts = Product::where('is_preorder', true)
                ->with(['variants.color'])
                ->latest()
                ->get()
                ->map(function ($product) {
                    return [
                        'id' => $product->id,
                        'name' => $product->name,
                        'variants' => $product->variants->map(function ($variant) {
                            return [
                                'id' => $variant->id,
                                'color' => $variant->color ? [
                                    'id' => $variant->color->id,
                                    'name' => $variant->color->name,
                                ] : null,
                                'price' => $variant->price ?? 0,
                                'size_name' => $variant->size_name ?? '',
                            ];
                        }),
                    ];
                });

            return Inertia::render('Admin/Promotions', [
                'campaigns' => $campaigns,
                'vouchers' => $vouchers,
                'preorders' => $preorders,
                'banners' => $banners,
                'products' => [],
                'productVariants' => $productVariants,
                'preorderProducts' => $preorderProducts,
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi load trang promotions: ' . $e->getMessage());
            return Inertia::render('Admin/Promotions', [
                'campaigns' => [],
                'vouchers' => [],
                'preorders' => [],
                'banners' => [],
                'products' => [],
                'productVariants' => [],
                'preorderProducts' => [],
                'error' => 'Có lỗi xảy ra khi tải dữ liệu: ' . $e->getMessage()
            ]);
        }
    }

    // ==================== CAMPAIGN METHODS ====================

    public function storeCampaign(Request $request)
    {
        try {
            DB::beginTransaction();

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Chiến dịch ' . now()->format('d/m/Y'),
                'type' => $validated['type'] ?? 'seasonal',
                'campaign_type' => 'campaign',
                'description' => $validated['description'] ?? '',
                'start_time' => $validated['startDate'] ?? null,
                'end_time' => $validated['endDate'] ?? null,
                'status' => $validated['status'] ?? 'scheduled',
                'priority' => $validated['priority'] ?? 0,
                'featured' => $validated['featured'] ?? false,
                'quantity' => $validated['quantity'] ?? 0,
                'discount_percent' => $validated['discountPercent'] ?? 0,
            ]);

            if (!empty($validated['products']) && is_array($validated['products'])) {
                $campaign->productVariants()->attach($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi tạo chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaign(Request $request, $id)
    {
        try {
            DB::beginTransaction();

            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'nullable|string|max:255',
                'type' => 'nullable|string|max:50',
                'description' => 'nullable|string',
                'startDate' => 'nullable|date',
                'endDate' => 'nullable|date|after_or_equal:startDate',
                'status' => 'nullable|in:scheduled,active,ended',
                'priority' => 'nullable|integer|min:0',
                'featured' => 'boolean',
                'quantity' => 'nullable|integer|min:0',
                'discountPercent' => 'nullable|numeric|min:0|max:100',
                'products' => 'nullable|array',
                'products.*' => 'exists:product_variants,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'type' => $validated['type'] ?? $campaign->type,
                'description' => $validated['description'] ?? $campaign->description,
                'start_time' => $validated['startDate'] ?? $campaign->start_time,
                'end_time' => $validated['endDate'] ?? $campaign->end_time,
                'status' => $validated['status'] ?? $campaign->status,
                'priority' => $validated['priority'] ?? $campaign->priority,
                'featured' => $validated['featured'] ?? $campaign->featured,
                'quantity' => $validated['quantity'] ?? $campaign->quantity,
                'discount_percent' => $validated['discountPercent'] ?? $campaign->discount_percent,
            ]);

            if (isset($validated['products'])) {
                $campaign->productVariants()->sync($validated['products']);
            }

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi cập nhật chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteCampaign($id)
    {
        try {
            DB::beginTransaction();
            
            $campaign = Campaign::findOrFail($id);
            
            Banner::where('campaign_id', $campaign->id)->update(['campaign_id' => null]);
            
            $campaign->configs()->delete();
            $campaign->productVariants()->detach();
            $campaign->delete();

            DB::commit();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chiến dịch thành công!'
            ]);

        } catch (\Exception $e) {
            DB::rollBack();
            Log::error('Lỗi xóa chiến dịch: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateCampaignStatus(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $request->validate(['status' => 'required|in:scheduled,active,ended']);
            $campaign->update(['status' => $request->status]);
            
            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== VOUCHER METHODS ====================

    public function storeVoucher(Request $request)
    {
        try {
            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:campaigns,code',
                'name' => 'nullable|string|max:255',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'] ?? 'Voucher ' . $validated['code'],
                'type' => 'voucher',
                'campaign_type' => 'voucher',
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'used' => 0,
                'expiry' => $validated['expiry'] ?? null,
                'status' => $validated['active'] ? 'active' : 'scheduled',
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updateVoucher(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'code' => 'required|string|max:50|unique:campaigns,code,' . $id,
                'name' => 'nullable|string|max:255',
                'target_type' => 'required|in:retail,wholesale,preorder,all',
                'discount_type' => 'required|in:fixed,percent,freeship',
                'discount_value' => 'required|numeric|min:0',
                'min_order' => 'nullable|numeric|min:0',
                'limit' => 'nullable|integer|min:0',
                'expiry' => 'nullable|date',
                'active' => 'boolean',
                'description' => 'nullable|string',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign->update([
                'name' => $validated['name'] ?? $campaign->name,
                'code' => strtoupper($validated['code']),
                'target_type' => $validated['target_type'],
                'discount_type' => $validated['discount_type'],
                'discount_value' => $validated['discount_value'],
                'min_order' => $validated['min_order'] ?? 0,
                'limit' => $validated['limit'] ?? 100,
                'expiry' => $validated['expiry'] ?? null,
                'status' => $validated['active'] ? 'active' : 'scheduled',
                'description' => $validated['description'] ?? "Giảm " . ($validated['discount_type'] === 'percent' ? $validated['discount_value'] . '%' : number_format($validated['discount_value']) . '₫'),
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deleteVoucher($id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $campaign->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa mã giảm giá thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa mã giảm giá: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function toggleVoucher($id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $newStatus = $campaign->status === 'active' ? 'scheduled' : 'active';
            $campaign->update(['status' => $newStatus]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật trạng thái thành công!'
            ]);

        } catch (\Exception $e) {
            return redirect()->back()->with([
                'success' => false,
                'message' => $e->getMessage()
            ]);
        }
    }

    // ==================== PRE-ORDER METHODS ====================

    public function storePreorder(Request $request)
    {
        try {
            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign = Campaign::create([
                'name' => $validated['name'],
                'type' => 'preorder',
                'campaign_type' => 'preorder',
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_time' => $validated['start_date'] ?? null,
                'end_time' => $validated['end_date'] ?? null,
                'status' => $validated['active'] ? 'active' : 'scheduled',
                'min_order' => $validated['min_order'] ?? 0,
                'current_buyers' => 0,
                'description' => "Giảm giá theo số lượt đặt trước",
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Thêm chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi tạo pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function updatePreorder(Request $request, $id)
    {
        try {
            $campaign = Campaign::findOrFail($id);

            $validated = $request->validate([
                'name' => 'required|string|max:255',
                'product_id' => 'required|exists:products,id',
                'tiers' => 'required|array|min:1',
                'tiers.*.from' => 'required|integer|min:1',
                'tiers.*.to' => 'required|integer|gt:tiers.*.from',
                'tiers.*.discount' => 'required|integer|min:0|max:100',
                'start_date' => 'nullable|date',
                'end_date' => 'nullable|date|after_or_equal:start_date',
                'active' => 'boolean',
                'min_order' => 'nullable|numeric|min:0',
                'campaign_id' => 'nullable|exists:campaigns,id',
            ]);

            $campaign->update([
                'name' => $validated['name'],
                'product_id' => $validated['product_id'],
                'tiers' => $validated['tiers'],
                'start_time' => $validated['start_date'] ?? null,
                'end_time' => $validated['end_date'] ?? null,
                'status' => $validated['active'] ? 'active' : 'scheduled',
                'min_order' => $validated['min_order'] ?? 0,
            ]);

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Cập nhật chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function deletePreorder($id)
    {
        try {
            $campaign = Campaign::findOrFail($id);
            $campaign->delete();

            return redirect()->route('admin.promotions.index')->with([
                'success' => true,
                'message' => 'Xóa chương trình pre-order thành công!'
            ]);

        } catch (\Exception $e) {
            Log::error('Lỗi xóa pre-order: ' . $e->getMessage());
            return redirect()->back()->with([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ]);
        }
    }

    public function getCampaignsList()
    {
        try {
            $campaigns = Campaign::select('id', 'name', 'status', 'start_time', 'end_time')
                ->where('campaign_type', 'campaign')
                ->orderBy('start_time', 'desc')
                ->get()
                ->map(function ($campaign) {
                    return [
                        'id' => $campaign->id,
                        'name' => $campaign->name,
                        'status' => $campaign->status,
                        'start_time' => $campaign->start_time,
                        'end_time' => $campaign->end_time,
                    ];
                });
            
            return response()->json($campaigns);
        } catch (\Exception $e) {
            return response()->json([]);
        }
    }
}
```

## 2. Vue Component `Promotions.vue` (Chỉnh sửa phần script)

Thay thế phần script trong file Vue của bạn bằng đoạn code dưới đây (phần computed và props):

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    vouchers: {
        type: Array,
        default: () => []
    },
    preorders: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const vouchers = ref(props.vouchers || []);
const preorders = ref(props.preorders || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = vouchers.value.find(v => v.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = preorders.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

// Campaigns
const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

// Vouchers
const filteredVouchers = computed(() => {
    let filtered = vouchers.value || [];
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const voucherCounts = computed(() => {
    const vouchersList = vouchers.value || [];
    return {
        all: vouchersList.length,
        active: vouchersList.filter(v => v.status === 'active').length,
        inactive: vouchersList.filter(v => v.status !== 'active').length
    };
});

// Pre-orders
const filteredPreorders = computed(() => {
    let filtered = preorders.value || [];
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const preorderCounts = computed(() => {
    const preordersList = preorders.value || [];
    return {
        all: preordersList.length,
        active: preordersList.filter(p => p.status === 'active').length,
        inactive: preordersList.filter(p => p.status !== 'active').length
    };
});

onMounted(() => {
    showFlashMessage();
});
</script>
```

**Quan trọng:** Phần template trong file Vue của bạn giữ nguyên như cũ, vì nó đã hiển thị đúng theo các biến `campaigns`, `vouchers`, `preorders`.

## Tóm tắt thay đổi:

1. **Controller**:
   - Thay đổi `index()` để trả về 3 danh sách riêng: `campaigns`, `vouchers`, `preorders`
   - Phân loại dựa trên trường `campaign_type`
   - Các method `storeCampaign`, `updateCampaign` đã được cập nhật để lưu `campaign_type`

2. **Vue Component**:
   - Props thay đổi: nhận `vouchers` và `preorders` thay vì `promotions`
   - Computed `filteredVouchers` lấy từ `vouchers.value` thay vì `promotions.value`
   - Computed `filteredPreorders` lấy từ `preorders.value` thay vì `promotions.value`

3. **Template** giữ nguyên, vì nó đã sử dụng các biến `filteredVouchers` và `filteredPreorders`


## User:
07/09/2026 19:48

at renderComponentRoot (vue.runtime.esm-bundler-CJPQuahF.js?v=fa1f6f1c:4707:35)
    at ReactiveEffect.componentUpdateFn [as fn] (vue.runtime.esm-bundler-CJPQuahF.js?v=fa1f6f1c:5557:41)
    at ReactiveEffect.run (vue.runtime.esm-bundler-CJPQuahF.js?v=fa1f6f1c:426:16)
    at setupRenderEffect (vue.runtime.esm-bundler-CJPQuahF.js?v=fa1f6f1c:5628:3)
    at mountComponent (vue.runtime.esm-bundler-CJPQuahF.js?v=fa1f6f1c:5512:10)
    at processComponent (vue.runtime.esm-bundler-CJPQuahF.js?v=fa1f6f1c:5492:8)


## Assistant:
07/09/2026 19:48

Lỗi này thường xảy ra khi Vue component không nhận đúng props hoặc dữ liệu bị undefined. Vì bạn đã thay đổi props từ `promotions` thành `vouchers` và `preorders` nhưng template vẫn đang gọi `promotions`.

Tôi sẽ đưa cho bạn code hoàn chỉnh đã sửa lỗi:

## File `Promotions.vue` - Hoàn chỉnh

```vue
<script setup>
import { ref, computed, watch, onMounted } from 'vue';
import AdminLayout from '@/Layouts/AdminLayout.vue';
import { Head, router, usePage } from '@inertiajs/vue3';

// Props - ĐÃ SỬA: nhận vouchers và preorders thay vì promotions
const props = defineProps({
    campaigns: {
        type: Array,
        default: () => []
    },
    vouchers: {
        type: Array,
        default: () => []
    },
    preorders: {
        type: Array,
        default: () => []
    },
    banners: {
        type: Array,
        default: () => []
    },
    products: {
        type: Array,
        default: () => []
    },
    productVariants: {
        type: Array,
        default: () => []
    },
    preorderProducts: {
        type: Array,
        default: () => []
    }
});

// Flash messages
const page = usePage();
const flash = computed(() => page.props.flash || {});
const showSuccess = ref(false);
const successMessage = ref('');

// State
const campaigns = ref(props.campaigns || []);
const vouchers = ref(props.vouchers || []);
const preorders = ref(props.preorders || []);
const banners = ref(props.banners || []);
const products = ref(props.products || []);
const productVariants = ref(props.productVariants || []);
const preorderProducts = ref(props.preorderProducts || []);

// Search & Filter
const searchQuery = ref('');
const activeTab = ref('campaigns');
const activeStatusTab = ref('all');

// Modal
const showModal = ref(false);
const showVoucherModal = ref(false);
const showPreorderModal = ref(false);
const editingCampaign = ref(null);
const editingVoucher = ref(null);
const editingPreorder = ref(null);
const isSubmitting = ref(false);
const errorMessage = ref('');

// Status options
const statusOptions = [
    { value: 'all', label: 'Tất cả' },
    { value: 'active', label: 'Đang diễn ra' },
    { value: 'ended', label: 'Đã kết thúc' },
    { value: 'scheduled', label: 'Sắp diễn ra' }
];

// Campaign Form
const campaignForm = ref({
    id: null,
    name: '',
    type: 'seasonal',
    description: '',
    startDate: '',
    endDate: '',
    status: 'scheduled',
    priority: 0,
    featured: false,
    quantity: 1,
    discountPercent: 0,
    products: []
});

// Voucher Form
const voucherForm = ref({
    id: null,
    code: '',
    target_type: 'all',
    discount_type: 'percent',
    discount_value: 0,
    min_order: 0,
    limit: 100,
    expiry: '',
    active: true,
    description: '',
    campaign_id: null
});

// Preorder Form
const preorderForm = ref({
    id: null,
    name: '',
    product_id: null,
    tiers: [
        { from: 1, to: 10, discount: 20 },
        { from: 11, to: 20, discount: 10 },
        { from: 21, to: 30, discount: 5 }
    ],
    start_date: '',
    end_date: '',
    active: true,
    min_order: 0,
    campaign_id: null
});

// ==================== HELPER FUNCTIONS ====================

const formatDate = (date) => {
    if (!date) return '';
    const d = new Date(date);
    return d.toLocaleDateString('vi-VN', { day: '2-digit', month: '2-digit', year: 'numeric' });
};

const formatPrice = (value) => {
    if (!value) return '0₫';
    return Number(value).toLocaleString('vi-VN') + '₫';
};

const getStatusClass = (status) => {
    const classes = {
        active: 'bg-green-100 text-green-700',
        ended: 'bg-gray-100 text-gray-600',
        scheduled: 'bg-blue-100 text-blue-700'
    };
    return classes[status] || 'bg-gray-100 text-gray-600';
};

const getStatusLabel = (status) => {
    const labels = {
        active: 'Đang diễn ra',
        ended: 'Đã kết thúc',
        scheduled: 'Sắp diễn ra'
    };
    return labels[status] || status;
};

const getDiscountTypeLabel = (type) => {
    const labels = {
        fixed: 'Giảm trực tiếp',
        percent: 'Giảm theo %',
        freeship: 'Miễn phí ship'
    };
    return labels[type] || type;
};

const getTargetTypeLabel = (type) => {
    const labels = {
        retail: 'Bán lẻ',
        wholesale: 'Bán sỉ',
        preorder: 'Pre-order',
        all: 'Tất cả'
    };
    return labels[type] || type;
};

const getProductName = (productId) => {
    if (!productId) return 'Chưa chọn';
    const product = products.value.find(p => p.id === productId);
    return product ? product.name : 'Sản phẩm không tồn tại';
};

// ==================== FUNCTION TÍNH TRẠNG THÁI ====================

const calculateCampaignStatus = (startDate, endDate) => {
    if (!startDate) return 'scheduled';
    
    const today = new Date();
    today.setHours(0, 0, 0, 0);
    
    const start = new Date(startDate);
    start.setHours(0, 0, 0, 0);
    
    if (start.getTime() > today.getTime()) {
        return 'scheduled';
    }
    
    if (endDate) {
        const end = new Date(endDate);
        end.setHours(0, 0, 0, 0);
        
        if (end.getTime() < today.getTime()) {
            return 'ended';
        }
        
        if (end.getTime() >= today.getTime() && start.getTime() <= today.getTime()) {
            return 'active';
        }
    }
    
    if (start.getTime() <= today.getTime()) {
        return 'active';
    }
    
    return 'scheduled';
};

// ==================== DATE FUNCTIONS ====================

const today = new Date().toISOString().split('T')[0];

const minEndDate = computed(() => {
    return campaignForm.value.startDate || today;
});

const updateStatusFromDates = () => {
    const startDate = campaignForm.value.startDate;
    const endDate = campaignForm.value.endDate;
    campaignForm.value.status = calculateCampaignStatus(startDate, endDate);
};

watch(() => campaignForm.value.startDate, updateStatusFromDates);
watch(() => campaignForm.value.endDate, updateStatusFromDates);

// ==================== RELOAD FUNCTION ====================

const reloadPage = () => {
    window.location.reload();
};

// ==================== CAMPAIGN FUNCTIONS ====================

const openCampaignModal = (campaign = null) => {
    editingCampaign.value = campaign;
    errorMessage.value = '';
    
    if (campaign) {
        campaignForm.value = {
            id: campaign.id,
            name: campaign.name || '',
            type: campaign.type || 'seasonal',
            description: campaign.description || '',
            startDate: campaign.startDate || '',
            endDate: campaign.endDate || '',
            status: campaign.status || 'scheduled',
            priority: campaign.priority || 0,
            featured: campaign.featured || false,
            quantity: campaign.quantity || 1,
            discountPercent: campaign.discountPercent || 0,
            products: campaign.products || []
        };
    } else {
        campaignForm.value = {
            id: null,
            name: '',
            type: 'seasonal',
            description: '',
            startDate: '',
            endDate: '',
            status: 'scheduled',
            priority: 0,
            featured: false,
            quantity: 1,
            discountPercent: 0,
            products: []
        };
    }
    showModal.value = true;
};

const saveCampaign = async () => {
    if (!campaignForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chiến dịch';
        return;
    }
    
    if (!campaignForm.value.quantity || campaignForm.value.quantity <= 0) {
        errorMessage.value = 'Số lượng tối thiểu phải lớn hơn 0';
        return;
    }
    
    if (campaignForm.value.discountPercent < 0 || campaignForm.value.discountPercent > 100) {
        errorMessage.value = 'Giảm giá phải từ 0% đến 100%';
        return;
    }
    
    if (campaignForm.value.startDate && campaignForm.value.endDate) {
        if (new Date(campaignForm.value.endDate) < new Date(campaignForm.value.startDate)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        const status = calculateCampaignStatus(campaignForm.value.startDate, campaignForm.value.endDate);
        
        const data = {
            name: campaignForm.value.name,
            type: campaignForm.value.type,
            description: campaignForm.value.description,
            startDate: campaignForm.value.startDate,
            endDate: campaignForm.value.endDate,
            status: status,
            priority: parseInt(campaignForm.value.priority) || 0,
            featured: campaignForm.value.featured || false,
            quantity: parseInt(campaignForm.value.quantity) || 1,
            discountPercent: parseFloat(campaignForm.value.discountPercent) || 0,
            products: Array.isArray(campaignForm.value.products) ? campaignForm.value.products : []
        };
        
        if (editingCampaign.value) {
            await router.put(`/admin/promotions/campaign/${editingCampaign.value.id}`, data);
        } else {
            await router.post('/admin/promotions/campaign', data);
        }
        
        closeModal();
        reloadPage();
        
    } catch (error) {
        console.error('Lỗi:', error);
        if (error.response?.data?.errors) {
            const errors = error.response.data.errors;
            errorMessage.value = Object.values(errors).flat().join(', ');
        } else {
            errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        }
        isSubmitting.value = false;
    }
};

const deleteCampaign = async (id) => {
    const campaign = campaigns.value.find(c => c.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chiến dịch "${campaign?.name}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/campaign/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleCampaignStatus = async (campaign) => {
    const newStatus = campaign.status === 'active' ? 'ended' : 'active';
    try {
        await router.put(`/admin/promotions/campaign/${campaign.id}/status`, { status: newStatus });
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRODUCT SELECTION ====================

const toggleProduct = (variantId) => {
    const index = campaignForm.value.products.indexOf(variantId);
    if (index > -1) {
        campaignForm.value.products.splice(index, 1);
    } else {
        campaignForm.value.products.push(variantId);
    }
};

const isProductSelected = (variantId) => {
    return campaignForm.value.products.includes(variantId);
};

// ==================== VOUCHER FUNCTIONS ====================

const openVoucherModal = (voucher = null) => {
    editingVoucher.value = voucher;
    errorMessage.value = '';
    
    if (voucher) {
        voucherForm.value = {
            id: voucher.id,
            code: voucher.code || '',
            target_type: voucher.target_type || 'all',
            discount_type: voucher.discount_type || 'percent',
            discount_value: voucher.discount_value || 0,
            min_order: voucher.min_order || 0,
            limit: voucher.limit || 100,
            expiry: voucher.expiry || '',
            active: voucher.active !== undefined ? voucher.active : true,
            description: voucher.description || '',
            campaign_id: voucher.campaign_id || null
        };
    } else {
        voucherForm.value = {
            id: null,
            code: '',
            target_type: 'all',
            discount_type: 'percent',
            discount_value: 0,
            min_order: 0,
            limit: 100,
            expiry: '',
            active: true,
            description: '',
            campaign_id: null
        };
    }
    showVoucherModal.value = true;
};

const saveVoucher = async () => {
    if (!voucherForm.value.code) {
        errorMessage.value = 'Vui lòng nhập mã giảm giá';
        return;
    }
    
    if (voucherForm.value.discount_value < 0) {
        errorMessage.value = 'Giá trị giảm giá không được âm';
        return;
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingVoucher.value) {
            await router.put(`/admin/promotions/voucher/${editingVoucher.value.id}`, voucherForm.value);
        } else {
            await router.post('/admin/promotions/voucher', voucherForm.value);
        }
        closeVoucherModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deleteVoucher = async (id) => {
    const voucher = vouchers.value.find(v => v.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa mã "${voucher?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/voucher/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const toggleVoucher = async (voucher) => {
    try {
        await router.put(`/admin/promotions/voucher/${voucher.id}/toggle`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra');
    }
};

// ==================== PRE-ORDER FUNCTIONS ====================

const openPreorderModal = (preorder = null) => {
    editingPreorder.value = preorder;
    errorMessage.value = '';
    
    if (preorder) {
        preorderForm.value = {
            id: preorder.id,
            name: preorder.code || '',
            product_id: preorder.product_id || null,
            tiers: preorder.tiers || [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: preorder.start_date || '',
            end_date: preorder.end_date || '',
            active: preorder.active !== undefined ? preorder.active : true,
            min_order: preorder.min_order || 0,
            campaign_id: preorder.campaign_id || null
        };
    } else {
        preorderForm.value = {
            id: null,
            name: '',
            product_id: null,
            tiers: [
                { from: 1, to: 10, discount: 20 },
                { from: 11, to: 20, discount: 10 },
                { from: 21, to: 30, discount: 5 }
            ],
            start_date: '',
            end_date: '',
            active: true,
            min_order: 0,
            campaign_id: null
        };
    }
    showPreorderModal.value = true;
};

const savePreorder = async () => {
    if (!preorderForm.value.name) {
        errorMessage.value = 'Vui lòng nhập tên chương trình';
        return;
    }
    if (!preorderForm.value.product_id) {
        errorMessage.value = 'Vui lòng chọn sản phẩm pre-order';
        return;
    }
    
    if (preorderForm.value.start_date && preorderForm.value.end_date) {
        if (new Date(preorderForm.value.end_date) < new Date(preorderForm.value.start_date)) {
            errorMessage.value = 'Ngày kết thúc phải lớn hơn hoặc bằng ngày bắt đầu';
            return;
        }
    }
    
    isSubmitting.value = true;
    errorMessage.value = '';
    
    try {
        if (editingPreorder.value) {
            await router.put(`/admin/promotions/preorder/${editingPreorder.value.id}`, preorderForm.value);
        } else {
            await router.post('/admin/promotions/preorder', preorderForm.value);
        }
        closePreorderModal();
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra';
        isSubmitting.value = false;
    }
};

const deletePreorder = async (id) => {
    const preorder = preorders.value.find(p => p.id === id);
    if (!confirm(`Bạn có chắc chắn muốn xóa chương trình "${preorder?.code}"?`)) {
        return;
    }
    
    try {
        await router.delete(`/admin/promotions/preorder/${id}`);
        reloadPage();
    } catch (error) {
        console.error('Lỗi:', error);
        alert('Có lỗi xảy ra khi xóa');
    }
};

const addTier = () => {
    const tiers = preorderForm.value.tiers;
    const lastTier = tiers[tiers.length - 1];
    const newFrom = lastTier ? lastTier.to + 1 : 1;
    const newTo = newFrom + 9;
    preorderForm.value.tiers.push({
        from: newFrom,
        to: newTo,
        discount: 5
    });
};

const removeTier = (index) => {
    if (preorderForm.value.tiers.length > 1) {
        preorderForm.value.tiers.splice(index, 1);
    }
};

// ==================== MODAL FUNCTIONS ====================

const closeModal = () => {
    showModal.value = false;
    editingCampaign.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closeVoucherModal = () => {
    showVoucherModal.value = false;
    editingVoucher.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

const closePreorderModal = () => {
    showPreorderModal.value = false;
    editingPreorder.value = null;
    errorMessage.value = '';
    isSubmitting.value = false;
};

// Show flash message
const showFlashMessage = () => {
    if (flash.value && flash.value.success && flash.value.message) {
        successMessage.value = flash.value.message;
        showSuccess.value = true;
        setTimeout(() => {
            showSuccess.value = false;
        }, 5000);
    }
};

watch(() => page.props.flash, () => {
    showFlashMessage();
}, { deep: true });

// ==================== COMPUTED ====================

// Campaigns
const processedCampaigns = computed(() => {
    return campaigns.value.map(campaign => ({
        ...campaign,
        status: calculateCampaignStatus(campaign.startDate, campaign.endDate)
    }));
});

const filteredCampaigns = computed(() => {
    let filtered = processedCampaigns.value || [];
    
    if (activeStatusTab.value !== 'all') {
        filtered = filtered.filter(c => c.status === activeStatusTab.value);
    }
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(c => 
            (c.name && c.name.toLowerCase().includes(query)) ||
            (c.description && c.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

const campaignCounts = computed(() => {
    const counts = {
        all: campaigns.value.length,
        active: 0,
        ended: 0,
        scheduled: 0
    };
    
    processedCampaigns.value.forEach(c => {
        if (c.status === 'active') counts.active++;
        else if (c.status === 'ended') counts.ended++;
        else if (c.status === 'scheduled') counts.scheduled++;
    });
    
    return counts;
});

// Vouchers
const filteredVouchers = computed(() => {
    let filtered = vouchers.value || [];
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(v => 
            (v.code && v.code.toLowerCase().includes(query)) ||
            (v.description && v.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

// Pre-orders
const filteredPreorders = computed(() => {
    let filtered = preorders.value || [];
    
    if (searchQuery.value) {
        const query = searchQuery.value.toLowerCase();
        filtered = filtered.filter(p => 
            (p.code && p.code.toLowerCase().includes(query)) ||
            (p.description && p.description.toLowerCase().includes(query))
        );
    }
    
    return filtered;
});

onMounted(() => {
    showFlashMessage();
});
</script>

<template>
    <Head title="Quản lý khuyến mãi - BigBag Admin" />
    
    <AdminLayout>
        <!-- Flash Message -->
        <div v-if="showSuccess" class="fixed top-20 right-4 z-50 bg-green-50 border border-green-200 text-green-700 px-6 py-4 rounded-xl shadow-lg max-w-md animate-slide-in">
            <div class="flex items-center gap-3">
                <span class="material-symbols-outlined text-green-500">check_circle</span>
                <span>{{ successMessage }}</span>
            </div>
        </div>

        <div class="p-4 md:p-8">
            <!-- Header -->
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý khuyến mãi</h1>
                    <p class="text-sm text-gray-500 mt-1">Quản lý chiến dịch, mã giảm giá và pre-order</p>
                </div>
                <div class="flex gap-2 flex-wrap">
                    <button @click="openCampaignModal()" class="bg-blue-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-blue-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">add</span>
                        Chiến dịch
                    </button>
                    <button @click="openVoucherModal()" class="bg-orange-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">local_offer</span>
                        Mã giảm giá
                    </button>
                    <button @click="openPreorderModal()" class="bg-purple-600 text-white px-4 py-2 rounded-xl flex items-center gap-2 hover:bg-purple-700 transition-colors text-sm">
                        <span class="material-symbols-outlined text-lg">schedule</span>
                        Pre-order
                    </button>
                </div>
            </div>

            <!-- Search -->
            <div class="mb-6">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="searchQuery" 
                        type="text" 
                        placeholder="Tìm kiếm..." 
                        class="pl-10 pr-4 py-2.5 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <!-- Main Tabs -->
            <div class="flex gap-1 border-b border-gray-200 mb-6 overflow-x-auto">
                <button 
                    @click="activeTab = 'campaigns'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'campaigns' ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎯 Chiến dịch
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ campaigns.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'vouchers'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'vouchers' ? 'text-orange-600 border-b-2 border-orange-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    🎫 Mã giảm giá
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ vouchers.length }}</span>
                </button>
                <button 
                    @click="activeTab = 'preorder'" 
                    class="px-5 py-2.5 text-sm font-medium transition-all whitespace-nowrap"
                    :class="activeTab === 'preorder' ? 'text-purple-600 border-b-2 border-purple-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    ⏳ Pre-order
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">{{ preorders.length }}</span>
                </button>
            </div>

            <!-- Status tabs for campaigns -->
            <div v-if="activeTab === 'campaigns'" class="flex gap-2 mb-6 border-b border-gray-200">
                <button 
                    v-for="status in statusOptions" 
                    :key="status.value"
                    @click="activeStatusTab = status.value"
                    class="px-4 py-2 text-sm font-medium transition-all"
                    :class="activeStatusTab === status.value ? 'text-blue-600 border-b-2 border-blue-600' : 'text-gray-500 hover:text-gray-700'"
                >
                    {{ status.label }}
                    <span class="ml-1 text-xs bg-gray-100 px-2 py-0.5 rounded-full">
                        {{ campaignCounts[status.value] }}
                    </span>
                </button>
            </div>

            <!-- ==================== CAMPAIGNS LIST ==================== -->
            <div v-if="activeTab === 'campaigns'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="campaign in filteredCampaigns" :key="campaign.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-36 bg-gradient-to-r from-blue-50 to-blue-100">
                        <div class="w-full h-full flex items-center justify-center text-blue-300">
                            <span class="material-symbols-outlined text-5xl">campaign</span>
                        </div>
                        
                        <div class="absolute top-2 right-2 flex gap-1 flex-wrap">
                            <span class="text-[10px] px-2 py-0.5 rounded-full font-medium" :class="getStatusClass(campaign.status)">
                                {{ getStatusLabel(campaign.status) }}
                            </span>
                            <span v-if="campaign.featured" class="text-[10px] px-2 py-0.5 rounded-full bg-yellow-100 text-yellow-700 font-medium">⭐</span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">{{ campaign.name }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ campaign.type || 'Seasonal' }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openCampaignModal(campaign)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteCampaign(campaign.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ campaign.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giảm giá</p>
                                <p class="text-sm font-bold text-orange-600">{{ campaign.discount || '0%' }}</p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">SL tối thiểu</p>
                                <p class="text-sm font-bold text-blue-600">{{ campaign.quantity || 0 }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">calendar_today</span>
                            <span>{{ formatDate(campaign.startDate) }} - {{ formatDate(campaign.endDate) }}</span>
                        </div>

                        <div class="mt-0.5 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">inventory_2</span>
                            <span>{{ campaign.products?.length || 0 }} sản phẩm</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleCampaignStatus(campaign)" 
                                class="text-[10px] px-3 py-1 rounded-full transition-colors font-medium"
                                :class="campaign.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-blue-100 text-blue-700 hover:bg-blue-200'"
                            >
                                {{ campaign.status === 'active' ? '✅ Đang diễn ra' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredCampaigns.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">campaign</span>
                    <p class="text-lg font-medium">Không có chiến dịch nào</p>
                    <button @click="openCampaignModal()" class="mt-3 text-blue-600 hover:underline font-medium">Thêm chiến dịch</button>
                </div>
            </div>

            <!-- ==================== VOUCHERS LIST ==================== -->
            <div v-if="activeTab === 'vouchers'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="voucher in filteredVouchers" :key="voucher.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-28 bg-gradient-to-r from-orange-50 to-orange-100">
                        <div class="w-full h-full flex items-center justify-center text-orange-300">
                            <span class="material-symbols-outlined text-4xl">local_offer</span>
                        </div>
                        <div class="absolute top-2 right-2">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="voucher.status === 'active' ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ voucher.status === 'active' ? '🟢 Hoạt động' : '🔴 Đã tắt' }}
                            </span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-base text-gray-800 truncate">{{ voucher.code }}</h3>
                                <p class="text-[10px] text-gray-500">Loại: {{ getDiscountTypeLabel(voucher.discount_type) }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openVoucherModal(voucher)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deleteVoucher(voucher.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <p class="text-xs text-gray-500 mt-1 line-clamp-1">{{ voucher.description || 'Không có mô tả' }}</p>

                        <div class="mt-2 grid grid-cols-2 gap-2">
                            <div class="bg-orange-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Giá trị</p>
                                <p class="text-sm font-bold text-orange-600">
                                    {{ voucher.discount_type === 'percent' ? voucher.discount_value + '%' : formatPrice(voucher.discount_value) }}
                                </p>
                            </div>
                            <div class="bg-blue-50 rounded-lg p-2 text-center">
                                <p class="text-[10px] text-gray-500">Đã dùng</p>
                                <p class="text-sm font-bold text-blue-600">{{ voucher.used }}/{{ voucher.limit }}</p>
                            </div>
                        </div>

                        <div class="mt-2 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">event</span>
                            <span>HSD: {{ formatDate(voucher.expiry) }}</span>
                        </div>

                        <div class="mt-1 flex items-center gap-1 text-[10px] text-gray-500">
                            <span class="material-symbols-outlined text-xs">target</span>
                            <span>{{ getTargetTypeLabel(voucher.target_type) }}</span>
                            <span v-if="voucher.min_order > 0" class="ml-1">- Đơn tối thiểu: {{ formatPrice(voucher.min_order) }}</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleVoucher(voucher)" 
                                :class="voucher.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'"
                                class="text-[10px] px-3 py-1 rounded-full transition-colors"
                            >
                                {{ voucher.status === 'active' ? '✅ Kích hoạt' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredVouchers.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">local_offer</span>
                    <p class="text-lg font-medium">Không có mã giảm giá nào</p>
                    <button @click="openVoucherModal()" class="mt-3 text-orange-600 hover:underline font-medium">Thêm mã giảm giá</button>
                </div>
            </div>

            <!-- ==================== PRE-ORDERS LIST ==================== -->
            <div v-if="activeTab === 'preorder'" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                <div v-for="preorder in filteredPreorders" :key="preorder.id" class="bg-white rounded-xl overflow-hidden border border-gray-200 hover:shadow-lg transition-all duration-300">
                    <div class="relative h-28 bg-gradient-to-r from-purple-50 to-purple-100">
                        <div class="w-full h-full flex items-center justify-center text-purple-300">
                            <span class="material-symbols-outlined text-4xl">schedule</span>
                        </div>
                        <div class="absolute top-2 right-2">
                            <span class="text-[10px] px-2 py-0.5 rounded-full" :class="preorder.status === 'active' ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-500'">
                                {{ preorder.status === 'active' ? '🟢 Hoạt động' : '🔴 Đã tắt' }}
                            </span>
                        </div>
                    </div>

                    <div class="p-3">
                        <div class="flex justify-between items-start">
                            <div class="flex-1 min-w-0">
                                <h3 class="font-bold text-sm text-gray-800 truncate">⏳ {{ preorder.code }}</h3>
                                <p class="text-[10px] text-gray-500 truncate">Sản phẩm: {{ getProductName(preorder.product_id) }}</p>
                            </div>
                            <div class="flex gap-0.5 ml-1 flex-shrink-0">
                                <button @click="openPreorderModal(preorder)" class="p-1 text-blue-600 hover:bg-blue-100 rounded transition-colors" title="Sửa">
                                    <span class="material-symbols-outlined text-sm">edit</span>
                                </button>
                                <button @click="deletePreorder(preorder.id)" class="p-1 text-red-600 hover:bg-red-100 rounded transition-colors" title="Xóa">
                                    <span class="material-symbols-outlined text-sm">delete</span>
                                </button>
                            </div>
                        </div>

                        <div class="mt-2 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-2">
                            <div class="flex justify-between items-center mb-1">
                                <span class="text-[10px] font-medium">Đã đặt: {{ preorder.current_buyers || 0 }} lượt</span>
                                <span class="text-xs font-bold text-blue-600">
                                    {{ preorder.tiers?.find(t => (preorder.current_buyers || 0) >= t.from && (preorder.current_buyers || 0) <= t.to)?.discount || 0 }}%
                                </span>
                            </div>

                            <div class="w-full h-1.5 bg-gray-200 rounded-full overflow-hidden">
                                <div 
                                    class="h-full bg-gradient-to-r from-blue-500 to-purple-500 rounded-full transition-all"
                                    :style="{ width: Math.min(((preorder.current_buyers || 0) / (preorder.tiers?.[preorder.tiers.length - 1]?.to || 100)) * 100, 100) + '%' }"
                                ></div>
                            </div>

                            <div class="mt-1.5 grid grid-cols-3 gap-1 text-[10px]">
                                <div v-for="tier in preorder.tiers" :key="tier.from" 
                                    class="text-center p-1 bg-white rounded border"
                                    :class="(preorder.current_buyers || 0) >= tier.from && (preorder.current_buyers || 0) <= tier.to ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                                >
                                    <div class="font-bold">{{ tier.discount }}%</div>
                                    <div class="text-gray-500 text-[8px]">#{{ tier.from }}-{{ tier.to }}</div>
                                </div>
                            </div>
                        </div>

                        <div class="mt-2 flex justify-between items-center text-[10px] text-gray-500">
                            <span>📅 {{ formatDate(preorder.start_date) }} - {{ formatDate(preorder.end_date) }}</span>
                            <span v-if="preorder.min_order > 0">💰 {{ formatPrice(preorder.min_order) }}</span>
                        </div>

                        <div class="mt-2 pt-2 border-t border-gray-100 flex justify-end">
                            <button 
                                @click="toggleVoucher(preorder)"
                                :class="preorder.status === 'active' ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'"
                                class="text-[10px] px-3 py-1 rounded-full transition-colors"
                            >
                                {{ preorder.status === 'active' ? '✅ Kích hoạt' : '🔄 Kích hoạt' }}
                            </button>
                        </div>
                    </div>
                </div>

                <div v-if="filteredPreorders.length === 0" class="col-span-full text-center py-16 text-gray-500">
                    <span class="material-symbols-outlined text-6xl mb-4 block">schedule</span>
                    <p class="text-lg font-medium">Không có chương trình pre-order nào</p>
                    <button @click="openPreorderModal()" class="mt-3 text-purple-600 hover:underline font-medium">Thêm pre-order</button>
                </div>
            </div>
        </div>

        <!-- ==================== CAMPAIGN MODAL ==================== -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingCampaign ? 'Sửa chiến dịch' : 'Thêm chiến dịch mới' }}
                    </h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chiến dịch *</label>
                        <input 
                            v-model="campaignForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            placeholder="VD: Sale Tết 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại chiến dịch</label>
                        <select 
                            v-model="campaignForm.type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                        >
                            <option value="seasonal">Theo mùa</option>
                            <option value="flash_sale">Flash Sale</option>
                            <option value="anniversary">Kỷ niệm</option>
                            <option value="holiday">Ngày lễ</option>
                            <option value="product_launch">Ra mắt sản phẩm</option>
                            <option value="other">Khác</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="campaignForm.description" 
                            rows="3" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20 resize-none"
                            placeholder="Mô tả chiến dịch..."
                        ></textarea>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="campaignForm.startDate" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Không thể chọn ngày trong quá khứ</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="campaignForm.endDate" 
                                type="date" 
                                :min="minEndDate"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Trạng thái (Tự động)</label>
                        <div class="flex items-center gap-2 p-2 bg-gray-50 rounded-lg border border-gray-200">
                            <span class="px-3 py-1 rounded-full text-xs font-medium" :class="getStatusClass(campaignForm.status)">
                                {{ getStatusLabel(campaignForm.status) }}
                            </span>
                            <span class="text-xs text-gray-500">Dựa trên ngày bắt đầu và kết thúc</span>
                        </div>
                        <input type="hidden" v-model="campaignForm.status">
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng tối thiểu *</label>
                            <input 
                                v-model.number="campaignForm.quantity" 
                                type="number" 
                                min="1"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="1"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số sản phẩm tối thiểu để áp dụng (phải lớn hơn 0)</p>
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Giảm giá (%)</label>
                            <input 
                                v-model.number="campaignForm.discountPercent" 
                                type="number" 
                                min="0" 
                                max="100"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Phần trăm giảm giá (0-100%). Có thể để 0%</p>
                        </div>
                    </div>

                    <!-- Sản phẩm áp dụng -->
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng</label>
                        
                        <div v-if="productVariants.filter(v => !v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-blue-600 bg-blue-50 px-2 py-0.5 rounded">📦 Thường</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => !v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => !v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'normal-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-blue-600 rounded focus:ring-blue-500"
                                    >
                                    <label :for="'normal-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-blue-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.filter(v => v.product?.is_preorder).length > 0" class="mb-2">
                            <div class="flex items-center gap-2 mb-1">
                                <span class="text-xs font-semibold text-purple-600 bg-purple-50 px-2 py-0.5 rounded">⏳ Pre-order</span>
                                <span class="text-xs text-gray-400">({{ productVariants.filter(v => v.product?.is_preorder).length }})</span>
                            </div>
                            <div class="border border-gray-300 rounded-lg p-1.5 max-h-28 overflow-y-auto">
                                <div v-for="variant in productVariants.filter(v => v.product?.is_preorder)" :key="variant.id" 
                                     class="flex items-center gap-1.5 py-0.5 hover:bg-gray-50 px-1.5 rounded text-xs">
                                    <input 
                                        type="checkbox" 
                                        :id="'preorder-' + variant.id"
                                        :checked="isProductSelected(variant.id)"
                                        @change="toggleProduct(variant.id)"
                                        class="w-3 h-3 text-purple-600 rounded focus:ring-purple-500"
                                    >
                                    <label :for="'preorder-' + variant.id" class="text-xs cursor-pointer flex-1 flex items-center gap-1 truncate">
                                        <span class="font-medium truncate max-w-[100px]">{{ variant.product?.name || 'Sản phẩm' }}</span>
                                        <span class="text-gray-400 text-[10px]">({{ variant.color?.name || 'Không màu' }})</span>
                                        <span class="text-purple-600 text-[10px] font-medium">{{ formatPrice(variant.price) }}</span>
                                    </label>
                                </div>
                            </div>
                        </div>

                        <div v-if="productVariants.length === 0" class="border border-gray-300 rounded-lg p-3 text-center text-gray-400 text-sm">
                            <span class="material-symbols-outlined text-2xl block mb-1">inventory_2</span>
                            <p class="text-xs">Không có sản phẩm nào</p>
                            <p class="text-[10px] mt-0.5">Vui lòng thêm sản phẩm và biến thể</p>
                        </div>
                        
                        <div class="text-xs text-gray-500 mt-1">Tổng: <span class="font-semibold">{{ campaignForm.products.length }}</span> sản phẩm</div>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Độ ưu tiên</label>
                            <input 
                                v-model.number="campaignForm.priority" 
                                type="number" 
                                min="0"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20"
                                placeholder="0"
                            >
                            <p class="text-xs text-gray-500 mt-1">Số càng nhỏ càng ưu tiên</p>
                        </div>
                        <div class="flex items-center pt-6">
                            <label class="flex items-center gap-2 cursor-pointer">
                                <input 
                                    type="checkbox" 
                                    v-model="campaignForm.featured" 
                                    class="w-5 h-5 text-blue-600 rounded focus:ring-blue-500"
                                >
                                <span class="text-sm text-gray-700">⭐ Nổi bật</span>
                            </label>
                        </div>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveCampaign" 
                        class="px-5 py-2.5 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu chiến dịch' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== VOUCHER MODAL ==================== -->
        <div v-if="showVoucherModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closeVoucherModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingVoucher ? 'Sửa mã giảm giá' : 'Thêm mã giảm giá mới' }}
                    </h3>
                    <button @click="closeVoucherModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mã code *</label>
                        <input 
                            v-model="voucherForm.code" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 uppercase focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="NHAPMA"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Áp dụng cho</label>
                        <select 
                            v-model="voucherForm.target_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="retail">🛒 Bán lẻ</option>
                            <option value="wholesale">🏭 Bán sỉ</option>
                            <option value="preorder">⏳ Pre-order</option>
                            <option value="all">📦 Tất cả</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Loại giảm giá</label>
                        <select 
                            v-model="voucherForm.discount_type" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option value="fixed">Giảm trực tiếp (₫)</option>
                            <option value="percent">Giảm theo %</option>
                            <option value="freeship">Miễn phí ship</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Giá trị giảm</label>
                        <input 
                            v-model.number="voucherForm.discount_value" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="50000"
                        >
                        <p class="text-xs text-gray-500 mt-1">Có thể để 0 nếu không giảm</p>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Điều kiện (đơn tối thiểu)</label>
                        <input 
                            v-model.number="voucherForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="500000"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Số lượng mã</label>
                        <input 
                            v-model.number="voucherForm.limit" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                            placeholder="100"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                        <input 
                            v-model="voucherForm.expiry" 
                            type="date" 
                            :min="today"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="voucherForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Mô tả</label>
                        <textarea 
                            v-model="voucherForm.description" 
                            rows="2" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 resize-none"
                            placeholder="Mô tả mã giảm giá..."
                        ></textarea>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="voucherForm.active" class="w-5 h-5 text-orange-600 rounded focus:ring-orange-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closeVoucherModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="saveVoucher" 
                        class="px-5 py-2.5 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu mã' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- ==================== PRE-ORDER MODAL ==================== -->
        <div v-if="showPreorderModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50 p-4" @click.self="closePreorderModal">
            <div class="bg-white rounded-xl max-w-2xl w-full p-6 shadow-xl max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">
                        {{ editingPreorder ? 'Sửa pre-order' : 'Thêm pre-order mới' }}
                    </h3>
                    <button @click="closePreorderModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Tên chương trình *</label>
                        <input 
                            v-model="preorderForm.name" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="Pre-order Sale 2025"
                        >
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Sản phẩm áp dụng *</label>
                        <div v-if="preorderProducts && preorderProducts.length > 0">
                            <select 
                                v-model="preorderForm.product_id" 
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                                <option :value="null">-- Chọn sản phẩm pre-order --</option>
                                <option 
                                    v-for="product in preorderProducts" 
                                    :key="product.id" 
                                    :value="product.id"
                                >
                                    ⏳ {{ product.name }} 
                                    <span v-if="product.variants && product.variants.length > 0" class="text-gray-400 text-xs">
                                        ({{ product.variants.length }} biến thể)
                                    </span>
                                </option>
                            </select>
                        </div>
                        <div v-else class="border border-gray-300 rounded-lg p-6 text-center text-gray-400">
                            <span class="material-symbols-outlined text-4xl block mb-2">inventory_2</span>
                            <p>Không có sản phẩm pre-order nào</p>
                            <p class="text-xs mt-1">Vui lòng thêm sản phẩm pre-order (is_preorder = true) trước khi tạo chương trình</p>
                        </div>
                        <p class="text-xs text-gray-500 mt-1">Chỉ hiển thị sản phẩm pre-order (is_preorder = true)</p>
                        
                        <div v-if="preorderForm.product_id" class="mt-2 p-2 bg-purple-50 rounded-lg border border-purple-200">
                            <p class="text-sm font-medium text-purple-700">
                                ✅ Đã chọn: {{ getProductName(preorderForm.product_id) }}
                            </p>
                            <p class="text-xs text-gray-500 mt-1">
                                ID: {{ preorderForm.product_id }}
                            </p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Đơn hàng tối thiểu</label>
                        <input 
                            v-model.number="preorderForm.min_order" 
                            type="number" 
                            min="0"
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            placeholder="0"
                        >
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày bắt đầu</label>
                            <input 
                                v-model="preorderForm.start_date" 
                                type="date" 
                                :min="today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                        </div>
                        <div>
                            <label class="text-sm block mb-1 text-gray-700 font-medium">Ngày kết thúc</label>
                            <input 
                                v-model="preorderForm.end_date" 
                                type="date" 
                                :min="preorderForm.start_date || today"
                                class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                            >
                            <p class="text-xs text-gray-500 mt-1">Có thể chọn cùng ngày hoặc sau ngày bắt đầu</p>
                        </div>
                    </div>

                    <div>
                        <label class="text-sm block mb-1 text-gray-700 font-medium">Liên kết chiến dịch</label>
                        <select 
                            v-model="preorderForm.campaign_id" 
                            class="w-full border border-gray-300 rounded-lg px-4 py-2.5 focus:outline-none focus:border-purple-500 focus:ring-2 focus:ring-purple-500/20"
                        >
                            <option :value="null">-- Không liên kết --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">{{ camp.name }}</option>
                        </select>
                    </div>

                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <label class="text-sm font-medium text-gray-700">Các mức giảm giá theo lượt</label>
                            <button @click="addTier" type="button" class="text-sm text-purple-600 hover:text-purple-700 font-medium">+ Thêm mức</button>
                        </div>
                        
                        <div class="space-y-2">
                            <div v-for="(tier, index) in preorderForm.tiers" :key="index" class="flex items-center gap-2 bg-gray-50 p-3 rounded-lg">
                                <div class="flex-1 grid grid-cols-3 gap-2">
                                    <div>
                                        <label class="text-xs text-gray-500">Từ</label>
                                        <input 
                                            v-model="tier.from" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Đến</label>
                                        <input 
                                            v-model="tier.to" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="1"
                                        >
                                    </div>
                                    <div>
                                        <label class="text-xs text-gray-500">Giảm %</label>
                                        <input 
                                            v-model="tier.discount" 
                                            type="number" 
                                            class="w-full border border-gray-300 rounded px-2 py-1 text-sm"
                                            min="0"
                                            max="100"
                                        >
                                    </div>
                                </div>
                                <button 
                                    @click="removeTier(index)" 
                                    type="button"
                                    class="text-red-500 hover:text-red-700 text-sm p-1"
                                    :disabled="preorderForm.tiers.length <= 1"
                                >
                                    ✕
                                </button>
                            </div>
                        </div>
                    </div>

                    <label class="flex items-center gap-2 cursor-pointer">
                        <input type="checkbox" v-model="preorderForm.active" class="w-5 h-5 text-purple-600 rounded focus:ring-purple-500">
                        <span class="text-sm text-gray-700">Kích hoạt ngay</span>
                    </label>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 mt-6 pt-4 border-t border-gray-100">
                    <button 
                        @click="closePreorderModal" 
                        class="px-5 py-2.5 border border-gray-300 rounded-lg text-gray-600 hover:bg-gray-50 transition-colors"
                        :disabled="isSubmitting"
                    >
                        Hủy
                    </button>
                    <button 
                        @click="savePreorder" 
                        class="px-5 py-2.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 transition-colors flex items-center gap-2"
                        :disabled="isSubmitting"
                    >
                        <span v-if="isSubmitting" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSubmitting ? 'Đang lưu...' : 'Lưu pre-order' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
}

@keyframes slideIn {
    from {
        transform: translateX(100%);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

.animate-slide-in {
    animation: slideIn 0.3s ease-out;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 0.8s linear infinite;
}
</style>
```

## Tóm tắt các thay đổi quan trọng:

1. **Props**: Thay `promotions` thành `vouchers` và `preorders`
2. **State**: Thêm `vouchers` và `preorders` thay vì `promotions`
3. **Computed**: `filteredVouchers` và `filteredPreorders` lấy từ `vouchers.value` và `preorders.value`
4. **Template**: Các tab hiển thị số lượng từ `vouchers.length` và `preorders.length`
5. **Trạng thái**: Sử dụng `voucher.status` và `preorder.status` thay vì `voucher.active`

Sau khi cập nhật code này, lỗi `promotions is not defined` sẽ được giải quyết.


## User:
07/09/2026 20:32

fix lại cho tôi trạng thái hoạt động của banner do chưa được hiển thị , fix luôn cả khi sửa thì hiển thị đúng trạng thái cho tôi 
chỉnh và đưa code hoàn chỉnh 
<?php

use App\Http\Controllers\Admin\DashboardController;
use App\Http\Controllers\Admin\OrderController as AdminOrderController;
use App\Http\Controllers\Admin\ProductController as AdminProductController;
use App\Http\Controllers\Admin\CustomerController as AdminCustomerController;
use App\Http\Controllers\Admin\CustomizeController as AdminCustomizeController;
use App\Http\Controllers\Admin\SettingController;
use App\Http\Controllers\Admin\CategoryController;
use App\Http\Controllers\Admin\ColorController;
use App\Http\Controllers\Admin\BrandController;
use App\Http\Controllers\Admin\BannerController;
use App\Http\Controllers\Admin\NewsController;
use App\Http\Controllers\Admin\PromotionController;
use App\Http\Controllers\PaymentController;
use App\Http\Controllers\OrderController;
use App\Http\Controllers\CartController;
use App\Http\Controllers\ProfileController;
use App\Http\Controllers\CategoryController as WebCategoryController;
use App\Http\Controllers\ProductController as WebProductController;
use App\Http\Controllers\HomeController;
use Illuminate\Foundation\Application;
use Illuminate\Support\Facades\Route;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\File;
use Inertia\Inertia;

// ==================== ROUTE ĐỂ PHỤC VỤ ẢNH ====================
Route::get('/image/{filename}', function ($filename) {
    $path = base_path('image/' . $filename);
    if (!File::exists($path)) {
        abort(404);
    }

    $file = File::get($path);
    $type = File::mimeType($path);

    return response($file, 200)->header('Content-Type', $type);
})->where('filename', '.*');

// ==================== WEB ROUTES (Frontend - Public) ====================

Route::get('/', [HomeController::class, 'index'])->name('home');

Route::get('/tim-kiem', function (Request $request) {
    return Inertia::render('Web/Category', ['search' => $request->get('q')]);
})->name('search');

// Product routes - PUBLIC
Route::get('/san-pham/{id}', [WebProductController::class, 'show'])->name('product.detail');
Route::get('/danh-muc/{slug}', [WebCategoryController::class, 'show'])->name('category');

// Other public frontend routes
Route::get('/mua-si', function () {
    return Inertia::render('Web/Wholesale');
})->name('wholesale');

Route::get('/khuyen-mai', function () {
    return Inertia::render('Web/Promotion');
})->name('promotion');

Route::get('/tuy-chinh', function () {
    return Inertia::render('Web/Customize');
})->name('customize');

// ==================== CART ROUTES (Yêu cầu đăng nhập) ====================

// Cart page
Route::get('/gio-hang', function () {
    return Inertia::render('Web/Cart');
})->name('cart')->middleware('auth');

// ⭐ QUAN TRỌNG: Cart API routes - Thêm middleware 'auth' và 'web'
Route::middleware(['auth'])->prefix('api')->group(function () {
    Route::get('/cart', [CartController::class, 'index']);
    Route::post('/cart/add', [CartController::class, 'add']);
    Route::put('/cart/update', [CartController::class, 'update']);
    Route::delete('/cart/remove/{variantId}', [CartController::class, 'remove']);
    Route::delete('/cart/clear', [CartController::class, 'clear']);
});

// ==================== CHECKOUT ROUTES (Yêu cầu đăng nhập) ====================
Route::middleware(['auth'])->group(function () {
    Route::get('/thanh-toan', [PaymentController::class, 'index'])->name('checkout');
    Route::post('/thanh-toan', [PaymentController::class, 'store'])->name('checkout.store');
    Route::get('/thanh-toan/thanh-cong', [PaymentController::class, 'success'])->name('checkout.success');
});

// ==================== AUTHENTICATED WEB ROUTES ====================
Route::middleware('auth')->group(function () {
    Route::get('/profile', [ProfileController::class, 'edit'])->name('profile.edit');
    Route::patch('/profile', [ProfileController::class, 'update'])->name('profile.update');
    Route::delete('/profile', [ProfileController::class, 'destroy'])->name('profile.destroy');
    Route::get('/don-hang', [OrderController::class, 'history'])->name('orders.history');
    Route::get('/don-hang/{id}', [OrderController::class, 'show'])->name('orders.show');
});

// ==================== ADMIN ROUTES ====================
Route::prefix('admin')->middleware(['auth', 'admin'])->name('admin.')->group(function () {
    // Dashboard
    Route::get('/dashboard', [DashboardController::class, 'index'])->name('dashboard');
    Route::get('/', [DashboardController::class, 'index'])->name('home');
    
    // Orders Management
    Route::prefix('orders')->group(function () {
        Route::get('/{type?}', [AdminOrderController::class, 'index'])
            ->where('type', 'retail|wholesale|preorder')
            ->name('orders.index');
        Route::get('/{id}', [AdminOrderController::class, 'show'])
            ->where('id', '[0-9]+')
            ->name('orders.show');
        Route::put('/{id}/status', [AdminOrderController::class, 'updateStatus'])->name('orders.update-status');
        Route::get('/export', [AdminOrderController::class, 'export'])->name('orders.export');
        Route::get('/export/filtered', [AdminOrderController::class, 'exportWithFilters'])->name('orders.export-filtered');
    });
    
    // Products Management
    Route::prefix('products')->group(function () {
        Route::get('/{type?}', [AdminProductController::class, 'index'])
            ->where('type', 'normal|preorder')
            ->name('products.index');
        Route::post('/', [AdminProductController::class, 'store'])->name('products.store');
        Route::put('/{product}', [AdminProductController::class, 'update'])->name('products.update');
        Route::delete('/{product}', [AdminProductController::class, 'destroy'])->name('products.destroy');
    });
    
    // Categories Management
    Route::get('/categories', [CategoryController::class, 'index'])->name('categories.index');
    Route::get('/categories/create', [CategoryController::class, 'create'])->name('categories.create');
    Route::post('/categories', [CategoryController::class, 'store'])->name('categories.store');
    Route::get('/categories/{category}/edit', [CategoryController::class, 'edit'])->name('categories.edit');
    Route::put('/categories/{category}', [CategoryController::class, 'update'])->name('categories.update');
    Route::delete('/categories/{category}', [CategoryController::class, 'destroy'])->name('categories.destroy');
    
    // Colors Management
    Route::get('/colors', [ColorController::class, 'index'])->name('colors.index');
    Route::get('/colors/data', [ColorController::class, 'getColors'])->name('colors.data');
    Route::post('/colors', [ColorController::class, 'store'])->name('colors.store');
    Route::put('/colors/{id}', [ColorController::class, 'update'])->name('colors.update');
    Route::delete('/colors/{id}', [ColorController::class, 'destroy'])->name('colors.destroy');
        
    // Brands Management
    Route::prefix('brands')->group(function () {
        Route::get('/', [BrandController::class, 'index'])->name('brands.index');
        Route::get('/data', [BrandController::class, 'getBrands'])->name('brands.data');
        Route::post('/', [BrandController::class, 'store'])->name('brands.store');
        Route::put('/{id}', [BrandController::class, 'update'])->name('brands.update');
        Route::delete('/{id}', [BrandController::class, 'destroy'])->name('brands.destroy');
        Route::get('/search', [BrandController::class, 'search'])->name('brands.search');
    });
    
    // Customers Management
    Route::prefix('customers')->group(function () {
        Route::get('/', [AdminCustomerController::class, 'index'])->name('customers.index');
        Route::get('/retail', [AdminCustomerController::class, 'retail'])->name('customers.retail');
        Route::get('/business', [AdminCustomerController::class, 'business'])->name('customers.business');
        Route::get('/{id}', [AdminCustomerController::class, 'show'])->name('customers.show');
        Route::put('/{id}', [AdminCustomerController::class, 'update'])->name('customers.update');
        Route::post('/export', [AdminCustomerController::class, 'export'])->name('customers.export');
    });
    
    // Customize Management
    Route::prefix('customize')->group(function () {
        Route::get('/', [AdminCustomizeController::class, 'index'])->name('customize.index');
        Route::put('/{id}/status', [AdminCustomizeController::class, 'updateStatus'])->name('customize.update-status');
        Route::put('/{id}/approve', [AdminCustomizeController::class, 'approve'])->name('customize.approve');
        Route::post('/send-quote', [AdminCustomizeController::class, 'sendQuote'])->name('customize.send-quote');
    });

        // News Management
    Route::prefix('news')->group(function () {
        Route::get('/', [NewsController::class, 'index'])->name('news.index');
        Route::get('/data', [NewsController::class, 'getNews'])->name('news.data');
        Route::post('/', [NewsController::class, 'store'])->name('news.store');
        Route::put('/{id}', [NewsController::class, 'update'])->name('news.update');
        Route::delete('/{id}', [NewsController::class, 'destroy'])->name('news.destroy');
        Route::patch('/{id}/status', [NewsController::class, 'updateStatus'])->name('news.update-status');
    });

       
    // Banner routes
    Route::get('/banners', [BannerController::class, 'index'])->name('banners.index');
    Route::get('/banners/data', [BannerController::class, 'getBanners'])->name('banners.data');
    Route::post('/banners', [BannerController::class, 'store'])->name('banners.store');
    Route::put('/banners/{id}', [BannerController::class, 'update'])->name('banners.update');
    Route::delete('/banners/{id}', [BannerController::class, 'destroy'])->name('banners.destroy');
    Route::patch('/banners/{id}/status', [BannerController::class, 'updateStatus'])->name('banners.status');
    Route::patch('/banners/{id}/order', [BannerController::class, 'updateOrder'])->name('banners.order');

    // Promotion routes
    Route::prefix('promotions')->group(function () {
        // Campaign routes
        Route::get('/', [PromotionController::class, 'index'])->name('promotions.index');
        Route::post('/campaign', [PromotionController::class, 'storeCampaign'])->name('promotions.campaign.store');
        Route::put('/campaign/{id}', [PromotionController::class, 'updateCampaign'])->name('promotions.campaign.update');
        Route::delete('/campaign/{id}', [PromotionController::class, 'deleteCampaign'])->name('promotions.campaign.delete');
        Route::put('/campaign/{id}/status', [PromotionController::class, 'updateCampaignStatus'])->name('promotions.campaign.status');
        Route::get('/campaigns/list', [PromotionController::class, 'getCampaignsList'])->name('promotions.campaigns.list');
        
        Route::put('/preorder/{id}/toggle', [PromotionController::class, 'togglePreorder'])->name('promotions.preorder.toggle');

        // Voucher routes
        Route::post('/voucher', [PromotionController::class, 'storeVoucher'])->name('promotions.voucher.store');
        Route::put('/voucher/{id}', [PromotionController::class, 'updateVoucher'])->name('promotions.voucher.update');
        Route::delete('/voucher/{id}', [PromotionController::class, 'deleteVoucher'])->name('promotions.voucher.delete');
        Route::put('/voucher/{id}/toggle', [PromotionController::class, 'toggleVoucher'])->name('promotions.voucher.toggle');

        // Pre-order routes
        Route::post('/preorder', [PromotionController::class, 'storePreorder'])->name('promotions.preorder.store');
        Route::put('/preorder/{id}', [PromotionController::class, 'updatePreorder'])->name('promotions.preorder.update');
        Route::delete('/preorder/{id}', [PromotionController::class, 'deletePreorder'])->name('promotions.preorder.delete');
    });


    // Reports
    Route::get('/reports', function () {
        return Inertia::render('Admin/Reports');
    })->name('reports.index');
    
    // Settings
    Route::get('/settings', [SettingController::class, 'index'])->name('settings.index');
    Route::put('/settings/general', [SettingController::class, 'updateGeneral'])->name('settings.updateGeneral');
    Route::put('/settings/password', [SettingController::class, 'changePassword'])->name('settings.changePassword');

    // User Management
    Route::get('/settings/users', [SettingController::class, 'getUsers'])->name('settings.users');
    Route::post('/settings/users', [SettingController::class, 'storeUser'])->name('settings.storeUser');
    Route::put('/settings/users/{id}', [SettingController::class, 'updateUser'])->name('settings.updateUser');
    Route::delete('/settings/users/{id}', [SettingController::class, 'destroyUser'])->name('settings.destroyUser');
    Route::patch('/settings/users/{id}/toggle', [SettingController::class, 'toggleUserStatus'])->name('settings.toggleUser');
});

require __DIR__.'/auth.php';

<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Storage;
use Illuminate\Support\Facades\Log;

class BannerController extends Controller
{
    public function index()
    {
        return Inertia::render('Admin/Banners', [
            'banners' => Banner::with('campaign')->orderBy('order', 'asc')->get()
        ]);
    }

    public function getBanners()
    {
        return response()->json(Banner::with('campaign')->orderBy('order', 'asc')->get());
    }

    public function store(Request $request)
    {
        try {
            Log::info('Banner store request:', $request->all());

            $rules = [
                'title' => 'nullable|string|max:255',
                'campaign_id' => 'nullable|exists:campaigns,id',
                'image' => 'nullable|url',
                'link' => 'nullable|string|max:255',
                'description' => 'nullable|string',
                'status' => 'boolean',
                'order' => 'nullable|integer|min:0'
            ];

            if ($request->hasFile('image_file')) {
                $rules['image_file'] = 'image|max:2048';
            }

            $validated = $request->validate($rules);

            $data = [
                'title' => $validated['title'] ?? 'Banner ' . now()->format('d/m/Y'),
                'campaign_id' => $validated['campaign_id'] ?? null,
                'link' => $validated['link'] ?? null,
                'description' => $validated['description'] ?? null,
                'status' => $validated['status'] ?? true,
            ];
            
            // Xử lý ảnh
            if ($request->hasFile('image_file')) {
                try {
                    $path = $request->file('image_file')->store('banners', 'public');
                    $data['image'] = '/storage/' . $path;
                } catch (\Exception $e) {
                    Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                    return response()->json([
                        'success' => false,
                        'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                    ], 500);
                }
            } elseif ($request->filled('image')) {
                $data['image'] = $request->image;
            } else {
                return response()->json([
                    'success' => false,
                    'message' => 'Vui lòng chọn ảnh hoặc nhập URL'
                ], 422);
            }

            // Xác định order mới
            $totalBanners = Banner::count();
            $newOrder = $request->input('order', $totalBanners);
            
            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder > $totalBanners) $newOrder = $totalBanners;

            if ($newOrder < $totalBanners) {
                Banner::where('order', '>=', $newOrder)->increment('order');
            }

            $data['order'] = $newOrder;
            $banner = Banner::create($data);

            Log::info('Banner created successfully:', ['id' => $banner->id]);

            return response()->json([
                'success' => true, 
                'data' => $banner->load('campaign')
            ]);
            
        } catch (\Illuminate\Validation\ValidationException $e) {
            Log::error('Validation error:', $e->errors());
            return response()->json([
                'success' => false,
                'message' => 'Dữ liệu không hợp lệ',
                'errors' => $e->errors()
            ], 422);
        } catch (\Exception $e) {
            Log::error('Lỗi tạo banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function update(Request $request, $id)
    {
        try {
            Log::info('Banner update request:', ['id' => $id, 'data' => $request->all()]);

            $banner = Banner::findOrFail($id);
            
            $rules = [
                'title' => 'nullable|string|max:255',
                'campaign_id' => 'nullable|exists:campaigns,id',
                'image' => 'nullable|string',
                'link' => 'nullable|string|max:255',
                'description' => 'nullable|string',
                'status' => 'boolean',
                'order' => 'nullable|integer|min:0'
            ];

            if ($request->hasFile('image_file')) {
                $rules['image_file'] = 'image|max:2048';
            }

            $validated = $request->validate($rules);

            $data = [
                'title' => $validated['title'] ?? $banner->title,
                'campaign_id' => $validated['campaign_id'] ?? $banner->campaign_id,
                'link' => $validated['link'] ?? $banner->link,
                'description' => $validated['description'] ?? $banner->description,
                'status' => $validated['status'] ?? $banner->status,
            ];
            
            // Xử lý ảnh
            if ($request->hasFile('image_file')) {
                try {
                    if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                        Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
                    }
                    
                    $path = $request->file('image_file')->store('banners', 'public');
                    $data['image'] = '/storage/' . $path;
                } catch (\Exception $e) {
                    Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                    return response()->json([
                        'success' => false,
                        'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                    ], 500);
                }
            } elseif ($request->filled('image')) {
                $data['image'] = $request->image;
            }

            // Xử lý thay đổi order
            $newOrder = $request->input('order');
            if (!is_null($newOrder) && $newOrder != $banner->order) {
                $oldOrder = $banner->order;
                $totalBanners = Banner::count();

                if ($newOrder < 0) $newOrder = 0;
                if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

                if ($oldOrder < $newOrder) {
                    Banner::where('id', '!=', $id)
                        ->where('order', '>', $oldOrder)
                        ->where('order', '<=', $newOrder)
                        ->decrement('order');
                } else {
                    Banner::where('id', '!=', $id)
                        ->where('order', '>=', $newOrder)
                        ->where('order', '<', $oldOrder)
                        ->increment('order');
                }

                $data['order'] = $newOrder;
            }

            $banner->update($data);

            Log::info('Banner updated successfully:', ['id' => $banner->id]);

            return response()->json([
                'success' => true, 
                'data' => $banner->load('campaign')
            ]);
            
        } catch (\Illuminate\Validation\ValidationException $e) {
            Log::error('Validation error:', $e->errors());
            return response()->json([
                'success' => false,
                'message' => 'Dữ liệu không hợp lệ',
                'errors' => $e->errors()
            ], 422);
        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }
    public function destroy($id)
    {
        try {
            $banner = Banner::findOrFail($id);
            
            if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
            }
            
            $deletedOrder = $banner->order;
            $banner->delete();

            Banner::where('order', '>', $deletedOrder)->decrement('order');

            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            Log::error('Lỗi xóa banner: ' . $e->getMessage());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function updateStatus(Request $request, $id)
    {
        try {
            $banner = Banner::findOrFail($id);
            $request->validate(['status' => 'required|boolean']);
            $banner->update(['status' => $request->status]);
            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            return response()->json(['success' => false, 'message' => $e->getMessage()], 500);
        }
    }

    public function updateOrder(Request $request, $id)
    {
        try {
            $banner = Banner::findOrFail($id);
            $request->validate(['order' => 'required|integer|min:0']);

            $newOrder = $request->order;
            $oldOrder = $banner->order;

            if ($oldOrder == $newOrder) {
                return response()->json(['success' => true]);
            }

            $totalBanners = Banner::count();
            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

            if ($oldOrder < $newOrder) {
                Banner::where('id', '!=', $id)
                    ->where('order', '>', $oldOrder)
                    ->where('order', '<=', $newOrder)
                    ->decrement('order');
            } else {
                Banner::where('id', '!=', $id)
                    ->where('order', '>=', $newOrder)
                    ->where('order', '<', $oldOrder)
                    ->increment('order');
            }

            $banner->update(['order' => $newOrder]);

            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            return response()->json(['success' => false, 'message' => $e->getMessage()], 500);
        }
    }
}
<script setup>
import { ref, onMounted, computed, watch } from 'vue'
import axios from 'axios'
import AdminLayout from '@/Layouts/AdminLayout.vue'
import { Head } from '@inertiajs/vue3'

// Props nhận từ controller
const props = defineProps({
    banners: {
        type: Array,
        default: () => []
    },
    campaigns: {
        type: Array,
        default: () => []
    }
})

// Pagination - 5 items per page
const currentPage = ref(1)
const perPage = ref(5)

// Search
const search = ref('')

// Lọc banners theo tên chiến dịch hoặc link
const filteredBanners = computed(() => {
    if (!banners.value || banners.value.length === 0) return []
    if (!search.value) return banners.value
    
    const keyword = search.value.toLowerCase().trim()
    return banners.value.filter(banner => {
        const campaignName = (banner.campaign?.name || '').toLowerCase()
        const title = (banner.title || '').toLowerCase()
        const link = (banner.link || '').toLowerCase()
        return campaignName.includes(keyword) || title.includes(keyword) || link.includes(keyword)
    })
})

// Sắp xếp banners theo order
const sortedBanners = computed(() => {
    return [...filteredBanners.value].sort((a, b) => (a.order || 0) - (b.order || 0))
})

// Pagination
const paginatedBanners = computed(() => {
    const start = (currentPage.value - 1) * perPage.value
    const end = start + perPage.value
    return sortedBanners.value.slice(start, end)
})

const totalPages = computed(() => {
    return Math.ceil(sortedBanners.value.length / perPage.value)
})

// Hiển thị số trang (tối đa 5 trang)
const displayedPages = computed(() => {
    const total = totalPages.value
    const current = currentPage.value
    const maxDisplay = 5
    
    if (total <= maxDisplay) {
        return Array.from({ length: total }, (_, i) => i + 1)
    }
    
    let start = Math.max(1, current - 2)
    let end = Math.min(total, start + maxDisplay - 1)
    
    if (end - start < maxDisplay - 1) {
        start = Math.max(1, end - maxDisplay + 1)
    }
    
    return Array.from({ length: end - start + 1 }, (_, i) => start + i)
})

// Reset về trang 1 khi tìm kiếm
watch(search, () => {
    currentPage.value = 1
})

const banners = ref(props.banners || [])
const campaigns = ref(props.campaigns || [])
const showModal = ref(false)
const showDeleteModal = ref(false)
const isEdit = ref(false)
const selectedBanner = ref(null)
const isLoading = ref(false)
const isSaving = ref(false)
const errorMessage = ref('')
const fileError = ref('')
const uploadSuccess = ref(false)

// Chọn phương thức nhập ảnh: 'url' hoặc 'file'
const imageInputMode = ref('url')
const selectedFile = ref(null)
const imagePreviewUrl = ref('')

const form = ref({
    id: null,
    title: '',
    campaign_id: '',
    image: '',
    link: '',
    status: 1,
    order: 0
})

// Xem trước ảnh
const imagePreview = computed(() => {
    if (imagePreviewUrl.value) return imagePreviewUrl.value
    if (form.value.image) return form.value.image
    return null
})

// Format date
const formatDate = (date) => {
    if (!date) return '---'
    const d = new Date(date)
    return d.toLocaleDateString('vi-VN')
}

// Lấy tên chiến dịch từ ID
const getCampaignName = (campaignId) => {
    if (!campaignId) return 'Chưa phân loại'
    const campaign = campaigns.value.find(c => c.id === campaignId)
    return campaign ? campaign.name : 'Chiến dịch đã xóa'
}

const fetchBanners = async () => {
    if (isLoading.value) return
    isLoading.value = true
    try {
        const response = await axios.get('/admin/banners/data')
        if (response.data && Array.isArray(response.data)) {
            banners.value = response.data
        } else {
            banners.value = []
        }
    } catch (error) {
        console.error('Lỗi lấy danh sách banner:', error)
        banners.value = []
    } finally {
        isLoading.value = false
    }
}

const fetchCampaigns = async () => {
    try {
        const response = await axios.get('/admin/promotions/campaigns/list')
        if (response.data && Array.isArray(response.data)) {
            campaigns.value = response.data
        }
    } catch (error) {
        console.error('Lỗi lấy danh sách chiến dịch:', error)
    }
}

const openCreateModal = () => {
    isEdit.value = false
    form.value = { 
        id: null, 
        title: '',
        campaign_id: '', 
        image: '', 
        link: '', 
        status: 1, 
        order: 0 
    }
    selectedFile.value = null
    imagePreviewUrl.value = ''
    imageInputMode.value = 'url'
    errorMessage.value = ''
    fileError.value = ''
    uploadSuccess.value = false
    showModal.value = true
}

const openEditModal = (banner) => {
    isEdit.value = true
    form.value = { 
        id: banner.id,
        title: banner.title || '',
        campaign_id: banner.campaign_id || '', 
        image: banner.image || '', 
        link: banner.link || '', 
        status: banner.status !== undefined ? banner.status : 1, 
        order: banner.order || 0 
    }
    selectedFile.value = null
    imagePreviewUrl.value = ''
    imageInputMode.value = 'url'
    errorMessage.value = ''
    fileError.value = ''
    uploadSuccess.value = false
    showModal.value = true
}

// Xử lý khi chọn file
const handleFileChange = (event) => {
    const file = event.target.files[0]
    fileError.value = ''
    if (!file) {
        return
    }
    // Kiểm tra định dạng file
    if (!file.type.startsWith('image/')) {
        fileError.value = 'Vui lòng chọn file ảnh (jpg, png, gif, svg, webp)'
        event.target.value = ''
        return
    }
    // Kiểm tra kích thước (2MB)
    if (file.size > 2 * 1024 * 1024) {
        fileError.value = 'Kích thước ảnh không quá 2MB'
        event.target.value = ''
        return
    }
    selectedFile.value = file
    // Tạo preview
    const reader = new FileReader()
    reader.onload = (e) => { 
        imagePreviewUrl.value = e.target.result 
    }
    reader.readAsDataURL(file)
    // Xóa URL cũ nếu có
    form.value.image = ''
}

const clearFile = () => {
    selectedFile.value = null
    imagePreviewUrl.value = ''
    fileError.value = ''
    if (imageInputMode.value === 'file') {
        const fileInput = document.getElementById('fileInput')
        if (fileInput) fileInput.value = ''
    }
}

const saveBanner = async () => {
    // Validate
    if (!form.value.campaign_id) {
        errorMessage.value = 'Vui lòng chọn chiến dịch'
        return
    }
    
    // Kiểm tra có ảnh không (URL hoặc file)
    if (!form.value.image && !selectedFile.value) {
        errorMessage.value = 'Vui lòng nhập URL ảnh hoặc tải ảnh lên'
        return
    }
    
    if (fileError.value) {
        errorMessage.value = fileError.value
        return
    }

    if (isSaving.value) return
    isSaving.value = true
    errorMessage.value = ''
    uploadSuccess.value = false

    try {
        let response
        
        if (isEdit.value) {
            // Cập nhật banner
            if (selectedFile.value) {
                // Có file mới -> upload
                const formData = new FormData()
                formData.append('_method', 'PUT')
                formData.append('title', form.value.title || '')
                formData.append('campaign_id', form.value.campaign_id)
                formData.append('link', form.value.link || '')
                formData.append('status', form.value.status)
                formData.append('order', form.value.order || 0)
                formData.append('image_file', selectedFile.value)
                
                response = await axios.post(`/admin/banners/${form.value.id}`, formData, {
                    headers: { 
                        'Content-Type': 'multipart/form-data',
                        'Accept': 'application/json'
                    }
                })
            } else {
                // Không có file mới, chỉ cập nhật thông tin
                const dataToSave = {
                    title: form.value.title || '',
                    campaign_id: form.value.campaign_id,
                    link: form.value.link || '',
                    status: form.value.status,
                    order: form.value.order || 0,
                    image: form.value.image || null
                }
                response = await axios.put(`/admin/banners/${form.value.id}`, dataToSave, {
                    headers: { 'Accept': 'application/json' }
                })
            }
            
            if (response.data && response.data.success) {
                // Cập nhật danh sách
                await fetchBanners()
                uploadSuccess.value = true
                alert('Cập nhật banner thành công!')
                showModal.value = false
                clearFile()
            } else {
                errorMessage.value = response.data?.message || 'Có lỗi xảy ra khi cập nhật'
            }
        } else {
            // Thêm mới banner
            if (selectedFile.value) {
                // Upload file
                const formData = new FormData()
                formData.append('title', form.value.title || '')
                formData.append('campaign_id', form.value.campaign_id)
                formData.append('link', form.value.link || '')
                formData.append('status', form.value.status)
                formData.append('order', form.value.order || 0)
                formData.append('image_file', selectedFile.value)
                
                response = await axios.post('/admin/banners', formData, {
                    headers: { 
                        'Content-Type': 'multipart/form-data',
                        'Accept': 'application/json'
                    }
                })
            } else {
                // Dùng URL
                const dataToSave = {
                    title: form.value.title || '',
                    campaign_id: form.value.campaign_id,
                    link: form.value.link || '',
                    status: form.value.status,
                    order: form.value.order || 0,
                    image: form.value.image || null
                }
                response = await axios.post('/admin/banners', dataToSave, {
                    headers: { 'Accept': 'application/json' }
                })
            }
            
            if (response.data && response.data.success && response.data.data) {
                banners.value.push(response.data.data)
                uploadSuccess.value = true
                alert('Thêm banner thành công!')
                showModal.value = false
                clearFile()
            } else {
                errorMessage.value = response.data?.message || 'Có lỗi xảy ra khi thêm mới'
            }
        }
    } catch (error) {
        console.error('Lỗi lưu banner:', error)
        if (error.response) {
            errorMessage.value = error.response.data?.message || 'Có lỗi xảy ra'
        } else {
            errorMessage.value = 'Không thể kết nối đến server'
        }
    } finally {
        isSaving.value = false
    }
}

const confirmDelete = (banner) => {
    selectedBanner.value = banner
    errorMessage.value = ''
    showDeleteModal.value = true
}

const deleteBanner = async () => {
    if (!selectedBanner.value) return
    if (isSaving.value) return
    isSaving.value = true
    errorMessage.value = ''
    try {
        const response = await axios.delete(`/admin/banners/${selectedBanner.value.id}`, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            showDeleteModal.value = false
            const index = banners.value.findIndex(b => b.id === selectedBanner.value.id)
            if (index !== -1) {
                banners.value.splice(index, 1)
            }
            selectedBanner.value = null
            alert('Xóa banner thành công!')
        } else {
            errorMessage.value = response.data?.message || 'Có lỗi xảy ra'
        }
    } catch (error) {
        console.error('Lỗi xóa banner:', error)
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra khi xóa'
    } finally {
        isSaving.value = false
    }
}

const toggleStatus = async (banner) => {
    try {
        const newStatus = banner.status === 1 ? 0 : 1
        const response = await axios.patch(`/admin/banners/${banner.id}/status`, { 
            status: newStatus 
        }, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            banner.status = newStatus
        } else {
            alert('Cập nhật trạng thái thất bại')
        }
    } catch (error) {
        console.error('Lỗi cập nhật trạng thái:', error)
        alert('Có lỗi xảy ra')
    }
}

const updateOrder = async (banner, newOrder) => {
    try {
        const response = await axios.patch(`/admin/banners/${banner.id}/order`, { 
            order: newOrder 
        }, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            banner.order = newOrder
            // Sắp xếp lại danh sách
            banners.value = [...banners.value].sort((a, b) => (a.order || 0) - (b.order || 0))
        }
    } catch (error) {
        console.error('Lỗi cập nhật thứ tự:', error)
        alert('Có lỗi xảy ra khi cập nhật thứ tự')
    }
}

const closeModal = () => {
    showModal.value = false
    showDeleteModal.value = false
    selectedBanner.value = null
    form.value = { id: null, title: '', campaign_id: '', image: '', link: '', status: 1, order: 0 }
    errorMessage.value = ''
    fileError.value = ''
    isSaving.value = false
    uploadSuccess.value = false
    clearFile()
}

const handleOverlayClick = (e) => {
    if (e.target === e.currentTarget) {
        closeModal()
    }
}

onMounted(() => {
    fetchBanners()
    fetchCampaigns()
})
</script>

<template>
    <Head title="Quản lý Banner" />
    <AdminLayout>
        <div class="p-4 md:p-8">
            <div class="flex justify-between items-center mb-6">
                <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý Banner</h1>
                <button @click="openCreateModal" class="bg-orange-600 text-white px-5 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors" :disabled="isSaving">
                    <span class="material-symbols-outlined text-lg">add</span>
                    Thêm banner mới
                </button>
            </div>

            <!-- Thanh tìm kiếm -->
            <div class="mb-4">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="search" 
                        type="text" 
                        placeholder="Tìm theo chiến dịch, tên hoặc link..." 
                        class="pl-10 pr-4 py-2 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <div v-if="isLoading && banners.length === 0" class="text-center py-8">
                <div class="inline-block animate-spin rounded-full h-8 w-8 border-4 border-orange-500 border-t-transparent"></div>
                <p class="mt-2 text-gray-500">Đang tải...</p>
            </div>

            <div v-else class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full min-w-[800px]">
                        <thead class="bg-gray-50 border-b border-gray-200">
                            <tr>
                                <th class="text-left p-4 font-semibold text-gray-700 w-16 whitespace-nowrap">STT</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Hình ảnh</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap min-w-[150px]">Chiến dịch</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Link</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Trạng thái</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Thứ tự</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Ngày tạo</th>
                                <th class="text-center p-4 font-semibold text-gray-700 w-40 whitespace-nowrap">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr v-for="(banner, index) in paginatedBanners" :key="banner.id" class="border-b border-gray-100 hover:bg-gray-50 transition">
                                <td class="p-4 text-gray-500 text-sm whitespace-nowrap">{{ (currentPage - 1) * perPage + index + 1 }}</td>
                                <td class="p-4">
                                    <img v-if="banner.image" :src="banner.image" class="h-12 w-20 object-cover rounded" :alt="banner.title || 'Banner'" @error="banner.image = null">
                                    <span v-else class="text-gray-400">---</span>
                                </td>
                                <td class="p-4">
                                    <div class="flex items-center gap-2">
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium" 
                                              :class="banner.campaign_id ? 'bg-blue-100 text-blue-800' : 'bg-gray-100 text-gray-500'">
                                            {{ banner.campaign?.name || 'Chưa phân loại' }}
                                        </span>
                                    </div>
                                </td>
                                <td class="p-4 text-gray-500 text-sm max-w-xs truncate">
                                    <a v-if="banner.link" :href="banner.link" target="_blank" class="text-blue-600 hover:underline">
                                        {{ banner.link }}
                                    </a>
                                    <span v-else class="text-gray-400">---</span>
                                </td>
                                <td class="p-4">
                                    <button @click="toggleStatus(banner)" class="px-2 py-1 text-xs rounded-full transition whitespace-nowrap" :class="banner.status === 1 ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'">
                                        {{ banner.status === 1 ? 'Hoạt động' : 'Tạm dừng' }}
                                    </button>
                                </td>
                                <td class="p-4">
                                    <div class="flex items-center gap-1">
                                        <input type="number" :value="banner.order || 0" @change="updateOrder(banner, parseInt($event.target.value))" class="w-16 px-1 py-1 border rounded text-center text-sm" min="0">
                                        <span class="text-xs text-gray-400">#</span>
                                    </div>
                                </td>
                                <td class="p-4 text-gray-500 text-sm whitespace-nowrap">{{ formatDate(banner.created_at) }}</td>
                                <td class="p-4 text-center whitespace-nowrap">
                                    <div class="flex items-center justify-center gap-2">
                                        <button @click="openEditModal(banner)" class="px-3 py-1.5 text-xs text-green-600 hover:bg-green-100 rounded-lg transition-colors font-medium" :disabled="isSaving">Sửa</button>
                                        <button @click="confirmDelete(banner)" class="px-3 py-1.5 text-xs text-red-600 hover:bg-red-100 rounded-lg transition-colors font-medium" :disabled="isSaving">Xóa</button>
                                    </div>
                                </td>
                            </tr>
                            <tr v-if="paginatedBanners.length === 0 && !isLoading">
                                <td colspan="8" class="p-8 text-center text-gray-400">Chưa có banner nào</td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <!-- Footer với phân trang căn giữa -->
                <div class="p-4 border-t border-gray-200">
                    <div class="text-center text-sm text-gray-500 mb-3">
                        Hiển thị {{ paginatedBanners.length }} / {{ sortedBanners.length }} banner
                    </div>
                    
                    <div v-if="totalPages > 1" class="flex justify-center items-center gap-2">
                        <button
                            @click="currentPage--"
                            :disabled="currentPage === 1"
                            class="px-3 py-1.5 text-sm border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            ◄
                        </button>
                        
                        <div class="flex gap-1">
                            <button
                                v-for="page in displayedPages"
                                :key="page"
                                @click="currentPage = page"
                                class="px-3.5 py-1.5 text-sm rounded-lg transition-colors font-medium"
                                :class="currentPage === page ? 'bg-orange-600 text-white' : 'border border-gray-300 hover:bg-gray-50'"
                            >
                                {{ page }}
                            </button>
                        </div>
                        
                        <button
                            @click="currentPage++"
                            :disabled="currentPage === totalPages"
                            class="px-3 py-1.5 text-sm border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            ►
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Modal Thêm/Sửa -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50" @click="handleOverlayClick">
            <div class="bg-white rounded-lg w-full max-w-lg p-6 max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">{{ isEdit ? 'Sửa banner' : 'Thêm banner mới' }}</h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Tiêu đề</label>
                        <input 
                            v-model="form.title" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" 
                            placeholder="Tiêu đề banner (tùy chọn)"
                            :disabled="isSaving"
                        >
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Chiến dịch *</label>
                        <select v-model="form.campaign_id" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                            <option value="">-- Chọn chiến dịch --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">
                                {{ camp.name }}
                                <span v-if="camp.status" class="text-xs text-gray-400">({{ camp.status === 'active' ? 'Đang diễn ra' : camp.status === 'scheduled' ? 'Sắp diễn ra' : 'Đã kết thúc' }})</span>
                            </option>
                        </select>
                        <p class="text-xs text-gray-400 mt-1">Banner sẽ thuộc về chiến dịch này</p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Ảnh banner *</label>
                        <div class="flex gap-2 border-b pb-2 mb-2">
                            <button type="button" @click="imageInputMode = 'url'" :class="['px-3 py-1 text-sm rounded-full transition-colors', imageInputMode === 'url' ? 'bg-orange-100 text-orange-600' : 'bg-gray-100 hover:bg-gray-200']">🔗 Nhập URL</button>
                            <button type="button" @click="imageInputMode = 'file'" :class="['px-3 py-1 text-sm rounded-full transition-colors', imageInputMode === 'file' ? 'bg-orange-100 text-orange-600' : 'bg-gray-100 hover:bg-gray-200']">📁 Tải ảnh lên</button>
                        </div>
                        <div v-if="imageInputMode === 'url'">
                            <input v-model="form.image" type="text" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" placeholder="https://example.com/banner.jpg" :disabled="isSaving">
                        </div>
                        <div v-else>
                            <input id="fileInput" type="file" accept="image/*" @change="handleFileChange" class="w-full" :disabled="isSaving">
                            <div v-if="fileError" class="text-red-500 text-sm mt-1">{{ fileError }}</div>
                            <button v-if="selectedFile" @click="clearFile" class="text-red-500 text-xs mt-1 hover:underline" type="button">✕ Xóa file đã chọn</button>
                            <p class="text-xs text-gray-400 mt-1">Hỗ trợ JPG, PNG, GIF, SVG, WEBP. Kích thước tối đa 2MB</p>
                        </div>
                        <div v-if="imagePreview" class="mt-2">
                            <p class="text-sm text-gray-600 mb-1">Xem trước:</p>
                            <div class="w-32 h-20 border rounded-lg overflow-hidden bg-gray-100 flex items-center justify-center">
                                <img :src="imagePreview" class="max-w-full max-h-full object-contain" @error="imagePreviewUrl = ''; form.image = ''" alt="Banner preview">
                            </div>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Link (URL)</label>
                        <input v-model="form.link" type="text" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" placeholder="https://example.com" :disabled="isSaving">
                        <p class="text-xs text-gray-400 mt-1">Đường dẫn khi người dùng click vào banner</p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Trạng thái</label>
                        <select v-model="form.status" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                            <option :value="1">Hoạt động</option>
                            <option :value="0">Tạm dừng</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Thứ tự</label>
                        <input v-model.number="form.order" type="number" min="0" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                        <p class="text-xs text-gray-400 mt-1">Số nhỏ hơn hiển thị trước</p>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                    
                    <div v-if="uploadSuccess" class="p-3 bg-green-50 border border-green-200 rounded-lg">
                        <p class="text-sm text-green-600">✅ Lưu thành công!</p>
                    </div>
                </div>
                
                <div class="flex justify-end gap-3 mt-6">
                    <button @click="closeModal" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition" :disabled="isSaving">Hủy</button>
                    <button @click="saveBanner" class="px-4 py-2 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition flex items-center gap-2" :disabled="isSaving || !!fileError">
                        <span v-if="isSaving" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSaving ? 'Đang xử lý...' : 'Lưu' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- Modal Xác nhận xóa -->
        <div v-if="showDeleteModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50" @click="handleOverlayClick">
            <div class="bg-white rounded-lg w-full max-w-md p-6">
                <h3 class="text-xl font-bold mb-4">Xác nhận xóa</h3>
                <p class="text-gray-600">Bạn có chắc muốn xóa banner "{{ selectedBanner?.title || '#' + selectedBanner?.id }}" này?</p>
                <p class="text-xs text-gray-400 mt-1">Hành động này không thể hoàn tác</p>
                <div v-if="errorMessage" class="mt-3 p-3 bg-red-50 border border-red-200 rounded-lg">
                    <p class="text-sm text-red-600">{{ errorMessage }}</p>
                </div>
                <div class="flex justify-end gap-3 mt-6">
                    <button @click="closeModal" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition" :disabled="isSaving">Hủy</button>
                    <button @click="deleteBanner" class="px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition flex items-center gap-2" :disabled="isSaving">
                        <span v-if="isSaving" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSaving ? 'Đang xóa...' : 'Xóa' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 1s linear infinite;
}
</style>


## Assistant:
07/09/2026 20:32

Tôi sẽ fix lỗi trạng thái của banner và cập nhật code hoàn chỉnh.

## 1. Controller `BannerController.php` (Sửa lỗi)

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\Banner;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Illuminate\Support\Facades\Storage;
use Illuminate\Support\Facades\Log;

class BannerController extends Controller
{
    public function index()
    {
        return Inertia::render('Admin/Banners', [
            'banners' => Banner::with('campaign')->orderBy('order', 'asc')->get()
        ]);
    }

    public function getBanners()
    {
        return response()->json(Banner::with('campaign')->orderBy('order', 'asc')->get());
    }

    public function store(Request $request)
    {
        try {
            Log::info('Banner store request:', $request->all());

            $rules = [
                'title' => 'nullable|string|max:255',
                'campaign_id' => 'nullable|exists:campaigns,id',
                'image' => 'nullable|url',
                'link' => 'nullable|string|max:255',
                'description' => 'nullable|string',
                'status' => 'boolean',
                'order' => 'nullable|integer|min:0'
            ];

            if ($request->hasFile('image_file')) {
                $rules['image_file'] = 'image|max:2048';
            }

            $validated = $request->validate($rules);

            $data = [
                'title' => $validated['title'] ?? 'Banner ' . now()->format('d/m/Y'),
                'campaign_id' => $validated['campaign_id'] ?? null,
                'link' => $validated['link'] ?? null,
                'description' => $validated['description'] ?? null,
                'status' => $validated['status'] ?? true,
            ];
            
            // Xử lý ảnh
            if ($request->hasFile('image_file')) {
                try {
                    $path = $request->file('image_file')->store('banners', 'public');
                    $data['image'] = '/storage/' . $path;
                } catch (\Exception $e) {
                    Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                    return response()->json([
                        'success' => false,
                        'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                    ], 500);
                }
            } elseif ($request->filled('image')) {
                $data['image'] = $request->image;
            } else {
                return response()->json([
                    'success' => false,
                    'message' => 'Vui lòng chọn ảnh hoặc nhập URL'
                ], 422);
            }

            // Xác định order mới
            $totalBanners = Banner::count();
            $newOrder = $request->input('order', $totalBanners);
            
            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder > $totalBanners) $newOrder = $totalBanners;

            if ($newOrder < $totalBanners) {
                Banner::where('order', '>=', $newOrder)->increment('order');
            }

            $data['order'] = $newOrder;
            $banner = Banner::create($data);

            Log::info('Banner created successfully:', ['id' => $banner->id]);

            return response()->json([
                'success' => true, 
                'data' => $banner->load('campaign')
            ]);
            
        } catch (\Illuminate\Validation\ValidationException $e) {
            Log::error('Validation error:', $e->errors());
            return response()->json([
                'success' => false,
                'message' => 'Dữ liệu không hợp lệ',
                'errors' => $e->errors()
            ], 422);
        } catch (\Exception $e) {
            Log::error('Lỗi tạo banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function update(Request $request, $id)
    {
        try {
            Log::info('Banner update request:', ['id' => $id, 'data' => $request->all()]);

            $banner = Banner::findOrFail($id);
            
            $rules = [
                'title' => 'nullable|string|max:255',
                'campaign_id' => 'nullable|exists:campaigns,id',
                'image' => 'nullable|string',
                'link' => 'nullable|string|max:255',
                'description' => 'nullable|string',
                'status' => 'boolean',
                'order' => 'nullable|integer|min:0'
            ];

            if ($request->hasFile('image_file')) {
                $rules['image_file'] = 'image|max:2048';
            }

            $validated = $request->validate($rules);

            // Lấy giá trị status từ request, mặc định giữ nguyên
            $status = $request->has('status') ? filter_var($request->status, FILTER_VALIDATE_BOOLEAN) : $banner->status;

            $data = [
                'title' => $validated['title'] ?? $banner->title,
                'campaign_id' => $validated['campaign_id'] ?? $banner->campaign_id,
                'link' => $validated['link'] ?? $banner->link,
                'description' => $validated['description'] ?? $banner->description,
                'status' => $status,
            ];
            
            // Xử lý ảnh
            if ($request->hasFile('image_file')) {
                try {
                    if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                        Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
                    }
                    
                    $path = $request->file('image_file')->store('banners', 'public');
                    $data['image'] = '/storage/' . $path;
                } catch (\Exception $e) {
                    Log::error('Lỗi upload ảnh: ' . $e->getMessage());
                    return response()->json([
                        'success' => false,
                        'message' => 'Lỗi upload ảnh: ' . $e->getMessage()
                    ], 500);
                }
            } elseif ($request->filled('image')) {
                $data['image'] = $request->image;
            }

            // Xử lý thay đổi order
            $newOrder = $request->input('order');
            if (!is_null($newOrder) && $newOrder != $banner->order) {
                $oldOrder = $banner->order;
                $totalBanners = Banner::count();

                if ($newOrder < 0) $newOrder = 0;
                if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

                if ($oldOrder < $newOrder) {
                    Banner::where('id', '!=', $id)
                        ->where('order', '>', $oldOrder)
                        ->where('order', '<=', $newOrder)
                        ->decrement('order');
                } else {
                    Banner::where('id', '!=', $id)
                        ->where('order', '>=', $newOrder)
                        ->where('order', '<', $oldOrder)
                        ->increment('order');
                }

                $data['order'] = $newOrder;
            }

            $banner->update($data);

            Log::info('Banner updated successfully:', ['id' => $banner->id]);

            return response()->json([
                'success' => true, 
                'data' => $banner->load('campaign')
            ]);
            
        } catch (\Illuminate\Validation\ValidationException $e) {
            Log::error('Validation error:', $e->errors());
            return response()->json([
                'success' => false,
                'message' => 'Dữ liệu không hợp lệ',
                'errors' => $e->errors()
            ], 422);
        } catch (\Exception $e) {
            Log::error('Lỗi cập nhật banner: ' . $e->getMessage() . "\n" . $e->getTraceAsString());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function destroy($id)
    {
        try {
            $banner = Banner::findOrFail($id);
            
            if ($banner->image && Storage::disk('public')->exists(str_replace('/storage/', '', $banner->image))) {
                Storage::disk('public')->delete(str_replace('/storage/', '', $banner->image));
            }
            
            $deletedOrder = $banner->order;
            $banner->delete();

            Banner::where('order', '>', $deletedOrder)->decrement('order');

            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            Log::error('Lỗi xóa banner: ' . $e->getMessage());
            return response()->json([
                'success' => false,
                'message' => 'Có lỗi xảy ra: ' . $e->getMessage()
            ], 500);
        }
    }

    public function updateStatus(Request $request, $id)
    {
        try {
            $banner = Banner::findOrFail($id);
            $request->validate(['status' => 'required|boolean']);
            $banner->update(['status' => $request->status]);
            return response()->json(['success' => true, 'data' => $banner->load('campaign')]);
        } catch (\Exception $e) {
            return response()->json(['success' => false, 'message' => $e->getMessage()], 500);
        }
    }

    public function updateOrder(Request $request, $id)
    {
        try {
            $banner = Banner::findOrFail($id);
            $request->validate(['order' => 'required|integer|min:0']);

            $newOrder = $request->order;
            $oldOrder = $banner->order;

            if ($oldOrder == $newOrder) {
                return response()->json(['success' => true]);
            }

            $totalBanners = Banner::count();
            if ($newOrder < 0) $newOrder = 0;
            if ($newOrder >= $totalBanners) $newOrder = $totalBanners - 1;

            if ($oldOrder < $newOrder) {
                Banner::where('id', '!=', $id)
                    ->where('order', '>', $oldOrder)
                    ->where('order', '<=', $newOrder)
                    ->decrement('order');
            } else {
                Banner::where('id', '!=', $id)
                    ->where('order', '>=', $newOrder)
                    ->where('order', '<', $oldOrder)
                    ->increment('order');
            }

            $banner->update(['order' => $newOrder]);

            return response()->json(['success' => true]);
        } catch (\Exception $e) {
            return response()->json(['success' => false, 'message' => $e->getMessage()], 500);
        }
    }
}
```

## 2. File `Banners.vue` (Hoàn chỉnh - đã fix trạng thái)

```vue
<script setup>
import { ref, onMounted, computed, watch } from 'vue'
import axios from 'axios'
import AdminLayout from '@/Layouts/AdminLayout.vue'
import { Head } from '@inertiajs/vue3'

// Props nhận từ controller
const props = defineProps({
    banners: {
        type: Array,
        default: () => []
    },
    campaigns: {
        type: Array,
        default: () => []
    }
})

// Pagination - 5 items per page
const currentPage = ref(1)
const perPage = ref(5)

// Search
const search = ref('')

// Lọc banners theo tên chiến dịch hoặc link
const filteredBanners = computed(() => {
    if (!banners.value || banners.value.length === 0) return []
    if (!search.value) return banners.value
    
    const keyword = search.value.toLowerCase().trim()
    return banners.value.filter(banner => {
        const campaignName = (banner.campaign?.name || '').toLowerCase()
        const title = (banner.title || '').toLowerCase()
        const link = (banner.link || '').toLowerCase()
        return campaignName.includes(keyword) || title.includes(keyword) || link.includes(keyword)
    })
})

// Sắp xếp banners theo order
const sortedBanners = computed(() => {
    return [...filteredBanners.value].sort((a, b) => (a.order || 0) - (b.order || 0))
})

// Pagination
const paginatedBanners = computed(() => {
    const start = (currentPage.value - 1) * perPage.value
    const end = start + perPage.value
    return sortedBanners.value.slice(start, end)
})

const totalPages = computed(() => {
    return Math.ceil(sortedBanners.value.length / perPage.value)
})

// Hiển thị số trang (tối đa 5 trang)
const displayedPages = computed(() => {
    const total = totalPages.value
    const current = currentPage.value
    const maxDisplay = 5
    
    if (total <= maxDisplay) {
        return Array.from({ length: total }, (_, i) => i + 1)
    }
    
    let start = Math.max(1, current - 2)
    let end = Math.min(total, start + maxDisplay - 1)
    
    if (end - start < maxDisplay - 1) {
        start = Math.max(1, end - maxDisplay + 1)
    }
    
    return Array.from({ length: end - start + 1 }, (_, i) => start + i)
})

// Reset về trang 1 khi tìm kiếm
watch(search, () => {
    currentPage.value = 1
})

const banners = ref(props.banners || [])
const campaigns = ref(props.campaigns || [])
const showModal = ref(false)
const showDeleteModal = ref(false)
const isEdit = ref(false)
const selectedBanner = ref(null)
const isLoading = ref(false)
const isSaving = ref(false)
const errorMessage = ref('')
const fileError = ref('')
const uploadSuccess = ref(false)

// Chọn phương thức nhập ảnh: 'url' hoặc 'file'
const imageInputMode = ref('url')
const selectedFile = ref(null)
const imagePreviewUrl = ref('')

const form = ref({
    id: null,
    title: '',
    campaign_id: '',
    image: '',
    link: '',
    status: 1,
    order: 0
})

// Xem trước ảnh
const imagePreview = computed(() => {
    if (imagePreviewUrl.value) return imagePreviewUrl.value
    if (form.value.image) return form.value.image
    return null
})

// Format date
const formatDate = (date) => {
    if (!date) return '---'
    const d = new Date(date)
    return d.toLocaleDateString('vi-VN')
}

// Lấy tên chiến dịch từ ID
const getCampaignName = (campaignId) => {
    if (!campaignId) return 'Chưa phân loại'
    const campaign = campaigns.value.find(c => c.id === campaignId)
    return campaign ? campaign.name : 'Chiến dịch đã xóa'
}

// Lấy danh sách banner từ API
const fetchBanners = async () => {
    if (isLoading.value) return
    isLoading.value = true
    try {
        const response = await axios.get('/admin/banners/data')
        if (response.data && Array.isArray(response.data)) {
            banners.value = response.data
        } else {
            banners.value = []
        }
    } catch (error) {
        console.error('Lỗi lấy danh sách banner:', error)
        banners.value = []
    } finally {
        isLoading.value = false
    }
}

// Lấy danh sách chiến dịch từ API
const fetchCampaigns = async () => {
    try {
        const response = await axios.get('/admin/promotions/campaigns/list')
        if (response.data && Array.isArray(response.data)) {
            campaigns.value = response.data
        }
    } catch (error) {
        console.error('Lỗi lấy danh sách chiến dịch:', error)
    }
}

// Mở modal tạo mới
const openCreateModal = () => {
    isEdit.value = false
    form.value = { 
        id: null, 
        title: '',
        campaign_id: '', 
        image: '', 
        link: '', 
        status: 1, 
        order: 0 
    }
    selectedFile.value = null
    imagePreviewUrl.value = ''
    imageInputMode.value = 'url'
    errorMessage.value = ''
    fileError.value = ''
    uploadSuccess.value = false
    showModal.value = true
}

// Mở modal sửa
const openEditModal = (banner) => {
    isEdit.value = true
    form.value = { 
        id: banner.id,
        title: banner.title || '',
        campaign_id: banner.campaign_id || '', 
        image: banner.image || '', 
        link: banner.link || '', 
        status: banner.status !== undefined ? banner.status : 1, 
        order: banner.order || 0 
    }
    selectedFile.value = null
    imagePreviewUrl.value = ''
    imageInputMode.value = 'url'
    errorMessage.value = ''
    fileError.value = ''
    uploadSuccess.value = false
    showModal.value = true
}

// Xử lý khi chọn file
const handleFileChange = (event) => {
    const file = event.target.files[0]
    fileError.value = ''
    if (!file) {
        return
    }
    // Kiểm tra định dạng file
    if (!file.type.startsWith('image/')) {
        fileError.value = 'Vui lòng chọn file ảnh (jpg, png, gif, svg, webp)'
        event.target.value = ''
        return
    }
    // Kiểm tra kích thước (2MB)
    if (file.size > 2 * 1024 * 1024) {
        fileError.value = 'Kích thước ảnh không quá 2MB'
        event.target.value = ''
        return
    }
    selectedFile.value = file
    // Tạo preview
    const reader = new FileReader()
    reader.onload = (e) => { 
        imagePreviewUrl.value = e.target.result 
    }
    reader.readAsDataURL(file)
    // Xóa URL cũ nếu có
    form.value.image = ''
}

// Xóa file đã chọn
const clearFile = () => {
    selectedFile.value = null
    imagePreviewUrl.value = ''
    fileError.value = ''
    if (imageInputMode.value === 'file') {
        const fileInput = document.getElementById('fileInput')
        if (fileInput) fileInput.value = ''
    }
}

// Lưu banner
const saveBanner = async () => {
    // Validate
    if (!form.value.campaign_id) {
        errorMessage.value = 'Vui lòng chọn chiến dịch'
        return
    }
    
    // Kiểm tra có ảnh không (URL hoặc file)
    if (!form.value.image && !selectedFile.value) {
        errorMessage.value = 'Vui lòng nhập URL ảnh hoặc tải ảnh lên'
        return
    }
    
    if (fileError.value) {
        errorMessage.value = fileError.value
        return
    }

    if (isSaving.value) return
    isSaving.value = true
    errorMessage.value = ''
    uploadSuccess.value = false

    try {
        let response
        
        if (isEdit.value) {
            // Cập nhật banner
            if (selectedFile.value) {
                // Có file mới -> upload
                const formData = new FormData()
                formData.append('_method', 'PUT')
                formData.append('title', form.value.title || '')
                formData.append('campaign_id', form.value.campaign_id)
                formData.append('link', form.value.link || '')
                formData.append('status', form.value.status)
                formData.append('order', form.value.order || 0)
                formData.append('image_file', selectedFile.value)
                
                response = await axios.post(`/admin/banners/${form.value.id}`, formData, {
                    headers: { 
                        'Content-Type': 'multipart/form-data',
                        'Accept': 'application/json'
                    }
                })
            } else {
                // Không có file mới, chỉ cập nhật thông tin
                const dataToSave = {
                    title: form.value.title || '',
                    campaign_id: form.value.campaign_id,
                    link: form.value.link || '',
                    status: form.value.status,
                    order: form.value.order || 0,
                    image: form.value.image || null
                }
                response = await axios.put(`/admin/banners/${form.value.id}`, dataToSave, {
                    headers: { 'Accept': 'application/json' }
                })
            }
            
            if (response.data && response.data.success) {
                // Cập nhật danh sách
                await fetchBanners()
                uploadSuccess.value = true
                alert('Cập nhật banner thành công!')
                showModal.value = false
                clearFile()
            } else {
                errorMessage.value = response.data?.message || 'Có lỗi xảy ra khi cập nhật'
            }
        } else {
            // Thêm mới banner
            if (selectedFile.value) {
                // Upload file
                const formData = new FormData()
                formData.append('title', form.value.title || '')
                formData.append('campaign_id', form.value.campaign_id)
                formData.append('link', form.value.link || '')
                formData.append('status', form.value.status)
                formData.append('order', form.value.order || 0)
                formData.append('image_file', selectedFile.value)
                
                response = await axios.post('/admin/banners', formData, {
                    headers: { 
                        'Content-Type': 'multipart/form-data',
                        'Accept': 'application/json'
                    }
                })
            } else {
                // Dùng URL
                const dataToSave = {
                    title: form.value.title || '',
                    campaign_id: form.value.campaign_id,
                    link: form.value.link || '',
                    status: form.value.status,
                    order: form.value.order || 0,
                    image: form.value.image || null
                }
                response = await axios.post('/admin/banners', dataToSave, {
                    headers: { 'Accept': 'application/json' }
                })
            }
            
            if (response.data && response.data.success && response.data.data) {
                banners.value.push(response.data.data)
                uploadSuccess.value = true
                alert('Thêm banner thành công!')
                showModal.value = false
                clearFile()
            } else {
                errorMessage.value = response.data?.message || 'Có lỗi xảy ra khi thêm mới'
            }
        }
    } catch (error) {
        console.error('Lỗi lưu banner:', error)
        if (error.response) {
            errorMessage.value = error.response.data?.message || 'Có lỗi xảy ra'
        } else {
            errorMessage.value = 'Không thể kết nối đến server'
        }
    } finally {
        isSaving.value = false
    }
}

// Xác nhận xóa
const confirmDelete = (banner) => {
    selectedBanner.value = banner
    errorMessage.value = ''
    showDeleteModal.value = true
}

// Xóa banner
const deleteBanner = async () => {
    if (!selectedBanner.value) return
    if (isSaving.value) return
    isSaving.value = true
    errorMessage.value = ''
    try {
        const response = await axios.delete(`/admin/banners/${selectedBanner.value.id}`, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            showDeleteModal.value = false
            const index = banners.value.findIndex(b => b.id === selectedBanner.value.id)
            if (index !== -1) {
                banners.value.splice(index, 1)
            }
            selectedBanner.value = null
            alert('Xóa banner thành công!')
        } else {
            errorMessage.value = response.data?.message || 'Có lỗi xảy ra'
        }
    } catch (error) {
        console.error('Lỗi xóa banner:', error)
        errorMessage.value = error.response?.data?.message || 'Có lỗi xảy ra khi xóa'
    } finally {
        isSaving.value = false
    }
}

// Chuyển đổi trạng thái
const toggleStatus = async (banner) => {
    try {
        const newStatus = banner.status === 1 ? 0 : 1
        const response = await axios.patch(`/admin/banners/${banner.id}/status`, { 
            status: newStatus 
        }, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            banner.status = newStatus
            // Cập nhật lại danh sách để đồng bộ
            await fetchBanners()
        } else {
            alert('Cập nhật trạng thái thất bại')
        }
    } catch (error) {
        console.error('Lỗi cập nhật trạng thái:', error)
        alert('Có lỗi xảy ra')
    }
}

// Cập nhật thứ tự
const updateOrder = async (banner, newOrder) => {
    try {
        const response = await axios.patch(`/admin/banners/${banner.id}/order`, { 
            order: newOrder 
        }, {
            headers: { 'Accept': 'application/json' }
        })
        if (response.data && response.data.success) {
            banner.order = newOrder
            // Sắp xếp lại danh sách
            banners.value = [...banners.value].sort((a, b) => (a.order || 0) - (b.order || 0))
        }
    } catch (error) {
        console.error('Lỗi cập nhật thứ tự:', error)
        alert('Có lỗi xảy ra khi cập nhật thứ tự')
    }
}

// Đóng modal
const closeModal = () => {
    showModal.value = false
    showDeleteModal.value = false
    selectedBanner.value = null
    form.value = { id: null, title: '', campaign_id: '', image: '', link: '', status: 1, order: 0 }
    errorMessage.value = ''
    fileError.value = ''
    isSaving.value = false
    uploadSuccess.value = false
    clearFile()
}

const handleOverlayClick = (e) => {
    if (e.target === e.currentTarget) {
        closeModal()
    }
}

onMounted(() => {
    fetchBanners()
    fetchCampaigns()
})
</script>

<template>
    <Head title="Quản lý Banner" />
    <AdminLayout>
        <div class="p-4 md:p-8">
            <div class="flex justify-between items-center mb-6">
                <h1 class="text-2xl md:text-3xl font-bold text-gray-800">Quản lý Banner</h1>
                <button @click="openCreateModal" class="bg-orange-600 text-white px-5 py-2 rounded-xl flex items-center gap-2 hover:bg-orange-700 transition-colors" :disabled="isSaving">
                    <span class="material-symbols-outlined text-lg">add</span>
                    Thêm banner mới
                </button>
            </div>

            <!-- Thanh tìm kiếm -->
            <div class="mb-4">
                <div class="relative max-w-md">
                    <span class="material-symbols-outlined absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 text-lg">search</span>
                    <input 
                        v-model="search" 
                        type="text" 
                        placeholder="Tìm theo chiến dịch, tên hoặc link..." 
                        class="pl-10 pr-4 py-2 bg-white border border-gray-300 rounded-full w-full focus:outline-none focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 text-sm"
                    >
                </div>
            </div>

            <div v-if="isLoading && banners.length === 0" class="text-center py-8">
                <div class="inline-block animate-spin rounded-full h-8 w-8 border-4 border-orange-500 border-t-transparent"></div>
                <p class="mt-2 text-gray-500">Đang tải...</p>
            </div>

            <div v-else class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full min-w-[800px]">
                        <thead class="bg-gray-50 border-b border-gray-200">
                            <tr>
                                <th class="text-left p-4 font-semibold text-gray-700 w-16 whitespace-nowrap">STT</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Hình ảnh</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap min-w-[150px]">Chiến dịch</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Link</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Trạng thái</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Thứ tự</th>
                                <th class="text-left p-4 font-semibold text-gray-700 whitespace-nowrap">Ngày tạo</th>
                                <th class="text-center p-4 font-semibold text-gray-700 w-40 whitespace-nowrap">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr v-for="(banner, index) in paginatedBanners" :key="banner.id" class="border-b border-gray-100 hover:bg-gray-50 transition">
                                <td class="p-4 text-gray-500 text-sm whitespace-nowrap">{{ (currentPage - 1) * perPage + index + 1 }}</td>
                                <td class="p-4">
                                    <img v-if="banner.image" :src="banner.image" class="h-12 w-20 object-cover rounded" :alt="banner.title || 'Banner'" @error="banner.image = null">
                                    <span v-else class="text-gray-400">---</span>
                                </td>
                                <td class="p-4">
                                    <div class="flex items-center gap-2">
                                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium" 
                                              :class="banner.campaign_id ? 'bg-blue-100 text-blue-800' : 'bg-gray-100 text-gray-500'">
                                            {{ banner.campaign?.name || 'Chưa phân loại' }}
                                        </span>
                                    </div>
                                </td>
                                <td class="p-4 text-gray-500 text-sm max-w-xs truncate">
                                    <a v-if="banner.link" :href="banner.link" target="_blank" class="text-blue-600 hover:underline">
                                        {{ banner.link }}
                                    </a>
                                    <span v-else class="text-gray-400">---</span>
                                </td>
                                <td class="p-4">
                                    <button @click="toggleStatus(banner)" class="px-2 py-1 text-xs rounded-full transition whitespace-nowrap" :class="banner.status === 1 ? 'bg-green-100 text-green-700 hover:bg-green-200' : 'bg-gray-100 text-gray-500 hover:bg-gray-200'">
                                        {{ banner.status === 1 ? '🟢 Hoạt động' : '⛔ Tạm dừng' }}
                                    </button>
                                </td>
                                <td class="p-4">
                                    <div class="flex items-center gap-1">
                                        <input type="number" :value="banner.order || 0" @change="updateOrder(banner, parseInt($event.target.value))" class="w-16 px-1 py-1 border rounded text-center text-sm" min="0">
                                        <span class="text-xs text-gray-400">#</span>
                                    </div>
                                </td>
                                <td class="p-4 text-gray-500 text-sm whitespace-nowrap">{{ formatDate(banner.created_at) }}</td>
                                <td class="p-4 text-center whitespace-nowrap">
                                    <div class="flex items-center justify-center gap-2">
                                        <button @click="openEditModal(banner)" class="px-3 py-1.5 text-xs text-green-600 hover:bg-green-100 rounded-lg transition-colors font-medium" :disabled="isSaving">Sửa</button>
                                        <button @click="confirmDelete(banner)" class="px-3 py-1.5 text-xs text-red-600 hover:bg-red-100 rounded-lg transition-colors font-medium" :disabled="isSaving">Xóa</button>
                                    </div>
                                </td>
                            </tr>
                            <tr v-if="paginatedBanners.length === 0 && !isLoading">
                                <td colspan="8" class="p-8 text-center text-gray-400">Chưa có banner nào</td>
                            </tr>
                        </tbody>
                    </table>
                </div>

                <!-- Footer với phân trang căn giữa -->
                <div class="p-4 border-t border-gray-200">
                    <div class="text-center text-sm text-gray-500 mb-3">
                        Hiển thị {{ paginatedBanners.length }} / {{ sortedBanners.length }} banner
                    </div>
                    
                    <div v-if="totalPages > 1" class="flex justify-center items-center gap-2">
                        <button
                            @click="currentPage--"
                            :disabled="currentPage === 1"
                            class="px-3 py-1.5 text-sm border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            ◄
                        </button>
                        
                        <div class="flex gap-1">
                            <button
                                v-for="page in displayedPages"
                                :key="page"
                                @click="currentPage = page"
                                class="px-3.5 py-1.5 text-sm rounded-lg transition-colors font-medium"
                                :class="currentPage === page ? 'bg-orange-600 text-white' : 'border border-gray-300 hover:bg-gray-50'"
                            >
                                {{ page }}
                            </button>
                        </div>
                        
                        <button
                            @click="currentPage++"
                            :disabled="currentPage === totalPages"
                            class="px-3 py-1.5 text-sm border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors disabled:opacity-50 disabled:cursor-not-allowed"
                        >
                            ►
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Modal Thêm/Sửa -->
        <div v-if="showModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50" @click="handleOverlayClick">
            <div class="bg-white rounded-lg w-full max-w-lg p-6 max-h-[90vh] overflow-y-auto">
                <div class="flex justify-between items-center mb-4">
                    <h3 class="text-xl font-bold text-gray-800">{{ isEdit ? 'Sửa banner' : 'Thêm banner mới' }}</h3>
                    <button @click="closeModal" class="text-gray-400 hover:text-gray-600 transition-colors text-xl">✕</button>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Tiêu đề</label>
                        <input 
                            v-model="form.title" 
                            type="text" 
                            class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" 
                            placeholder="Tiêu đề banner (tùy chọn)"
                            :disabled="isSaving"
                        >
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Chiến dịch *</label>
                        <select v-model="form.campaign_id" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                            <option value="">-- Chọn chiến dịch --</option>
                            <option v-for="camp in campaigns" :key="camp.id" :value="camp.id">
                                {{ camp.name }}
                                <span v-if="camp.status" class="text-xs text-gray-400">({{ camp.status === 'active' ? 'Đang diễn ra' : camp.status === 'scheduled' ? 'Sắp diễn ra' : 'Đã kết thúc' }})</span>
                            </option>
                        </select>
                        <p class="text-xs text-gray-400 mt-1">Banner sẽ thuộc về chiến dịch này</p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Ảnh banner *</label>
                        <div class="flex gap-2 border-b pb-2 mb-2">
                            <button type="button" @click="imageInputMode = 'url'" :class="['px-3 py-1 text-sm rounded-full transition-colors', imageInputMode === 'url' ? 'bg-orange-100 text-orange-600' : 'bg-gray-100 hover:bg-gray-200']">🔗 Nhập URL</button>
                            <button type="button" @click="imageInputMode = 'file'" :class="['px-3 py-1 text-sm rounded-full transition-colors', imageInputMode === 'file' ? 'bg-orange-100 text-orange-600' : 'bg-gray-100 hover:bg-gray-200']">📁 Tải ảnh lên</button>
                        </div>
                        <div v-if="imageInputMode === 'url'">
                            <input v-model="form.image" type="text" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" placeholder="https://example.com/banner.jpg" :disabled="isSaving">
                        </div>
                        <div v-else>
                            <input id="fileInput" type="file" accept="image/*" @change="handleFileChange" class="w-full" :disabled="isSaving">
                            <div v-if="fileError" class="text-red-500 text-sm mt-1">{{ fileError }}</div>
                            <button v-if="selectedFile" @click="clearFile" class="text-red-500 text-xs mt-1 hover:underline" type="button">✕ Xóa file đã chọn</button>
                            <p class="text-xs text-gray-400 mt-1">Hỗ trợ JPG, PNG, GIF, SVG, WEBP. Kích thước tối đa 2MB</p>
                        </div>
                        <div v-if="imagePreview" class="mt-2">
                            <p class="text-sm text-gray-600 mb-1">Xem trước:</p>
                            <div class="w-32 h-20 border rounded-lg overflow-hidden bg-gray-100 flex items-center justify-center">
                                <img :src="imagePreview" class="max-w-full max-h-full object-contain" @error="imagePreviewUrl = ''; form.image = ''" alt="Banner preview">
                            </div>
                        </div>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Link (URL)</label>
                        <input v-model="form.link" type="text" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" placeholder="https://example.com" :disabled="isSaving">
                        <p class="text-xs text-gray-400 mt-1">Đường dẫn khi người dùng click vào banner</p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Trạng thái</label>
                        <select v-model="form.status" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                            <option :value="1">🟢 Hoạt động</option>
                            <option :value="0">⛔ Tạm dừng</option>
                        </select>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-gray-700 mb-1">Thứ tự</label>
                        <input v-model.number="form.order" type="number" min="0" class="w-full border border-gray-300 rounded-lg p-2 focus:ring-orange-500 focus:border-orange-500 outline-none" :disabled="isSaving">
                        <p class="text-xs text-gray-400 mt-1">Số nhỏ hơn hiển thị trước</p>
                    </div>

                    <div v-if="errorMessage" class="p-3 bg-red-50 border border-red-200 rounded-lg">
                        <p class="text-sm text-red-600">{{ errorMessage }}</p>
                    </div>
                    
                    <div v-if="uploadSuccess" class="p-3 bg-green-50 border border-green-200 rounded-lg">
                        <p class="text-sm text-green-600">✅ Lưu thành công!</p>
                    </div>
                </div>
                
                <div class="flex justify-end gap-3 mt-6">
                    <button @click="closeModal" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition" :disabled="isSaving">Hủy</button>
                    <button @click="saveBanner" class="px-4 py-2 bg-orange-600 text-white rounded-lg hover:bg-orange-700 transition flex items-center gap-2" :disabled="isSaving || !!fileError">
                        <span v-if="isSaving" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSaving ? 'Đang xử lý...' : 'Lưu' }}
                    </button>
                </div>
            </div>
        </div>

        <!-- Modal Xác nhận xóa -->
        <div v-if="showDeleteModal" class="fixed inset-0 bg-black/50 flex items-center justify-center z-50" @click="handleOverlayClick">
            <div class="bg-white rounded-lg w-full max-w-md p-6">
                <h3 class="text-xl font-bold mb-4">Xác nhận xóa</h3>
                <p class="text-gray-600">Bạn có chắc muốn xóa banner "{{ selectedBanner?.title || '#' + selectedBanner?.id }}" này?</p>
                <p class="text-xs text-gray-400 mt-1">Hành động này không thể hoàn tác</p>
                <div v-if="errorMessage" class="mt-3 p-3 bg-red-50 border border-red-200 rounded-lg">
                    <p class="text-sm text-red-600">{{ errorMessage }}</p>
                </div>
                <div class="flex justify-end gap-3 mt-6">
                    <button @click="closeModal" class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition" :disabled="isSaving">Hủy</button>
                    <button @click="deleteBanner" class="px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition flex items-center gap-2" :disabled="isSaving">
                        <span v-if="isSaving" class="inline-block w-4 h-4 border-2 border-white border-t-transparent rounded-full animate-spin"></span>
                        {{ isSaving ? 'Đang xóa...' : 'Xóa' }}
                    </button>
                </div>
            </div>
        </div>
    </AdminLayout>
</template>

<style scoped>
@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
.animate-spin {
    animation: spin 1s linear infinite;
}
</style>
```

## Tóm tắt các thay đổi:

1. **Controller `BannerController.php`**:
   - Sửa lỗi cập nhật trạng thái: thêm xử lý `status` đúng cách
   - `updateStatus` trả về `data` để cập nhật danh sách

2. **File `Banners.vue`**:
   - Sửa lỗi hiển thị trạng thái: thêm icon 🟢 cho Hoạt động, ⛔ cho Tạm dừng
   - Cập nhật `toggleStatus`: sau khi chuyển trạng thái, gọi `fetchBanners()` để đồng bộ
   - Sửa dropdown trạng thái trong modal: hiển thị rõ ràng "Hoạt động" và "Tạm dừng"
   - Thêm `await fetchBanners()` sau khi cập nhật để refresh danh sách



---
Powered by [DeepSeek Chat Exporter](https://deepseekchatexporter.com)
